// Сокетные префиксы вердикта уборки гейта релиза. Не `*.test.mjs` — раннер
// берёт из каталога только их, и этот файл в прогон не попадает.
//
// Каталоги unix-сокетов живут в `/tmp`, а не в `$TMPDIR` прогона: лимит `sun_path`
// ([sandbox.mjs](sandbox.mjs)). Уборка набора их не метёт — `/tmp` общий, чужое
// сносить нельзя. След оборванного прогона ловит вердикт [release-gates.mjs](../scripts/release-gates.mjs)
// «после прогона не осталось сокетов»: он смотрит `/tmp` по этому перечню.
//
// Перечень собран руками, как `SUITE_PREFIXES`. Полноту сторожит
// [tmpdir-sweep.test.mjs](tmpdir-sweep.test.mjs): каждый литерал `makeSockPath('…')` и
// `makeSockDir('…')` в `cli/test/` и `cli/scripts/` обязан начинаться с одной из записей.
// Без сторожа новый префикс утекал бы молча — живой случай: `ags-` в
// `promptobus-guard.test.mjs` в литерале гейта не было.
export const SOCK_PREFIXES = ['a2l-', 'a2e-', 'a2h-', 'a2s-', 'adoc-', 'ags-', 'a2m-'];
