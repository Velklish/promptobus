# Changelog

All notable changes to this project are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- **The machine lease** (PB-241, [ADR-018](docs/adr/adr-018-the-bus-leases-the-machine-for-measurements.md)). `promptobus lease [--as <address>] [--task <id>] [--wait <seconds>] -- <command…>` runs one measuring command at a time per machine, across tasks and workspaces: the lease is a directory lock under `/tmp/promptobus-<uid>/` held by the wrapper's own pid and refused when that directory is a symlink or another user's, a lease holder that dies is dropped by liveness, a waiter prints who holds the machine and gives up at the bound (default 1800 s) without running anything. `status` opens with the lease holder and the waiters. The worker and approver preambles carry the command, addressed, with the rule for what counts as a measurement; the reviewer preamble says the lease is not its own. The orchestration skill drops orchestrator-issued slots for it.

### Fixed

- **A Claude Code participant no longer registers a Remote Control session on claude.ai** (PB-253). A `--bg`
  session registers an account Remote Control session by default, and an abrupt stop can leave it listed
  indefinitely as "Remote Control · offline". A participant is driven over the bus and has no need for the entry.
  The participant settings file now carries `"disableRemoteControl": true` for every role.
  [03-cli § Spawn](docs/reference/03-cli.md#spawn), [05-drivers](docs/reference/05-drivers.md#a-stop-returns-after-the-record-is-gone-not-after-the-command-returns).
- **A reviewer whose session is gone can be re-bound to another harness.** `review <path> --harness <h>` on an address already in the journal lifts a new session on `<h>` when that session is the measured `dead` state (no live record — absent or stale) and appends the closed generation to `metadata.harnessHistory`. Every later lift of that address copies the history; only a re-bind appends. A live session is still refused, and the refusal names `promptobus stop <address> --task <id>` then the same review with `--harness`. A session the driver cannot answer for stays `unknown` and is not treated as gone; the refusal names why the read failed and which session list to check, and does not name `stop`, which refuses the same unknown. `status` and the telemetry row at `done` count each generation from the previous generation's end, so a same-harness re-lift in between does not drop the earlier rounds. A closed row keeps that generation's model and routing decision. One live session per address is the caller's discipline, not a lock. Idle and delivery on the open row use the same bound as the round counts, so a same-harness re-lift of an address that was never re-bound keeps its earlier samples. The unknown-liveness refusal names the registry read that decided the state. An approver's harness is not changed, and that refusal no longer names a task the command cannot open. A worker's harness is unchanged. (PB-235)

- **The warden stops knocking a session whose harness named a reset time** (PB-236). Measured on 2026-09-17 on
  another consumer's run: a Codex participant whose every turn was refused with `You've hit your usage limit. … try again
  at Sep 19th, 2026 12:15 PM.` was knocked 102 times in about six minutes, because each failed turn rewrote the contact
  point, while `status` printed `the turn is running`. A refusal that names a time to retry after is now a state: the
  stall carries `reset: { said, at }` (`namedReset` in the package — a `limit` word plus `try again at` or
  `resets [at]`; `at` is a dated time, or Claude's `6:20am (Europe/Moscow)` as its next occurrence after the line was
  written — read from the daemon's timeline — and `null` otherwise), attached by the Codex `inspect` from a failed
  turn's error and by the Claude `sessionStall` from a limit line. The round holds every knock on that address
  until the named time, journals `knocks held` once per sighting and keeps it as `hold` in `health`; a time that does
  not parse is said as such and probed at once, then only at the `KNOCK_RETRY_SEC` pace. A knocked address is
  re-inspected at once, past the heartbeat's session-list cache, so it is knocked at most once more after its refusal is
  on record. `status` and the stall line print
  `UNREACHABLE until <time>`, and the Codex view puts that above a running turn. The orchestrator gets one postcard per
  sighting (a preview of type `unreachable` from `promptobus`), the only stall report that knocks; its own refusal is
  journalled instead. A failure with no
  named reset retries as before, with its "a later turn may succeed" hint.
  [03-cli § Guard and warden](docs/reference/03-cli.md#guard-and-warden),
  [05-drivers § A refusal that names a reset](docs/reference/05-drivers.md#a-refusal-that-names-a-reset).

- **The standalone host reads `claude --version` when the Claude driver declares it, so the version floor and the effort refusal see the build.** `resolveToolBin` itself does not start a process. The preflight asks before the probe, and a real lift asks once when the bin still has no version; `--dry-run` does not. A build below 2.1.280 drops `claude-opus-5-5` from the inventory on `promptobus models --refresh`; 2.1.280 keeps it. A timed-out, non-zero, or empty answer hands over no version and is not remembered, so the next probe asks again. Cursor and Codex do not declare the read, so their proven-version warnings stay silent. [02-host](docs/reference/02-host.md#the-standalone-host), [03-cli](docs/reference/03-cli.md#claude-code-what-its-adapter-asks).

- **A burst of mail to one address is one knock, not one per message** (PB-229). Measured on a run of 2026-09-16:
  a worker's hand-off — records, then `result` — was four sends in eighteen seconds, and the warden knocked four
  times (`unread 1, knock 1` to `unread 4, knock 4`), each one more paid orchestrator turn for one delivery. A knock is
  now outstanding until the mailbox is taken; mail landing under it counts in `unread` and in `health`'s `coalesced`
  without knocking again. The next knock goes out after the take, or once `KNOCK_COALESCE_SEC` (90 s, beside
  `KNOCK_RETRY_SEC`) has passed since the knock, and says `(C coalesced under the previous knock)`; the take line says
  `(had N, knocks K, coalesced C)`. A take is seen by the knocked message leaving the inbox, so mail landing in the same
  round as the take is knocked at once. A hold stays a hold; the first knock and a rewritten contact point do not wait.
  [03-cli § Guard and warden](docs/reference/03-cli.md#guard-and-warden).

## [0.17.0] — 2026-09-25

### Added

- **Grok 4.7 in the catalog** (PB-254). `cursor-agent` 2026.09.23 lists `grok-4.7-low` … `grok-4.7-xhigh`, without the
  `cursor-` prefix the Grok 4.6 ids carry, and a routed lift refused them (`constraint-unknown`). The catalog gains
  the `cursor-grok-47` ladder — `high` base, `low`, `medium` and `xhigh` interpolated — and the Cursor priority block is
  renumbered 300…500. `quality` 8 is a stated hypothesis under the successor rule: Terminal-Bench 2.1 under Vals AI,
  73.41 % at xhigh, would band 5, and every head-to-head on xAI's launch page puts 4.7 above 4.6. `speed` cites
  Artificial Analysis at 52.4 tokens/s (band 1) and `quotaCost` the xAI list price of $2 / $6 (band 1), while the Grok 4.6
  rows keep their `quotaCost` hypothesis of 10 — so under every weight set but `speed` a routed pick now puts 4.7 above
  4.6. The `-fast` twins stay unrated. The Cursor listing fixture is recaptured from 2026.09.23: 241 ids, 30 new and none
  gone. [guides/model-routing](docs/guides/model-routing.md#the-catalog-file).

### Changed

- **Codex 0.156.1 is the proven version** (PB-196). `PROVEN_CODEX_VERSION` moves from 0.146.0, so the lift now refuses
  an older binary, and `npm run codex-schema` is green again on a machine with 0.156.1. The protocol fixtures under
  `test/fixtures/codex-app-server/0.156.1/` are the same selection as before, generated on 2026-09-25. The three dated
  records were re-taken on that binary: the override-form MCP probe, the `disabled_tools` enforcement probe and the
  token-count record. The suite reads the directory off the constant, and the 0.146.0 directory stays as a record that
  no test reads. The holder's answers are unchanged. `ReviewDecision` gained an arm that the holder never sends. A
  command approval's new `kind` (`command` or `writeStdin`) gets one decision for both kinds. The new `openaiForm`
  elicitation mode is declined. The 0.146.0 facts were re-measured in two paid turns and a series of no-turn probes. A
  worker's plain shell now writes its worktree and commits. An in-worktree `apply_patch` applies with no approval
  request, while `item/fileChange/requestApproval` still carries no path. `listen` is still refused, and no hook event
  fired. `debug models` and `model/list` answer alike in the owner's home and a participant home. The owner's default
  model, `gpt-6-sol` today, drove a one-shot turn with exit 0. [03-cli § The Codex holder](docs/reference/03-cli.md#the-codex-holder),
  [05-drivers](docs/reference/05-drivers.md#codex--codex-harness-driver--the-third-production-bus-driver).

### Fixed

- **A Codex participant home no longer runs codex-cli's built-in `codex_apps` MCP server** (PB-196). On 0.156.1 the
  `apps` feature is on by default. A home holding only a copy of `auth.json`, the trust record and the mechanism's MCP
  entries reported `codex_apps` in the thread inventory: connected, 52 tools, some of them account writes such as
  `sites.delete_site` and `plugin_management.uninstall_app`. The reviewer's `disabled_tools` did not cover it, and the
  reviewer prompt said the session had no server but the mechanism's. Every participant home now carries
  `[features] apps = false`, for every role. Measured with no turn, the thread then lists the mechanism's server and
  nothing else. [05-drivers](docs/reference/05-drivers.md#the-built-in-codex_apps-server-stays-off-in-the-participant-home).

- **The restart route a repeat spawn prints for a live participant stops that one participant** (PB-252). For Codex it
  named `done`, which closes the whole task and every managed session in it, while `promptobus stop <address>` stops one
  thread through the driver. The refusal now prints `promptobus stop <address> --task <id>`, both filled in, for every
  driver that declares `stop` — Claude, Cursor and Codex — in place of the driver's phrase, which for Claude and Cursor
  was a harness command with a literal `<id>`. The Codex stop phrase no longer names `done`. [03-cli](docs/reference/03-cli.md#spawn).
- **A live Cursor session no longer reads dead when `tmux` is off the caller's `PATH`** (PB-239.2). Every tmux call
  used the bare name, while the lift's check also searches the Cursor install directories, and a tmux that could not be
  started read as an empty server: `inspect` answered `stale`, which counts as dead, so `stop` said "nothing to stop"
  with exit 0, and the driver's own `stop`, called directly, dropped the record of a running session. Every call now
  runs the tmux that search finds, by absolute path, with the caller's environment unchanged — a widened `PATH` would
  reach the server's panes — and the lift's check calls the same search. A tmux that cannot be run is an unread state:
  `inspect` refuses naming tmux and the session, the participant reads `unknown` with that reason on its `status` line,
  `stop` and `sweep` refuse with exit 1, and the driver's `stop` is `ok: false` and leaves the record.
  `PROMPTOBUS_CURSOR_INSTALL_DIRS` is a new suite seam that replaces the install-directory list; the suite's hygiene
  sets it to the sandbox `~/.local/bin`, so an unstubbed tmux is not found rather than the machine's.
  [05-drivers](docs/reference/05-drivers.md#cursor-tmux-by-absolute-path).
- **A Claude approver writes to the clone root with its own `Write` and `Edit`** (PB-240). Claude Code refuses those
  tools in a background session's main checkout until the session moves into a worktree, and the approver is seated in
  the clone root on purpose, to merge, run `archive` and fill `result.md` there — so it was left with the shell. The
  switch is the harness's settings key `worktree.bgIsolation`; the Claude driver now writes `"none"` into the approver's
  participant settings file, and into no other role's, so workers keep the guard. Measured on claude 2.1.280: without
  the key the `Write` is refused, with it in the `--settings` file the `Write` lands, and the key there wins over
  `"worktree"` in the clone's own `.claude/settings.json`; `CLAUDE_BG_ISOLATION` in the lift's environment does not
  reach the session. A managed (policy) setting of `"worktree"` presumably still wins and is not measured. Read from the
  binary: the same key drops the harness's git paragraph from the session's prompt, "Never push to main/master,
  force-push, or merge." with it, so the approver preamble now says it never pushes, force-pushes or rewrites commits
  already on the remote, and that the orchestrator pushes. The key reaches a fresh lift only: a repeat `--approver`
  onto a live session reuses it without rewriting its settings file. [05-drivers](docs/reference/05-drivers.md#the-approver-writes-to-the-shared-clone-the-harness-guard-and-the-key-that-lifts-it).
- **`sweep` and `stop` from a lifted participant no longer depend on `claude` being on that session's `PATH`** (PB-239).
  The Claude driver's state query (`claude agents --json`) and `claude stop` called the bare name, while the lift resolves
  the binary through `host.resolveToolBin`. A background Claude session inherits its daemon's environment, and that `PATH`
  need not hold the install directory: an approver's `sweep` answered "session is unknown", and `stop` said "no live
  session — nothing to stop" with exit 0 while the session could still be running. Both calls now run the binary the host
  names, through the host `hostOf` and `runPromptobus` already bind for the process; the host contract is unchanged. When
  the state still cannot be read, the refusal says why — the harness binary was not found or does not start (naming what
  was looked for), the harness registry could not be read, a registry home nobody named, or a record with no session
  reference — and `stop` now exits 1 instead of 0 for any harness whose session state is unknown. The driver's own `stop`
  on an unread registry is now `ok: false` rather than "nothing to stop". A binary that is not found
  now marks only the Claude participants unknown, with the reason on their `status` line, instead of blanking the session
  state of every participant.

## [0.16.0] — 2026-09-24

### Changed

- **The Claude driver is proven on 2.1.280, and the `opus` alias resolves to `claude-opus-5-5`** (PB-245.1). A participant lifted
  with `--model opus` on claude 2.1.280 took one bus message while idle and replied in 6.2 s, as on 2.1.263, and `claude agents
  --json` printed the same nine fields; its transcript names `claude-opus-5-5` as the model of every turn. The binary also carries a
  second table that still points `opus` at `claude-opus-5`, so the alias moved on that live reading rather than on the offline one.
  `PROVEN_CLAUDE_VERSION` is `2.1.280` and `MODEL_ALIAS_IDS.opus` is `claude-opus-5-5`: `models calibrate` now books a `--model
  opus` run — and every lift without `--model`, since `DEFAULT_MODEL` is `opus` — to Opus 5.5's rows, runs recorded before the
  re-point included. The calibrate fixture follows: its aliased key and its pivot are Opus 5.5 at `xhigh`. The other three aliases
  were re-read on 2.1.280 and did not move: `fable` and `sonnet` by the baked tables, which agree, and `haiku` by one turn whose
  transcript names `claude-haiku-4-5-20251001`, the same dated name the full-id check left on 2.1.263.
- **On a host that hands over the binary's version, routing on a claude build older than 2.1.280 no longer picks Opus 5.5**
  (PB-245.1). Such a build refuses the id: on 2.1.263, `claude -p --model claude-opus-5-5` exits 1 with `API Error: 400 Claude
  Code 2.1.263 does not support this model; version 2.1.280 or newer is required`. Until now the inventory was static, so a
  routed pick on an Opus 5.5 tuple lifted a session the API refuses with that 400 (measured with `claude -p`). `MODEL_MIN_VERSION`
  in `lib/driver-claude.js` now gives `claude-opus-5-5` a floor of 2.1.280, and the Claude availability adapter reports
  `inventoryFor(version)`: below the floor the id is left out, so its rows are excluded as `model-not-in-inventory`. A version
  that cannot be read, or none at all, drops nothing, and `claudeAvailability` takes that function where it took the array. The
  floor needs a host that hands over `HostToolBin.version`. **The standalone host does not, so there the floor drops
  nothing**: on an old build under it, run `claude update` or, until then, deny the id with an overlay, `deny: { models:
  ["claude-opus-5-5"] }` (PB-245.3 asks whether that host should read the version). An explicit `--model claude-opus-5-5`
  passes through unchanged.

## [0.15.0] — 2026-09-24

### Added

- **GPT-6 Astra, Sol and Luna in the catalog** (PB-246). codex-cli 0.156.1 lists `gpt-6-astra`, `gpt-6-sol` and `gpt-6-luna`,
  and the catalog now carries the full ladders the listing offers: Astra and Sol `low` … `ultra`, and Luna `low` … `max`. That is 17
  rows, and the catalog grows from 55 tuples to 72, 35 of them Codex. Before this, a routed pick could not reach these models, and
  an explicit `--model gpt-6-sol` was refused with `constraint-unknown`. These are the first `low` rungs in the catalog, so
  `--effort low` is now a known value. `codex-gpt6-astra-max` has quality 9, from Terminal-Bench 2.1 under Codex CLI at 87.4 %.
  The page names no effort, so the figure sits on `max`, like every GPT-5.6 base row. The ladder then puts `high` at 7 and
  `xhigh` at 8, although Terminal-Bench 4.0 measures Astra's `high`, `xhigh` and `max` level with each other. That is a
  re-rating question for the owner. Astra has `speed` 1 and `quotaCost` 10, from $10 / $50. Sol and Luna have no SWE-bench or
  Terminal-Bench figure. Each is a `quality` hypothesis at its predecessor's band, 8 for Sol and 6 for Luna. Sol has `speed` 4
  and `quotaCost` 2. Luna has `speed` 4 and `quotaCost` 1. Astra's `max` and `ultra` rungs are offered as reviewers; no Sol
  rung is. The approver rows stay at 15, all of them Claude. The Codex priority block is renumbered 600…940.

### Changed

- **The backslop pin is `v0.10.0`** (PB-247). `upgrade` moved it in `backslop.json`, `package.json`, the CI workflow and the live
  markdown, and created the empty closed-task journal `docs/archive/LOG.md`; the pin in `test/gate-record.test.mjs`, which `upgrade`
  does not read, moved by hand. `docs/backlog/README.md` and `docs/archive/README.md` are re-rendered from the v0.10.0 templates,
  which `upgrade` leaves alone: a minor finding requires evidence, placeholders in `triage/` no longer fail `lint`, closure is
  `archive` then `fold`, and `mv N queue --restore` returns a task to its saved "Previous order". `backslop.json` declares `probe` as
  `npm run probe`, so step 4 of the generated `AGENTS.md` block now names the command. The contributing guide's approver path gains
  `fold N`, a minor finding its `--evidence`, and its pin paragraph says that `upgrade` has moved `package.json` and CI pins since
  v0.8.0 and still reads no other file.
- **The open backlog is re-triaged by the cost rule** (PB-249). Every card in `queue/`, `deferred/` and `minor/` carries a `Cost`
  label. Each queue and deferred card filed before this run has a dated re-triage section, which lists the claims checked against the
  tree with each command and exit code and states the stale ones as corrections or assumptions. Minor cards and hypotheses moved to
  `minor/` with an `Evidence` section. Four return conditions had fired: PB-65.2 came back to the queue, and PB-146.3, PB-66.2 and
  PB-13.2, labelled `minor`, went to `minor/`. Two return conditions were reworded to be checkable. The queue is ordered by cost, and
  [ROADMAP](docs/ROADMAP.md) says so. The exception is PB-248, which heads the queue because the owner ordered it for this run. Two
  minor batches were cut for the scopes the top of the queue touches. No runtime behaviour changed.
- **The closed-task archive is folded into [docs/archive/LOG.md](docs/archive/LOG.md), and the backslop pin is `v0.10.1`**
  (PB-248). `upgrade` moved the pin in `backslop.json`, `package.json`, the CI workflow and the live markdown, and re-rendered the
  `AGENTS.md` block: no commit between `archive N` and `fold N`, the fold draft goes under the git directory, and `lint` checks
  that every journal line's revision is reachable from `HEAD`; the pin in `test/gate-record.test.mjs` moved by hand. `fold`
  without a number turned all 312 `PB-*` directories into 312 journal lines, each naming the commit that holds the task's
  definition and result, which `show N` prints; links in seven files (`docs/TRACKS.md`, ADR-013, ADR-015, ADR-016, ADR-017 and the
  cards PB-234.3 and PB-234.4) moved onto `LOG.md#pb-N` anchors, and so did six evidence citations in cards that named removed
  archive files in backticks (PB-24.1, PB-24.2, PB-139.1, PB-232, PB-234.1, PB-249.2). 30 lines carry the fold date, 2026-09-23,
  as their closing date: 27 `result.md` files name no closing date (their revisions are dated 2026-09-06 to 2026-09-12), and
  PB-245, PB-247 and PB-249 did close that day. One line has the outcome `—`: PB-171's `result.md` names none. Six outcomes `fold`
  misread are corrected to `completed` by hand, each `result.md` opening with "Completed", "Done" or "Closed on a recorded
  decision": PB-6, PB-52, PB-131 and PB-169 (read as `rejected`), PB-117 and PB-249 (read as merged). The revision column is the
  commit the body is read from: `fold` names the last commit that touched `task.md`, and for nine tasks whose `result.md` was
  edited after that (PB-15.7, PB-37.3, PB-161.2, PB-161.3, PB-161.4, PB-166, PB-167, PB-203, PB-206) `show N` printed the earlier
  text. Those lines now name the last commit that touched the directory, and all 312 revisions hold `task.md` and `result.md`
  byte-equal to the files the fold removed. ADR-001 gains a dated amendment, and the archive README and the docs index name the
  journal line as the closed-task form and a directory as a task not yet folded, and the contributing guide's example of a generic
  ref points at the PB-94 line. No runtime behaviour changed.
- **Six ratings on three Codex base rows re-banded on the sources re-read on 2026-09-23** (PB-246). This changes routing.
  - `codex-luna-max` `quality` goes from 7 to 6. It had been a hypothesis. It now cites the Terminal-Bench 2.1 leaderboard, where
    GPT-5.6 Luna scores 75.7 % ±1.3 under Codex CLI, on the Codex CLI 60 → 90 pair. Its `speed` stays in band 4 at 142.3
    tokens/s, and its `quotaCost` stays in band 1 at $0.20 / $1.20.
  - `codex-terra-max` `quotaCost` goes from 3 to 2. The vendor price table lists $2 in / $12 out, blended $7.
  - `codex-terra-max` `speed` goes from 3 to 2. Artificial Analysis measures 83 tokens/s at max.
  - `codex-sol-max` `quality` goes from 10 to 8 and stays a hypothesis. Its Terminal-Bench 2.1 figure of 88.8 is no longer on
    the page. Every measure that covers both neighbours puts it below `codex-gpt6-astra-max`, which is 9, and above
    `codex-terra-max`, which is 7. On Terminal-Bench 4.0 under Codex that is 58.2 %, 37.3 % and 21.5 %. On Terminal-Bench 3.0
    Sol scores 34.6 % against Terra's 20.8 %. On the Artificial Analysis index it is 53, 47 and 42.
  - `codex-sol-max` `speed` goes from 3 to 2, at 72.6 tokens/s.
  - `codex-sol-max` `quotaCost` goes from 6 to 4 on the vendor promotion of $4 / $20, which runs at least through 2026-11-21. The
    row records the list price of $5 / $30, which gives band 6.

  The rungs of all three ladders follow their base rows. Every Terra rung is now speed 2, and `codex-luna-high` and
  `codex-luna-medium` are quality 4 and 3. The GPT-5.6 Sol ladder is no longer offered as a reviewer. With the GPT-6 rows, the
  reviewer rows go from 16 to 15: Astra's two rungs replace Sol's three. `codex-luna-max` now sits below `codex-terra-max` in
  the Codex priority block.

## [0.14.0] — 2026-09-23

### Added

- **Claude Opus 5.5 in the inventory and the catalog** (PB-245). `claude-opus-5-5` joins `MODEL_IDS` beside `claude-opus-5`, read
  offline off the claude 2.1.280 baked table, and the `Opus` limit scope names both ids. The catalog gains the `claude-opus-55`
  ladder — `xhigh` base, `max`, `high` and `medium` interpolated — so a routed pick and an explicit `--model claude-opus-5-5` reach
  it; before, a routed lift refused the id (`constraint-unknown`). `quality` 10 is a stated hypothesis under the successor rule, `speed`
  cites Artificial Analysis at 93 tokens/s (band 3) and `quotaCost` the $4 / $20 launch price (band 4). The Claude priority block is
  renumbered 100…290. The proven binary stays 2.1.263, so the `opus` alias still resolves to `claude-opus-5` until PB-245.1. The
  inventory is static: on a claude build older than the one read (2.1.263 carries no such id) a routed pick can still land on
  Opus 5.5, unmeasured — deny it with an overlay, `deny: { models: ["claude-opus-5-5"] }`, on such a build.
- **A gate on the Codex protocol fixtures** (PB-242). The harness names the fixture directory off `PROVEN_CODEX_VERSION` instead of
  repeating the number, `test/codex-fixtures.test.mjs` fails when the constant names a directory that does not exist, and
  `npm run codex-schema` compares the installed `codex --version` with the version the fixtures were taken from, printing the
  regeneration command on a mismatch. With no binary on the machine it says so and exits 0, so it belongs to the local gates.

## [0.13.0] — 2026-09-17

### Added

- **The handover record can now name the verdict its mutation probe was made to redden, and `send` refuses a record whose probe reddened something else.** `mutationProbe` carried `reddened` — what went red — and nothing about what the author was aiming at, so a probe that reddened the wrong thing was indistinguishable from one that worked. Both cases behind this were caught by a person reading the content of the redness rather than its presence: a fixture on a relative path whose refusal came from `path.resolve` and never reached the gate under test (green with the gate removed), and a file that exited through `fail()` partway and produced no verdicts at all below that line. `mutationProbe.expected` (`schemas/v1/handover-record.schema.json`) takes the names written down before the run, and `recordRefusal` (`lib/handoff.js`) refuses a record whose `reddened` does not carry every one of them, printing both lists whole and then the names that never reddened — whole, because the missing part alone made a target that WAS hit read as a stray. The diagnosis after the data branches on whether ANY declared target reddened: "it reddened other things" is true when none did and misleading when some did. **Subset, not equality:** more names in `reddened` is a wider net, not a wrong probe. The comparison sits beside the schema pass rather than inside it because JSON Schema cannot compare two of its own fields and the reader in `lib/schema.js` deliberately cannot either, and it goes through the one door both records already pass — there is no second validator to drift from the first. **The field is OPTIONAL:** consumers hold records written by 0.12.1 and read them mid-run, so a record with no `expected` passes exactly as before, and two suites send one through the door. What it does not catch is stated where it is defined rather than left to be discovered: an author who writes `expected` after reading `reddened`, and the aborted run — a file that dies partway can hold every declared name in `reddened` while every verdict below that line silently produced nothing. Counting executed verdicts is the consumer's own suite, not this schema. 04-protocol § The handover record.

## [0.12.1] — 2026-09-17

### Fixed

- **A second task lifted from a bound session left its orchestrator with no contact point: the warden knocked zero and the loop guard watched only the bound task.** A session bound to task A that ran `promptobus review` opened task B with itself as `orchestrator`, and nothing ever handed B a socket — the contact point was written by `joinBus` (`lib/server.js`) on the first bus call FOR THAT TASK, and a task the session had not yet called a bus tool on never got one. B's warden journal read `knocks 0`, `status --task B` read `self-wake (no contact point)`, and the reviewer's first `result` woke nobody; the loop guard did not return the turn either, because it resolves the task to guard as `identity.declaredTask ?? boundTaskId(...)` — task A. The contact point now follows the SESSION: `handOverContactPoints` (`lib/warden.js`) hands this session's socket to every active task whose `orchestrator` address it provably owns, called from `review` and `spawn` right after the task is created and from the loop guard at every end of turn, so a restarted session replaces a stale socket everywhere it orchestrates. One binding per session is unchanged, and so is the guarded task: the guard gains one more verdict as the LAST branch, below this address's own unread, its `UNANSWERED` debt and its pending hand-off — unread in any other task this session orchestrates, keyed `lifted:<id>:<n>` and naming each by id and title, so what the session owes on its own account is repaired before what was sent to a task it lifted. 03-cli § Guard and warden says what such a task gets today.

## [0.12.0] — 2026-09-17

### Added

- **`lastActivation` is a public export.** `src/supervisor.ts` exported the three stall predicates and kept the function they all read private; `src/index.ts` re-exports the module wholesale, so the name is now part of the package surface, and `lib/status.js` re-exports it beside `stallStands` for the CLI side. It answers when a participant was last REACHED: the newest of the record's `started`, the health `knockedAt` and the health `deliveredAt`, with a failed `triedAt` deliberately left out. Three predicates read it and not for the same thing — `stallStands` and `promptStands` compare it with the participant's last send, while `sessionBusy` compares it with the last end of turn on its no-session branch and answers `false` on `null` — and the guide section spells that out. What asked for the export is a check rather than a caller: a test that asserts "this participant is not stalled" owes itself the same `sent >= since` the predicate compares, and a private maximum over the three marks written inside the test drifts from the predicate the moment a fourth mark is added or one is dropped. Nothing in the package calls it that did not call it before.

### Changed

- **The Cursor hook-event list is the bundle inventory of a named build — twenty-one names, of which five are proven live — and it says which is which.** `KNOWN_HOOK_EVENTS` knew five of them, the set a live spike drove, and `lib/install.js` carried the same five under `CURSOR_HOOK_EVENTS`. Five names say "these work"; they do not say "there are no others", and the list is a GATE: an unknown event name in `.cursor/hooks.json` silently disables every hook in the file, so a name missing from the list turns a legal file into a refusal, while the harness itself would drop the loop guard and the wake channel without a sound. `PB-230` takes the list from the `_E` table of the `cursor-agent` `index.js` bundle, read on 2026-09-17 from the installed `2026.09.10-fd3934a` with no live session, and records that build beside it as `HOOK_EVENTS_SOURCE_VERSION` — a different number from `PROVEN_CURSOR_VERSION` on purpose, since that one dates a lift and this one dates a read. The spike's five stay named, as `PROVEN_HOOK_EVENTS`, and the suite holds them to a subset of the inventory; the other sixteen are documented as inventory only, unseen firing here in `agent -p` or in the IDE. Being outside the five is not evidence against a name either: `afterMCPExecution` is inventory only here while a consumer outside this package hangs a tracker on it and measured it firing on 2026-08-24. The driver writes one event from a constant, so nothing promptobus itself writes moved; what moved is the answer to a foreign name already in a project's file — a `postToolUse` group there used to make `install` refuse and now installs, because the recorded build knows that name, while a misspelling of one (`postToolUseFailed` for `postToolUseFailure`) is still refused by name before any write. The second copy in `lib/install.js` stays a copy, held equal by the suite rather than imported: the installer would otherwise pull the whole Cursor driver graph in for a list of strings. The driver suite's `length === 5` is gone — it read a count nobody could derive — and in its place the list is read: twenty-one distinct names, the recorded build, and the five proven ones inside them. Which names are proven live and which are inventory only is written down in [03-cli](docs/reference/03-cli.md#cursor-hook-events) § Cursor hook events. **What the wider list cannot do is know a second build**, and review round 1 named it: the gate compares names and never a version, so on a build where one of the sixteen unproven names is absent, a file carrying it passes and every hook in it dies in silence — the original failure, moved one build along. Raising `PROVEN_CURSOR_VERSION` (2026.09.02, older than the build the inventory came from) would refuse builds that work today, so what stands instead is a warning and never a refusal: `install` reads the merged event map for names inside the inventory and outside `PROVEN_HOOK_EVENTS` and says the name, the build the inventory was read from, and that another build would kill the whole file without a word. The group is written and the person decides, and a verdict pins both halves — the warning on a seeded `afterMCPExecution`, and silence on an install that writes only proven names. The line is said on four paths, not three: `install`, `--check`, `--dry-run` and `uninstall` share one plan, and `uninstall` leaves a foreign inventory-only group in the file exactly as the other three do, so it warns about it too — named in the code and pinned by a verdict of its own. [guides/install.md](docs/guides/install.md) carries both halves: the validation paragraph says a known-but-unproven name is kept and warned about rather than failing the command, and § After install shows the line, since that block is what a person reads as the whole of the output.

- **`dismiss` and `stop` admit the approver of the task, by the same positive proof `sweep` uses: the three-command cleanup recipe can now be executed by the role it names.** Acceptance of one piece is `sweep`, `dismiss`, `stop` — and the package admitted the approver to one of them. Measured live on 2026-09-16 by two approvers in a row: both were refused with the owner-gate text, whose only route is `mailbox {claim: true}`, and taking the mailbox off a live orchestrator mid-run is not a route an approver may walk. Both did the right thing — reported the refusal and left the worktree and branch alone — and the orchestrator then did the cleanup by hand, three `dismiss` and three `stop` per piece. `PB-231` gives the three commands ONE proof and one home for it: `approverHere` in `lib/store.js`, moved out of `lib/sweep.js` so there is no second copy to drift — a participant of THIS task with role `approver` whose recorded session is the calling one, read from the record's own session the way direct worker↔approver traffic is proven, the full id by equality and the short prefix only where no full id was recorded. The owner gate is untouched and nobody else moved: a stranger and a call that names no session get the same head and the same route they got before, `done` is deliberately not lifted with the pair — closing the run stays the owner's — and a verdict holds it there. What the refusal gained is the reason, where there is one to give (`unprovenApproverLine`): an approver record carrying no session of its own, or one on record under a session that is not this one, with both ids printed; it is said only where the task has an approver record and the call named a session, since a call that can name neither would be told about a door it cannot open either way. [03-cli](docs/reference/03-cli.md#ownership--the-owner-gate-of-done-stop-and-dismiss) § The owner gate now says that the three cleanup commands share one proof and names it, and the `!allowed` paragraph says that for `stop` and `dismiss` that is where the refusal starts rather than where it ends. [ADR-017](docs/adr/adr-017-the-owner-gate-is-a-positive-proof.md) carries an amendment rather than a successor: its decision stands word for word for `done`, and what changed is that the second door now stands on the other two, leaving the ownerless branch as the only difference between the sets of callers. The refusal is built per RECORD and not per task — with one approver on record under a session and another with none, a single sentence about "the approvers" would tell the session-less record a foreign id and agree with neither half, so each is named by its own state and a verdict holds the mixed set.

### Fixed

- **Step 7 of the shared scenario carried two different reds under one title, and the one in the title's own words went red on sound code.** The check asked `stallStands` for `false` on a participant that had just answered, and for `stall.kind === 'unknown'` the predicate is a comparison of two stamps: `sent < since`, where `since` is the newest activation mark. Nothing in the step held that order — the warden re-knocks on its own retry threshold, and a knock landing between the reply and the verdict moves `since` past `sent`. Reproduced by hand on 2026-09-17, no load and no live participant, with the stamps written straight into the task store: one reply and one end of turn, `knockedAt` 60 s after the reply gives `true`, 60 s before it gives `false`. The decision the card asked for: the MECHANISM is right and the TEST owed the precondition. A fresh un-answered knock has had no answer, which is what the `unknown` branch asks about, so giving the send a grace of its own would have weakened the stall predicate for every caller, so the predicate is untouched and no behaviour in `src/supervisor.ts` changes. The step is now four lines instead of one, and each names its class in its own red text. The check takes `since` from the mechanism through the export named under Added below, so the class split does not rest on a copy. The precondition — `sent >= since`, waited for and asserted on its own line, the shape `PB-159.1` used for the `LISTED` branch — is the re-knock class. The yielded-turn line is the timed-out class, split off so an expired `waitFor` is not reported under somebody else's name. The snapshot line, `kind === 'unknown'`, is the lost-session class: a persist session off its tmux server reads as `stale`, which is the second red recorded on a clean CI runner and once locally, and until now a reader could not tell it from the first. The predicate keeps the original verdict name, so no name leaves the suite. The snapshot and the predicate are now read in the two calls right after the wait, with every verdict below told what they said: a `check` detail is eager and `diagnose` reaches tmux, so a verdict standing between the wait and the predicate held the window open for a late warden tick — the very race the split exists to name. The comparison itself gets a verdict of its own in the warden suite: the same record, the same end of turn, the answer flipping with the knock's stamp alone. `node test/promptobus-mixed.test.mjs` green twice in a row, 51/51, exit 0 both times, measured on the branch head that carries this entry. Not closed, and left open on the card: what took the tmux server down under a persist session — reproducing it needs a clean runner and a real `cursor-agent`, and this machine has neither. The class is named in the verdict text instead, which is the alternative the card's own verification allows.

- **A provenance header rebuilt from a record that carried no package field was signed with the READER's own path and version.** `provenanceFromRecord` filled `packagePath`, `packageVersion` and `packageIssue` from the module-level `OWN_PACKAGE` of whichever copy happened to be reading, so a session record written by another copy — an earlier release that had no such fields — got a holder-journal header naming a copy that never raised it. Measured from the tree on 2026-09-17: `provenanceLine(provenanceFromRecord({}))` printed `package=<tree>/package.json@0.11.0`. The reconstruction now reports `package=unresolved (the record does not name the copy that wrote it)`. The lift path is untouched: `launchProvenance` reads `import.meta.url` of the executing copy and always fills the fields, so a live lift line and a fresh record are unchanged. The second half is the check. All five existing provenance verdicts assert the TREE's own package path while running from the tree, and that is true of any resolution at all — including a wrong one taken from `cwd` or from the record — so nothing pinned the discriminator. A second copy of `lib/provenance.js` is now run from another directory and has to name itself (`0.0.0-other`, against the tree's own version), and the reconstruction branch gets a verdict of its own. That copy is one file, which works only while `lib/provenance.js` imports nothing local; the import is wrapped so a local import added there lands as one red verdict naming the cause rather than as `ERR_MODULE_NOT_FOUND` taking the whole file down. Path is the only discriminator when a tree copy and an installed copy report the same version, which is exactly the ambiguity `PB-189` was opened on. Not demonstrated here: that the reported CLI path is the executable copy on a LIVE lift — this worktree carries no installed `promptobus` copy to raise a participant from.

## [0.11.0] — 2026-09-17

### Changed

- **The review subject now lists every file the reviewed address attached to the task — name, type and time — and not only the two schema-shaped records.** On 2026-09-16 both reviewers of one wave raised round-1 majors of the shape «no mutation probe on record» while the workers had attached the probe transcripts minutes earlier as `type=artifact` files, thirteen probes in one of them: the files were in the task files folder, the subject named only `gates-*.json` and `handover-*.json`, and a reviewer that reads what its subject names saw no probe. Each false major cost one worker turn and one reviewer turn. `PB-225` takes the first of the card's two contracts: the list is built from the task journal, the only place that binds a sender to an artifact, so a file cannot be attached and stay invisible whatever the author called it — one line per file carrying the landed path, the type its name claims (`gate record`, `handover record`, `other`) and the send moment, in send order. Only the reviewed address's own files are listed; an artifact from the orchestrator or another participant is not this author's evidence. An address that attached nothing is said so out loud, since an omitted block reads as «nothing attached» — the very report the list exists to make impossible. The two record blocks stay below it, resolved by their stems as before. The contract sentence is one constant, `ATTACHMENT_CONTRACT` in `lib/handoff.js`, quoted word for word by the reviewer's subject and by the worker preamble: the two sides cannot drift apart on it because there is only one copy of it.
  The list carries the moment it was read — the same snapshot stamp the diff beside it already carries — and says that a file attached after it arrives only with a re-review: a list read at plan time that promised completeness in the present tense would file the same false major one round later. An artifact record of that address the store could not read is named in the block by count and note, so a short list is never a silent one — counted from the journal rather than from the store's note, because `readArtifact` moves a corrupt record into `broken/artifacts` before it refuses and the note therefore reaches one reader only: the next `review` would have gone short in silence again.
  A message whose record is in neither place is a swept piece, its file gone with it, and that branch is silent on purpose and pinned by a verdict of its own. The artifact id is checked against the record grammar before it becomes the attic path — the journal is read as raw JSON, and `..` in an id would take the lookup out of the task.

- **The two comment gates are back, rebuilt on a lexer and on multisets, and this time they say what they do not catch.** `PB-172` built them, five review rounds found them weaker than they claimed four ways over, and `PB-186` withdrew both from the release rather than patch a gate that had issued a certificate it could not back. `PB-188` rebuilds the three pieces that failed. The scanner is a lexer carrying state ACROSS lines — an open block, a string, a template literal with a stack for the `${…}` nested inside it, a regular-expression literal, and the continuation after a `*/` followed by more comment on the same line — where the old one restarted at every newline and stopped at the first marker on a line. Measured over the same 85 tracked files: 188 long runs before, 190 after, the difference being three runs the old walk never joined (`lib/model-routing/adapter-cursor.js:16`, `lib/model-routing/cache.js:15`, `lib/worktree.js:280`, each a short block touching a `//` line) and one pair it split (`lib/store.js` 1331 and 1335 are one run of ten). Two of the lexer's states are measured rather than assumed, in the consumer that keeps a gate of the same shape: on 2026-09-16 a stateful scanner without regex state read the backtick inside `/…/` as the start of a template and stopped seeing 66 blocks, 413 lines, in 10 files, and the repair that looked only at the previous character then lost 5 more to nested `${…}`. Both baselines are **multisets**: the length list owes a run once per copy, so a second copy of an already-allowed block is refused, and the link baseline counts targets per file, where a `Set` was hiding 10 duplicate pointers on today's tree — three of them copies of one target in `src/supervisor.ts`. The pending list is regenerated from the tree at the moment the gate is switched on — 190 runs in 10 files, replacing 1282 entries of which 1102 no run answered any more — and from there it may only shrink; `PB-188` does not sweep debt. The trees the gates walk are named and checked through one shared walk: `templates/` had been on the list for four days after its last file went, and a walk over nothing reported the same green as a walk over everything. The link gate goes by comment LINES where the length gate goes by runs, so a pointer trailing code on its line — `foo(); // [x](…)` — is classified like any other rather than dropped with the line it sits on; the tree carries none today, and the count of classified pointers is now held against a walk that does no grouping, where it used to be compared with its own sum. And "the list only shrinks" is held by two literal ceilings on the pending list, since every other check here is satisfied by a list that grew — though not a swap at an unchanged count, which only the fixture's diff separates from sweeping, and contributing says so. Anything that decides whether a line DECLARES a symbol is fed the code half of that line and never the raw one: a `;` written inside a trailing comment satisfied the end-of-statement guard, and `run(job);` read as a declaration named `run` — the closed "a call is not a declaration" defect returning on the new path. Code on a block's own line binds it whether it stands before the comment or after, so `/* … */ export function f() {}` is bound to `f` rather than to whatever follows on the next line, and only the half of that line the comment adjoins is read — with both halves, `a(); /* … */ b();` declared a symbol named `a`.

- **What encloses a pointer is now decided by its line's indentation rather than by its comment's column**, which is a change to binding and not only a repair. A comment trailing code sits far to the right of the statement it ends, and passing that column into the walk outward let the walk accept SIBLINGS: a pointer after `const A = 1;` inside a function was enclosed by `A` instead of the function, and at the top level it was enclosed by `A` instead of by nothing — which turned an `unbound` refusal into a `bound` pass. The green side of that error is why it survived three review rounds.

- **The length gate says how much it read.** Without that it proved nothing: every verdict it carries asserts that a list is EMPTY, and a walk that examines no file fills those lists exactly as a walk that examines all of them does. Measured before the floor: emptying the walk outright — reading and judging not one file — left **11 of 11 verdicts green**, and so did disabling each of its five refusal branches one at a time. The floor is the pattern the pin gate and the tmpdir sentinel already use here, counted rather than assumed: files judged against files tracked, and long runs seen against the number the tree is known to carry, the second a literal that comes down with the ceilings. Found by self-check against this repository's own rule that a gate must say what it looked at, not by review. **The floor turned out to be half the fix**, which the same self-check then measured: it catches a walk that read nothing, not one that reads all 85 files and judges none, since `judged` and `seen` fill either way — four of the five mutations that exposed the original hole stayed green with the floor in place. The walk is therefore a function the checks drive themselves, over real files with a listing written for the occasion, and each refusal is compared with a count computed from the same file instead of a literal — which reddens a branch that keeps one long run of seventy-six as surely as one that keeps none.

- **Every refusal the link gate can give is now produced through the walk, not only by a fixture holding a row it wrote itself.** On a green tree all five refusal classes are empty by construction, so the baseline that guards the class counts sees nothing when the walk gives up on a shape the tree happens not to contain — and refusing is the only thing the gate exists to do. `broken`, `brokenAnchor` and `mismatched` are produced end to end against a real document; `malformedSource` cannot be, because all 109 `Source:` lines in this repository name a file, so its row comes from the walk and its section is written by hand, which is stated where it is done. The direct fixtures stay as what they always were — a check on the predicate rather than on the gate. [contributing](docs/guides/contributing.md) § the sweep now carries the definition of a comment run and, for each gate, what it catches and what it does not.

- **`tsc` now catches dead locals and parameters in `src/`.** `noUnusedLocals`/`noUnusedParameters` are on in `tsconfig.json`; the two errors they surfaced are fixed — `finish()` in `src/v1/engine.ts` dropped its unread `meta` parameter (and the now-unused `meta` destructure at both call sites), and `src/migrate.ts` dropped its unused `node:process` import — plus one more the same flags found on the current tree, an unused `taskDir` import in `src/v1/messages.ts`. A first pass of this card also marked 19 `lib/*.js` files with `// @ts-check`; measured afterward, that marker is checked by no gate and is not clean (1605 diagnostics across those files under a real `allowJs` run), so it has been removed — tracked as PB-139.1. `lib/` stays outside `tsc`'s `include` entirely, by any gate. (PB-139)

- **The Claude driver's `optionRefusal` now reads `effortMinVersion` as a generic per-effort map instead of hardcoding the `ultracode` check against a module constant, and `driver-cursor.js` drops a `fresh` flag `resolveTmux` never accepted.** Before, a second `effortMinVersion` entry (say, for `max`) would refuse nothing — `optionRefusal` special-cased `effort === 'ultracode'` and read `ULTRACODE_MIN_VERSION` directly, never the map. It now looks up `claudeDriver.options.effortMinVersion?.[effort]` and builds the refusal from whatever level and version it finds; a fixture test adds a second entry and confirms it refuses on its own, and confirms an effort absent from the map still passes. Separately, `lib/driver-cursor.js`'s `resolveTmux` had no cache for `{ fresh: true }` to bypass — the flag and the unused `_name` parameter it also carried are gone. (PB-120)

- **`done()` parses the task tree once per call instead of three times.** `sweepWorktrees`, `sweepParticipantSecrets` and `sweepJournals`/`pruneCandidates` each called `listTasks(home)` on their own — a fresh `readdirSync` + parse + schema-validate of every `task.json`, and a broken journal warned about up to three times in one `promptobus done`. `done()` now calls `listTasks` once and passes the array to all three; `pruneCandidates` and `sweepJournals` take an optional pre-fetched list and default to calling `listTasks` themselves, so the standalone `prune` CLI entry point is unaffected. A test fixture with a task journal that fails to parse confirms the warning now prints exactly once. (PB-118)

- **The three availability adapters' duplicated helpers are consolidated into one shared leaf, `lib/model-routing/adapter-common.js`.** PB-149 already fixed the Cursor adapter's missing stdout-error guard; this pass removes the duplication that made such a fix land unevenly in the first place. One `spawnAndCapture(bin, argv, timeoutMs)` now backs the Claude adapter's `runCapture` and the Cursor adapter's `runOut` — the timeout/SIGKILL/stdout-error/close dance that used to be a near-identical copy in each. One `readKeychainSecret(service, { timeoutMs })` backs both adapters' `/usr/bin/security` read (`SECURITY_BIN` is now defined once, not twice). One `requestJson(url, { method, headers, body, timeoutMs })` backs both adapters' single outbound HTTP-JSON call. One `verdict(state, reason, message, extra)` factory, built on one call to `isoStamp()`, replaces four separate verdict literals in `preflight.js`, one in `lib/drivers.js`'s stand-in adapter, and the three adapters' own copies — `checkedAt` no longer drifts between a `Date.now()`-based stamp and a cache-derived one. The Codex adapter's `probeCodex` is split after its `model/list` call into the lifecycle function and a pure `limitVerdict`, matching the shape the other two adapters already used; its own app-server child lifecycle (a persistent RPC process, not a spawn-and-capture) is untouched and stays its own function. No verdict state, reason or message changes — this is a code-structure change behind the existing contract. (PB-81)

- **`lib/model-routing/telemetry.js`'s role-summary algorithm is a re-export of `src/telemetry.ts`'s, not a second implementation.** The two copies of `telemetrySummary` (and the `quotaEvidenceOf`/`trustedQuotaWindowOf`/`canonicalQuotaScope` machinery it calls) were near-identical, and PB-205's review found the same defect in both, three times over — an output-parity test between the two copies stayed green through it, because a defect common to both copies is exactly what that kind of test cannot see. `lib/model-routing/telemetry.js` now does `export { telemetrySummary } from '../../dist/index.js'`, the pattern `lib/warden.js` already uses for its own threshold, and the local duplicate is gone. The test suite's former "public and private summaries stay in sync" checks compared two implementations that can no longer disagree by construction; they are replaced by a single check that they are the same function object, and the semantic checks around them (already present, already asserting expected values) are what is left to catch a defect in the one algorithm that remains. (PB-212)

- **`lib/store.js` no longer re-exports `INTENT_STALE_MS`.** Nothing inside `lib/` read it, and the consumer CLI's own lint gate that quotes its value imports it from the package root (`'promptobus'` → `dist/index.js`, per `src/v1/index.ts`), not from `lib/store.js` — the re-export had no reader anywhere. (PB-127)

- **The long-comment debt of `lib/`, `src/` and `bin/` is swept: 1078 runs over two lines are gone, and not one line of code moved with them.** The rule is the owner's decision of 2026-09-12 — code is self-documenting, an inline comment runs at most two lines, and a nuance that does not fit moves into the documentation. `PB-172` swept part of the tree and left the rest as a pending list; the list had 1282 entries, 58 of which no run answered any more, and 42 more runs sat outside it — 1266 across the 85 tracked code files, of which 1078 lie outside the files another track holds. Every one of them was sorted into three piles and the decision written down per run: **30** carried a fact the documentation did not have and moved there, **11** were a retelling of a section that already said the same thing and were removed against a grep of that section, and **1037** were constraints compressed to two lines. Provenance left the code entirely — task numbers, dates, "review remark", and the retelling of the lines below a comment belong to git and to the tracker. The reason is measurable rather than stylistic: this code is read by agents, and a paragraph above a function is paid for in tokens on every read of the file. What moved into the documentation is what a reader could not have got from the code: **02-host** gained § Launching a process (the `cmd.exe` command-line rules, the three Windows routes, quoting, `PATHEXT`); **03-cli** gained § The session id after a lift, the `done` cleanup order with its 2026-09-02 measurement, the notification budget sized from 768 live messages, the journal-growth measurement behind `PRUNE_DEFAULT_DAYS`, the `history` unit, and the sticky-`knockError` rule; **05-drivers** gained § The two `mcp_servers` transports and the field that kills the config load, § The holder's record watch (with the twelve holders that outlived a run on 2026-09-04), and § A stop returns after the record is gone, not after the command returns (the 2026-09-03 stop timings); **hooks-and-trust** gained the `undefined`-versus-`null` snapshot guard of the watch round; **model-routing** gained § Killing a probe's child: why three lines and not one, and the live-participant cap's own measurement. One misfiled block was corrected in passing: the `HostLegacyLayout` documentation stood above `HostClone`. The pending list itself is untouched — it is `PB-188`'s file, and that card regenerates it from the tree. (PB-227)

### Fixed

- **A `type=artifact` message with no file was accepted and delivered, and a misspelt key was the whole of it.** Measured live on 2026-09-16: an approver sent a gate record under `artifact`, then `artifactName`, instead of `artifactPath`. The tool took the call, dropped the unknown key in silence, and delivered a `type=artifact` message with a body and no attachment; nothing landed in the task files folder. The recipient could not tell a missing attachment from a described one — the type said "artifact" either way — and a less careful sender would have reported "record attached" about a record that did not exist. Two refusals close it. **An artifact message always carries a file:** `sendMessage` refuses `type: "artifact"` with no `artifactPath` and names that parameter. It stands there and not at the tool because that function is where both callers meet — the `promptobus_send` tool and the `send` command, which is built and covered but not registered ([03-cli](docs/reference/03-cli.md) § Send — built, not published) — so both are refused in the same words, both halves are covered by a test, and a later registration of `send` inherits the refusal instead of needing its own. **An undeclared top-level key is refused, not dropped:** all three tool declarations now carry `additionalProperties: false`, and the server checks the call against that same declaration before it resolves the task — MCP clients do not validate a tool's input schema, so the schema alone would have changed nothing. The allowed keys are read from the declaration rather than repeated in the handler: two lists drift, and the drift shows itself only when a key goes missing in silence. The rule is all three and not `send` alone because the defect is the class rather than the one call: `promptobus_mailbox` given `claimed: true` used to drop the key and perform an ordinary take, leaving the mailbox with the session that already held it — a typo that reads as success. **This is a refusal a caller can now meet:** a harness that adds keys of its own to `arguments` will be told which key, by name, instead of having it silently ignored. Nothing in this repository calls the tools with keys beyond the declarations. **The engine holds the same rule one layer down, at the write and only there:** `prepare` refuses a `type: "artifact"` that names no file at all, before the first side effect, so a message built past the adapter — through `send`/`sendSync`, which are public API — cannot claim an attachment it does not carry. Either field satisfies it: `artifact` attaches a new file, `linkArtifact` names one the task already holds, and `finish` writes that id into the record — forwarding a landed artifact without duplicating its blob stays lawful. **The linked id is checked, not trusted:** `prepare` asks whether that artifact record is in this task and refuses `artifact-not-found` naming the id if it is not, because an id of the right shape pointing at nothing would have written precisely the record this whole entry is about — one claiming an attachment it does not carry. It asks by presence rather than by reading: `readArtifact` sets a corrupt record aside on its way to refusing, and prevalidation runs before the routing policy has spoken, so reading there would let a sender the policy is about to refuse move a record into `broken/artifacts` first. Classifying a broken record stays with whoever reads it next. **Record validation is deliberately left alone, and that is the whole of the second review round.** Binding the two fields in `validate` looked like the tidier place and was tried there first; it is retroactive. Every read validates — `readInbox`, `peekInbox`, `history` — and a schema-invalid record is moved to `broken/inbox`, so the rule would have reached backwards over journals already on disk and stopped delivering `type=artifact` records that earlier releases wrote without a file. Two such records were measured in this run's own store, both of the same defect; the earlier one is the incident this task was opened for. A record of that shape now reads exactly as written, and no send can produce another. Tightening what a READER accepts is a protocol-version change, not a patch, and `04-protocol` § Validation says so with this as the worked example. What the attached file HOLDS is still judged elsewhere — the gate record and the handover record against their own schemas. (PB-226)

- **`peekInbox` was the one mailbox read with no fault seam, so its failures were built on the disk instead of injected.** `readInbox` and `glanceInbox` take their reads through `FaultHook`; `peek` read the disk directly, and the tests that closed `PB-193` had to arrange the two failures physically — a dangling symlink for the ENOENT race and a `chmod 000` file for everything else. The second one cost a real guarantee: `chmod` refuses nothing on Windows or under `uid 0`, so the case was skipped there, and the uid-0 branch was never once exercised. `peekInbox` now takes the same `fault('inbox-read', …)` the other two take, under `mode: "peek"`, and `engine.peek` hands it the engine's hook. The two failure cases are errno injections and run everywhere with no skip; the dangling symlink stays as the single witness that the real disk behaves the way the injection claims. That asymmetry is deliberate — an injection proves the classification, and only the disk proves the injection is describing it. (PB-193.1)

- **An orphaned second `Source:` line in [the install guide](docs/guides/install.md).** One section carried two, and only the first is read by anything — the gate takes the line two below the heading. Found by review while the link gate was being rebuilt; the section keeps the first line, which names both the file and its symbol.

- **A fourth wrong pointer binding, of the class the withdrawn gate certified as absent.** The round-four report closed the pointer work with "98 pointers, 0 discrepancies by symbol"; that number came from a check that never compared `section.file`, accepted `if (…)` and a bare call as declarations, and silently skipped every pointer it could not classify — 98 of the 161 it knew about. The rebuilt semantic gate classifies all 173 pointers of the tree into one counted class each and refuses to leave one unclassified. It found that `lib/driver-codex.js` carried a pointer to a section headed `bindParticipantHomeRemoval` — a symbol declared in `lib/codex-session.js`, not in the file the section's `Source:` line named — whose prose describes `sweepParticipantHomes`, declared one line below the call the comment stood above. The heading, its `Source:` line and the pointer now name `sweepParticipantHomes`, and the comment stands above that declaration. Checked against the tree at the commit before the three bindings were repaired by hand: the rebuilt gate is red on two of them (`lib/model-routing/catalog.js` pointing at `mergeWeights`, `src/supervisor.ts` pointing at the symbol `if`) and on this fourth one, and green on the third (`src/legacy-store.ts`), whose defect was a module contract published as the documentation of `seq` while the comment did stand above `let seq = 0;` — a gate reads the binding, never the prose, and `contributing` now says so.

- **Removing a blob raced a publication path that took no lock, and the question "does anything still name this payload" had two answers.** The piece sweep asked it twice — the surviving artifact records, and the hard-link count of the payload — and both readings under the journal lock, which publication never takes: between `stashBlob` finding the payload already there (`EEXIST` is dedup) and the record landing, nothing a sweep can see names that payload. Recorded as not reproduced when it was found; it reproduces now, in `test/v1-races.test.mjs`, with a real second process held inside the publication stretch — the sweep removes the blob and the sender's artifact reads back as `artifact-not-found`. Two changes close it. **One reading:** `blobNamed` in `src/v1/artifacts.ts` is the single answer — a metadata record of the task, or a hard link beyond the blob file itself — and both `orphanBlobs` and the piece sweep ask it. Both callers hand it one snapshot of the records rather than re-reading them per blob — that was measured at 400 artifacts, 3.8 s against 16 ms — and what differs is when the snapshot is read: the sweep reads it after the lock is in hand, so a record that landed while it waited is in it, and the only later change is the sweep's own removals, tracked as they are made. The sweep's separate `held` set is gone with the second reading it was. **`orphanBlobs` answers a wider question than before, and that is the point:** a payload the task's `files/` folder still names by a hard link is no longer listed as nameless, because that folder entry is a name and a human reads it. The one reading is what the card asked for; splitting the predicate back apart for the listing would restore the defect it closed. **One short lock:** a send that carries an artifact writes the payload, names it and writes the record inside `.lock-blobs/` of the task, and the sweep decides under the same lock, so the window is not observable. It is a lock of its own and not the journal's on purpose — the journal lock is held by a sweep across `git worktree remove`, and an artifact send waiting on git would start refusing with `lock-busy`. A send with NO artifact takes no lock at all: measured over 1000 sends of each shape, three runs, an ordinary message is unchanged (p50 1.58 ms with the lock against 1.60 ms without) and an artifact send pays about half a millisecond (p50 3.60 ms against 3.15 ms). `withDirLockAsync` is new beside `withDirLock` because the streaming publication path writes its blob across an await, where the synchronous wrapper would drop the lock directory the moment `fn` handed back its promise — **and where its nesting licence would be worse than useless.** That licence exists because a synchronous stretch cannot be interleaved, so a nested call is the same critical section; an `await` breaks the premise, and a second publication of the same process would have read "we already hold it", run with no lock at all, and had the directory removed from under it by the first. So asynchronous holders of one lock path queue inside the process and each takes the directory in turn, the licence is neither asked nor granted there, and a synchronous take arriving over a live asynchronous holder of this process is refused outright rather than waited out, under a code of its own, `lock-self-async`: `lock-busy` means "wait and retry", and here the wait is `sleepSync` and would block the very loop that has to release the lock. The refusal words reach the caller through `DirLockOptions` like the other two, so the engine raises a typed `PromptobusError` and the adapter a `GateError`, not a bare one. **The asynchronous path waits for a foreign process on a timer, not on `sleepSync`.** A send carrying an artifact would otherwise freeze the loop of its own session for the whole of another process's hold — up to the full 5 s wait — stopping every unrelated timer in it; the wait is now the send's and not the loop's. The sweep line about a blob that stays now names both reasons rather than only the hard link. (PB-208.1)

- **Artifact hand-off was silent on three counts: a hidden name, a byte-for-byte repeat, and a record nobody checked.** All three were measured live in one round of hand-off and sit on one path — a participant sends a file, the mechanism takes it, a reviewer reads it. **The name.** The landed name was taken from the source basename with the leading dot untouched, so a record sent as `.gates-artifact-r2.json` landed hidden: an ordinary listing missed it, a `*.json` glob missed it, and the reviewer handed that folder as its source of evidence wrote into a formal verdict that none of the attached records carried the tree — the record was there, with the right sha. Leading dots are now dropped at placement, the collision numbering runs on the name that LANDS (so two sources differing only by a dot take two names instead of one shadowing the other), and a name of nothing but dots is refused to the sender with the reason rather than landed invisibly. A name with no leading dot is unchanged to the byte. **The repeat.** Blobs are content-addressed, so a second send of the same payload is lawful and free — two names, one inode — and the confirmation reported only the new name: a participant who believed they had sent a corrected record got a reply indistinguishable from the reply to a real new version, and the mechanism was holding the hash equality that would have said otherwise. The send reply now names the file those bytes already landed under, and how many names the payload has where the hard-link count is readable. It is an assertive line, not a warning: the repeat is lawful. The reading is of content, not of the source name. **The record.** The package published `schemas/v1/gate-record.schema.json`, required the record of every participant, and checked it nowhere: of three records attached in one run, two diverged from the schema in two different ways, and both authors learnt it from a reviewer reading the schema by eye. A document its own schema rejects is not evidence and looks exactly like evidence until someone opens the schema. `send` now reads a file whose name claims the stem — `gates-*.json` and `handover-*.json`, through the same two recognisers `review` resolves the attached records with, one door and not two — checks it against the published schema BEFORE its payload is written, and refuses an invalid one naming the faults by field. A valid one passes byte for byte; a file claiming neither stem stays opaque bytes. The reader is `lib/schema.js`: it reads the schema FILE rather than a second copy of its rules, so the two cannot drift, and it throws rather than answering on anything it does not implement — a keyword by name, and also a SHAPE it reads only bare: `$ref` and `oneOf` answer a whole node and drop its siblings, and `items` and `type` are each read in one form only, so a node carrying `$ref` or `oneOf` beside another constraint, a tuple-form `items`, or a `type` given as a list of names is refused rather than half-checked. `propertyNames` judges a field name by its WHOLE subschema rather than by a `pattern` field lifted out of it — a bound or a `$ref` standing beside the pattern had been read as no constraint at all, and a name the schema refuses went through. A partial verdict handed over as a pass is the failure the check exists against. The worker preamble states the same two halves as the reference does — the bus checks the shape and refuses; it does not run the checks or grade them — and a regression holds the two sentences against each other, because the preamble had said both. `ajv` stays the reference in the suite and the package still ships zero runtime dependencies, paired the way `test/v1-validate.test.mjs` already pairs the four engine models: one fixture set, two verdicts, agreement on every document. The mutation probe gets no place in the gate-record schema, deliberately — a probe is not a gate command, and its home is the handover record's `mutationProbe` check; the worker preamble now says so where it asks for the record. (PB-217)

## [0.10.0] — 2026-09-16

### Added

- **The worker preamble now asks for a handover record beside the gate record, and the reviewer preamble is told to read it.** The gate record made "the gates are green" a machine fact; what stayed prose was everything that makes a green run mean anything. `schemas/v1/handover-record.schema.json` is the second shape: the branch sha, the base sha it was compared against, and five checks — the difference of verdict NAMES against the base commit (counts are not this check: a suite going 244 to 242 to 246 reads as growth while a guarantee is gone, and a removed test never turns red), the mutation probe with the `file:line` broken, the exit code of the run made WITH the mutation in place and the names that reddened, `git status --porcelain` at both ends of that probe, every red called environmental with the same verdict run on the base commit and the symmetric difference of the failure sets, and every gate command that was not run with its reason. All five are required: a check left out is not a check passed, and the only way past one is its own `notRun` branch carrying a reason — a check honestly declared impossible passes, because a form with no such branch rewards invented numbers. Each check refuses its own defeat in the schema rather than in prose: a removed name with no reason, a probe whose mutation never applied, a probe that reddened nothing, a dirty tree at either end, a claim of "environmental" whose base run is green, and a skipped gate with no reason are all refused. The interrupted run — the one that prints `0/1` against a base of 26 and reads as catastrophe — is caught by `verdicts.unaccounted`, which the author computes and states and which only accepts `0`: JSON Schema cannot compare two of its own fields, and PB-201 had already refused to pair a schema here with a second validator. A separate schema rather than a wider gate record, because "what I ran" and "what makes that run mean anything" are two claims. Two of the three preambles carry the requirement with different duties — the worker attaches the record as `handover-<worker slug>.json`, its own `artifact` message before the result, quoted in the **Gate** line after the gate record's; `review` resolves every `handover-*.json` into the first prompt and the re-review, and the reviewer names which checks are absent or `notRun` while running nothing itself; the approver carries no such record, because it merges rather than writes the code and its own gate run is the integration gate. The loop guard holds a turn on either record now, not only on the gate record: an artifact sent with the result still owed is one state whichever of the two it was. The package delivers the requirement and publishes the shape: it runs no check and validates nothing at `send`, for the reason the gate record already gave — an artifact's bytes are opaque to the engine. (PB-213)

### Changed

- **Windows is outside the participant contract.** Owner decision of 2026-09-16: participant sessions — a Cursor tmux pane, a Codex app-server holder — are lifted on macOS and Linux only, and neither driver probes, adapts or promises Windows. The two cards that carried the Windows line (Cursor headless turns, the Codex holder on Windows) are closed with this boundary written into README § Requirements and the CLI reference, not returned to the queue. The CLI and host layer stay cross-platform.

### Fixed

- **The owner-home rule's exemption and its detector now read one token boundary.** They used two grammars for where a path token ends: the detector accepted `~` inside a child path, the code that strips an exempt literal did not — so a text beginning with an allowed prefix and continuing with `~` had the prefix removed and yielded no finding, though what was left is exactly the shape the rule exists to catch. `\`, `%` and `:` escaped the same way. The claim the rule makes — a longer path built on an exempt prefix is still a finding — did not hold for those. `TOKEN_STOP` is now the single source: it builds the detector's character classes and the `isPathCharacter` the exemption reads, so a character the detector would have taken into the path keeps the literal in place and the finding stands. The regression walks every exemption against every accepted character and against the delimiters the detector refuses, so widening one side alone reddens it. The set of exempt fixtures is untouched. Measured on this tree: no tracked file changes verdict, and the card's claim that one such string was live in a session fixture does not reproduce here — the detector matches nothing containing `~` or `\` across all 939 tracked text files. (PB-200)

- **`peek` no longer reports a message it cannot open as a message that is not there.** The read was wrapped in a `catch` that skipped every refusal, and the comment beside it named only the one where skipping is right: the file went between the listing and the read because its owner claimed it, and that owner will deliver it. That is `ENOENT`. `EACCES`, `EPERM`, `EIO`, a directory where a file was expected, a full or unmounted volume — each was dropped with the same silence, and the caller was told the inbox held one message fewer. The function already had the channel for exactly this fact and used it for records that do not parse or do not validate, so the one kind of unusable record going nowhere was the unreadable one. `ENOENT` keeps today's behaviour; every other refusal becomes a `BrokenNote` with its errno and `attic: null`, because the ref stays in place for a later visit. **What `peek` returns for an unreadable ref is now fixed: present and broken, never absent, and never an error of the peek itself** — the same policy `read` and `glance` already had, so all three mailbox readers now agree. Run 0912c made the case concrete rather than hypothetical: its participants hit read and write refusals repeatedly and with system codes, and in an environment that refuses reads a peek that drops what it cannot open reports "no mail" where the truth is "cannot read the mail". (PB-193)

- **Who the dead-warden advice reaches, and where the turn return stops, is written down.** The workspace root guard prints `NO WARDEN … raise it` for every active task whose warden is dead, whose mailbox owner is alive and whose inbox has unread — and the circle of recipients is every session standing in the workspace root that is not already on the bus, owning the task or not. `sessionOnBus` is the one door above it, holding back four: a declared participant, a session with a task binding, the owner of any active task, and a session written on a participant record of any address but `orchestrator` — a worker or reviewer whose `PROMPTOBUS_ROLE` is absent from the environment while its id stands in the journal. The owner is not left without the advice — it reaches the same line through the loop guard at its own end of turn. The behaviour is the owner's decision of 2026-09-14 and is unchanged: the state the advice exists for is an owner alive but making no moves because it waits for a delivery nobody is left to make, and its own hook never fires while it waits, so a session with no connection to the task is the only one in that root able to notice. One run was lost exactly that way on 2026-09-13, four hours fourteen minutes of silence with the owner alive. What was missing was the line that makes it affordable, which existed in the code and nowhere in the text: **the advice reaches every such session, the turn comes back only to the owner** — `successorHint` answers code 0 and never code 2, and the turn is returned only where the guard has already proven the session is the address it guards. The reference states both halves and a check pins them: a session that owns nothing gets the advice and does not get the turn back, the owner gets both, and a live warden is silence for either. (PB-224)

- **The live-participant ceiling is pinned per harness, and the reference says where in the `balance` order it sits.** The gate itself shipped with the per-harness cap: `caps.liveParticipants.<harness>` is read after the representatives are picked and before the harness is chosen, so a harness at its ceiling is out of the pace comparison rather than penalised inside it. What was missing was the evidence and the statement of order. The reference now names the read as step 3 of the `balance` procedure — after the representatives are picked, before the pace comparison — and fixes what happens at the ceiling — the choice moves to the next candidate under its own ceiling, never a refusal, with the fallback to the uncapped comparison only when every paced harness is at its ceiling. One routing case per harness backs it: each of `claude`, `codex` and `cursor` gets a snapshot where it leads on pace, reaches its ceiling with a live participant, loses the pick while keeping every score, place and representative byte for byte, and comes back into the field when the ceiling is one above the live count. (PB-198)

- **A merged-catalog warning now names the layer whose value raised it.** `validate()` returned `priority-duplicate`, `priority-not-canonical` and `ladder-indistinguishable` with `code`, `message` and the tuple ids and nothing else, so a warning the shipped catalog raises and one the reader's own overlay raises were the same record — and "is this mine?" is the only question that decides whether to act on one. Measured by the consumer: a personal overlay that collapsed two rungs of a ladder produced `ladder-indistinguishable` with an empty `errors` list and no layer field anywhere in the result. All three now carry `layer`, the highest-precedence layer among the values the warning was raised from, and `catalog` when no overlay patched any of them: layers apply in precedence order, so the last one to touch a value in the finding is the one that made it appear. The values each code reads are fixed and stated — the two canonical priorities; the priorities that set the order together with the `quality` ratings that set the verdict; every rating of both rungs, `roles` being a key no overlay may patch. Provenance for these three is defined and always present rather than absent-or-maybe: `mergeRouting` already recorded `sources.ratings.<tupleId>.<rating>` and now records `sources.priority.<tupleId>` beside it, which is the same record the existing finding `layer` is read from. `quality-floor-alias` is unchanged — it always carried one. `models validate` prints the field the way it prints a finding's, as `<code> · <layer>: <message>`, leaving the plain `<code>: <message>` where a warning carries no layer: the question is asked at the command far more often than at the library call. (PB-197)

- **`near-limit` now says in the output what it measures and when it falls silent.** Three lifts printed `warnings: unknown-remaining, near-limit, near-limit` while `models`, read a minute later, printed a set with no `near-limit` in it at all — and the orchestrator went looking for an exhausted window that did not exist. Measured in the code rather than inferred: both outputs read one `decision.warnings`, so for ONE decision the short line and the detailed section print the same codes in the same order, and the dictionary was already single — `DECISION_WARNINGS`, the `warningCode` enum of the decision schema and the reference table are cross-checked in both directions. What differed was the decision, not the vocabulary: `near-limit` is computed from the pace of each harness's best eligible representative, and it is deliberately not raised when the strategy it would propose is the one already running, so one snapshot raises it under one strategy and not under another. The detailed output now says both of those once under the `warnings:` heading — once per document rather than repeated in each of the three lines a short account set raises — that it is a pace measurement rather than a missing limit source, which is `unknown-remaining`, and that it is not raised at all under the strategy it would propose — and the reference says the same. The property itself is now locked rather than described: a test compares the short line's codes with the detailed section's over four decisions, refuses a code outside the declared dictionary, and holds the negative control — a snapshot with no warnings prints `no warnings` on the short line and no `warnings:` heading at all, never a heading with an empty tail. (PB-216)

- **A red run now carries the machine's load beside it, and four wall-clock budgets that measured the neighbours instead of the mechanism are gone.** With four worker suites on one eight-core machine (load averages 115–198) reds appeared in files beyond the preflight test, each on a wall-clock budget rather than on a broken mechanism. The runner now prints what it can measure — cores and the load average before the pool, the load before the serial group, and for every failed file the load when it started and when it finished — with a line saying the load is the machine and not the run. It does NOT classify assertions: it cannot see one from the outside, and a hand list of "files with budgets" would rot with no sentinel able to catch it. Three checks stopped depending on neighbours outright: the `long-first` fixture in `promptobus-driver-codex.test.mjs` dropped its own 3 s lift window and takes the stand's 20 s, because the subject there is that the lift SUCCEEDS (the neighbouring `worker:hang` fixture keeps 3 s — its subject is the refusal); step 5 of `promptobus-cursor-wake.test.mjs` waits for the previous writer to let the inject lock go and asserts that precondition on its own line, instead of racing the step-4 wake; and every verdict check in `model-routing-adapter-cursor.test.mjs` moved off the 10 s budget onto a named `VERDICT_BUDGET_MS` of 60 s — a hundredfold margin over the 0.5 s a probe costs here. The three checks that measure a deadline itself keep their own 300, 700 and 2000 ms and never see the new number; one further check with a wall-clock assertion does take it, "the probe does not block the event loop", and is left there deliberately — its assertions are a counted tick and a lower bound on elapsed time, neither of which a raised upper bound can break. `lockFile` is exported from `lib/cursor-persist.js` so the wake test asks production for the lock path instead of spelling it. File duration is unchanged by that raise: 16.2 s against 16.3 s before.

- **The sweep suite stopped making its own fixture in the working directory, and a bare `mkdtemp` prefix is now a named red rather than merely absent.** `tmpdir-sweep.test.mjs` asserts of `release-gates.mjs` that its run directory is made under `os.tmpdir()` by absolute path, and then built its own owner fixture with `mkdtempSync('promptobus-sweep-owner-')` — a bare prefix Node resolves against `process.cwd()`. On a runner with a writable working directory the suite littered the tree it was testing; for a bus participant, whose shell cannot write its own worktree, the call refused `EPERM` and the file yielded no verdicts at all — measured: `mkdtemp 'promptobus-sweep-owner-XXXXXX'` refuses `EACCES` under a read-only working directory. The fixture now goes through `path.join(os.tmpdir(), …)`, and the prefix sentinel — the one already grepping `test/` and `scripts/live-*.mjs` in both directions — gained a third question: every `mkdtemp`/`mkdtempSync` whose first argument is a string literal not beginning with `/` is reported with its prefix and its file. The second path the card named (`SOCKET_FIXTURE`) had already left with PB-206.3, which replaced it with `makeSockDir('a2s-')`.

- **Eight status-line pickers in the suite stopped passing on a miss.** A check took a participant's line out of `promptobus status` with `find((l) => l.includes(ADDRESS)) ?? out` and dropped the WHOLE output into the assertion when the line was not found: the regexps then found their text on a neighbour's line and the check went green having picked nothing, while a negative regexp (`!/STALLED/`) went red about somebody else. Proved on `8452e1c` by a mutation probe — both pickers in `promptobus-driver-codex.test.mjs` neutered to `(l) => false` at once left the file `exit 0`. Each now matches the WHOLE address plus the `·` that follows it, because one address contains another (`worker:hang` is a prefix of `worker:hangchild` and `worker:hangwrite`; `worker:cdx` of `worker:cdx-second`) and the journal order is not the test's to rely on — `upsertParticipant` moves a touched record to the tail. A miss falls to `''` so it reddens, and the miss gets its own evidence instead of an empty third argument to `check`. The card's inventory was short by two: `test/driver.test.mjs:574,587` carry the same shape into `assert.ok` and are fixed with the other six.

- **The owner gate of `done`, `stop` and `dismiss` is now a positive proof, and a call with no session identity is refused.** `ownership` answered `gated: false` — pass — whenever it had nothing to compare, so a shell carrying no harness identity variable reached `done` over **any** task in the store, a live foreign one included: the close stops participant sessions, removes their worktrees and deletes branches it judged merged. The answer now carries two fields that are not each other's negation. `allowed` is the right, granted only when the call names a session and that session is the recorded owner; `gated` keeps its old meaning — the call names a session, the task names an owner, and the two differ — byte for byte, and every advisory reader that takes it is untouched: the foreign-mailbox heading, the unread tail, the `status` warning, the attach gates of `spawn` and `review`, the restamp door of `retitleTask` and the guard's successor hint must all stay silent where nothing can be compared. `right` names which of `owner`, `ownerless`, `no-identity`, `foreign` and `other-address` it is, and the refusal is built from it: `unprovenOwnerLine` says what could not be proven instead of what was missing, and every branch of `ownerRoute` is walkable by the caller that gets it: `mailbox {claim: true}` goes to the owner whose daemon died, a task with no recorded owner has no owning session to send anyone to and its route is any session that names itself, and an environment where two harnesses name themselves is told to remove a variable — that last case carries the resolver's own reason in the head, so the gate does not call `contested` an absence in the same pass PB-218 separated the two. **A task with no recorded owner belongs to nobody, and the exception is named rather than silent**: the owner is written only when the environment supplied identity at creation, so nothing beyond "the caller named itself" can be proven about such a task, and exactly that much is asked — a session naming itself passes, a call naming nothing does not. Tasks opened before the owner mechanism existed need no migration for the same reason. The cost is named too: a shell with no harness identity can no longer run these three verbs, and the route is a session that names itself. An address other than `orchestrator` grants no right at all, and has no refusal text either — its `owner` is `null` because the gate never looked it up, so both builders throw rather than print "records no mailbox owner" about a task that has one. The piece sweep keeps its own gate — the directions now agree, but `requireSweeper` also admits the approver of this task and refuses an ownerless one. ([ADR-017](docs/adr/adr-017-the-owner-gate-is-a-positive-proof.md), PB-223)

- **Two identity variables at once are no longer reported as none.** `resolveSessionIdentity` returned `null` both when nothing in the environment named a session and when more than one variable did, and the direct `worker` ↔ `approver` route refused the second case with the words "the calling harness supplied no session identity" — sending its reader to hunt for a variable that was there twice over. Measured live on 2026-09-13: `promptobus mcp` started from a shell one harness had raised, with another harness's variable set by hand; sending to the orchestrator kept working while the direct route refused, so the participant saw a selective failure explained by the wrong cause. The resolver now names its state — `resolved`, `none`, `contested`, `contested-records` — and both contested reasons list the variables found and say to clear the environment down to one of them: what is missing is nothing, what is extra is a variable. The refusal stands rather than picking a winner, because a silent preference would hand the participant somebody else's identity. `sessionIdentityReport` gives the whole answer to a caller whose refusal must say why, and the direct route uses it; with no variable at all the old wording is unchanged, because there identity really is absent. (PB-218)

## [0.9.0] — 2026-09-14

### Changed

- **`promptobus review --approver` lifts an approver after a reviewer result is on record.** The flag requires `--task`, refuses when no matching `type=result` from `reviewer:<slug>` at the same review subject is in the journal, and does not judge that result. Session cwd is the clone root with the worker worktree attached through `addDirs`. Only Claude Code lifts an approver; Cursor and Codex refuse before start with a named reason. Routing keeps the raw harness declaration for constraint checks and a separate eligible set for preflight. Repeat lift reuses an alive or unknown session. Gate-record `by` accepts `approver:<slug>`. ADR-015 records the single-harness release and points clone-root rental at PB-222. (PB-206.5)

### Added

- **`promptobus sweep <address>` cleans up after one accepted piece and leaves the task active.** Before it, everything the mechanism cleaned it cleaned for a whole task: `done` swept worktrees, `dismiss` dropped a watch, `stop` killed a session, and `engine.prune` refuses on an active task — while at the moment one piece is accepted the task is active by definition. The verb removes the participant's worktree and its `worktree-` branch on the same two content measurements `done` uses, the metadata records of the artifacts it sent, their `files/` entries and the blobs no surviving record names, its files in `workers/` and its contact point. Its gate is not the owner gate of the other three, and the difference is the direction of the default rather than the set of callers: `ownership` passes when a task records no owner or a call carries no session identity, which is a considered fail-open for the owner's own run and would be an open door here. The right is proven positively — the task mailbox owner, or an approver of this task holding its own recorded session — and every absence refuses. A closed task is refused too, explicit `--task` included. A session that is not dead refuses before anything is touched, naming `promptobus stop` as the step before; the destructive stretch then runs under the task journal lock, re-reading the record and the liveness there, because `spawn` writes through that same lock and a re-lift would otherwise land a new worktree under a walk taking the old one away. Every removal path is built from artifact-record fields, so every record is validated against the schema and must stay a direct child of the task's own directories before anything is touched; a record the sweep cannot read stops it with nothing removed. A blob leaves only when neither a surviving record nor a second hard link holds it. A worktree the journal names and disk does not have is its own state: nothing of that piece is judged taken, and its blobs and files stay with the branch. A piece whose merge is not provable keeps its tree, its blobs and its files, and the command says so instead of removing them silently. The keep list — the journal, the canonical messages, the mailboxes, `health.json`, `supervisor.log`, `stalls.json` and the `waits/` sidecars — is a check rather than a comment: `keptBy` refuses a removal aimed inside any of them, and the suite closes the swept task afterwards and reads the telemetry row back, because those paths are exactly what `recordTelemetry` reads. The `brief-<slug>.md` and `review-<slug>.diff` of the piece stay: they are the evidence the acceptance rests on, not its product. ([ADR-016](docs/adr/adr-016-cleaning-up-after-one-accepted-piece-is-a-verb-of-its-own.md), PB-208)

### Fixed

- **`workers/<stem>.settings.json` is now removed with the rest of a participant's files.** The secrets sweep of `done` took the contact point, the mcp-config and the `<stem>.*` directories, and the settings file was in none of those lists: a task closed on 2026-09-10 still carried three reviewer settings files. It now leaves on the same condition as the mcp-config beside it — the participant session is dead. (PB-208)

- **The built-in finding format now defines the severity label instead of only listing the three names.** The label is the finding's cost — what breaks, or stays unverified, if nobody fixes it — and `critical`, `major` and `minor` are anchored on that axis. The size of the fix is not the label and belongs in the suggested fix: a two-line fix is `major` when the cost is high, and a large rewrite stays `minor` when the cost is low. Writing in prose that the cost is really higher than the label is forbidden, and doubt resolves upwards, because an understated label hides an expensive finding among cheap ones while an overstated one costs one more read. Measured by the consumer on 2026-09-12: of 154 findings labelled `minor`, 123 were `minor` by cost. A module skill that declares a scale of its own is untouched — the definition sits in the built-in branch alone. (PB-210)

- **Artifact hand-off order is now guarded at send and at turn end.** Only a **landed** filename that appears in the **Gate** or **Decide** line counts as a claim — dotted prose that matches no task artifact is not judged. `send` refuses a landed name with no `artifact` message from any sender; citing another participant's landed file is lawful. The canonical result carries the first claimed name in header order that this sender also sent, in its frozen `artifact` field; a citation alone leaves the field unset. A result with no landed-name claim stays lawful. The loop guard blocks ending a turn after a gate-record artifact with no `result` — a case answer expectation cannot see because any send clears `answerOwedSince`. (PB-204.2)

- **The warden working ceiling no longer ends the warden process while participants are alive or mail is unread.** `beatRound` defers `WARDEN_TOTAL_SEC` until the task is idle and keeps a separate unconditional `WARDEN_ABSOLUTE_SEC` (72 h) with its own journal line. Before exit the warden writes `waits/warden.exit.json` when it can; the loop guard names the prior reason on the orchestrator's next Stop and clears the mark then — not on warden claim. With no live warden it prints `NO WARDEN` and the raise route; with a successor already watching it names the gap and unread mail without that mark or route. Without a note, unread mail alone triggers the raise route when nobody is watching. No-note dedup keys include the warden generation bumped on each `claimWarden`, and drain clears the hint marks so a second death with the same unread count warns again. The workspace root guard uses `contactSocketPath` for `#<turn>` wake sockets.

- **Cursor and Codex cannot lift an approver.** Cursor reads project configuration only from the selected workspace's `.cursor/`; Codex would have to write into the shared clone root to reach the session. `review --approver --harness cursor` or `--harness codex` refuses before start; routing and the catalog carry approver tuples on Claude Code only (`approverLift` there alone). (PB-206.5)
- **Approver lift binds reviewer results to the assignment watermark, not the diff snapshot.** `reviewerResultSent` requires a result at or after `metadata.reviewAssignedAt` (stamped from the sent `type=task` on re-review, from lift on a fresh reviewer, or `metadata.started` on older records); reuse and journal skip require matching `repoAbs`. The removed clone-root liveness check is not replaced — one approver per clone is caller discipline (ADR-015). `harness-refused` is declared in `ERROR_CODES`; `models --role approver` on a harness-only workspace prints `chosen: null` and exits 0. (PB-206.5)

- **The reviewer-result gate matches the review subject, not the address alone.** `--approver` requires the matching reviewer participant and its recorded `repoAbs` to equal the supplied path before routing or reuse; a same-slug result from another clone no longer unlocks a writable approver in the wrong tree. (PB-206.5)

- **Codex approver lift no longer mutates the shared `PARTICIPANT_ARGV` tail or drops the prompt.** `planApprover` builds the prompt before `driver.prepare()` and assigns it through `launch.prompt` only. The approver hand-off may cite another participant's landed artifact by name — the worker gate record in the **Gate** line is the usual case — while own artifacts still bind only to the sender's message. (PB-206.5)

- **The Codex driver's LISTED check no longer depends on how fast the file runs.** A stale inspect view inside the 30 s registration window prints "is starting" by design, and on a fast runner the check reached that branch — locally the record was only 3.5 s past the window. It is now aged past the window by hand, with the precondition asserted. Aging moves the record to the tail of the journal, so its status line is picked by the whole address rather than a substring a neighbour's address also carries (`worker:cdx-second`), and a picker miss now reads as empty instead of falling back to the whole output. Test-only; the status branches themselves are unchanged. (PB-159.1)

## [0.8.0] — 2026-09-13

### Changed

- **Participant telemetry now separates usage from throughput evidence.** PB-202 documents Codex's `event_msg.token_count.info` rollout producer, refuses the participant-owned rollout as a throughput-sidecar source because it is outside the bus and lacks model-active time, and states that attached-session spend (`0/36` in the baseline) is unavailable. `turns` is a bus-message count, not model effort. (PB-202)
- **Run telemetry now retains a descriptive summary by role.** `done` projects mailbox idle and delivery latency into additive record fields before the journal can be pruned; the typed `promptobus/telemetry` entry exposes wall-clock time, bus-message counts, timing and the bottleneck role, while run-wide quota evidence keeps harness/window coverage instead of assigning overlapping deltas to roles; same-harness records without a matching window count as unavailable, malformed windows contribute no quota evidence, absent or invalid scopes never become account-wide, only validated measured quota coverage makes a role `ambiguous`, and incomplete per-role numeric coverage stays null (including `bottleneckRole`). Missing per-role quota cost, model turns, model-active time and attached-session spend remain unavailable, and no journal content enters the projection. (PB-205)

- **The Cursor event-loop adapter check no longer relies on machine-speed process launches.** Its `event-loop-probe` fixture mode makes each of the two stub replies wait a fixed 100 ms, so the elapsed guard is a deterministic validity precondition while `ticks >= 5` remains the evidence; other fixture modes are unchanged. (PB-209)

- **A result body now opens with a fixed four-line header, and the rest travels as an artifact.** Across 73 runs a `task` body had a median of 1340 characters and a `result` body 5254, with p90 9870 and a maximum of 28 593 — 6.57 M characters of results in total, all of it converging on one sequential reader: the orchestrator takes 2921 messages from workers and 489 from reviewers against 1831 it sends. What it needs from a result to act is four facts, and it read all of the rest to find them. The head of a result is now **Done / Gate / Open / Decide**, the body is bounded at `RESULT_BODY_MAX` = 2400 characters, and whatever does not fit goes into a file attached with the `artifactPath` that already existed. The number is measured rather than chosen: five real bodies already written in this shape ran 1035, 1389, 1674, 2045 and 2100 characters, so the bound sits above what worked — a header the rule refuses would be a defect of the rule. **The Gate line is never omitted**: a gate that was not run says `not run, because …`, because an omitted line reads as a green one, and the reviewer — which runs nothing by [ADR-009](docs/adr/adr-009-reviewer-resolves-no-discrepancy.md) — writes that line on every report it sends, which is what keeps it from being an escape hatch. **The rule is stated in the preambles this package ships** (`lib/spawn.js`, `lib/review.js`) rather than in a consumer's own rules: a convention that lives only in the caller's rules is not delivered to a participant the package lifts. Both preambles take the number from one constant, since a number written twice drifts and the reviewer would then hold a different rule from the worker it reviews. **The overflow rule is the worker's alone.** A reviewer cannot attach anything — file writes are denied to it and its isolation text forbids publishing an artifact — so for a reviewer the bound covers the header and the findings follow it unbounded in the same body; a limit its holder cannot satisfy is a wall, and one broken silently on every long review. Nothing in the schema changed — `message.schema.json` is byte-identical, the type list stays frozen (PB-131), and the header is a convention inside `body`. (PB-204)

- **Artifact names in result headers now use the landed name.** A worker sends the result artifact first, reads the name from the immediate reply to its own `promptobus_send` call — not from the passed path or the orchestrator — and, without ending the turn, quotes it in the header. The rule applies to gate records too: the bus may number a colliding stem, while reviewers have no artifact filename to name. (PB-204.1)

- **Model-routing role projections now share one canonical runtime contract.** `ROUTED_ROLES` is a frozen list; catalog defaults derive their role maps from it, compatibility exports are identity aliases, and resolver, validation, live-tuple accounting and telemetry use it. JSON schemas remain static artifacts under an explicit parity test. Unknown or deliberately unrouted roles are rejected in policy and selection and excluded explicitly from live projections. (PB-206.4)

- **Participant records now expose an unrecorded session at write time.** A lift without a confirmed session writes `metadata.session: null`. `promptobus status` keeps its existing session-state line based on `metadata.name` and the supplied snapshot, and adds `session reference was not recorded at write time`; for a legacy record with neither `session` nor `sessionId`, it adds `participant record is incomplete: no session reference`. Thus a missing reference no longer disappears into a liveness claim: the snapshot can still say `session "…" is alive` or `session "…" is not in the list`, while the journal honestly says what was not recorded. The 2026-09-04 `worker:lint` record had neither field; its 33-second gap between `started` and the supervisor's missing-registry note does not establish whether a session existed briefly, while the current dead-lift path passes null to `persist`. (PB-203.1)

- **A worker's gate claim now travels as a machine record, not as a sentence.** The reviewer runs nothing — `Edit`, `Write`, `Bash` and the rest are denied to it by [ADR-009](docs/adr/adr-009-reviewer-resolves-no-discrepancy.md), and 242 reviewer sessions made 0 Bash calls — so "gates green, 52/52" in a result was indistinguishable from a wish, and the one participant whose attention is scarcest re-ran the gates instead: 2043 runs, 16.8 % of all the orchestrator's Bash calls. `schemas/v1/gate-record.schema.json` gives the claim a shape: one entry per gate command with the command as executed, its exit code, the counts the runner printed, the HEAD sha it ran on, whether that tree was dirty, the timestamp and the address. The worker preamble tells the author to attach it before the result as its own `artifact` message; the reviewer preamble tells the reviewer to read it and compare its sha with the tree under review — it already reads that directory, which is why this needed no new permission. **`tree` is the HEAD commit sha and not a git tree-object sha**, deliberately: the reviewer is handed `worktree HEAD <sha>` from the same `rev-parse` call, and a tree-object sha would never compare equal, leaving the comparison dead and silent. `dirty` is beside it because a green run on a dirty tree is not evidence about the committed tree. **Absent, dirty, mismatched or non-zero is not a refusal**: the reviewer names which of the four it was and the acceptance goes on with the gap named — refusing outright would make a participant whose sandbox cannot run part of the set unacceptable for ever. And the limit is stated rather than left to be discovered: the record binds a claim to a tree, it does not prove the run happened, and only a re-run on the same sha by someone allowed to run catches an author who wrote `exit: 0` having run nothing. The record is bounded where nothing in the package bounded an artifact before — 2000 characters of tail per entry, 16 entries — against the 350 480 characters one green run of this repository's own chain prints. **The file is resolved, not described:** a worker attaches it as `gates-<worker slug>.json` and `review` puts every `gates-*.json` of the task into both the first prompt and the re-review as absolute paths — the files folder holds every worker's attachments and every round's, so "a JSON file beside the diff" was not an address; several at once is normal and the `tree` field is what tells them apart. `tree` carries the full sha, because the comparison is exact and an abbreviation would be a schema-valid record that could never match. And a matching sha is evidence about **the commit at that sha and nothing else**: uncommitted tracked changes and untracked files both leave HEAD where it was, and `snapshot.clean` ignores untracked paths, so the reviewer is told to name that gap rather than let one sha stand for the whole subject. (PB-201)

- **The protocol now says which message types expect an answer, and both participant preambles say the same thing.** `status` is the most frequent type on the bus — 2167 messages against 1151 results — and it was the only one with no stated expectation: the code read a type only for the telemetry tally, which does not count `status` at all. The reference gains the table per type — `task`, `question`, `review` and `result` ask for an answer; `status`, `answer` and `artifact` do not — and it is one list, `ANSWER_EXPECTED`, read by the guard rather than copied into it. The worker and reviewer preambles both gained the same two sentences: a status expects no answer, so do not end a turn waiting for an acknowledgement that is not coming; and do not end a turn having sent nothing since the last thing that asked for one. **The guard's broken promise is fixed on both sides, each on the cheaper one.** Its verdict told a participant to fetch its mail "and reply in your role" while checking only the unread count: the text now names the types that ask for an answer instead of promising a reply to everything, and the verdict itself began checking that something was sent (PB-203). Two traps were left standing on purpose. "No answer expected" is not "no need to read": the warden still escalates an unread mailbox to `SILENT` for every address, the orchestrator included, and that trigger is untouched. And the table says what a TYPE asks for, not what a participant owes — the orchestrator is outside it, and the edge that leaves is written down rather than left to be discovered. (PB-207)

- **The backslop pin is `v0.7.0`, and the worker brief now names three orchestrator decisions.** `brief` gained `--entry` (where the subject lives and what to read first), `--autonomy` (what the participant closes by its own decision and what it brings to the orchestrator) and `--handover` (the gate protocol and the report header); an unfilled slot prints a TODO stub naming what is missing rather than an empty line. The same release makes a template placeholder without a key in `vars` a refusal instead of a literal `{{…}}` in the participant's text, and adds a twelfth `lint` gate over that pair. `upgrade` moved the pin in `backslop.json`, in the one `gates` command that names it and in the prose of nine files; the pin in `package.json` and `.github/workflows/ci.yml` was moved by hand, as PB-167.1 records, and `npm run pins` confirms 48 live refs at `#v0.7.0` with 150 historical refs left alone.

- **The PB-178 identity contract remains driver-owned while its live status/claim observables stay open.** The PB-179 root measurement records that an unbound participant record is treated as unknown: foreignSessionOf returns null for it, the same value as for a matching session, so the check is fail-open. (PB-178, PB-179)

- **Repository-local rules now name `npm run probe` for mutation verification.** A one-verdict guard keeps that name in the local section outside the generated backslop block; the rule records commit-first and snapshot restore, while the contributing guide keeps the full probe contract. (PB-190)

- **Codex model comparisons now record the executable, participant `CODEX_HOME` and row visibility.** A model present but hidden remains rated in the catalog, is excluded visibly as `model-not-in-inventory`, and is not treated as a generic `models validate` defect; the measured binary debug list is not claimed equivalent to `model/list`. (PB-187)

- **Codex holder journals now retain complete `account/rateLimits/updated` payloads at debug level.** The session record still carries the latest `rateLimits` object; the live notification shape and fallback exhaustion semantics remain open under PB-24.1/PB-192.

- **The PB-194 participant boundary is now stated by method.** Two independent Codex participants on codex-cli 0.146.0 had refused ordinary shell worktree writes; the ordinary empty-commit probe also returned rc=128 at the linked worktree's index.lock. Worktree and Git metadata writes passed through explicitly escalated exec_command (generated git apply rc=0, reversible empty-commit/reset rc=0), while apply_patch did not pass in either measured participant. The earlier mid-session-drop story remains unproven, and the route and version boundary are recorded rather than generalized. (PB-191, PB-194)

- **The publicity audit now checks tracked text and packed-tarball text for absolute owner-home paths.** Five named synthetic literals in four fixtures are removed before matching, while any other path in those files remains a finding; the clean verdict reports 855 tracked text files and 126 packed text entries. The approver cleaned the six historical archive records, while the participant owns the all-surface rule and its mutation probes. The rule is assembled from fragments rather than a specific login. (PB-195)

- **Mailbox record readers now share parse, validation and isolation, including one `BrokenNote.note` wording in history.** Read and peek retain their different read-error catches, while history never isolates delivered records. (PB-134)

- **Store paths and compact record-id stamps now have one source each.** Layout owns the history root, task `files/` directory and both task-lock callers; the host and standalone routing/project names reuse the canonical store-root constant, and migration/legacy/live message writers share `compactStamp`. (PB-133)

- **Worktree dependency installs and repository generators now share one logged run classifier.** Their elapsed time, output log and ENOENT/timeout/exit-code wording come from one helper; a missing executable is reported uniformly as `was not found in PATH (<argv[0]>)`. (PB-129)

- **Registry and driver helpers now have one implementation each.** Cursor and Codex share a parameterized registry in `lib/harness-registry.js`; tmux sessions and registry records are named `tmuxSessions` and `registrySessions`. Atomic writes, JSON writes, shell quoting and process liveness come from one package-owned source through built implementation modules, and all three drivers share preview arithmetic while retaining their established notification frames plus the driver-common helpers. The adapter gate checks both shared leaves and their direction. Registry writes retain mode `0600`; the old Cursor `secret` option was dead, so it was not carried forward. The ambiguous session-list exports are replaced by `tmuxSessions` and `registrySessions` rather than kept as aliases; these are internal module names, not published subpaths. The two live scripts had 17 stale callers outside the package gates; a whole-tree scan found them and they now use `tmuxSessions`. Body, internal import route and public boundary remain separate, so helpers needed by `lib/` are not re-exported accidentally. Raw driver version output accepts product and `v` prefixes, while an unparsed nonempty version is refused explicitly. (PB-117, PB-130)

- **Lift and holder records now carry copy provenance.** Spawn/review lines and Codex/Cursor session journals record the resolved CLI entry, the executing package path/version, the host version, the participant binary, and its known version; live measurements without that header are not attributable. The fields identify location and reported release, not exact code identity, and repeat holder lifts and Cursor updater symlinks use the current normalized copy. The optional Driver.normalizeTool contract keeps that normalization explicit. (PB-189)

- **Codex hook enablement was read and the installed-copy firing run was recorded.** `features list` reports hooks enabled, plugin inventory is empty, and the six-turn live check still leaves the silent-hook cause open. (PB-185)

- **Acceptance is the fourth addressed role, `approver:<slug>`.** The task orchestrator lifts it for one piece after green review, never at the start of a run; the consumer's pipeline and live lift remain the consumer's own card. The package keeps one glossary word, distinct `approver-<slug>` sidecars, end-to-end protocol and MCP addressing, and model-routing coverage across the mapped runtime, default-policy and schema surfaces; the new parity check found and fixed two pre-existing silent disagreements. Its package deny list is empty because merged-tree gates, squash and archive require writes and shell; a host may add exact external MCP write tools through `participantDenyTools('approver')`; those entries disable only the named MCP tools and never turn the Codex repository sandbox read-only, while the reviewer's deny lists remain byte-for-byte unchanged. The soft quality floor is 7: 6 admits ordinary implementation rows to an irreversible state transition, while 8 pays for independent defect discovery already completed by the floor-9 reviewer; 31 shipped tuples qualify across all three harnesses, and validation applies the same effective `roleRatings` quality as runtime to both a tuple and its assessed base row. Worker↔approver routing is a deliberate exception to orchestrator-only traffic: it bypasses the orchestrator mailbox but remains in the canonical task journal, and only an address already registered in that task and held by the calling session may use it; explicit foreign-task sends auto-register only on the orchestrator route. [ADR-013](docs/adr/adr-013-approver-is-a-fourth-addressed-participant.md). (PB-206)

- **Suite fixtures now start without ambient harness identity.** One shared four-variable list is applied by both the pooled runner and direct-file home boundary before a fixture installs its own session; a Codex participant therefore no longer turns Claude-shaped foreign-session checks into owner actions. Live scripts retain their intentionally narrower identity policy. (PB-206.2)

- **A real participant MCP child can now prove a direct worker–approver sender without a harness identity variable.** `DriverOptions.mcpIdentity` declares a session-record pointer and id field independently of the command-path `identityVar`; the common resolver accepts only a non-empty id whose record and process resolve to the same physical home and name the exact task and address. Codex already injected its pointer, Cursor now carries its pointer through the environment replacement, and Claude explicitly declares none. Tool calls refresh the proof because Codex writes its record before `thread/start` supplies `threadId`. Tests use the measured eleven-variable generic child environment for both Codex and Cursor, accept both canonical and symlink spellings of one home, retain the pointerless refusal, and reject relative pointers or readable records bound to another physical home, task or address. [ADR-014](docs/adr/adr-014-mcp-session-proof.md). (PB-206.6)

- **Socket-dependent suite groups now finish honestly in a sandbox that forbids unix-socket bind.** Seven test files used to abort on `listen EPERM`; moving their paths cannot fix the capability because the same refusal was measured in writable `/private/tmp`, by absolute and relative paths, and under Darwin's separate system temp. The shared probe treats only `listen` `EACCES`/`EPERM` as unavailable, prints a named skip for that integration group, and keeps other errors red. Skips are not passes: per-file totals separate them, the runner lists every skipped check and names a file with no passed checks in its final summary. Independent assertions continue, while live harness children are never started after a refused preflight. Where bind is available, all integration assertions still run; the implementation tree completed `65/65` files with zero runtime skips. Codex launch plans also name every participant's working directory explicitly among `runtimeWorkspaceRoots`, closing the adjacent implicit-root gap without pretending a writable path grants a network capability. (PB-206.3)

## [0.7.0] - 2026-09-12

### Added

- **A turn that ends with nothing sent is now a state of its own, `UNANSWERED`, and the loop guard holds that turn the way it holds an unread mailbox.** The mechanism guarded the mirror case and not this one: the guard's verdict was built from the unread count alone, so a participant that read its mail, worked, ended its turn and sent nothing was invisible to the guard and to the supervisor alike. Of 258 worker participants recounted across the 74 recorded tasks, 11 sent no `result`; setting aside the two of the run that was live at the time, nine — five Codex, two Cursor, two Claude. The debt is anchored on the later of two hand-overs: the last message that ASKS for an answer — found by walking history back past every type that expects none, so a `status` read after a `question` leaves the question owed — and the participant's SPAWN. The spawn is in that list because an assignment arrives as the session's opening prompt rather than as a bus message, and three of the nine have no inbound bus message in their whole life — without it the state would miss exactly the cases the card was written from. **`UNANSWERED` is not `SILENT` and not a stall, and the three are separated because their repairs are:** `SILENT` is mail that was not read, a stall is a session that never yielded the turn, and this one is a turn that ENDED owing an answer. Nor do the first two print together: unread mail is the first repair, so while any is left `status` prints the count and not the debt. What tells a turn that ended from one that never did is the end-of-turn mark `waits/<address>.turn.json`, written by the guard on a clean end and by nothing else; no mark means no turn ended, and `status` stays quiet rather than guessing. The mark says a turn ended in the past, not that the session is still there — so the line names the debt and leaves liveness to the session part beside it, which is the part to read before choosing between a nudge and a respawn. Measured on the recorded store rather than a fixture: evaluated over 460 worker and reviewer participants, the gate fires on six of the nine and is silent on the other three — and those three are precisely the ones whose harness logs show a death mid-turn (`task_started` 1602 against `task_complete` 1601; 1 against 0; a Claude session gone after six turns), where the Stop hook never ran and no end-of-turn gate could have fired at all. Nothing is stored for any of this: `health.json` keeps the shape it had, and the debt is computed from the canon and the journal on each call. **Who owes is decided by one address, not by a list of roles** — every participant address except the orchestrator, so a role the bus gains later is covered the day it exists rather than dropping out of the state in silence. The one list that must learn each role is the guard's `PARTICIPANT_PREFIXES`, which answers "is this session already a participant" for the root's successor detector; a role missing from it has its session taken for a stranger and offered a mailbox that is not its own, and it now carries `approver:` beside `worker:` and `reviewer:`. The orchestrator is outside the table by decision — every recorded case is a worker, and a `status` needs no answer — and the edge that leaves is named in the reference rather than left to be found. (PB-203)

- **`promptobus stop <address>` closes ONE participant's session and leaves the task open, and the session record goes with the process.** Between `done`, which closes the whole task, and `dismiss`, which touches the watch and never a process, there was nothing — so a run that needed to stop one of two participants killed the holder by hand from the pids in its registry record: 172 MB freed, a 45 MB holder and its 127 MB `app-server`, on a machine at 89.3 % swap under four workers, and **the record left reading `state: alive`** with only `done` able to clear it. The command calls the driver's own `stop` rather than a signal, so the record is retired by the thing that owns it — `dropSession` for Codex and Cursor, the registry entry leaving for Claude — and the command keeps no record-keeping of its own, because a second place that retired records would drift from the first. It takes the owner gate of `done` and `dismiss`, and refuses three addresses rather than guessing: one that is not a participant of this task (the line names who is), the orchestrator (which has no session this mechanism started, being the one that started the others), and an `attached` participant (a person's own window `spawn` never owned). Each refusal is asserted twice in the suite — by its exit and by the driver not having been called — because a refusal that returns non-zero after doing the work is not a refusal. A participant with no live session is not an error, an unconfirmed stop is not a success and says the record may still read alive, and the watch is left alone: `stop` does not do `dismiss`'s work behind its back. Why a verb and not `dismiss --stop` or `done --only`: ADR-012. (PB-174)
- **A per-harness ceiling on live participants bounds the harness choice under `balance`.** The overlay key `caps.liveParticipants.<harness>` is a count of participants of ONE task, merged harness by harness: under `--strategy balance` a harness that already carries that many leaves the pace comparison, so the next worker goes to another subscription. It bounds a run and not an account — the count is the task's own participant list — and it is counted in participants, where the similarly named `penalties.liveParticipantCap` is a ceiling on the live-participant penalty in score points. A ceiling and not a steeper penalty — `penalties.liveParticipantPerHarness` only orders candidates inside a harness, and the pick is made by pace, so an account ahead of the others attracted the third and the fourth worker whatever the penalty cost. A harness no layer names is unbounded and behaves exactly as before; `0` means never that harness, and is reached with nothing live on it. A capped harness keeps its candidates, their scores, its pace rows and its representative — the cap is a choice rule like the quality floor, not a filter — and its rows carry `pace.atCap`. When the ceiling moves the pick, the decision carries a `live-participant-cap` warning naming the harness the cap actually held out — read off the removed rows, never off the pick the comparison would otherwise have made, because removing a capped representative lowers the leader and can widen the band onto a harness no cap touched — its count against the ceiling, both tuples, any further harnesses at their ceiling, and the overlay key, and `spawn` and `review` print that code on their routing line; a ceiling that held out a harness the comparison would not have taken is silent. Every paced harness at its ceiling is a soft fallback with the same code, not a refusal. `models validate` refuses a fractional or negative count and a ceiling on a harness this CLI does not drive. (PB-162)
- **A lifted participant no longer inherits its parent's session identity or its messaging channel — the Claude driver's half, and one shared list for all three.** `PARENT_SESSION_ENV` (`lib/session-env.js`) names the twelve variables that belong to no single driver, and `SESSION_ENV_DROP` in the Claude driver is now that list. Measured before the change, on a synthetic parent environment through each driver's own `sessionEnv` — the function `lib/spawn.js` calls, so no live harness and no model turn were needed: eight of ten watched parent values reached a Claude participant, nine a Codex one, eight a Cursor one, and `sessionIdentity()` answered with the **parent's** id in all three, because it reads `CLAUDE_CODE_SESSION_ID` and nothing else. After the change the Claude driver passes none of the ten and that call answers `null` — honest, and as far as this goes: what it should answer instead is a separate question, and a second environment read beside it would look like a fix without being one. **Claude → Claude was never protected by the list**: a participant's own session id and messaging socket are written by the harness over the inherited ones, measured on a live participant, so that case was correct by the overwrite rather than by the mechanism. The messaging pair is why this is more than wrong data — `registerWake` writes exactly those two into the contact point and the warden knocks with them, so a participant of another harness held its orchestrator's wake credentials; whether a knock from a stranger holding that token is accepted was not measured and is not claimed. `extra` now merges **on top of** the drop in the Claude driver, matching Codex: a caller that names one of these deliberately wins, an ancestor never does, and the old order silently removed the caller's value — harmless while the list held one name. `test/session-env.test.mjs` pins it per driver — all three checks are live in this release, because the Codex and Cursor halves landed with it. (PB-182)
- **The mutation probe has a script — `npm run probe -- <file> (--mutate <sed-expr> | --stdin-patch) [--run <command>]`, `scripts/mutation-probe.mjs`.** It refuses on a non-empty `git status --porcelain` and names what is dirty, because "commit first" was held by nothing but the agent remembering: a probe over uncommitted work loses it, `git checkout --` taking the edit along with the mutation, three times in two days. Two of the three were caught by their own author; the first pair was reported as done and found by a reviewer. The third was caught by a harness notification that the file on disk had changed — a property of the tool the agent happened to be running in, not of this repository, and absent in a non-interactive session. The restore is from a snapshot taken before the mutation rather than from git, so the probe cannot repeat the loss it exists to prevent. `--run` is why it is not a test-only tool: the loss that returned this card to the queue happened while probing a **gate** — `scripts/check-pins.mjs` driven by mutations of `package.json` and `ci.yml` — where a refusal inside a test-shaped probe would never have been called; without `--run` the default is `node <file>`, the form the rule prescribes and the one `test/run.mjs` itself uses. A mutation that changes nothing is a refusal, not a pass, and so is a check that stays green with the mutation in place or red after the restore. `--mutate` takes `s<sep><regexp><sep><replacement><sep>[gi]` and applies it in node rather than through the `sed` binary — `sed -i` differs between BSD and GNU, and the suite's own environment has a PATH without `sed` at all, where the first version died `spawnSync sed ENOENT`; anything richer than one substitution goes through `--stdin-patch`. `test/mutation-probe.test.mjs` pins the four outcomes apart against a throwaway `git init` repository of its own, never this one, so the checks do not depend on whether this tree happens to be clean. **Not done here, and it is not a documentation gap:** `AGENTS.md:13` still describes the manual steps, and it cannot be edited in this repository — the line sits inside the `backslop:start` block, `init` reports `AGENTS.md: backslop block updated` and restores it, and CI runs `init` on every push. That text lives in backslop's template and is filed upstream. (PB-95)
- **A pin gate — `npm run pins`, `scripts/check-pins.mjs` — fails when a tracked file names a backslop release that disagrees with `cli` in `backslop.json`.** `upgrade` rewrites the pin in `backslop.json`, in `docs/**` and in the root `*.md`, and `lint` warns over that same set; neither reaches `package.json` or `.github/workflows/ci.yml`, where the pin is a live command rather than prose. PB-167 moved both by hand and left the next raise with nothing but a paragraph in the contributing guide between it and the same silence. The gate reads the expected version out of `backslop.json` and carries no literal pin of its own — a gate with one would be one more place for a raise to miss. It skips exactly what `upgrade` skips, restated rather than imported because the CLI arrives by npx and is not a dependency: `CHANGELOG.md`, `docs/adr/**`, `docs/archive/<PREFIX>-N…` and task cards by basename, all of which cite a version as evidence of a moment. Green is not silent — it prints what the walk saw, today `777 of 777 tracked file(s) read · 43 live ref(s) in 10 file(s), 149 historical ref(s) in 134 record(s) left alone, 41 live ref(s) outside backslop.json` — and a live set holding nothing but `backslop.json` is a failure rather than a pass, because a gate that matches nothing is green for the wrong reason. The config is discounted from that floor on a measurement and not on reasoning: the first version of this gate counted it, and pointed at a spec no file in the tree carried it reported `1 live pin(s) in 1 file(s)` and exited 0 — the config matches itself whatever the walk does. It catches any ref after the separator and not only a semver one — `#main`, `#v0.6` and a sha are divergences too, and a gate blind to them stays green while two live files agree with each other on the wrong ref — and a tracked file it cannot read is a finding rather than a skip, since it cannot vouch for what it did not open. It joins `gates` in `backslop.json` and CI, where it stands ahead of `backslop init`. The reason for that order is narrower than the one the guide hedges: `init` at the pin the workflow names rewrites `AGENTS.md` to that version, so after that step the gate would name `AGENTS.md` rather than the `ci.yml` that sent it there. Nothing is committed in CI and the rewrite dies with the runner, so no push is undone by it — which narrows the "undo the raise" framing of the PB-167 entry below, itself written as an inference; whether a stale workflow costs anything beyond a gate blaming the wrong file is still unsettled, and no CI run has been made to settle it. (PB-167.1)

### Changed

- **The `PostToolUse` feed hook is gone, and an install removes one an earlier version wrote.** It showed the orchestrator an excerpt of the message it had just written itself, once per bus call, and nothing of the working machinery ran through it: the turn is returned by the `Stop` guard, unread counts ride in the MCP reply, and delivery to a participant is the warden's over its own channel. The guard is untouched. `install` now takes the old entry out of `.claude/settings.json`, `.cursor/hooks.json` and `.codex/hooks.json` and deletes the runner script with it, leaving foreign hooks and unknown fields in those files alone; nothing generates a runner any more and `templates/` is empty. `BUS_HOOK_EVENT` and `busHookRel()` survive with a new meaning — they are how a leftover is recognised, not how anything is written — and the package suite now gates that, because deleting them as unused would take the removal with them. `planPromptobusHooks` returns `{ settings }` alone: `rel` and `text` named the runner file and there is none.
- **BREAKING for a consumer driver: `DriverOptions.identityVar` is a new REQUIRED member.** Every driver must declare the environment variable its harness uses to name its own session, or `null` when it has none — `claude` answers `CLAUDE_CODE_SESSION_ID`, `cursor` `CURSOR_CONVERSATION_ID`, `codex` `CODEX_THREAD_ID`. A driver that declares neither does not type-check. The core stopped reading one harness's variable directly: identity is resolved from the drivers, and two claimants at once are refused rather than picked, so a leaked ancestor identity produces a named refusal instead of a confident wrong answer. The member answers for ONE path — a command the session runs — and not for an MCP server child, whose environment a harness may scrub entirely. [ADR-010](docs/adr/adr-010-session-identity-is-a-driver-member.md), PB-178.
- **`promptobus send` was built and withdrawn before release.** The code and its checks stay in the tree (`lib/send.js`, `test/send.test.mjs`) and the command is **not registered**: `promptobus send` answers `unknown command`. Four review rounds produced four major findings of one class — the command granting more rights than it promised — ending with an ownership check that is fail-open for an unbound record: `foreignSession` returns `null` both when the session matches and when the participant record carries no session id, so the check cannot distinguish an intentional match from an unknown binding. The decision it was built for is unchanged ([ADR-011](docs/adr/adr-011-a-session-address-is-per-task.md)); PB-179 keeps that unknown case open.
- **Codex reads a project's `.codex/skills` from a directory that is not a git repository — measured, so the reviewer sandbox keeps no `git init`.** The reference and [ADR-008](docs/adr/adr-008-codex-reviewer-working-directory.md) said the point was unverified and refused to invent the precondition; it does not exist. The stand: a standalone-host workspace inside no repository at all, one stub skill in its canon carrying a marker string, and one live reviewer turn on a four-line diff. Codex put the sandbox first in its own skill-roots table, listed the stub by name and description, and the model read `SKILL.md` at the path that table gave it and opened its report with the marker — the path reached it only through the harness, since the review prompt names no skill and no such directory. The planned second run after `git init` was not needed. **The same turn also showed that the reviewer could not deliver that report**: all four of its bus calls failed with `user rejected MCP tool call`, each after the holder declined an `mcpServer/elicitation/request`, so the marker was read from the session rollout rather than from the `result` message it was meant to arrive in. The bus server is not the source — started as the participant starts it and driven by a client that declares the elicitation capability, it issues no server→client request and its `promptobus_send` succeeds. Both facts are recorded together in the reference and the ADR on purpose: the first without the second reads as a working reviewer. (PB-161.3, and PB-161.4 for what is still open)
- **The register rule is stated to cover a worker's own scaffolding, not only the suite's checks.** `contributing.md` § Suite isolation says machine-wide reads must be scoped by a path, a pid or a mark the run owns, and a program name is not one. It now says so about the throwaway tooling a worker builds around the suite as well, with the case that taught it: two `until ! pgrep -f "test/run.mjs"` wait loops matched the consumer's `cli/test/run.mjs` by substring, watched a neighbouring repository's runner, and outlived their own run until the system killed them. (PB-166)
- **A suite file that outlives its work is now caught, and a take-down reaches its children.** Two changes to the test suite, from one measurement. The shared helper every suite file imports, `test/home.mjs`, arms an unref'd watchdog at 240 s: a file that has FINISHED empties its event loop and exits, and the timer never fires, so a finished file pays nothing, while a file still alive at the deadline reports what `getActiveResourcesInfo()` names as holding its loop and exits non-zero. It is a second ceiling on a file's whole work and not a free one — `unref` saves the file that finished, not the file still working, and a file that legitimately runs past the deadline is taken down too, so the message names both readings instead of accusing it. That covers the single-file run — `node test/<file>.test.mjs`, the form the mutation-probe rule prescribes — which had no deadline, no child bookkeeping and no parent to notice, and whose process became an orphan at `PPID 1` the moment its session died. The number sits below the runner's 300 s so that under `npm test` a file names what holds it before the runner kills it blind; the runner's deadline stays as the backstop for the one shape a timer cannot reach, a file spinning inside synchronous work, which never returns to its event loop at all. Separately, `test/run.mjs` now spawns each file `detached` and takes a late one down by the negative pid: `child.kill` signals one process, and a suite file spawns them by the dozen, so its children used to be reparented to `init` and stay — reproduced on a fixture that spawns a marked child and hangs, which survived the deadline before the change and does not after it. What the first bullet of the card asked for is closed by a negative measurement instead: all 55 suite files, run one by one by hand on 2026-09-12, exit on their own — `lingered=0 failed=0` — so no file holds its loop open today, and the orphan source in evidence was a hand-built measurement stand rather than a suite file. (PB-166)
- **A Codex participant now runs in a `CODEX_HOME` of its own.** The driver builds one directory per participant under `$TMPDIR` at mode 0700 before the lift and removes it with the session — on `stop`, on `done` through it, and on a failed lift. It holds a copy of the owner's `auth.json` at mode 0600, the mechanism's MCP entries as `[mcp_servers]` of a `config.toml` written at 0600, and, for a worker, `[projects."<realpath of the worktree>"] trust_level = "trusted"`. The owner's `~/.codex` is read once for that credentials file, and the participant path never writes to it: no `[projects]` record and no session rollout land there. The availability probe still starts app-server in the owner's home to read the account limit before any participant exists, so the marketplace snapshot refresh is not something this isolation stops. Consequences: the owner's personal MCP set no longer reaches any participant, so the reviewer's mechanical write deny now covers every server the participant has; `thread/start` no longer carries a `config.mcp_servers` override, and the session record no longer copies the MCP set; the session rollout an operator may want lives in that home, and `phrases.logs` names it. `~/.agents/skills` is the one channel the home does not isolate — that root follows `HOME` — and the lift says so. ([ADR-007](docs/adr/adr-007-codex-participant-isolated-home.md), PB-161)
- **Workspace skills reach a Codex participant as files.** `<workspace root>/.codex/skills` is copied into the working directory's `.codex/skills` with a self-ignoring `.gitignore` beside it, the way the Cursor driver copies `.cursor/skills`; the lift line reports the count and source, or says why there is none. Both roles get it — a worker's copy lands in its worktree, a reviewer's in the directory of its own. (PB-161, PB-161.2)
- **The lift asks Git what it is about to overwrite, for every harness and not only for `.cursor`.** A driver now names the directories its launch files claim inside the participant's working directory — `options.launchDirs`, `.cursor` for Cursor and `.codex` for Codex — and `spawn` and `review` run `git ls-files` for each of them before the first write, naming the tracked paths in a warning. It matters most for a directory copied whole, which wipes its destination first: a repository that lawfully tracks `.codex/skills` of its own used to lose it in silence and have its branch dirty from the first second. Git is asked about the paths the lift will WRITE and not about the claimed directory whole, so a project that keeps its own `.codex/config.toml` or `.cursor/rules` — which a worker is meant to read — is not told on every lift that they are about to be overwritten; the copy destination is among those paths, so it is noticed without a sibling file beside it. Claude Code claims none: its mcp-config and settings file go to the task store. (PB-161.1)
- **A Codex reviewer works in a directory of its own, and that directory is trusted.** `<participant settings stem>.codex-sandbox` in the task store, the Cursor sandbox's shape: the skills canon is copied there instead of nowhere, the tree under review is attached as a read through `runtimeWorkspaceRoots`, and approval containment follows the session record's `cwd` and `addDirs` without a second decision. The trust record now names the participant's own working directory for both roles — a worker's worktree, a reviewer's directory — and goes, as before, into that participant's disposable `CODEX_HOME` and never into the owner's `~/.codex/config.toml`; the tree under review is never trusted, which was the whole reason the record used to be withheld. The reviewed tree gains no file from the lift and loses none. `review` now prints `reviewer home: <path>` on a real lift as well as in `--dry-run`, because the `reviewer … started in <repo>` line names the repository and not the working directory; `spawn` and `review` also print the `workspace skills:` line on a real lift, where it had been visible only in `--dry-run` — the canon travels as files, so whether it arrived is a fact about that lift. ([ADR-008](docs/adr/adr-008-codex-reviewer-working-directory.md), PB-161.2)
- **A Codex command approval that asks to leave the network sandbox is refused, for a worker as for a reviewer.** `networkApprovalContext` and a non-empty `proposedNetworkPolicyAmendments` were read by nothing, so a network escalation whose `cwd` was inside the roots was answered `accept` like any in-root command. The check now runs before containment and before the role split, and the reply is the plain `decline` — the holder never sends the `acceptWithExecpolicyAmendment` or `applyNetworkPolicyAmendment` arms. (PB-88.3)
- **The Codex reviewer's MCP deny is no longer a hedge.** On codex-cli 0.146.0 a tool named in a server's `disabled_tools` is absent from the thread's tool inventory: two threads differing only in that key, the MCP server asked `tools/list` and answering with both of its tools in each, and `mcpServerStatus/list` reporting one tool where the key was set and two where it was not. The filtering is app-server's, above the server. The reviewer prompt phrase and 03-cli § Review now state that instead of "enforcement was not observed", and both name the ceiling: no client-visible method returns the tool payload of the model request, so the inventory is what was measured. The two paid turns the owner authorised answered nothing — the second, captured from the rollout, shows an empty assistant message and `last_agent_message: null` — and the fixture records them rather than hiding them. (PB-87.3)
- **The backslop pin is `v0.6.0`, and the gates runner the Definition of Done names now exists.** `v0.4.0` carried no `gates` command — `backslop gates` answered `unknown command “gates”` and exited 1 — so the `gates` key in `backslop.json` named three commands that nothing executed, the required `gates N, green N` summary was unproducible, and two runs on consecutive days spent a turn diagnosing it and ran the three commands by hand instead. `upgrade` moved the pin in `backslop.json`, in the one `gates` command that names it, and in the prose of seven files — including a line in `docs/archive/README.md` that had been two versions behind at `v0.3.0`; `migrate` reported no format change between v0.4.0 and v0.6.0 and only advanced the version stamp, so no task card was rewritten. The same pin in `package.json` and `.github/workflows/ci.yml` is moved by hand: `upgrade` does not reach those two files and `lint` does not report the divergence. A workflow left on the old pin would be expected to regenerate the old `AGENTS.md` block on every push and undo the raise — inferred from what `init` does locally, where it rewrote fourteen lines of that block, and not from a CI run. The contributing guide now records that instead of promising the opposite, and the missing guard is filed as PB-167.1. (PB-167)
- **Both READMEs rewritten around the standard sections — features, requirements, installation, usage, library, development, contributing.** They state that the package installs from a GitHub release tag rather than the npm registry, and move the long model-routing walkthrough to the reference and the guide. The English one was generated with a README-generator skill and the Russian one follows it section for section, so the two no longer describe different layouts; both carry the CI, licence and Node badges and link to each other.
- **The two comment-sweep gates ship disabled, and the sweep they guard is not certified by a gate.** `test/comment-length.test.mjs` and `test/comment-links.test.mjs` are marked `todo`: they run and report, and they cannot fail the suite. The reason is measured rather than precautionary. In one review round the same ratchet was bypassed four ways, and the link gate certified a repair it could not check — its symbol rule reads `if (` as a declaration, and it never compares the file a section names against the file the pointer sits in, so its "98 pointers, 0 symbol mismatches" counted comparisons it had not made. The scanner `test/comment-scan.mjs` and both pending lists stay in the tree, run identities and all, because the next pass has to see debt disappear rather than start from a fresh baseline that forgives it. Until a gate is restored, an over-long comment run and a pointer that leads to the wrong section are caught by reading and by nothing else. **One consequence to expect in the suite output:** a `todo` test prints as `✖` in `node:test` and does not count as a failure, so a green run of the package now shows **ten crosses** that mean *withdrawn*, not *broken* — `npm test` still exits 0. Read the exit code, not the tail of the log.

### Fixed

- **The warden switch and its trace now reach a Cursor participant's MCP server too, and the parent-session list gained the name a live measurement found missing.** Measured 2026-09-12 on one participant per harness: Claude MERGES that child's environment (99 names, workspace credentials among them), while Codex and Cursor REPLACE it (11 and 9). Cursor is where the mechanism was proved rather than counted — a sentinel standing on the lift command reached the harness process and not its MCP child, so the environment is not lost on the way to the harness but replaced at the MCP layer. Under a replacing harness both halves of the suite's auto-lift rule travel only if the driver's entry names them, so the Cursor entry now carries them exactly as the Codex one does, and the list they come from moved to `lib/session-env.js`, since a constant two drivers read belongs to neither. `CLAUDE_CODE_HOST_SESSION_ID` was found on a live Claude participant, carrying the orchestrator's value, and was absent from the parent-session list; it is on it now. No stand can show any of this: a stand lifts no MCP server at all.
- **A Codex participant no longer inherits its parent's session identity or the messaging pair the warden knocks with.** `SESSION_ENV_DROP` in the Codex driver carried one name, `CODEX_HOME`, so a lifted participant kept `CLAUDE_CODE_SESSION_ID` and — the part that matters — `CLAUDE_CODE_MESSAGING_SOCKET` and `CLAUDE_CODE_MESSAGING_TOKEN`: the orchestrator's own delivery channel and its credential, in the environment of a process the orchestrator started. It now drops the whole parent-session list, which lives in `lib/session-env.js` because the names belong to no single driver. Measured after the change: of twelve watched parent names, zero survive, `sessionIdentity()` of the result is `null` instead of the parent's id, unrelated names are untouched, and a caller-supplied value still wins over the drop. What a harness would do with that socket and token was not probed — the limit is stated rather than exploited. `sessionIdentity()` answering `null` for a Codex participant is left as it is: what it should answer instead is a separate decision.
- **A Codex participant's hooks are now written where that participant looks.** `install` writes hook files at the workspace root; a worker works in its worktree and a Codex reviewer in a sandbox of its own, so neither had a project hook file at all — measured on a live reviewer, `grep -c "hook/started"` on the holder journal was 0 even after hook trust stopped being the blocker. The driver now writes the participant's own `.codex/hooks.json` into its working directory, which for a Codex participant is also the one project its home records as trusted, carrying that participant's `--role`, `--task` and `--home` where the workspace's own guard carries none. The self-ignoring `.codex/.gitignore` is no longer conditional on a skills canon travelling, since that directory now always holds a file. **This is not yet the whole fix:** measured on a live reviewer the same day, a full turn produced no `hook/*` event at all, so the file in the trusted working directory is not sufficient. That measurement does not separate a wrong location from the right one with hooks not enabled — `--dangerously-bypass-hook-trust` waives trust for *enabled* hooks — and it does rule out the workspace-root file being read instead.
- **A successful `install` no longer leaves a person one failed command away from the spawn allow-list.** `install` writes `harnesses` — where hooks went — and the standalone host reads `tools` — which harnesses this workspace may spawn or review with. Both hold harness names and one command is named after the other, so following the documented order produced `promptobus install --harnesses codex` succeeding and `promptobus review … --harness codex` refusing with `declared: none` on the next line. The fields stay two, because writing a hook file into a project must not by itself grant that project permission to spawn: instead `install` now names the gap when it finishes, the refusal says that a successful install does not satisfy it and which key each command uses, and the install guide tells the two apart in a table before either command is run.
- **A participant lifted with `bypassPermissions` no longer holds the warden's postcard, and a held message is no longer reported as a permission prompt.** The participant settings file the Claude driver writes carries `"crossSessionInbound": "accept"` under a prompt-bypassing permission mode and under no other — the key the harness's own held-message dialog names. Measured 2026-09-11 on claude 2.1.263, one background session per mode with the postcard knocked into its messaging socket: `bypassPermissions` held the message and its record went to `waitingFor: "permission prompt"` while the session's turn ran on and finished; the same lift with the key took the postcard with no dialog at all; `dontAsk`, `acceptEdits` and `auto` each took it without the key, so `dontAsk` is not in the list however its name reads. The stall predicate now separates the two dialogs that arrive through that one field: a prompt suspends a turn, so a participant that BOTH ended its turn and reported after its last activation was not stopped by the dialog standing on it, and no person is called to it. Neither mark lifts the stall alone — an end-of-turn mark alone would silence a dialog that ended a turn in silence, a message alone would silence a prompt hit later in a turn that had already spoken. A participant with no turn mark, or one that ended its turn before the activation, keeps its stall — the second is genuinely deaf and is something a person has to fix. The route printed for a dialog names both possibilities and the key that ends the second. `spawn` does not refuse the mode. (PB-165)
- **A Cursor participant no longer inherits its parent's session.** `SESSION_ENV_DROP` there carried four names, all of them Cursor's own, so a participant kept the orchestrator's `CLAUDE_CODE_SESSION_ID`, its `CLAUDE_CODE_MESSAGING_SOCKET` and the token that goes with it, plus `CODEX_HOME` and `CODEX_THREAD_ID` — every other harness's identity and the mechanism's own delivery channel. The list is now the shared `PARENT_SESSION_ENV` (`lib/session-env.js`), and the drop happens BEFORE the caller's `extra` is merged rather than after, so a host that names one of these wins and an ancestor that happens to have one never does — Codex's order, which was the right one. Measured after the change: of twelve parent names, zero survive, unrelated variables are untouched, a caller's `CODEX_HOME` wins, and `sessionIdentity()` of a participant environment is `null` instead of the parent's id. PB-182.
- **A Cursor participant is told what a question actually does, and the human-facing route for a held dialog now comes from a verdict that fires.** The prompt said `AskQuestion` "gets a skip"; measured on a live background participant it is neither answered nor skipped — the turn HELD 278.497 s until a person pressed Escape, and no upper bound was found. The rule stays, the reason is now the measurement, and `SwitchMode` is named beside it: without a person it comes back as `rejected by the user` in under a second, which the participant is told to read as the harness answering for an absent person. The `question` stall kind is retired — nothing in production ever produced it, only the stand did, so the check on that branch was green independently of the product — and its guidance is folded into the watchdog verdict, which already fires on exactly that silence with a number per signal. That verdict also stops promising the message will run on the next turn: a held turn does not end, so it holds the message with it. PB-181, PB-183.
- **A Codex participant's bus calls survive codex-cli's own per-call MCP approval.** `mcpServer/elicitation/request` carries two different questions, and the holder now answers by which one it is instead of by the method name. A server's own elicitation is still declined — a background participant has no person to fill a form — but codex-cli 0.146.0 raises the same method for its per-call MCP tool approval («Approve app tool call?», marked in `_meta` with `codex_approval_kind`, declined through the `__codex_mcp_decline__` const), and declining THAT ends the call with `user rejected MCP tool call`. A live reviewer lost all four of its bus calls that way, its report among them. A request whose kind is exactly `mcp_tool_call` is now accepted for both roles — it asks whether the participant may call a server of its own disposable `CODEX_HOME`, which the lift put there and nothing else is in. The kind is matched as a string rather than tested for truth: it is a discriminator with more values, and `tool_suggestion` on the same key asks to install a tool, which is a question to a person. The accept is `{action: "accept", content: {}}`, which is what the request's empty object schema calls for. The holder log gains two shape fields, `kind` and whether a schema came with the request, and still never writes the prompt, schema, URL or params blob. The suite now asserts the reply that goes back on the wire instead of the decision function's return value. Verified live 2026-09-12 on one paid turn: a reviewer's three bus calls each raised the approval, each was allowed with `kind=mcp_tool_call` in the log, each call `completed`, and the report reached the task journal. PB-161.4.
- **Hook trust stops blocking a Codex participant: the bypass flag is built ahead of the subcommand, where codex-cli accepts it.** `--dangerously-bypass-hook-trust` is a global option — measured on 0.146.0, accepted before `app-server` and rejected after it with exit 2 — and both argv sites built it nowhere at all, so the memory hook and the turn guard never ran and the lift line said «app-server has no bypass flag». That line was true about the subcommand's option list and wrong about the invocation. One constant now carries the participant argv for the holder and the driver alike, and the checks assert the flag's POSITION, including in the stand, which now refuses the flag after the subcommand the way the real binary does. The flag buys trust and nothing else, and the lift line says only that: measured on the same live turn, `hook/started` over the holder log is `0`, because nothing is laid out where that participant reads hooks from — `install` writes `.codex/hooks.json` at the workspace root, and a reviewer's cwd is its own sandbox. The owner's own Codex sessions are untouched and still approve hooks through `/hooks`. PB-170.
- **The hand-written backslop list in `.gitignore` is gone; the managed `backslop:start` block is the single source.** From v0.5.0 `init` writes that block itself, so the raise in PB-167 left thirteen hand-written adapter paths restating what the block already said and `/CLAUDE.md` listed twice. Nothing was broken by the duplication — git takes the last matching rule, and `git check-ignore -v` already named the managed block for all four adapter paths before the cleanup as well as after it — so this is dead weight removed, not a defect fixed. `.claude/worktrees/` moved up into the general section instead of going with its neighbours: it is where this repository's worktrees live, not an adapter output, and the managed block does not cover it; `git check-ignore -v .claude/worktrees/x` still names a rule. `init` at the same pin now reports `.gitignore: unchanged` where the run that found this reported `appended`. (PB-167.2)


- **The two longest comment blocks of the Claude driver are prose in the drivers reference, and the code keeps two lines each.** The 41-line block over `sessionStall` and the 33-line one over the late-start limit mark leave `lib/driver-claude.js`: 74 lines out, 4 in. Nothing measured is lost — the 2026-09-02 snapshot series behind "the parse entry is the turn end", the 2026-09-11 held-postcard measurement, and the reasoning for `resetAt` staying `null` on both exhaustion codes all move across, and the stall prose gains the third reading of the dialog field that PB-176 added. The remaining two lines named `reference/05-drivers.md` in words rather than as a markdown link while the page was arriving on another branch; **both are markdown links to their own anchors now, made at the merge as promised.** **The condition that makes this safe, written here because nothing checks it mechanically:** this branch must not reach the default branch without that page. A bare-text pointer is invisible to the link gate by construction, so if the page were dropped the repository would keep a signpost to nothing and lose 74 lines of measurement with it — the 2026-09-02 snapshot series and the 2026-09-11 held-postcard run among them. Merge order is what satisfies it today, and merge order is not a guarantee the tree can state. **What this is not:** the file still holds 61 blocks longer than two lines, the longest 25. Two were taken as a checkable batch to see whether the form of the move suits drivers at all; the sweep is its own work. (PB-172, two blocks)
- **`promptobus-driver-codex` no longer decides by the machine's load: every read of a stand thread file waits for it, and a wait that runs out says it timed out.** The file had reddened twice at `load ≈ 40` with different counters — `126/140` and `136/139` — and been green twice at `load ≈ 10` and on the base, `186/186` each. The mechanism behind that: the thread file is written by the stand's holder, a separate process, and seven places read it once with `try { JSON.parse(readFileSync(…)) } catch { null }`. Under load the reader wins the race, the value is `null`, and the check reports the product wrong; different counters are different files won at different points, which is why no two failures matched. The same file already used `waitFor` for one such read, so the fix is the file's own idiom applied to the six that lacked it, `harnessThread` included. On expiry the helper returns a marker instead of `null`, and **four** checks carry it in their failure detail — `TIMEOUT after 15000 ms waiting for <file> — the stand had not written the thread; this says nothing about the product` — because a red that blames the product for a slow machine is what sends a run off to re-run instead of to read. **Four, not all of them:** when no thread file appears at all, eighteen checks fail and the other fourteen fail downstream of the same absence without naming it. Carrying the marker into every detail is its own pass; what is done here is the wait, which is what removes the dependence on the machine. (PB-184)
- **The Cursor stand modelled a question as a turn that ENDS, which is the claim PB-181 measured false, and the comment beside it was only the caption on that claim.** `test/harness-cursor.mjs` returned its own `question` outcome, and in that stand every outcome except `hang` appends `turn_ended` and calls the Stop hook — so the fixture asserted that a question finishes the turn, while the measurement is a hold of 278.497 s ended by a keypress. The flag now returns `hang`: the branch that appends nothing, calls no hook and keeps the process, which is what a held dialog looks like through every observable the mechanism has. That moves the fixture onto the production path — three silent liveness signals, the `watchdog` verdict — instead of a shortcut only the stand could reach, which is what the card asked for. It also stops producing a kind the contract is losing: `stallRoute`'s `question` branch — which still carries the "a question gets a skip" claim the measurement contradicts — is retired with its text folded into `watchdog` **on the branch that closed PB-181**, and arrives with it. Both halves are needed and only one is here; the stand is corrected from this side, the route from that one. **The condition that makes this safe, written here because nothing checks it mechanically:** this branch must not reach the default branch without the one that closed PB-181. Dropped, it leaves the production route still telling a person that "a question gets a skip, not an answer" — a claim measured false at 278.497 s — while the stand no longer produces the kind that route answers, and no gate reads prose for truth. Merge order satisfies it today, and merge order is not a guarantee the tree can state. Worth recording with it: the flag has **no setter anywhere in the tree** — `git grep askQuestion` finds the branch and one unrelated comment — so the outcome was reachable neither from production nor from any check, and nothing existing exercises the new behaviour either. (PB-183, the stand half)
- **03-cli now says in one place what `done`, `dismiss` and a hand kill each do to a process, and names the door that does not exist.** `done` stops every session of the task and `--keep-sessions` leaves them all; `dismiss` stops the warden's reports about one address and never touches its process. Nothing stops one participant while the task stays open — measured on 2026-09-12, when a two-participant measurement task needed exactly that and the holder was killed by hand from the pid in its registry record, freeing 172 MB (a 45 MB holder and its 127 MB `app-server`) on a machine at 89.3 % swap under four workers. The hand kill left the record at `state: alive`, which only `done` will clear, so the reference says to read a Codex participant's liveness from its process rather than that record — the same rule the contact point needs, and for the same reason: both artefacts are written at birth and nothing is obliged to erase them at death. The door itself is PB-174 and is not built here. (PB-174, the reference half)
- **`status` no longer presents a contact point as reachable without checking, and the check is the socket path — not the `pid` in the file, and not the raw address.** A `wake/<address>.json` outlives the session behind it, so a reader counting files counts a killed participant as live. The `pid` field looks like the answer and is not: `writeWake` stores `process.pid` of whatever wrote the file, which for a Claude participant is the Stop hook that exits at once. Measured 2026-09-12 across a live store of nine contact points — eight named a pid that was already `ESRCH`, and five of those eight belonged to sessions that were alive, so checking it would have called almost every live participant dead. The socket separates them, but only read as a path: a Codex address carries a `#<n>` thread suffix on one socket file, and `existsSync` over the whole address called both live Codex reviewers gone, while the path before the `#` split all nine correctly and named as gone exactly the one participant whose process had been killed. A line that cannot be vouched for now says so — `alarm: socket handed over <at> — STALE: the socket it names is gone` — and the orchestrator's own check, which had been reading the raw address and was right only because that participant is never Codex, reads the path too. (PB-175)
- **The route printed for a stalled Claude participant no longer asserts that a person is wanted, because the field it reads has a third reading.** Observed 2026-09-12 on a live worker: `promptobus status` printed `STALLED: permission prompt — only a person can answer: claude attach <id>`, the orchestrator relayed it to the owner as fact, and no dialogue existed — the session was working throughout (eight commits, `npm test`, `npm run audit`, the gates), and four of its `Bash` calls had been refused at once by the harness's auto-mode classifier. A standing prompt is cleared by a person attaching; a refusal that has already returned cannot be, since nothing is standing and what is wanted is a permission rule decided outside the session. The route now names all three readings — own prompt, held peer message, returned refusal — and claims none. **What set the mark is not measured and is written as an inference:** `sessionStall` reaches `kind: 'permission'` only through a non-empty `waitingFor`, whose value is a short label from a fixed set naming no cause, and that field is absent from the same record now, so the mark was transient; a held peer message is ruled out for that session, since its settings file carries no `crossSessionInbound` and `auto` takes a postcard without one. No stand can close it: the refusal is not reproducible on demand — the same command shape refused an hour earlier went through later — so nothing can pair a refusal with a registry read. (PB-176)
- **03-cli § Review now records why the reviewer gets no second immovable artefact, instead of leaving the absence to read as an oversight.** A reviewer that meets "the working copy contradicts the snapshot" resolves nothing itself: `Bash` is denied to it whole, and the snapshot is the only fixed thing it holds. The candidate fix — the worktree's `HEAD` content of every changed path, taken beside the diff — is refused, and the three reasons are in the reference so it is not re-proposed as new: it closes the discrepancy only on the diff's own paths while the prompt sends the reviewer into the working copy far wider; it costs 13.6× the bytes in the budget that matters (measured 2026-09-12, a seven-file branch: 22 422 bytes of diff against 306 067 bytes of full `HEAD` content), on every review and every re-review; and it guards a report the prompt already forbids, since on a contradiction the reviewer files no finding at all. The read-only guarantee is named as untouchable in the same place — `Bash` stays in `REVIEWER_DENY` and is not relaxed for convenience of resolution — and so is what the orchestrator already holds: the exact `git show <sha> -- <path>` inside the report, and `review --task` to re-snapshot. (PB-169, recorded first in the consumer's own tracker and moved here so the release carries it)
- **03-cli § `done` now names what each of the three drivers does about a stopped session's processes, including the one that does nothing.** Cursor reaps a tree and an orphan marker, Codex kills a process group, and the Claude driver's `stop` runs `claude stop <id>` and waits for the record to leave the registry — it reads no process, walks no tree and signals no group. Whether it has to is written as open rather than closed either way: nothing observed says `claude stop` fails to reap a session's grandchildren, and nothing observed says it does, and inventing a reap on that silence is what PB-161.2 refused for the Codex sandbox. The stand PB-166 asked for was then run (claude 2.1.263, 2026-09-12) and moved the question upstream of `stop` rather than answering it in `stop`'s favour. A participant's turn started a marked long-lived process; `done` closed the session and the process was in `ps` before and after, one occurrence each time — readable as a result only because the "before" reading exists, since "nothing is left" is equally green on a stand that started nothing. The columns are the finding: `ppid=1 pgid=30358` in **both** readings, so the process was an orphan in a group of its own before the stop, `nohup … &` having detached it behind a short-lived tool shell. Neither shape the card proposed would have reached it — a tree walk from the pid `claude agents --json` names finds nothing outside that tree, and a group kill on the session pid finds nothing outside that group; only the stand's own mark did, and the mechanism cannot mark a command a worker writes. So `stop` has nothing to walk by the time it is called, and what is open is upstream of it: how the mechanism delimits "this session's processes" when neither the tree nor the group holds them. One candidate — lifting a session into a process group the mechanism creates and sweeping by it — is a change to the lift and is not made here. The stand's boundary is recorded with it: its process was detached, so a descendant still attached at the moment of the stop is not covered. (PB-166.1)
- **The three non-approval Codex server requests are now derived from the measured schema instead of a typed list.** `item/tool/call`, `account/chatgptAuthTokens/refresh` and `attestation/generate` are still answered with `-32601`, and a re-capture that adds a fourth now fails the suite rather than having it answered by an undecided fallback. 03-cli names them and says what the token-refresh refusal costs: a turn whose ChatGPT token expires mid-way ends with an authentication failure, which is expected rather than a fault of the holder. (PB-88.4)
- **The warden switch and the run's auto-lift trace now reach a Codex participant's MCP server.** The bus entry `mcpConfig` writes for a Codex participant composes its `env` rather than inheriting one, so a name it does not carry never reaches that child; `PROMPTOBUS_WARDEN` and `PROMPTOBUS_WARDEN_TRACE` are now forwarded there when the lifting command has them set. Both halves of the suite's run-level auto-lift gate are those names, so a warden raised from a participant's MCP server used to be neither stopped by the switch nor recorded in the trace — the gate was silent about exactly the process class it exists to catch, and silent for a benign reason: the suite lifts no real participant on a real binary, so a participant MCP server never runs under it. Nothing changes in a real run, which sets neither variable. The boundary itself is now written down where the gate is described, in `test/run.mjs`, `test/hygiene.mjs` and the [contributing guide](docs/guides/contributing.md) § Suite isolation: the gate reaches as far as the environment does, and a child that composes its own environment is inside it only because the composing code names the two variables. The Claude driver composes no `env` for the bus entry at all; Cursor did not either until the entry above, which carries the same two names there for the same measured reason.
- **`promptobus status` no longer prints one `self-wake` label over three states whose prognosis differs.** The warden falls back to `self-wake` from three branches — the participant has not handed over a contact point, a foreign session holds the contact point, or the driver's channel refused the notification — and the status line showed the same word for all three plus the raw reason, so a reader could not tell waiting from fixing. Live cost: the orchestrator of the 2026-09-10 run read the start-up label on its own address as a broken channel. The round now records WHICH branch it took (`selfWake` in the health record, with `selfWakeChannel` for the refusing one), and the line carries a prognosis after the reason: starting up clears on the first knock; a held contact point clears when the address's own session takes it back, and not at all if that session is gone; a refused channel names the channel the journal names (`socket` / `inject` / `rpc`) and stays until the channel accepts. A health record written before the field still prints its line, reason and all — the prognosis is then not said rather than guessed, except where the wake record itself shows no contact point at all. The finding came from the consumer's tracker; its claim that a held contact point never clears is not what the code does, and the line follows the code.

## [0.6.0] - 2026-09-10

### Added

- **Recovery results expose unfinished and permanently lost fan-outs.** The exported `RecoverFailure` and `RecoverResult.failed` identify a retryable hard-link refusal as `link-refused` and an intent lost before materialization as `intent-lost`, with the affected task, message, and diagnostic note. (PB-65)
- **Bulk task listings retain machine-readable failure codes.** Each `BrokenTask` now carries the exact typed refusal code, so callers can distinguish a newer mechanism record from a damaged or unreadable journal without parsing diagnostic text or reading the task again. (PB-146)
- **Artifact metadata and blob read refusals have their own typed code.** `artifact-not-found` is reserved for `ENOENT` on both the metadata and the blob read; another read errno is reported as `artifact-broken` with the errno in `context`. (PB-146.1)

### Changed

- **The orchestrate skill's CLI synopsis now tracks spawn, done and review flags through a drift gate.** (PB-47)
- **The model-routing reference now distinguishes hidden rows retained in availability snapshots from rows filtered by the resolver and `models` output.** (PB-104)
- **ADR-004 now records the shipped hidden-row projections.** Hidden rows remain in availability snapshots, while the resolver's inventory, `runtime` list and `models` output filter them; hidden-model exclusions name the account. (PB-104.1)
- **The documented meaning of `migrationNeeded` now includes mark-only sweeps and points callers to `preflight().sweep` to distinguish them from data moves.** (PB-63.1)
- **The engine reference now documents that `history()` and `recover()` cover readable tasks, while callers pair `listTasks().broken` with those results for unreadable journals.** (PB-146.2)
- **Reviewer MCP classification now reports whether the host finished inspecting its canonical external servers.** `participantDenyTools(role)` returns `{ tools, complete }`, and a complete empty list is distinct from an unclassified server. A legacy array answer is incomplete, so hosts must ship `{ tools, complete }` in the same pin; drivers must declare `phrases.mcpBoundary`, the sentence the reviewer prompt carries about their MCP write boundary. (PB-87.1)
- **CLI help now explains the recorded strategy default that effectiveStrategy applies for spawn, review and models.** (PB-84)
- **`status` now prints each participant's review-round, question and result counts from canonical messages before `done`.** (PB-48)
- **Calibration documents and tests now pin the deterministic eligible-key pivot tie-break.** (PB-145)
- **The glossary now defines holder, contact point, persist session, thread and session record, and the Codex holder reference points to those terms.** (PB-110)
- **`models calibrate --write` now prints its text proposal before confirmation and refuses non-interactive writes without `--yes` even when no rating would move.** (PB-62)
- **`guardArgv(args)` is now required on `PromptobusHost`.** Existing host implementations must provide the layout guard argv before calling `install`, `spawn`, or `review`; otherwise guard hook assembly throws `TypeError` in `lib/install.js`, `lib/spawn.js`, or `lib/review.js`. (PB-103)
- **Participant telemetry now ends completion duration at the latest result message, falling back to dismissal and then task close.** The record carries that additive timestamp so worker and reviewer runs no longer share a close stamp. (PB-56)
- **The reference now matches the current store protocol, host surface and task layout, including recovery, validation and fault-injection contracts.** (PB-112)
- **ROADMAP now matches the shipped catalog's tuple count and default quality floors, with a catalog regression check.** (PB-46)

### Fixed

- **Codex participants are told the MCP tool names Codex actually exposes.** The order text, the wake text and the consumer host's `toolName()` glued the override key verbatim (`mcp__acme-tools-promptobus__promptobus_send` for a consumer CLI named `acme-tools`), while Codex lists the key sanitized (`mcp__acme_tools_promptobus__…`, every character outside `[A-Za-z0-9_]` → `_`, measured on codex-cli 0.146.0); a call by the told name died in 0 ms inside Codex's `exec` harness with no text, and the participant spent its turn on CLI workarounds (found by a consumer on a live run). `codexToolSegment` in `lib/driver-codex.js` is the one place the sanitization lives; the config key stays raw. The holder log now names a failed MCP call with server, tool, status and reason (`event item/completed mcp <server>/<tool> failed {…}`) instead of a bare `event item/completed`. (PB-160)
- **Warden postcards and participant status now name unreadable mailbox refs with their errno and ref name, while retries remain unbounded until the filesystem refusal clears.** (PB-66.1)
- **Classified recipient inbox directory refusals now use the retryable `link-refused` classification during fan-out.** The intent stays open while recovery and store commands continue. (PB-65.1)
- **Refusal and stall guidance now uses the host's bus command formatter, with a regression gate for hardcoded subcommand literals.** (PB-140)
- **Review layout and argument refusals now print as one plain CLI error without a stack trace.** (PB-54)
- **Solo review now picks up an active task from its reviewer record when the target directory has no worker worktree claim.** (PB-96)
- **Prune and calibration hints now use the host's bus command formatter for package-owned commands.** (PB-55)
- **Review snapshots now refresh Git's index stat data, so touching or rewriting a tracked file with identical content no longer reports a dirty tree.** (PB-157.1)
- **`done` now removes a mechanism-created `worktree-` branch together with its worktree when content measurements prove the work merged, including a squash merge.** Branches moved by a worker and worktrees without proof remain in place. (PB-158)
- **The live canary now narrates and verifies the tarball install, version check and whole-run home snapshot that it actually performs.** (PB-113)
- **The publicity audit now rejects private runtime identifiers and workspace ids while preserving historical evidence cards.** (PB-73)
- **The publicity audit now enforces the ROADMAP English-runtime claim on tracked runtime files and the packed tarball, with narrow exemptions for functional Cyrillic data.** (PB-75)
- **The contributing guide now matches the current gates, publicity audit, suite-isolation count and Cyrillic sweep scope.** It no longer duplicates the backslop version pin and records that `scripts/` and `test/` remain outside the current sweep. (PB-72)
- **The process-wide child output ceiling is named independently of the git callers while the legacy util export remains compatible.** (PB-50.1)
- **The publicity audit resolves its root portably and launches npm and tar through the shared process runner.** (PB-138)
- **CI now pins ast-grep, names the existing TypeScript build, covers Node 20 and 22, and relies on the package test for tarball coverage.** (PB-94)
- **Package metadata now declares the public GitHub repository so registry-rendered README links resolve to their source files.** (PB-137)
- **The suite sandbox-prefix sentinel now rejects both uncovered literals and dead list entries.** (PB-143)
- **Cursor and Codex harness diagnostics now surface scenario errors before the trace tail.** (PB-142)
- **The publicity audit now resolves the file half of Markdown links that carry fragments, while pure in-file anchors remain exempt.** (PB-107)
- **Bus-hook headers now use the host's regeneration hint, and the reference documents the shipped bin's fixed command name and package version.** (PB-109)
- **Install now takes the PostToolUse, Stop and SessionStart hook event names from the compiled declarations, so generated settings cannot drift from `src/hooks.ts`.** (PB-119.1)
- **Codex approval checks use named request fields.** Text in patch diffs and commands no longer triggers config or privilege denials, while dangerous values in named top-level `sandbox`, `approvalPolicy`, and `permissions` fields remain denied unless an approval-policy or sandbox value echoes the session's recorded mode. (PB-88)
- **Codex approval handling now follows the measured codex-cli 0.146.0 app-server schemas, returns JSON-RPC method-not-found errors for requests without approval rows, and uses lawful permission-response scopes, root-grant denials and pathless file-change refusals. Before this fix, every approval denial was schema-invalid: `denied`, `reject` and `scope: 'once'` are in no response enum.** (PB-88.2)
- **Codex permission requests are refused for workers and reviewers, with the requested filesystem entries and network flag named in the refusal (each rendered value bounded to 400 characters); the holder never grants a permission and uses the lawful turn-scoped empty response.** (PB-88.1)
- **The Codex holder and availability probe now share the `initialize` handshake builder, so the probe reports the package version instead of `0.0.0`.** (PB-116)
- **Codex session guidance now names the host-resolved registry directory, with a source fallback when no home is declared.** (PB-98)
- **The Codex session record no longer persists the caller's environment.** The detached holder inherits the sanitized launch environment directly, while newly created session directories use mode `0700` without changing the mode of an existing directory. (PB-152)
- **Codex dry-run output no longer treats the prompt as an app-server argument; it identifies the `turn/start` request that carries it.** (PB-121)
- **Codex threads now use the mechanism's readable session name, with a machine-name fallback for older records.** (PB-156)
- **Codex holder fallback sockets are isolated by registry home.** When a registry's nested session socket path is too long, the `/tmp/pb-cdx-…` fallback now hashes the registry `sessions` directory together with the session ref, so two registries cannot take over the same socket. (PB-115)
- **Status, history, dismiss, and prune now use the task-store home declared by the host.** A host can place the bus outside `<workspaceRoot>/.promptobus` without making those four commands read an empty journal. The swap loses no former-layout migration on either shipped host: the ATI host member already routes through the migration-aware store door, while the standalone host declares no former layout. (PB-101)
- **Mixed-version task journals now name the real writer and reader releases.** CLI dispatch selects the host-home commands' version, while the guard, warden, and MCP bootstrap select the home they resolve; engines are cached per home and reader version, and new or claimed orchestrator records plus automatically registered senders carry the selected version. An unfamiliar field written by a newer plain, prerelease, or build version therefore returns `schema-version-unsupported` instead of generic journal corruption. (PB-102)
- **Participant records no longer invent a `0.0.0` writer release.** `mechanismVersion` is present when an entry point selected the host version; low-level reads and the three adapter writers warn with their store or record when no version was selected, and leave the public metadata field absent. (PB-102)
- **The synchronous send input type now leaves the package.** `SendSyncInput`, the parameter type of public `Engine.sendSync`, is exported from the `.` entry point beside `SendInput`, so TypeScript consumers can name the contract without copying its fields or using a deep import. (PB-135)
- **The exported message-type list is immutable at runtime and readonly in TypeScript.** `MESSAGE_TYPES` is one frozen list and `MESSAGE_TYPES_V1` exposes that same object, so a JavaScript consumer cannot make the record or engine validator accept a foreign type for the rest of the process. (PB-131)
- **Task IDs now have one 128-character grammar at the CLI gate and in store v1.** The bounded regex is canonical in the protocol dictionary and the v1 model re-exports that same object, so CLI-facing validation and legacy migration cannot admit an ID that store paths later reject as `task-not-found`. Migration refuses before mutation if a non-empty entry under the former `tasks/` has a name outside that grammar, rather than skipping unknown data and deleting it during normal or resumed cleanup; empty invalid directories remain ignored. Exact-bound fixtures also hold the task ID, participant ID, title, and artifact filename limits against their schemas. (PB-132)
- **Routing policy overlays no longer move policy-block prototypes through `__proto__`.** The overlay merge skips the JSON key in weight and flat policy blocks, keeping routed policy fields own data and matching the existing guards for defaults and account blocks. (PB-155)
- **The Cursor availability adapter guards its stdout pipe against stream errors.** Like Claude and Codex, it lets `close` carry the outcome without an uncaught stream exception. (PB-149)
- **The overlay parity corpus covers balance, near-limit, defaults and account blocks.** It also checks that an account answer for an unknown harness is rejected before the overlay can persist. (PB-136)
- **The models command refuses flags outside their owning subcommand.** `--set` and `--clear` require `models strategy`, while `--write` and `--yes` require `models calibrate`; none is silently ignored on bare `models`. (PB-61)
- **The live Cursor review step now requires a result from the reviewer participant, so stale worker results cannot satisfy it.** (PB-68)
- **Live canaries no longer prepend a PATH-only resolver result, so the current directory cannot shadow child commands.** (PB-69)
- **Availability cache writes drop invalid harness keys.** `writeEntries` applies the shared `HARNESS_RE` before adding a harness to the snapshot, reports dropped names through preflight, and keeps malformed workspace names out of the schema-validated cache. (PB-125)
- **Promotional quota-cost citations carry an observation date and list-price replacement.** The two shipped Gemini rows warn from 2026-09-07 until re-verified; `models validate` names the row, promotional figure, list figure and list-price band without invalidating the catalog. (PB-37.1)
- **The two Gemini promotion citations now name the promotional figure.** Their expired-promotion warnings distinguish the $2.25 promotional blend from the $4.50 list blend. (PB-37.4)
- **Cursor wake buffers are private and cleaned up on every delivery exit.** Wake text is written with mode `0600`, regardless of umask, and the temporary `.buf` file is removed in `finally`, including when `tmux load-buffer` refuses the file. (PB-151)
- **Status names a missing harness registry instead of saying nobody can be asked.** A `GateError` caught while assembling the participant snapshot stays on that participant as the reason of its `unknown` state, while unrelated driver errors remain reasonless and the walk continues. (PB-45.1)
- **Routed catalog failures name the layer that caused them.** Host overlay declarations, unreadable or invalid overlay files, unsupported overlay schemas, and shipped-catalog failures now keep their source through the merge so routed calls return the same `overlay-invalid` or `catalog-invalid` attribution as `models validate`. (PB-78)
- **A broken routing overlay no longer silently drops the recorded strategy default.** `spawn` and `review` warn with the layer, path and `promptobus models validate` route before taking the legacy path. (PB-60)
- **Transient mailbox and intent I/O failures no longer masquerade as corrupt records or discard a partial pass.** An unreadable ref stays in the mailbox and is reported while messages already moved to history are returned. An unreadable intent is likewise reported and retained for retry while recovery continues through other intents and tasks. (PB-66)
- **Unreadable records no longer report that they were absent.** An existing task journal refused with a non-`ENOENT` errno is `task-broken`, while `ENOENT` remains `task-not-found`. Unparseable artifact metadata returns `schema-invalid`; genuinely missing metadata remains `artifact-not-found`. (PB-146)
- **A completed migration marker can no longer authorize deletion of a legacy store that later reappears at the same path.** New migrations use `migrating.json` only across the atomic switch and legacy cleanup, then remove it after cleanup succeeds; if that final removal was interrupted, the next open sweeps the mark while its former path is absent. The `migrated.json` files left by older releases are not resume tokens, so two stores beside one another are refused without changing either one. (PB-63)
- **Claude late-start limit classification checks reset wording independently.** A refusal containing both `hit your ... limit` and `limit ... resets` is recorded as `subscription_exhausted`. (PB-148)
- **A failed availability-cache write no longer hides a successful preflight.** The probed snapshot is returned with a warning naming the cache file, while explicit `clearExhausted` writes remain refusals. (PB-58)
- **Cursor status reports the recorded turn count.** A participant's status uses the hook-owned `record.turns` counter instead of the transcript's end-of-file marker count. (PB-153)
- **Balance keeps the quality floor above pace.** When a paced representative meets the role's floor, only floor-meeting representatives enter the balance comparison; the existing floor-warning wording is reserved for runs with no scored candidate at the floor, while a scored but unpaced floor candidate is named beside the below-floor pick. (PB-59)
- **Near-limit strategy proposals use both threshold tests.** When every paced harness is short by either level or rate, the warning proposes `economy` even if no binding window has crossed the usage-level threshold. (PB-105)
- **Cursor tmux discovery matches Cursor binary install locations.** The driver probes PATH plus the known install directories before refusing a persist session. (PB-85)
- **A classified hard-link refusal no longer blocks every store command during open-time recovery.** Recovery retains that intent for retry and continues through neighbouring work; a message whose intent and canon disappeared before materialization is reported as permanently lost instead. The bus warns about either result while `status`, `history`, and `prune` remain usable; unrelated exceptions still escape. (PB-65)
- **Cursor teardown prefers the declared binary and caller environment.** Stop resolution now prefers `cursor-agent` over a bare `agent` and uses the stop caller's environment for both lookup and execution. (PB-93)
- **Balance compares every eligible pool of a harness.** Each pool contributes its best eligible tuple to the pace comparison, and `models` prints one pace row per pool so a roomier Cursor pool is not hidden by a higher-scoring one. (PB-43)
- **Terminal-Bench 4.0 now has a dated Claude Code anchor pair.** Fable 5.1 records the 4.0 leaderboard figure as supporting evidence but remains at its predecessor's band as a stated hypothesis under the successor rule; re-rating guidance keeps figures without an exact pair as hypotheses. (PB-37.3)
- **Claude Code's proven inventory now includes entitled Haiku.** Claude 2.1.263 accepted `claude-haiku-4-5` on a one-turn liftoff, so the driver publishes its `haiku` alias and model-scoped limit ids and the catalog carries a worker ladder rated from Anthropic's 73.3 % SWE-bench figure, Artificial Analysis's 81 tokens/s and the $1/$5 list price. The same check refused `claude-mythos-5-1`, which remains out. (PB-34.1)
- **Telemetry no longer treats exhausted cache entries as fresh end readings.** A future-reset or sticky exhaustion remains live for routing, but its window percentage is `null` in participant telemetry once the sixty-second window TTL has passed, so `models calibrate` cannot mistake a frozen value for spend evidence. (PB-77)
- **Live mixed-lineup matchers now read protocol v1 sender IDs.** Worker and reviewer checks share the normalized sender predicate, so a live run no longer waits for messages under the caller's unstored address spelling. (PB-67)
- **Loop guard hooks now use the host's launch argv.** The generated command calls the layout entry and host-declared subcommand path, so a non-default command name reaches `guard` instead of carrying a package-specific word. (PB-103)
- **`done` now refreshes the window-bearing harnesses represented by telemetry records before writing them.** It reuses the 15 s preflight budget, and a refusal or timeout leaves that harness's end reading `null`, prints a warning, and still closes the task. (PB-37.2)
- **Guard ownership survives copied project hooks.** When the install manifest has no matching id, any `Stop`/`SessionStart` (`stop` for Cursor) command with the rendered guard shape is treated as ours regardless of its binary or paths and is replaced or removed; other guard-like commands remain foreign. (PB-52)
- **Install leaves existing unselected harness files byte-identical, and check no longer reports drift for files it was not asked to manage.** (PB-53)
- **Install check now includes the machine-local manifest, and dry-run lists only writes or removals that would change content.** (PB-100)
- **Cursor install validates the merged hook event map before writing, so unknown event names refuse the install without changing the existing file.** (PB-154)
- **Install documentation now describes Cursor's stop-only project hook and driver-injected bus feedback, including the invalid event-name warning.** (PB-76)
- **The package test resolves every public exports-map specifier from an installed tarball, including host, hooks, driver, CLI and a protocol schema.** (PB-108)
- **Live-run sweeps now age-filter mixed sandboxes, cover Codex and E2E prefixes, and report refused cleanup entries.** (PB-114)
- **The test runner sweeps stale socket directories and preserves live neighbouring runs through owner and listener liveness checks.** (PB-144)
- **Package tests now require the first dated release heading and any exact `v<version>` tag to match package.json; tag pushes run the existing CI workflow.** (PB-70)
- **`models calibrate` derives one constant `speed` band per harness/model from pooled throughput observations; completion duration remains evidence only, and missing throughput never produces a rating.** Participant records retain the harness-reported output-token evidence with missing components as `null`; the guard appends per-turn evidence to `waits/<addr>.throughput.jsonl` for `done` to project. (PB-57)
- **Codex mutation approvals compare canonical paths and fail closed.** Symlinked roots are accepted; segment-wise traversal follows symlink targets before containment, including missing tails, and denies escapes or resolution failures while retaining requested and resolved spellings and the TOCTOU boundary. (PB-89)
- **Warden wake fingerprints now separate turn ends from session restarts.** A pid or hand-over-time change on the same socket no longer triggers an immediate duplicate knock, while turn-counter socket rewrites still wake immediately and repeat only messages a restarted session has not seen. (PB-64)
- **Self-wake health replaces a stale knock error when a contact point disappears.** The warden records the no-contact-point reason, clears the old fingerprint, and status prints the current diagnosis. (PB-79)
- **Review and spawn Git reads now share bounded wait/output ceilings and consistent non-ASCII path rendering; tracked `.cursor` checks are included in the execution trace.** (PB-126)
- **Spawn preambles now report dependency-install outcomes, and launch files are written after repository generation and dependency installation.** (PB-83)
- **Claude reviewer launches now mechanically deny host-classified external MCP write tools; participant capability snapshots gain the optional `mcpDenyTools` flag, while Cursor retains an explicit prompt-only MCP boundary.** (PB-87)
- **Codex reviewer MCP write tools now translate into per-server `disabled_tools` entries on `thread/start`.** Cursor remains prompt-only pending its separate live verification, and the 0.146.0 no-turn fixture records the accepted configuration and server readiness. (PB-87.1)
- **The CLI now recognizes expected refusals by their exported error classes, so an unrelated class with a colliding constructor name retains its stack.** (PB-80)
- **`models calibrate --write` now names higher-layer rating shadows in its warning and JSON write outcome.** (PB-91)
- **Guard successor detection now uses the production hint seam, and hook stdin has a deadline.** The unused verdict entry point is gone, while an unclosed event stream becomes an empty payload after the shared 200 ms budget. (PB-123)
- **`models calibrate --write` and `models strategy` now refuse non-object overlays before writing, preserving the person's original file.** The calibration reference also states that a proposed rating replaces a hand-set value for the same tuple and field. (PB-106)
- **Spawn and review now print the rules file list on real lifts as well as dry-runs, so the participant preamble is available for the first-status check.** (PB-44)
- **Standalone plugin-less hosts no longer emit a missing-plugin warning; participant skills notes name that no workspace-skills plugin is shipped, and declared plugin hosts name the manifest path actually checked.** (PB-51)

## [0.5.1] - 2026-09-09

### Changed

- **Project gates run the full test suite before lint and the package audit.** Added `npm test` as the first backslop gate so a broken suite stops worker verification before the slower build-and-pack audit and before it reaches CI on `main`. (PB-71)

### Fixed

- **A Codex reviewer is lifted with `turn/start`, not `review/start`.** `review/start` was an observed stall of the bus mailbox on 0.146.0; a worker `turn/start` on the same account completed mailbox. Both roles now send `turn/start` so bus tools stay on the participant thread. Read-only is the sandbox. (PB-41.1)

- **A Codex holder declines `mcpServer/elicitation/request` with `{ action: "decline" }`.** The participant has no person to fill a form. The log names the method plus allowlisted `server` / `mode` and the JSON-RPC `id` only — never the prompt, schema, URL, or params. (PB-41)

- **A Codex reviewer's routed effort is sent on `thread/start`.** The holder puts `config.model_reasoning_effort` on `thread/start` for both roles and also sends `effort` on the first `turn/start`. Measured on codex-cli 0.146.0 with no paid turn: the thread echoes the asked level as `ThreadStartResponse.reasoningEffort`. Catalog Codex reviewer tuples keep the role. (PB-90)

- **A Codex turn is watched by its events.** The holder records `lastEvent` / `lastEventAt` / `pendingRequest` (method, server, mode — never the prompt or params). Silence past `PROMPTOBUS_CODEX_IDLE_MS` (default 180 s) is a watchdog stall. A pending server request is a waiting note immediately and a `pending-request` stall only after that budget; overlapping requests are keyed by exact id. A `turn/completed` with status `failed` stays visible even after a prior bus status and clears on a later successful turn. Unmatched JSON-RPC responses are logged as `orphan id=<id>`. (PB-42)

- **Codex JSON-RPC dispatch no longer treats a server-to-client request as the reply to a pending client call when the integer ids collide.** A message that carries `method` is handled as a request or notification first; only an id without `method` resolves the pending pool. An unmatched response is logged as `orphan` instead of disappearing. (PB-97)

- **A Codex holder that cannot spawn app-server fails the session record with that error instead of dying on an unhandled `error` event and waiting out the ready budget.** Pipe errors on the child's stdin and stdout are handled so a write to a dead app-server is not an uncaught exception. (PB-49)

- **A Codex participant stuck at `starting` with a dead holder is no longer shown as rising forever.** An error on the record, or a `startedAt` older than the ready budget, is `stale` so `status` can print a LISTED relift route. Inside the budget the rising note is unchanged. (PB-99)

- **Registry readers no longer turn a missing harness-state home into an empty registry.** Cursor and Codex session reads now preserve the `GateError` that names `PROMPTOBUS_<HARNESS>_HOME` and `harnessStateHome`, and direct driver `inspect`, `stop` and `activate` calls propagate it. The aggregate `snapshotSessions` path still degrades driver errors to `unknown`, so it does not surface the refusal, while wake-side `registerWake` and `sessionPrefix` retain their safety catches. `gone` and “no session record” now mean that a declared registry was actually opened but held no matching record. (PB-45)

- **Review snapshots expose a dirty tracked tree at the same pass that reads the patch and stat.** `promptobus review` still compares the selected base with the working tree so purely uncommitted work remains reviewable, but its output, reviewer prompt and participant journal now say whether that tree matched `HEAD` and name modified tracked paths when it did not. The prompt requires a current-file check even after a clean snapshot and routes contradictions back to the orchestrator as unresolved. A mutation probe can no longer silently make committed content disappear from the review subject, or make an uncommitted edit look committed, without the state being recorded. (PB-157)

- **Nested suite hygiene keeps the runner's Claude config diversion.** A suite file that imports `home.mjs` now preserves an existing `CLAUDE_CONFIG_DIR=<run home>/.claude` while that diverted home is live, instead of deleting the runner-issued value before the file's own child processes inherit it; the runner probe now exercises that second hygiene pass. (PB-141)

- **Harness process launches have shared time and output ceilings.** Every launch through `run` now carries the project's 60-second timeout and 32 MiB output budget unless its caller overrides either value, so a wedged or over-verbose driver read becomes the existing unknown-liveness result instead of blocking the warden indefinitely. The same budget keeps Cursor's dirty-worktree liveness read from collapsing to “no writes” when `git ls-files` output crosses Node's default. (PB-50)

- **Windows launch planning and tracing honor the caller's environment.** `run` now gives `planRun` the same caller-supplied environment it passes to the child, and the execution trace resolves Windows commands through `PATHEXT`, so a sealed or deliberately reduced `PATH` cannot be replaced by the parent process's paths or reported as `(unresolved)`. (PB-122)

### Documentation

- **Backlog triage and execution tracks.** Reviewed the accumulated findings, consolidated duplicate records, separated focused repairs from deferred structural work, and recorded priorities, dependencies and shared-file boundaries in `docs/TRACKS.md`. No runtime behavior changed.

## [0.5.0] — 2026-09-06

### Added

- **`promptobus models calibrate` reads this machine's own runs back against the shipped catalog and proposes overlay lines.** PB-36 began writing `telemetry.jsonl` at every `done` and nothing read it: a person could see that their serving stack or their subscription behaved unlike the catalog and had no reproducible way to turn that into an overlay correction. The command groups records by `(harness, model, effort)` and never by `tuple` — a lift made with an explicit `--model` carries no routing decision, and 18 of the owner's first 21 records had `tuple: null`, so grouping by tuple would have dropped most of the evidence and then reported a confident median over the rest. A model alias is resolved to a full id through the driver's own dictionary, handed over as `options.modelAliases`, so runs typed `--model opus` land on the row that rates `claude-opus-5`.

  Per key it prints the run count, the medians of `durationSec`, `turns` and `reviewRounds` **per accepted piece** — not dismissed before `done`, and at least one result sent — and the median window movement, which is one number per run: the largest delta among the windows that applied to it, because the resolver scores that tuple on `100 − max(usedPercent)` over the same set and a sum would count one account's session and weekly windows twice for the same tokens. A dismissed run counts as a run and as spend and never as a finished piece; a window that went backwards mid-run is a reset and contributes nothing.

  **The catalog is the anchor, not the local extremes.** Mapping the local minimum and maximum onto 1 and 10 was considered and rejected: with two eligible keys they become opposite extremes however close their measurements, which is exactly the relative-field defect the catalog itself just shed. Instead the eligible key with the most runs is the pivot and keeps its catalog bands, and every other key moves from **its own** band — one band at a 1.5× surprise against the ratio its bands already imply, two at 3×, capped at ±2 and clamped to 1…10. The evidence threshold is 5 runs per key and is a constant of the ADR rather than an overlay key; below it the line reads `insufficient data: N of 5`, and a key no catalog row rates gets its medians and no proposal. `quality` is never proposed from telemetry — review rounds measure how work was received, not how good a model is — and the accepted-piece counts are printed as a note.

  `--json` prints the whole report. `--write` merges only the proposed `ratings` into the layer whose id is `user`, per tuple and per rating, keeping every other key of that file and every rating set by hand; only ratings that MOVED are written, since an override equal to the catalog band would go on overriding after the next catalog update moves the row underneath it. Every band it compares against is the **shipped catalog's**, never the merged stack's: otherwise a person's own override would become the base of the next comparison and the rating would walk a step per run on unchanged data, so running the command twice on one file proposes the same thing twice, and an existing override is printed beside the catalog band rather than standing in for it. It asks on the terminal and takes `y` or `yes`; **without a terminal it refuses unless `--yes`** is given, which is the record that the person already agreed to exactly those lines — `--yes` without `--write` is refused rather than ignored. This is the one exception to the `user` layer being read-only for the tool, and it is that narrow: only this command, only `ratings`, only after agreement. (PB-37)

### Changed

- **The Claude driver is proven on `claude` 2.1.263, and Claude Fable 5.1 is routable.** The driver proved 2.1.251, whose baked model table resolved the `fable` alias to `claude-fable-5`; `MODEL_IDS` is read off that table, so `claude-fable-5-1` — the model 2.1.257 made the default of the family, and the best model the account can run — was in no inventory, could carry no rated catalog row, and could never be a **routed** pick. Nothing went red about it: `--model claude-fable-5-1` was passed through and would have started, so the harm was a silently smaller candidate set in which the run takes the second-best model and reports success.

  Read offline out of 2.1.263 (PB-34, no paid turn): `fable:{default:"claude-fable-5-1",per_provider:{gateway:"claude-fable-5"}}`, `latest_per_family.fable:"claude-fable-5-1"`, `alias_migration` still `{}`, and **both** ids present as first-party rows of `family:"fable"` — "Fable 5" with a January 2026 cutoff and "Fable 5.1" with a June 2026 one — in the binary's own array of accepted ids. So `MODEL_IDS` gains `claude-fable-5-1` and keeps `claude-fable-5`: the alias moved, the predecessor did not stop being a name the binary takes, and dropping it would have been a guess where a read was available. `MODEL_SCOPE_IDS.fable` now names both, because the harness prints one "Fable" weekly window and every model of the family spends it — a table naming only the current alias target would leave a tuple on the predecessor bound by no window at all, which the resolver reads as "no limit known" rather than as the shared limit it is spending. `MODEL_ALIASES`, `PERMISSION_MODES` and `EFFORT_LEVELS` were re-read from `claude --help` on 2.1.263 and are unchanged.

  **The alias table and the limit-scope table are two tables now.** The Claude driver handed one object to both `options.modelAliases` (which `models calibrate` resolves a recorded `--model fable` through) and the availability adapter's model scopes. They agreed for as long as every family served one model. Giving `MODEL_SCOPE_IDS.fable` its second id would therefore have put two ids behind the `fable` **alias**, and `calibrate` resolves an alias in front of two ids to none of them — so every aliased Fable run would have grouped under the unresolved name, matched no catalog row and quietly proposed nothing, looking exactly like a key with too few runs. `MODEL_ALIAS_IDS` now names the one id each alias points at today, read from the same baked catalog; `MODEL_SCOPE_IDS` stays the family table the weekly window is scoped by. The reference states the limit this leaves: an alias resolves to where the proven binary points it **today** and a telemetry record carries no binary version, so runs recorded before a re-point are rated against the successor.

  **The catalog gains a `claude-fable-5-1` ladder, and the `claude-fable-5` one stays.** Three rows under the ADR-005 rule — `claude-fable-51-xhigh` as the assessed base with `high` and `max` interpolated one step either side, `quality` 10 / `speed` 2 / `quotaCost` 10 at the base — and the whole Claude priority block renumbered from the highest quality down in steps of ten, 100 to 220. `quality` is a **stated hypothesis** and says so in `evidence.hypothesis`: no SWE-bench Verified figure is published for that exact model, on the leaderboard this catalog reads or on Anthropic's own launch page (both seen 2026-09-06). The vendor published a head-to-head against Fable 5 on six other benchmarks instead — Terminal-Bench 4.0 55.8 % against 42.0 %, Terminal-Bench-Science 0.1 52.6 % against 24.7 %, Humanity's Last Exam 60.9 % against 57.8 %, GDPval-AA v2 1853 against 1723, OSWorld 2.0 strict 41.7 % against 36.1 %, CursorBench 3.2.0 73.4 % against 70.5 % — with 5.1 ahead on every one, and none of those has an anchor pair in ADR-005's table, so banding from them would be a catalog-wide re-band rather than a row. Hence the reasoning written into the row: Fable 5's 95.0 % bands 10, the scale stops at 10, and a successor ahead on every published comparison cannot sit below its predecessor. `speed` and `quotaCost` are **cited**, not hypothesised: Artificial Analysis at 70 tokens/s → 2, and Anthropic's own launch page at $10 in / $50 out → blended $30 → 10 (the cache-read cut to $0.25 is real and the blended rule does not read it). The same leaderboard read turned `claude-fable-5`'s `speed` from a hypothesis into a citation at 72 tokens/s, in the band 2 it already held.

  **The `claude-fable-5` rows stay, and the evidence for keeping them is the binary's own table.** 2.1.263 holds both ids as first-party rows of `family:"fable"`, lists both among the ids it accepts, leaves `alias_migration` empty so nothing is rewritten, and gives Fable 5.1 `fallback_3p:"claude-fable-5"` — the vendor saying the predecessor is still served. A rated row goes when the vendor stops serving the model; an alias moving off an id is not that fact, and is in fact the case id-keyed rows exist to survive. Two `ladder-indistinguishable` warnings are expected and correct: `xhigh` and `max` coincide on both Fable ladders because `quality` and `quotaCost` are already clamped at 10, and both rungs stay because the effort is still a launchable choice.

  `PROVEN_CLAUDE_VERSION` is 2.1.263. It is still a **measurement and not a floor** — no lift is refused on it — and what it records is that warden delivery by injection into a live session's messaging socket was observed to work on that build. Re-measured live on 2026-09-06 in a throwaway standalone workspace: a participant on `idle`/`done`, a bus message out at 07:56:26.508Z, the session back on `busy`/`working` and its reply in the orchestrator mailbox at 07:56:32.744Z — 6.2 s, nothing else touching it — and the `claude agents --json` record form unchanged from 2.1.251. `promptobus status` and `doctor` now print `injection proven on claude 2.1.263`. The reference records how the proven version is chosen, what a lag costs, and the lift-and-knock procedure for re-proving it.

- **Ratings move to a 1–10 scale with absolute bands, and every overlay that carries `ratings` must be rewritten.** [ADR-005](docs/adr/adr-005-ten-point-scale-absolute-bands-calibrate.md) records the owner's decisions of 2026-09-06 and closes the five defects PB-29.1 found in ADR-004 § Catalog ratings. Five ranks cut over a changing field could not discriminate at the top — Opus 5 at 96.0 and Fable 5 at 95.0 necessarily shared the highest band — and a row moved when a neighbour was added or removed. So a band is now absolute: `clamp(1 + roundHalfUp((x − floor) / (ceiling − floor) × 9), 1, 10)` against a **dated anchor pair** per benchmark, version and agent harness, fixed in the ADR and revisited at every catalog update. There is no field any more, and a figure without its version and its harness is not a figure — the same Grok 4.6 was published at 88.4 on Terminal-Bench 2.1 and 26 on 3.0.

  **Migration, in one line: rewrite `ratings` on the 1–10 scale and set `schemaVersion: 2`.** An overlay on version 1 that carries any value on the rating scale is refused by the load and by `models validate` with that sentence, which names the keys it found. Three keys are on that scale: `ratings`, `qualityFloor` and `reviewerQualityFloor` — the floors included because `reviewerQualityFloor: 5` meant the top band of five and reads here as half way up ten, so a file read unchanged would lower a person's reviewer floor from the new default of 9 to 5 in silence. Nothing is scaled for them, because a 3 was a middle of five and is a low third of ten and a value on a replaced scale is indistinguishable from one on the new scale. An overlay on version 1 carrying **none** of the three — a deny or allow list, weights, a strategy default, an account answer, a consumer's policy layer — is read exactly as before: none of those changed shape, and nobody has to edit a file whose meaning did not move. The catalog is `schemaVersion` 2 and a catalog on any other version is refused, as it always was.

  **What moved with the scale.** Every strategy's rating components become `(r − 1) / 9 × 100`, `quotaCost` inverted as `(10 − r) / 9 × 100`, and `balance`'s `spendPenalty` becomes `balance.spendUnit × (quotaCost − 1) / 9`; the weights are untouched and the components still sum to the score. The role floors are restated as `qualityFloor: { worker: 5, reviewer: 9 }`, with `reviewerQualityFloor` still read as an alias. Every catalog row with a published figure is **re-banded from the figure** already in its `evidence`, not translated from its old band; the nine base rows without one are assessed anew on 1–10 against measured neighbours, with the reasoning in `evidence.text` and the rating named in `evidence.hypothesis`. Each base row's `evidence.text` names the anchor pair it was banded with. `fieldSize` is gone from the citation shape: absolute bands read no field.

  **`speed` no longer moves along an effort ladder.** Throughput is a property of the model and its serving stack, not of how hard it is thinking, so every rung of one model carries the base row's `speed`; the extra output tokens of a deeper effort are represented in `quotaCost` alone, and `quality` and `quotaCost` each move one band per step. One band and not two: two preserved ADR-004's relative step but spans eight bands over a five-rung ladder, so both ends of a long ladder ran into the clamp and `claude-opus-medium` came out sharing band 1 with `gpt-5.4-mini` at $2.63 blended — a top-tier model at medium effort reading as cheap as the cheapest row in the catalog. Every rung stays in the catalog even when the clamp makes two of them identical — the effort is still a launchable choice — and `models validate` says so with the new `ladder-indistinguishable` warning instead of collapsing them.

  **Upward interpolation no longer confers the reviewer role.** An interpolated rung could cross the reviewer floor on arithmetic alone, an unmeasured rung claiming a role its measured base never earned; a tuple is now offered as `reviewer` only when its own rating **and** its base row's assessed rating reach the floor, and `models validate` refuses a catalog that says otherwise. `codex-gpt55-xhigh` is the live case and is a worker row. In the other direction the wider scale opens the role up: `cursor-kimi-max` bands 9 from SWE-bench Verified 93.4, so all three harnesses offer a reviewer for the first time and ADR-003's diversity bonus has three harnesses to move between.

  **A hypothesis is a row that says so, not a row that is missing.** ADR-005 supersedes the wording ADR-004 left behind, which the catalog schema still carried: a row is omitted only when maintainers made **no assessment**: an assessed rating with no published figure stays in the catalog, named in `evidence.hypothesis`, so a model a person can actually launch is not silently removed from routing because a leaderboard has not got round to it. (PB-37, closing PB-29.1)

- **The reviewer is told its diff file is a snapshot, and the journal says how far behind it is.** `promptobus review` writes `files/review-<worker>.diff` once, at the call, while the worker goes on committing — on 2026-09-06 two reviewers of the same run read an older state and reported findings the author had already closed. Nothing locks the worker's tree, so the file is not made to follow it; its age is named instead. The reviewer's prompt now calls the file a snapshot, gives the moment it was taken and the worktree HEAD it was taken from, and sends the reviewer to the working copy for the current state before it reports a finding. The command prints the same pair beside the `diff base` line, and the reviewer's participant record carries it (`metadata.diffAt`, `metadata.diffHead`), so `promptobus status` and `promptobus_task` answer "how far behind" without opening the file. A repeat of the command with `--task` re-snapshots — a new numbered diff file and its path to the live reviewer — and both the record and the line move with it, which is why there is no `--fresh` flag.

### Fixed

- **Cursor's grok-4.6 tuples are paced against the pool Cursor actually bills them to.** The adapter mapped a model to the `auto` pool only when `autoBucketModels` named it, and that list lags Cursor's own billing: on the owner's account it names `grok-4.5` and `cursor-grok-4.5-*` and no `grok-4.6`, so every grok-4.6 tuple was paced against the `api` pool at 98.4 % and, under `balance`, looked exhausted and was never picked — while those are the account's most used models. One measured turn on 2026-09-06 settled which pool bills them: `cursor-grok-4.6-medium`, 14 950 input / 24 output tokens, 3.222 cents, moved `planUsage.autoPercentUsed` from 86.1025 to 86.105 and left `apiPercentUsed` at 98.4, with the aggregation row for that turn carrying `tier: 2`. So the adapter now asks a third method, `POST <backendUrl>/aiserver.v1.DashboardService/GetAggregatedUsageEvents`, which states the pool **per model** for every model with an event this cycle — `tier: 2` the Auto bucket, `tier: 1` the api pool — and the `auto` scope is the union of the bucket list's expansion and the `tier: 2` ids. The tier route matches by exact id and infers no family, which is ADR-004's rule unchanged; there is no prefix exception anywhere, and a model with neither a row nor a bucket entry stays in `api`, the conservative reading. The call is optional in the way the near-limit nudge is: a refusal, a timeout or an empty preflight budget costs the second route and leaves the bucket list as the whole answer. Cursor's own `thirdPartyUsageNudge`, which offers Grok 4.6 when the third-party pool runs short, corroborates the measurement and is not its source. (PB-38)
- **A Codex worker now lifts when its first turn starts, rather than only when the whole task-shaped turn ends.** On codex-cli 0.146.0 the rollout is absent immediately after `thread/start` but already present at `turn/started`; the holder now marks the registry record alive at that event, records the eventual first-turn end only as history, and `status` reports the running participant as alive. Mail arriving during the turn is accepted with a queued `turn/start` instead of being steered into work already in progress. The default ready budget is now the app-server preamble plus the 15-second `turn/started` wait; `PROMPTOBUS_CODEX_TURN_MS` applies only to later wake request acknowledgements, and the wake's own socket wait follows that budget instead of a fixed 30 seconds, so a value above 30 s is no longer truncated in silence (PB-39).

## [0.4.0] — 2026-09-06

### Added

- **Subscription balance: the CLI now knows what each account has left, and `--strategy balance` spends the three of them evenly.** Three subscriptions pay for three harnesses, and routing by rating sent work to the best-rated tuple until one account was out while the other two sat idle — which is what the owner was correcting by hand. A spike on 2026-09-06 disproved the assumption [ADR-003](docs/adr/adr-003-model-routing.md) recorded, that no harness but Codex exposes a remaining limit: all three answer from local credentials with no paid turn. [ADR-004](docs/adr/adr-004-subscription-balance.md) records the owner's nine decisions of that date, supersedes ADR-003 section by section — the replace-by-selector-kind merge, the reviewer floor of 4, the four values of `--strategy`, "`promptobusHome()` is not used for routing", and "a call with no `--strategy` routes nothing" where a default is set — and PB-24…PB-33 with PB-36 implement it.

  **What `promptobus models` shows now.** Under the candidates comes an `availability:` block: one line per harness with its state and its tier, and under it one line per limit window — the window's id, its `kind` (`session`, `weekly` or `monthly`), the percentage used, its `lengthSec`, what it binds, and its reset time. A `kind` is a **name** and `lengthSec` is the **number**, and neither is derived from the other: a billing cycle is `monthly` and is not thirty days. All three harnesses can reach `available` for the first time — the word still means auth, model **and** limit confirmed. Claude Code reads its tier out of the local credential record and its session, account-wide weekly and per-model-family windows out of one `GET /api/oauth/usage`; Cursor reads its billing cycle as **two pools of the same length**, `monthly-auto` and `monthly-api`, and one spent pool does not exhaust the harness, because the resolver reads a scope per tuple; Codex reports its plan, whether it has credits, how many "Full reset" credits are waiting and whether a spend control was reached, all off the call it was already making. Every harness that could not be read stays `unknown` / `quota_unknown` and is penalised, never blocked. Beside the decision the command prints one line about the telemetry file — how many records it holds and its size — and, where no plan name has been recorded, the one question no harness method answers.

  **What each adapter reads, and what it refuses to read.** Claude Code takes three fields out of the local credential record — a keychain generic password on macOS, service `Claude Code-credentials`, read through `/usr/bin/security` by absolute path; `~/.claude/.credentials.json` elsewhere, a path the spike did not measure and the reference says so — the access token, its expiry and `rateLimitTier`, which is the tier at no request at all; one `GET https://api.anthropic.com/api/oauth/usage` then maps `limits[]` onto a `session` row at five hours, a `weekly_all` row at seven days and a `weekly_scoped` row per model family, whose scope carries the ids the driver's own alias table resolves the harness's display name to. `GET /api/oauth/profile` is asked for the tier only when the record names none. A 401 or 403 from the usage endpoint is `unavailable` / `not_authenticated`; a `session` or `weekly_all` row at 100 % or carrying `locked_reason` is `exhausted` with that row's reset — and where the harness named no reset the mark is **sticky**, the message says so and names `promptobus models --clear-exhausted claude`. A spent **model scope** exhausts nothing, being one family the resolver reads per tuple, and neither does a row the harness marks `is_active: false`: the limit is not being enforced, and exhausting on it would take every Claude tuple out of routing on a number nothing is applying. Cursor keeps its two binary calls and adds the one the binary does not make, `POST <backendUrl>/aiserver.v1.DashboardService/GetCurrentPeriodUsage`; the cycle becomes two windows of the length the answer states, the `auto` pool naming the inventory ids it covers and the `api` pool carrying no list, being the complement; the tier is `{ name: "included:<cents>", source: "derived" }` because no method returns the plan name; a token that could not be read is `quota_unknown` rather than `not_authenticated`, since `status` already answered the auth question; and Cursor's own third-party usage nudge becomes a sentence this adapter writes from the threshold and the target model rather than the nudge's own label, with `displayMessage` — the one piece of harness prose allowed into a diagnosis — travelling on the exhausted branch only and bounded either way: control characters out, whitespace collapsed, 120 characters, and a string containing `@` refused outright. Codex reports `planType` as its tier, `spendControlReached` named in the message only when true, and the count of "Full reset (Weekly + 5 hr)" credits — **nothing spends one**, and the credit balance travels as two booleans because a count is a fact about the account rather than about the plan. `account/read`, the method that answers the account e-mail, is still never called: `planType` was already inside the call the probe makes anyway.

  **`--strategy balance`, the fifth strategy.** The four ADR-003 strategies answer how to weigh the qualities of a tuple and are untouched; `balance` answers a different question — which account to spend from. For each candidate the **binding window** is the applicable one with the highest `usedPercent`, where applicable means the account-wide windows plus the scope covering that tuple; `underspend = (elapsedShare − usedShare) × 100` is how far behind its own pace that window is, in **percentage points of that window**; and `effective = underspend − balance.spendUnit × (quotaCost − 1) / 4` discounts a heavy tuple in the same unit before harnesses are compared. Each harness is represented by the tuple that would actually be picked on it, the largest `effective` leads, harnesses within `balance.band` (5 points, as is `spendUnit`) are tied and their `balanced` scores decide. It is a **choice layer, not a filter**: every candidate keeps its score and its place and gains a pace block, and when no harness can be paced the pick is the best `balanced` score with a `balance-fallback` warning rather than a refusal. `models` prints the pace table under the candidates and `--json` carries the same numbers. **Nothing in this package pins the reviewer to a harness** — it is routed by pace like a worker, with the reviewer quality floor above it and ADR-003's diversity bonus unchanged on top.

  **`near-limit`, and the switch a person agrees to holds for the next spawn.** A `near-limit` warning names a harness whose binding window is at or past `nearLimit.usedPercent` (80) or whose underspend is below `nearLimit.underspend` (−15 percentage points), with the window, its reset, **which of the two tests tripped** — a level and a rate are different facts about one window — and the strategy it would switch to: `economy` when every paced harness is past the threshold, `balance` when at least one still has room. No line when the strategy it would name is already the one running, and an exhausted harness is not repeated as near-limit. It is raised under every strategy, not only under `balance`: a person running `--strategy quality` is owed the same warning about their windows. **Nothing switches on its own** — `skills/orchestrate` tells the agent to propose the switch to the person, and `skills/solo-review` that the reviewer follows the same default. On agreement, `promptobus models strategy --set <name>` writes `defaults.strategy` into the host's writable layer, keeping every other key of that file, creating it with a `schemaVersion` when absent, atomically and mode `0600`, and refusing when no layer is writable rather than choosing a file itself; `--clear` removes it, and the command warns when what it just wrote is shadowed by a layer above. `promptobus models strategy` with no argument prints the effective default and the layer it came from.

  **Precedence, everywhere: flag → overlay default → none.** `--strategy` on the command line always wins, which is the rule ADR-003 fixed for every named value. Below it, `defaults.strategy` from the merged overlays; below that, nothing — and nothing is the legacy path, so ADR-003's "a call with no `--strategy` routes nothing" still holds word for word where no layer sets one. `models` without `--strategy` takes the same default and falls back to `balanced` where none is set, so it and the next `spawn` cannot disagree about what is in force. A lift routed by the default records `strategySource: "overlay:<layer>"` on its decision and in `metadata.routing`, so a run made under an agreed switch is auditable as one. Reading the default costs the overlay files and no probe.

  **One question the tool cannot answer, and no command writes it.** Cursor's plan name is returned by no method. It lives at `account: { "<harness>": { "plan": "<name>" } }` in the **user** overlay: `models` prints the key, the value where a person set one and the path to add it to where they have not, a person adds the line once, and the value is displayed and scored by nothing. There is no writer because the writable layer is per-workspace — a tool-written answer would be asked again in every workspace — and because a second writable layer to carry one string would make "exactly one writable layer" false the first time it was used.

  **Quality floors per role, `remaining` per tuple, hidden rows carried.** `qualityFloor: { worker: 3, reviewer: 5 }` supersedes ADR-003's reviewer floor of 4 and its absence of a worker floor: three for the worker because "a worker on a cheap model where the task needed the expensive one is paid for in review rounds" describes a floor ADR-003 never set, five for the reviewer because a defect the last reader misses costs the defect and a round trip. Both keep the shape the reviewer's had — a choice rule, not a filter — with `worker-floor-not-met` beside `reviewer-floor-not-met`; `reviewerQualityFloor` is still read as an alias, and a layer stating both raises the `quality-floor-alias` warning while the explicit key wins. `remaining` is now computed over the applicable windows **of each tuple** rather than of its harness, so a Cursor tuple in a spent pool scores below one in a pool with room under `quality`, `balanced`, `speed` and `economy` alike — a refinement of ADR-003's own word, which already said "applicable". A model row the harness lists and declines to offer is carried with `hidden: true` for a person to see and is not inventory: a tuple naming one is excluded as `model-not-in-inventory`, and a hidden unrated row is not a `runtime` row either.

  **Overlay rules accumulate instead of replacing each other, and two selectors are added.** A `deny` list of any selector kind is now the **union** across every layer that states one: a ban written in any layer stands, no layer above lifts it, and lifting one means editing the layer that wrote it. An `allow` list is the **intersection**, so one sentence covers both — a layer's rule survives every layer above it. This supersedes ADR-003's replace-per-selector-kind merge whole, and it closes what the first consumer had measured: to make its policy bans hold it had to sit on top of a person's workspace file and erase that person's own `deny.tuples` with it. Two rules it could not write at all are now writable — `byRole: { reviewer: { … } }` scopes a block to one role, and `flags: ["no-zdr"]` names a mark the availability snapshot carries on a model row. A `denied-by-policy` candidate names the rule and **every** layer that wrote it, not one; the flag selector is applied after the inventory step, because that is where the row it reads arrives, and **silence is not absence** — a harness that lists no models has no flag to match, which a run reports as the new `flag-not-in-inventory` warning instead of appearing to give a guarantee. Because an allow list can now be unsatisfiable, `models validate` gains `allow-intersection-empty` and `deny-covers-allow`, the old cross-layer contradiction becomes the `allow-shadowed-by-deny` warning — lawful under union, since deny simply wins, while a name allowed and denied in ONE layer stays an error — and the command prints every deny rule in force with the layer that can lift it. The overlay `schemaVersion` stays 1: the new keys are optional and every file on disk stays valid, and what changed is what those files do.

  **The catalog is rebuilt from the live inventories, and every rating now says what is behind it.** `models/catalog.json` goes from nineteen tuples rated off the models' own descriptions to forty-five rated by the ADR-004 rule, `source: "public benchmarks, subscription-balance series, 2026-09-06"`. Claude Code gains `claude-fable-5` — the id the binary's baked model catalog resolves the `fable` alias to, read offline out of 2.1.251 with no paid turn, which also joins `MODEL_IDS`; Codex gains `gpt-5.5` and the `ultra` effort on `gpt-5.6-sol` and `gpt-5.6-terra`; and the Cursor rows are now **only** the seven families neither of the other two harnesses serves — `composer-2.5`, `cursor-grok-4.6`, `gemini-3.8-flash`, `gemini-3.7-flash`, `gemini-3.1-pro`, `kimi-k3`, `glm-5.2` — because Cursor's second pool bills a third-party model against the same included amount, so routing there a model another subscription already pays for spends two subscriptions for one run. A `-fast` id and Codex's `fast` service tier get no rows: ADR-004 keeps the service tier out of the tuple key. **Not done, deliberately:** per-model effort ladders and speed tiers on the snapshot's model object — it is closed to four fields and ADR-004 says the service tier "is not modelled in v1 at all", so widening it is an ADR pass rather than a schema edit; PB-24.2 puts the question to the owner, and nothing is blocked meanwhile because the catalog took the ladders from the spike document. `roleRatings` is gone, being the one judgement in the file with no figure behind it. One consequence to know: **no Cursor row reaches the reviewer floor of 5**, so ADR-003's reviewer diversity bonus works over Claude Code and Codex today rather than over all three harnesses.

  **`evidence` can carry the citation, and `validate` refuses a rating that has none.** The field still takes the v1 string; the shipped rows use the object form ADR-004 asks for — `{ text, sources, interpolatedFrom, hypothesis }`, each entry of `sources` naming one rating with its `basis`, `version`, `agentHarness`, `provenance`, `figure`, `fieldSize`, `url` and `date`. For each of `quality`, `speed` and `quotaCost` a row cites a figure, or is `interpolatedFrom` a base row that cites one, or names that rating in `hypothesis` — "no figure is published for this exact model, and here is the reasoning instead". Anything else is `catalog-invalid`, as is an `interpolatedFrom` naming no tuple, or a rating claimed as both cited and hypothetical. So the sentence the catalog has made since 0.3.0 — that a rating without a source stays out — is now checked rather than promised, and a reader can see at a glance which half of a row rests on a published number. Effort ladders are generated by the ADR arithmetic (quality one band per two steps, speed one band per step inverted, `quotaCost` one band per step, clamped to 1…5) and the suite pins every rung against it.

  **`promptobus done` starts collecting local statistics: one telemetry record per participant.** Closing a task appends one JSON Lines record per participant that lifted a session to `telemetry.jsonl`, beside the availability cache the host names, mode `0600` and validated by `schemas/model-routing/telemetry.schema.json`: the tuple, the strategy and where it came from, the lift and end stamps with the duration, the bus traffic (turns, review rounds, questions, results), how many other participants of the task were live on the same harness at the lift, and — the point of the whole record — the limit windows that applied to that tuple with their `usedPercent` at spawn and at the close. A **positive delta on the binding window is what the run spent, in the account's own unit**, which is the evidence the `quotaCost` rating never had. An unrouted participant — a legacy lift or an explicit `--model` — is recorded too, with `strategy: null`, so a hand-picked tuple is measured beside a routed one. The record is written at `done` and **not at `dismiss`**: a dismissal is not the end of a participant, so a record per dismissal would put several rows on one run with nothing to merge them by, and `dismissedBeforeDone` carries the dismissal into the single row instead. **Local only:** nothing sends the file anywhere, nothing but the count line of `models` reads it, and a record carries no prompt text, no message body, no repository path, worktree or branch, no session id, no email and no token — it is assembled by copying declared fields one at a time, never by spreading a participant record, and every object of its schema is closed. The only identifier is an opaque local key made from the task id. The end reading of a window is `null` rather than a number when the cache entry is past its TTL, and since `done` probes nothing, **`promptobus models --refresh` right before `promptobus done` is what records an end value at all**; without it the record carries the start value only. The tool never rotates the file — `rm` clears it — a failure to write is a warning and never a refusal, and **reading the records back is not in this release**.

  (PB-23, PB-24, PB-26, PB-26.1, PB-26.2, PB-27, PB-28, PB-29, PB-30, PB-31, PB-32, PB-33, PB-36.)

### Changed

- **For a host implementer: one new field on a routing layer, a snapshot at `schemaVersion` 2, and one file that lives beside the cache without a method of its own.** Everything a consumer host has to do for this release is in this entry, and it is one field.

  **`HostRoutingPaths`'s layer gains `writable?: boolean`, and exactly one layer carries it whenever any layer is declared.** `readLayers` refuses zero and refuses two, naming the layers it found. The refusal is at the **declaration** and not at the write, for the reason `harnessStateHome` refuses instead of guessing: a person who learns their host names no writable layer from `models strategy --set` learns it after making the edit it refuses to keep, and two writable layers would make the file the tool writes depend on iteration order. A host that declares no overlay at all declares nothing to write, which stays lawful. **`tsc` does not say so** — the field is optional, so a host that omits it compiles — and the place it is learnt is the first routing command, which refuses with a `GateError` naming every layer the host declared; `models validate` reports the same thing as a finding rather than a refusal, and prints which layer is writable beside its path. A host should mark the **highest-precedence** layer, or the tool would write a value a layer above it overrides; `models strategy --set` warns when that happens. **The writable layer is state, not configuration, so it must not be a file anybody commits** — a consumer keeps it wherever its own state lives, under that one condition.

  **Under the standalone host the `workspace` overlay moves to `<promptobusHome>/model-routing.json`, and `<workspaceRoot>/model-routing.local.json` is no longer read.** Its content is written by the tool now — the strategy an agent proposed and a person agreed to is recorded there — and a file the tool rewrites cannot live in the repository root, where a `.gitignore` is a contract with the repository rather than with this package. It is per-workspace exactly as the old path was; what changed is which per-workspace directory. **There is no migration and no fallback**: two paths under one layer id would make the file a person edits depend on which of them exists, so rules kept in the old file are not read and have to be moved by hand. The cache and the `user` overlay are untouched and stay account-scoped.

  **The availability snapshot moves to `schemaVersion` 2.** A window now states its `kind` — `session`, `weekly` or `monthly` — its `lengthSec`, and an explicit `scope` that is `null` for the whole account, `{ model, models? }` for one model family, or `{ pool: "auto", models }` / `{ pool: "api" }` for a harness with two pools in one cycle; a harness carries `tier: { name, source } | null` and, where it has them, informational `credits` and `resetCredits`; and a model row the harness lists and declines to offer is carried with `hidden: true`. **A scope that covers models names them by id**, so nothing infers a model family from a display name the harness printed. **A cache written by an older version is discarded, not migrated**, and every harness then reports `unknown` / `stale_cache` — the shortest fact in that file lives sixty seconds and the longest an hour, so a reader for the old shape would buy one hour of not asking and be kept forever; the message says a discard happened rather than "no cache entry", so an upgrade does not read as a bug. Three rules keep the new fields honest at the disk boundary: a tier name is shaped like a code — no spaces, no `@` — because it is the second adapter-authored string that reaches disk and `message` is meant to be the only free-text one; the credit **amount** is deliberately not carried, a count being a fact about the account rather than about the plan; and the model-flag list is closed to the one mark that exists, `no-zdr`, because an overlay may deny by a flag and a name checked against nothing would let a typo ban silently nothing. A window with no length, an unknown kind or an unreadable scope is dropped rather than repaired — a garbled scope would otherwise widen into `null`, which is the claim "this binds the whole account".

  **The decision document and the participant record carry the new facts.** `harnesses` in the decision is the `availability:` block, assembled by the command from the snapshot after the resolver has answered, so `--json` carries exactly what the text form shows and `render` still reads the document and nothing else. A routed lift records `windows` on its participant — the applicable windows of the tuple it chose with the `usedPercent` they had at that moment — which is the starting value the spend of a run is later read as a delta from, and `promptobus status` prints it beside the routing line. `strategySource` says whether the strategy came from a flag or from a layer.

  **`telemetry.jsonl` lives beside `cacheFile` and the host does not name it.** It is deliberately not a method: it is one leaf inside a directory the host already owns, and a second path to declare would let a consumer put the telemetry of one account beside the availability of another. **The consequence for a consumer is that moving the cache moves the telemetry with it** — the two are read together by the calibration pass, and a `cacheFile` that changes between releases leaves the old records behind rather than migrating them. Like the cache it is the ACCOUNT's, not one workspace's: `promptobusHome()` is not used for it at all.

  **Token handling in the three adapters, since a consumer inherits it.** Claude Code's access token is read from the credential record, used as one header, and never written or refreshed — `refreshToken` is not parsed out of the record at all, the refresh endpoint is never called, and the suite greps both the verdict and the written snapshot for the token. Cursor's bearer comes from the macOS keychain item `cursor-access-token` first and from `CURSOR_API_KEY` only as a fallback; `cursor-refresh-token` is never asked for, and the backend URL read off `~/.cursor/cli-config.json` is accepted **only** as `https:`, because a bearer is about to be sent to it. Codex's tier comes off the call the probe already makes, and `account/read` — the method that answers the account e-mail — is still not called at all. Every keychain read and every HTTP call arrives as a parameter with a live default, so no test reaches the network or a keychain.

  (PB-24, PB-25, PB-26, PB-27, PB-28, PB-30, PB-36.)

## [0.3.0] — 2026-09-05

### Added

- **Model routing: name an intent, get a tuple.** `promptobus models [--strategy quality|balanced|speed|economy] [--role worker|reviewer] [--refresh] [--json]` prints what the resolver would pick right now — the chosen `role + harness + model + effort` tuple, every candidate it considered with its four score components or the reason it was excluded, the models the account exposes that the catalog does not rate, and the warnings — as text, or with `--json` as the document `schemas/model-routing/decision.schema.json` describes. The command has no `--dry-run` because it is one: it reads the availability cache and asks no harness anything, and `--refresh` is the only flag that probes and therefore the only one that writes a cache entry. A decision is aged from the OLDEST availability entry's own `checkedAt` rather than from the moment the command ran, so "12 s old" says how old the facts are, and a snapshot nothing in which was ever checked prints as `never checked` instead of an age counted from the epoch. `models validate` checks the shipped catalog and every overlay layer and names, for each finding, the layer that wrote the key it is about. The decision the whole surface implements is [ADR-003](docs/adr/adr-003-model-routing.md), fixed with its schemas and golden fixtures before any of the code (PB-11, PB-12, PB-18, PB-21). The text form prints at most eight unrated rows per harness and counts the rest; `--json` keeps every row.

- **`--strategy`, `--allow-payg` and `--refresh` on `spawn` and `review`.** The resolver picks the harness, the model and the effort, and an explicit `--harness`, `--model` or `--effort` becomes a **constraint** on that choice rather than a value — a named value is never replaced by a neighbouring one, and a named combination that is unavailable or exhausted ends the command with diagnostics instead of a substitution. `auto` is not a CLI value: the orchestrating agent turns a task into one concrete strategy before the call. Routing joins the gate order those commands already state — everything before the first disk write — so a strategy with no candidate ends with the rendered decision and leaves no task, no worktree and no participant behind; explicit constraints are checked against the merged catalog and the workspace tool manifest before the resolver is asked, because the resolver cannot tell a harness this workspace never declared from nothing surviving. `--dry-run` reads the cache and writes neither a cache entry nor task state, and `--refresh --dry-run` probes and still writes nothing. Without `--strategy` the command takes its previous path down to the driver's own defaults (PB-21).

- **A rated model catalog ships with the package, and overlay layers sit above it.** `models/catalog.json` holds the first rated matrix — nineteen tuples across the three harnesses, each rating carrying a source and evidence a later maintainer can check; a model the harness exposes and nobody could rate from a named source gets no row and appears as a runtime `unrated` line that nothing picks. Above it, `host.routingPaths()` names the overlay layers, lowest precedence first — under the standalone host `~/.promptobus/model-routing.json` and `<workspaceRoot>/model-routing.local.json`, with room for a consumer's own policy layer between them. A weight set is replaced whole, an allow or deny list whole per selector kind, everything else field by field, and a missing overlay file is normal. **A ban is final from below**: the overlay schema refuses `deny: {}` and `deny: { models: [] }` alike and every denied name must exist, so "deny nothing" cannot be written and a consumer's ban holds whatever a person writes in their workspace file. Two hazards are written down where a person meets them: Cursor carries the effort level inside the model id, so a Cursor row names the full id and no effort; and a Claude row names `claude-opus-5` or `claude-sonnet-5` rather than the `opus` / `sonnet` aliases, because an alias re-points under a rating and nothing goes red. The overlay file to copy is in [guides/model-routing.md](docs/guides/model-routing.md) (PB-13, PB-13.1, PB-13.3).

- **The three accounts can be asked what they can run, before a session exists.** Each driver declares an availability adapter — Claude Code reads `claude auth status --json` and its own model dictionary (the binary publishes no listing); Cursor reads `status` and `models`; Codex opens `codex app-server --stdio`, asks `account/rateLimits/read` and `model/list` and kills it again, with no `thread/start` and no turn, so asking costs neither a session nor a run. The preflight runs all of them at once under one 15 s budget and resolves each binary once before the race, so a harness that never answers is `probe_timeout` rather than a command that hangs, and a host that spends the budget inside its own synchronous `resolveToolBin` stops the run instead of extending it. The result is the availability snapshot, kept at the file the host names with mode `0600` and a TTL per entry — an hour for auth and inventory, a minute for limit windows, five minutes for a failed probe, a confirmed exhaustion until its reset, and one with no reset until `promptobus models --clear-exhausted <harness>` lifts it. **The file holds no prompt, no token, no e-mail and no open account id**: a verdict is projected field by field onto the closed snapshot shape before it is written, so anything an adapter attached beside the contract never travels, and a writer holds a lock across the read, the merge and the rename so two commands probing at once do not lose each other's entries. A limit hit between the preflight and the launch marks the harness exhausted and refuses with `limit-hit-at-start`, naming the file the mark went into and the two ways out of it. `available` means auth, model **and** limit confirmed: only Codex reaches it, and Claude Code and Cursor stay `unknown` / `quota_unknown` because neither exposes a stable source for the remaining limit — which the resolver penalises rather than blocks (PB-14, PB-15, PB-16, PB-17, PB-21.1).

- **A routed run is auditable while it runs, not only at its start.** The decision is kept in the participant's `metadata.routing` — strategy, role, tuple, score, the snapshot it was made on and the warnings — and `promptobus status` prints that line per routed participant. The protocol version does not move for it: `metadata` is declared open in `schemas/v1/participant.schema.json`, so a record carrying a decision is readable by a mechanism of any version, and a reader that does not know the field ignores it (PB-21).

- **The orchestrating agent has the rubric that turns a task into one strategy, and the package now ships it.** `skills/orchestrate` carries the `auto` rubric — contracts, architecture, security, migrations, incidents and any still-ambiguous statement are `quality`; ordinary development is `balanced`; reconnaissance and a small precise change are `speed`; bulk low-risk routine is `economy`; and the price of a mistake moves a track up one row — together with the strategy envelope agreed before the first spawn (strategy per track, allowed harnesses, PAYG policy), where a fallback inside the envelope needs no second approval and leaving it does. A `--harness`, `--model` or `--effort` the person named travels to the CLI unweakened; two review rounds without progress step the track up once. `skills/solo-review` gets the reviewer's default `quality` and the diversity rule — a reviewer off the worker's harness or model scores higher, and a second reading with the same blind spot is not a review. Both files are in `package.json` `files` now, so a consumer references the rubric out of the installed package rather than out of a URL it has to keep in step (PB-19, PB-20).

- **A repository can declare how to restore the process skills it does not keep in git.** A worker repository's own `promptobus.json` may carry an optional `generate` field — the argv of a command, never a shell line — and `spawn` runs it in the fresh worktree: after the participant record reaches the task journal, so an interrupted generator cannot leave a directory the next spawn refuses to reuse; before the launch files, so a generator writing into the harness config directories is not the last writer; and before the dependency install, which is the trade the field makes — an `npx …` generator works in a worktree with no `node_modules`, an `npm run …` one does not. It is read from the WORKTREE at run time, because that is the file the participant will see. A refusal does not refuse the lift: the operator gets a warning with the exit code and a log beside the worktree, and the participant preamble names the outcome in **every** case, including "no generator declared" — a participant that is never told cannot notice that its repository's rules are missing. Skills committed to git stay the default and cost this step nothing (PB-8).

### Changed

- **Host contract: `PromptobusHost` gains two required methods and one declared field, and a consumer meets all three at one release.** `routingPaths()` names the availability cache and the ordered overlay layers — account-scoped, so they do not hang off `promptobusHome()`, which stays the per-workspace task store, and an ordered list rather than a getter per layer so a consumer can insert its own policy layer without another change to the interface. `harnessStateHome(harness)` names where the package keeps its session registry for one harness and may answer `null`; precedence at the call site is `PROMPTOBUS_<HARNESS>_HOME`, then the host, then a refusal naming both, with no default any more — the default is what went wrong, a consumer that had named its own variables had two harness registries writing into the operator's real home while `inspect` read the sandbox, with no error anywhere. And `HostToolBin` now declares the `version` field three drivers and three availability adapters were already reading: optional, and its absence means UNREAD rather than "old". The standalone host answers the same paths it used to guess and returns no version deliberately — it does not search for the binary, and `resolveToolBin` is synchronous, so reading a version would mean `spawnSync` inside the preflight budget. `tsc` refuses an existing host implementation until the two methods are added (PB-11, PB-2, PB-15.1).

- **The Cursor driver declares its binary as `cursor-agent`, not `cursor`.** `options.tool` is the name the package hands to `host.resolveToolBin` and from there straight to `run`, so it must be the name of the binary that actually starts; it said `cursor`, which is the harness name, and a host that hands a name back without searching sent that through `PATH` into an operator's own `~/.local/bin/cursor`. A host that switches on that name meets `cursor-agent`; the harness id, `--harness cursor` and the `tools` list are unchanged (PB-2).

- **The Codex MCP key prefix is the consumer's name, not a constant in the package.** The `config.mcp_servers` override merges with the operator's personal file by field, so the package moves its records into a namespace that file does not use — and the word is the consumer's identity, which the host already declares. The prefix is `${host.commandName}-`: standing alone it is `promptobus-` exactly as before, and inside a larger CLI a participant reads `mcp__<commandName>-promptobus__promptobus_send` and sees the tool it is running under. The config key and the tool name in the prompt still come from one function called twice; the prefix reaches the detached holder through the session record, because that process is handed a record file and has no host to ask. A key built with no prefix refuses instead of falling back to the canonical name, which would be the collision itself (PB-3).

- **Everything the package ships describes itself in English.** `description` and `title` in `schemas/v1/*.json` are translated, with every structural keyword, `$id`, pattern and required list untouched byte for byte, so the two schema directories in one tarball agree on language; the suite's own check titles and comments were translated with them, one to one, leaving fixture literals that stand in for a person's words alone (PB-12.1, PB-9).

### Fixed

- **A Codex holder dies with its session, and a run that leaves one behind now says so.** The holder is detached on purpose — `spawn` returns after the first turn and someone has to hold the app-server stdio — but it was reaped only from cleanup hooks, and the take-down that leaks reaches none of them: a suite file taken down with SIGKILL left its holder and app-server alive, and one 2026-09-04 run left twelve such processes alive into the next day, each holding a session file under a directory that had since been removed. The holder now watches for its own record every five seconds and, when it is gone, kills its app-server and exits without writing to its log — a log write would recreate the tree that was just removed. `stop` and `done` are unchanged: they reap first and remove the record after, so the watch usually has nothing to do (PB-2.1).

- **`done` left the worktree of a squash-merged branch behind and blamed a conflict.** Cleanup decided by a re-merge, and that question stops having an answer the moment the base moves over the same lines — after a squash and a second worker on the same file, `git merge-tree` conflicted on a branch whose work was already in. A second measurement now answers where the merge cannot: the branch's own patch id (`git patch-id --stable` over `git diff <fork> <branch>`) looked for among the commits the base gained since the fork. The report names the state instead of the obstacle — `merged as a squash`, `is entirely in <base>`, `is not merged` — and recognition stays deliberately narrow: a squash whose content was edited while merging, and work taken as several cherry-picks, keep the directory, because a directory is cheap to delete and impossible to return (PB-6).

- **The Cursor stall report fired on a session that was editing files.** Liveness had two signals — the chat transcript growing and an instrumental process under the session's tmux pane — and both are blind to the same phase: the agent edits files itself, inside one long call, and spawns nothing. Twice on live runs a participant was announced stalled while the owner's panel showed sixteen files edited. A third signal now covers it: how long ago the participant last wrote in its own working tree, the newest mtime among `git ls-files -mo --exclude-standard` plus its HEAD commit time. It is positive only — a recent write lifts the verdict, no write raises none — and the verdict names every measurement and its span, because an orchestrator has to tell a silent transcript from a dead session (PB-7).

- **The one message an operator sees at an undeclared harness sent them to a command that does not exist.** `--harness <name>` outside the workspace tool manifest was refused with `Declare it: promptobus tools add <name>`, and there is no `tools` subcommand. The refusal now names the file and the field instead — declaring a harness is a hand edit — and the only command left in the message is the host's own sync, which exists. A gate reads the dispatcher's own `case` labels and fails on any command string under `lib/` that is not among them, so the next inherited command cannot arrive quietly (PB-1).

- **`HostClone` now leaves the package.** `PromptobusHost.cloneOf(abs)` returns it and neither entry point re-exported it, so a consumer implementing the host could not name that method's return type without copying the interface — and `tsc` said nothing, because the interface compiles and the package builds; only the consumer's own build failed. It is in the `export type` list of `promptobus` and `promptobus/host` now, and the check that keeps those lists honest reads every `export interface` / `export type` out of `src/host.ts` instead of naming two by hand (PB-11.2).

- **The suite could reach a real user binary, count a neighbouring run's processes, and read a file from inside an unterminated string.** Six defects of one family, none of them shipped and all of them able to make a green run mean nothing: `PATH` is now sealed to one directory holding a symlink per binary the suite may reach, so a name nobody stubbed fails to resolve instead of launching whatever is installed; machine-wide `pgrep` patterns are qualified by a path the run alone owns, after two runs 3.2 s apart reddened the same check in both by counting each other's pids; the temp-directory sweep sentinel reads both spellings of `tmpdir()` and gained the eight prefixes it had never demanded; the package import scanner is a tokenizer that knows strings and comments, after a message fragment `' from '` was read as a module specifier and everything past it parsed from inside a string that never closes; nine checks that asserted the absence of text nothing writes are gone or made real; and the home diversion every suite file needs now lives in a module every file imports first, rather than in a helper 22 of the 46 files never load (PB-2, PB-2.2, PB-4, PB-9.1, PB-14.4, PB-14.5).

- **The publicity audit reads no link out of a code span or a fenced block.** A markdown sentence that quoted the link pattern was reported as a link to its example, so the gate refused documentation about its own rules (PB-22).

## [0.2.1] — 2026-09-05

### Changed

- **`spawn` keeps the brief as a task artifact.** The assignment file the orchestrator passes was read into the participant prompt and forgotten, so orchestrators invented a home for brief files outside the bus. After a successful lift the brief is copied into the task files folder as `brief-<worker>.md` — next to the review diffs — and the lift output names the path. Every lift stores the brief it was given: a repeat at the same address takes the next number instead of overwriting the previous assignment, and a refused spawn keeps nothing (PB-10).

## [0.2.0] — 2026-09-05

### Changed

- **Host contract: `cloneOf(abs)` replaces `reposRoot()`.** The host names the clone a directory belongs to and its namespace path; the package no longer walks a single "repos root" or requires a two-segment namespace — `need-pair` and `cwd-need-pair` are gone from `reviewLayoutError`. A host with several zones (`repos/<group>/<repo>` beside `external/<repo>`) could not be expressed before (PB-5).

## [0.1.0] — 2026-09-04

### Added

- **Public repository skeleton:** package manifest, MIT license, backslop tracker layout, and a GitHub Actions matrix for Node 20.
- **The bus itself:** TypeScript core with tasks, mailboxes, artifacts and the driver contract; a JavaScript runtime; drivers for Claude Code, Cursor and Codex; and the `promptobus` command.
- **A host contract instead of an ambient workspace:** `PromptobusHost` is passed explicitly on every call, with a standalone implementation that reads `cwd`, Git and `promptobus.json`. A consumer with no previous store declares no legacy layout, and that is a normal state rather than a failure.
- **Documentation for a first-time reader:** README in English and Russian, an architecture decision on the standalone boundary, and guides for installation, hooks and trust, and contributing.
