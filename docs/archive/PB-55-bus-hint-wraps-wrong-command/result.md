# PB-55 · Result

**Closed 2026-09-10.** Completed. The two hints that named bus subcommands through `formatNpx` — `prune --older-than <days> --yes` in `lib/prune.js` and `models calibrate --write` in `lib/models.js` — go through `host.busCommand(...)`, the pattern `lib/done.js` already used, so under a consumer host they print the command the dispatcher can actually run. The gate in `test/cli.test.mjs` now checks both the command name and its wrapper: any `formatCommand`, `busCommand` or `formatNpx` call under `lib/` whose first literal element is one of `lib/cli.js`'s own subcommand labels must use `busCommand`. `formatNpx` stays on `PromptobusHost` — removing a member would break a consumer host typed against the interface — and 02-host now says it exists for consumer-owned commands only and must never wrap a bus subcommand; 03-cli describes the widened gate.

**Verification.** Worker commit `9c710cb`, worktree of `worker:launch2`, squashed. Reproducer red before the repair: `node --test test/cli.test.mjs` exit 1, 5/6, naming `lib/models.js: formatNpx(models)` and `lib/prune.js: formatNpx(prune)`; the consumer-host stubs also red — promptobus-host 17/18 (`npx consumer prune` instead of `consumer promptobus prune`) and calibrate 43/44 (the same for `models calibrate --write`). After the repair: cli 6/6, host 18/18, calibrate green. Gates on `9c710cb`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, tarball 119 entries. Mutation probe, tests kept: the two hints returned to `formatNpx` → the gate and the two stubs red with the same assertions; restored → green. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/02-host.md` (`formatNpx` row and the consumer-only paragraph), `docs/reference/03-cli.md` (the gate checks name and wrapper), CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:launch2`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator (the keep-and-document decision for `formatNpx`); approver Павел Ким's orchestrator session.
