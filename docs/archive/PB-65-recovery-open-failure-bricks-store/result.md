# PB-65 · Result

**Closed 2026-09-09.** Completed. Recovery is local to one intent. `recoverTask` in `src/v1/messages.ts` wraps only `completeFanout`: a `PromptobusError` with code `link-refused` is recorded on the new `failed` list of `RecoverResult` as `{ task, message, code, note }` and recovery continues through the task's other intents and, under `recover()` without an argument, through the other tasks; the intent stays for the next pass. The two producers of that code are told apart by `context.errno`: a hard-link refusal from `linkFailure` (EXDEV, ENOTSUP, EOPNOTSUPP, EPERM, EACCES, EMLINK) stays `link-refused` and retryable, while `materialize`'s ENOENT — intent and canon both gone — is recorded as `intent-lost`, a permanent loss that is reported and never promised a retry. Anything that is not a classified `PromptobusError` still escapes. `lib/store.js`'s `bus()` opens the engine with `recover: false`, calls `recover()` once per process and reports repairs, unreadable records, retryable refusals and permanent losses through `warn` before caching the engine, so `status`, `history`, `prune`, the warden and MCP calls open a store whose links are refused; activation events stay with the warden. `RecoverFailure` is exported from `src/v1/index.ts`. A recovery-only fault point `intent-materialize` lets the suite reach the `materialize` producer deterministically.

**Verification.** Worker commits `aa5a5f3` + `edf91de` (worktree of `worker:s-store`). Reproducer red before the repair: `node --test --test-name-pattern="FS refused a hard link" test/v1-races.test.mjs` exit 1, 3/7 — `PromptobusError: hard link was not created (EACCES)` from `recover()` and `openEngine({ recover: true })`, child `status`/`history`/`prune` each exit 1; the review round's `intent lost before materialize` red on `aa5a5f3` (expected `intent-lost`, actual `[]`). Gates on `edf91de`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 589 tracked files, 119 tarball entries; focused v1 engine/races suite 149/149. Mutation probes after commit, tests kept: the classified catch removed → exit 1, 3/7 with the same EACCES escapes and three child exits 1; the errno mapping replaced by `failureCode = e.code` → exit 1, 7/8 (actual `link-refused`, expected `intent-lost`); fix restored → exit 0, 8/8. Isolated review (Claude `claude-opus-5 xhigh`, `reviewer:s-store`): one major (`link-refused` overloaded — `materialize`'s ENOENT is a loss filed as a retry) and three minor (`mkdirSync` before the classified `try` escapes, recorded as PB-65.1; the public surface only under Fixed; `reportRecovery` dropping `events` unmarked), all closed in `edf91de` except the deliberately unbroadened taxonomy; closure checked by the orchestrator on the diff. Approver: squash of the worker branch onto `main` at `ebd1ebe`; the only merge conflict was the `[Unreleased]` section of `CHANGELOG.md`, resolved as the union of entries; `backslop lint` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/04-protocol.md` § Engine (per-intent recovery, `failed` with `link-refused` and `intent-lost`, the bus adapter's explicit recovery and warnings); `CHANGELOG.md` `[Unreleased]` › Added and › Fixed; the `FaultHook` and `RecoverFailure` doc comments.

**Acceptance.** Implementation: Codex `gpt-5.6-sol` max (`worker:s-store`, strategy `quality`, bus task `pb-run-0909-t20260909-132344`). Review: isolated Claude reviewer under the same task. Findings: PB-65.1 (a permission refusal on creating the recipient inbox directory still escapes the classification), in triage.
