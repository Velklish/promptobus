# PB-126 · Result

**Closed 2026-09-10.** Completed. The Git reads of `review` and `spawn` share one bounded helper: `gitIn` moved from `lib/worktree.js` to `lib/util.js` and carries the process-wide output ceiling (`PROC_MAX_OUTPUT`), the shared Git wait ceiling (`GIT_NET_TIMEOUT_MS`) and `core.quotePath=false`, so non-ASCII paths have one spelling across base detection, snapshots and refusal diagnostics. The base-detection helpers of `review` (`merge-base`, `--is-ancestor`, `rev-parse`, `symbolic-ref`, the toplevel probes) go through it and treat a launch error the same as a non-zero status — a spawn error is a refusal or `null`, not a `stdout` TypeError; the two diff wrappers gained the wait ceiling beside their output budgets. The tracked-`.cursor` check of `spawn` goes through `runProc`, so it is bounded and visible in the sealed-PATH execution trace like every other child at that boundary.

**Verification.** Worker commit `ad67937`, worktree of `worker:launch`, squashed. Reproducer red before the repair: the review test's new case ": a git spawn error in detectBase is a refusal, not a stdout TypeError" and the Cursor test's ": tracked .cursor warning git launch is present in the execution trace". Gates on `ad67937`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 671 tracked files, 119 tarball entries. Mutation probes, tests kept: the `detectBase` error guard reverted → `node test/promptobus-review.test.mjs` exit 1, 205/206, twice, "Cannot read properties of null (reading 'trim')"; the `runProc` route of the `.cursor` check reverted → the Cursor test exit 1 twice with the execution-trace assertion "(missing)"; restored → `npm test` 54/54. The worker's direct (non-suite) runs of the Cursor test inside its sandbox also showed three liveness assertions failing (step 5 both process kinds alive, transcript silence with a live pane child, status on silence); the approver measured the same direct run unsandboxed: base `main` at `f0800ae` exit 0 (117 checks), `ad67937` exit 0 twice (118 checks) — the three failures are the worker sandbox's process visibility, not this change. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/03-cli.md` (spawn's tracked-`.cursor` read and review's Git reads: shared ceilings, `core.quotePath=false`), CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:launch`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator diff read (helper moved without a behaviour change for worktree callers; `null`/boolean outcomes of the base probes preserved); approver Павел Ким's orchestrator session.
