# PB-19 · Result

**Closed 2026-09-05.** Completed. `skills/orchestrate/SKILL.md` gains `## Model routing`: the rubric table (`quality` for contracts, architecture, security, migrations, incidents and ambiguous statements; `balanced` for ordinary development; `speed` for reconnaissance and small precise changes; `economy` for bulk low-risk routine), the price-of-a-mistake rule with the explicit order `economy → speed → balanced → quality` and two worked examples, one strategy per track, the strategy envelope agreed in the same approval as the split (strategy per track, allowed harnesses, PAYG) with the fallback-inside / leaving-it distinction, `promptobus models` as the way to see the pick and `promptobus status` as where the envelope is audited, constraints the person named travelling unweakened, the once-only step-up after two review rounds, and one line on a consumer layering its own policy — no consumer named. The CLI block cites `--strategy`, `--allow-payg` and `models`. `skills/solo-review/SKILL.md` gains `## Reviewer strategy`: default `quality`, the diversity rule, `models --strategy quality --role reviewer`. Track `skill` of the routing run, worker in Claude Code (opus, high); documentation only, reviewed by the orchestrator from the diff. The approver added the glossary row `strategy envelope` the worker proposed and listed the skills-shipping question (`skills/` is not in `files`) under PB-20.

**Verification.** `npm test` exit 0 — 44/44 files; `npm run audit` exit 0 — 292 tracked files and the 109-entry tarball, the markdown links of both skills resolved; `backslop lint` exit 0. No suite check reads `skills/`, so no mutation probe applied. Read-through with a fresh session on three task descriptions (an event-contract change → `quality`; a filter on an existing endpoint with a neighbouring pattern → `balanced`; an internal interface rename across forty files → `economy`), each with the deciding row quoted and the up-one-row rule checked. Gates re-run on the merged `main` tree by the approver.

**Documentation in the same pass.** The two skills are the documentation; `CHANGELOG.md` under `[Unreleased]`; `docs/GLOSSARY.md` (`strategy envelope`, by the approver). Finding filed: PB-19.1 (the `models` paragraph in 03-cli implied a `--dry-run` its synopsis does not list) — resolved by the orchestrator's decision that `models` has no `--dry-run`; PB-21 corrects the paragraph.
