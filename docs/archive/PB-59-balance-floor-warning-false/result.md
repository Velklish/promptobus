# PB-59 · Result

**Closed 2026-09-09.** Completed, on the owner's decision of 2026-09-09: the quality floor sits above the balance comparison. In `lib/model-routing/resolver.js` each `(harness, pool)` group is represented by its best floor-meeting tuple (or its best remaining one when it has none); when any representative meets the role's floor, only floor-meeting representatives enter the leader band and the `balanced`-score tie-break, and when none does every representative stays in for the soft fallback. Below-floor candidates remain scored rows. The `*-floor-not-met` warning no longer states a falsehood: its old wording ("no candidate reaches the floor") is used only when no scored candidate meets the floor; when a scored floor-meeting candidate exists but is not paced (its window spent or without a future reset) and a below-floor representative is taken, the same warning code names the taken tuple and harness, the floor-meeting tuple and harness, and says its binding window was not paced — so the fact survives into `routingMetadata`. ADR-004 § The balance strategy steps 1–2 carry a dated PB-59 amendment; 03-cli § Model routing describes the rule and both warning texts. A mixed-pool renderer regression pins the PB-43 guard (a no-window tuple prints no `no-pace` row beside a paced pool of the same harness).

**Verification.** Worker commits `9b41e26` + `d7b887a` (worktree of `worker:routing`). Reproducer red before the repair: `node --test --test-name-pattern='balance gives the quality floor precedence over a better-paced below-floor representative' test/model-routing-resolver.test.mjs` exit 1, `a floor-meeting representative wins before the balance comparison` (actual `codex-sol`, expected `claude-opus`), on the new two-harness fixture pair `balance-floor-{catalog,snapshot}.json` (codex effective +12.61 at quality 8, claude +5.48 at quality 10). Gates on `d7b887a`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 613 tracked files, 119 tarball entries; targeted 90/90. Mutation probes after commit, tests kept: all representatives compared → exit 1 (same assertion); the `!floorReached` guard restored → exit 1 at :939 (`assert.ok(warning)`); the renderer guard removed → exit 1 at :1192 (Cursor rows 2, expected 1); fix restored → exit 0. Isolated review (Claude `claude-opus-5 xhigh`, `reviewer:routing`): one major (the scored-but-unpaced floor candidate case went silent) and one minor (the renderer guard untested), both closed in `d7b887a`; closure checked by the orchestrator on the diff. Approver: squash of the worker branch onto `main`; the only merge conflict was the `[Unreleased]` list of `CHANGELOG.md`, resolved as the union of entries; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/adr/adr-004-subscription-balance.md` § The balance strategy (steps 1–2, PB-59 amendment); `docs/reference/03-cli.md` § Model routing (the extra rules, the balance pick, the pace table); `test/fixtures/model-routing/README.md`; `CHANGELOG.md` `[Unreleased]` › Fixed.

**Acceptance.** Policy: owner's decision 2026-09-09 (the floor above the balance). Implementation: Codex `gpt-5.6-luna` max (`worker:routing`, strategy `balance`, bus task `pb-run-0909-t20260909-132344`). Review: isolated Claude reviewer under the same task.
