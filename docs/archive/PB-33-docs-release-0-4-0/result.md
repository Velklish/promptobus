# PB-33 · Result

**Closed 2026-09-06.** Completed. The twelve per-task entries under `## [Unreleased]` in `CHANGELOG.md` became two release notes for 0.4.0 — one for the user (what each account has left, `--strategy balance` spending the three evenly, the pace table, `near-limit` and `models strategy`, the moved workspace overlay and the root `model-routing.local.json` no longer read, the telemetry file `done` appends to and what it never holds) and one for the host implementer (the `writable` layer, the snapshot at `schemaVersion` 2, the overlay merge by union and the two selectors, the decision document at 2, the telemetry file beside the cache) — with every fact of the twelve kept, checked by a second pass. `README.md` and `README.ru.md`: five strategies, the precedence flag → overlay default → none in its own paragraph, `near-limit` and propose-not-switch, the Cursor plan line, the telemetry file and the refresh-before-`done` step, both command tables; the sample output rebuilt from the suite's `balance-snapshot.json` against the shipped catalog and labelled reproducible (a live sample would print one account's usage into a public README). `docs/guides/model-routing.md`: the three sentences PB-25 made false (the layers table, "Save it as …", "both overlays sit under the home directory") and a paragraph on the writable layer. `docs/reference/01-overview.md` (the store home and what is not under it), `03-cli.md` (the `Files` row naming the writable layer). `docs/GLOSSARY.md`: window, binding window, pace, tier, writable layer, participant telemetry, telemetry record, window delta, each with its evidence. `docs/ROADMAP.md`: goal 6 marked with shipped evidence, goal 4's tuple count corrected to 45. `skills/orchestrate/SKILL.md`: the rubric row for a person who pays for several harnesses, the reviewer inside the balance, § The one question the tool cannot answer, § Refresh right before you close; `skills/solo-review/SKILL.md`: the same refresh step. Two findings filed in `lib/`: PB-33.1 (the `--clear-exhausted` sentence promises a probe the next run does not make) and PB-33.2 (the per-harness `snapshot-stale` line prints the epoch for a never-checked harness) — closed by the approver before the tag.

Review: documentation and skills only, read by the approver — the two release notes, the README sample's provenance, the glossary rows, the skills' sections; nothing sent back.

**Verification.** On `main` after the squash merge (identical to the worker's tip outside `docs/backlog`): `backslop lint` no errors; `npm run audit` clean, 405 tracked files and the tarball; `npm test` 47/47 files; `npm pack --dry-run` 116 entries listing `skills/`, `models/catalog.json`, `dist/`.

**Acceptance evidence.** *Offline (worker, outputs in the run's file `PB-33-acceptance-evidence.md`)*: the fixture snapshot rebased onto the run clock against the shipped catalog — `balance` for the worker picks the pace leader outright (codex +15.25 against claude +9.23, outside the band), for the reviewer the band ties codex and claude (4.77 apart) and the `balanced` score decides for `claude-opus-high`, two rows because no Cursor tuple reaches the reviewer floor; the precedence walk `none` → `--set economy` (file `0600`, one key) → `spawn --dry-run` routes economy → `--strategy balance` wins → `--clear` returns the legacy path with no routing line; a staged exhaustion moves the pick. *Live (approver, owner's machine, `main` 73f24f7 and later)*: `models --refresh` — all three harnesses `available`, tiers `default_claude_max_20x` (credentials), `included:7000` (derived), `plus` (probe); Claude Code with `session`, `weekly` and a `weekly` window scoped to `claude-fable-5`; Cursor with two `monthly` pool windows (`auto` naming the composer ids the live bucket list covers, `api` with no list); Codex with `session` and `weekly`, `credits` booleans, two reset credits, `spendControlReached: false`; a `near-limit` warning on Cursor's api pool; the leak grep on the cache at 0 and the file `0600`. `models --strategy balance` for the worker picked `claude-opus-medium` (Claude on its weekly pace, Codex behind by 8.8 points, Cursor by 54.6) and for the reviewer `claude-opus-high`; `spawn --dry-run --strategy balance` on a fresh snapshot routed the same with no warnings, `review --dry-run` likewise; `models strategy --set economy` → `spawn --dry-run` routed economy without a flag → `--clear`. A live bus round in a throwaway repository under `--strategy balance` lifted a Claude Code worker on `claude-opus-medium`; its close and the telemetry record are recorded in the release's handoff. The catalog table was confirmed by the owner: bands stand as the rule produces them, disagreements go to the user overlay. The one live measurement PB-38 asks for (which Cursor pool bills grok-4.6) was not taken and is deferred until the tag.

**Documentation in the same pass.** This task is the documentation pass; the files are listed above.
