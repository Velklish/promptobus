# PB-34.1 · Result

**Closed 2026-09-09.** Completed. Both names the binary's baked table carries beyond the driver's dictionary were checked on the installed `claude` 2.1.263 with a one-turn liftoff each, as the card asks: `claude-haiku-4-5` answered `ok` (exit 0); `claude-mythos-5-1` was refused (exit 1, "There's an issue with the selected model (claude-mythos-5-1). It may not exist or you may not have access to it.") — an account-dependent refusal, recorded verbatim in the guide's liftoff block beside the 2026-09-05 runs. Haiku therefore enters the inventory: `claude-haiku-4-5` in `MODEL_IDS`, `haiku` in `MODEL_ALIASES`, the Haiku family in `MODEL_SCOPE_IDS` and the alias map, and three worker-only rows in `models/catalog.json` — `high` 3/2/1, `xhigh` 4/2/1, `max` 5/2/2 (quality/speed/quotaCost) — the `xhigh` base rated under ADR-005 from Anthropic's 73.3 % SWE-bench Verified figure (highest available fallback, no Claude Code figure published), Artificial Analysis 81 tokens/s and the $1/$5 list price, with the liftoff method in its `evidence.text`; the rungs follow the effort-ladder rule. Mythos stays out, pinned by a test. The reference now states the rule for an alias beyond the `--model` help sentence: it must be in the baked `aliases` block of the proven binary version, its full id must pass a one-turn liftoff on this account, and the catalog must rate that id.

**Verification.** Worker commits `08db2f1` (repair) and `1e7abee` (review follow-up), worktree of `worker:routing`, squashed. Reproducer red before the repair: `node --test test/model-routing-catalog.test.mjs` exit 1 at :117, `assert.ok(MODEL_IDS.includes('claude-haiku-4-5'))`. Gates on `1e7abee`: `node bin/promptobus.js models validate` exit 0, "the catalog and 2 overlay layer(s) hold · 4 warning(s)" (the pre-existing `ladder-indistinguishable` pairs and the two Gemini `promotion-expired` warnings); catalog suite 59/59; `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 631 tracked files, 119 tarball entries. Mutation probe after the commit, test kept: `claude-haiku-4-5` removed from `MODEL_IDS` → :117 red with the same assertion; restored → 1/1. Review: isolated reviewer (`reviewer:routing`) recomputed the three bands and the ladder, checked the four driver maps, the canonical priorities (100…250) and the reviewer floor (9, so worker-only is required), and found five wording minors — two counts in the guide (seventeen base models, thirteen reviewer rows), the missing 2026-09-09 liftoff records, the liftoff method absent from the row's evidence, the alias-admission rule stated only in a driver comment — all closed in `1e7abee`, read by the orchestrator. Approver: squash of the worker branch onto `main`; no merge conflict; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** 03-cli.md § Claude (inventory, scope map, the help-sentence-versus-baked-aliases rule), guides/model-routing.md (rows and aliases paragraph, liftoff block, the two counts), CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:routing`, strategy `balance`, bus task `pb-run-0909-t20260909-132344`). Review: Claude `claude-opus-5` xhigh (`reviewer:routing`, strategy `quality`).
