# PB-247 · Result

**Closed 2026-09-23.** Completed by worker pb247 in two commits, squashed into the closing one. `947b6510`: `npx --yes github:Velklish/backslop#v0.10.0 upgrade` (exit 0) moved the pin in `backslop.json`, `package.json`, the CI workflow and the live markdown, and created the empty closed-task journal `docs/archive/LOG.md`; the pin in `test/gate-record.test.mjs:24`, which `upgrade` does not read, moved by hand, and `git grep -n "backslop#v0.9.0"` outside historical files then found nothing (exit 1). `docs/backlog/README.md` and `docs/archive/README.md` are the v0.10.0 template rendering byte for byte; the archive README's former "Move a task" line was a strict subset of the template's line, template drift rather than a repository addition, and is not carried over. `backslop.json` declares `"probe": "npm run probe"`, and `init` renders it into step 4 of the generated `AGENTS.md` block. `docs/guides/contributing.md` restates v0.10.0: the pin paragraph (since v0.8.0 `upgrade` moves the `package.json` and CI pins and reads no other file), `--minor --evidence` in the worker's report, `archive N` then `fold N` on the approver path. `6eee058a` closed the two review findings in the same guide. The task is closed as an archive directory and is not folded: folding the accumulated archive is PB-248.

**Verification.** Worker, on `947b6510`, clean tree: `npx --yes github:Velklish/backslop#v0.10.0 gates --keep-going --require-clean` exit 1, gates 5, green 4 — `npm test` exit 0 (70/70 files, 2320/2320 checks, node:test 932 pass, 0 fail), `backslop lint` exit 0 (0 errors, 1 warning: the empty "Scope" of PB-247.1), `npm run audit` exit 0, `npm run pins` exit 0 (75 live refs in 14 files), `npm run codex-schema` exit 1, "codex 0.156.1 is installed, the fixtures were taken from 0.146.0" — twice standalone on the branch and once on the base `9692b10c` with the same exit and lines, so it is the machine, not this change (PB-196); the fixtures and `PROVEN_CODEX_VERSION` are untouched. `npm test` on the base `9692b10c` exit 0, 70/70; verdict names base → branch: none removed. Mutation probe on `947b6510`: `npm run probe -- test/gate-record.test.mjs --mutate 's/backslop#v0\.10\.0 gates/backslop#v0.9.0 gates/' --run "npm run pins"` — mutated exit 1 naming `test/gate-record.test.mjs:24`, restored exit 0, tree clean before and after. On `6eee058a`: `backslop lint`, `npm run audit` and `npm run pins` exit 0; `node test/comment-links.test.mjs` exit 0, 20/20. Approver, on the squash-merged tree (base `9692b10c` plus `6eee058a` and this closure, before the commit): `npx --yes github:Velklish/backslop#v0.10.0 gates --keep-going` exit 1, gates 5, green 4 — `npm test` exit 0 (70/70 files, 2320/2320 checks, node:test 932 pass, 0 fail), `backslop lint` exit 0 (0 errors, the same 1 warning), `npm run audit` exit 0, `npm run pins` exit 0 (75 live refs in 14 files), `npm run codex-schema` exit 1 with the same line, the machine-side red above; `git diff --cached 6eee058a` outside `docs/backlog`, `docs/archive` and `CHANGELOG.md` is empty, and `CHANGELOG.md` does not differ.

**Review.** One isolated round (a fresh read-only session) on `947b6510`: 2 findings. `critical` — the approver path put `fold N` beside the unchanged "squashed before that push" sentence without saying how the two combine; in v0.10.0 a commit between `archive N` and `fold N` leaves the journal line naming a sha the squash drops, and a `fixup` or a new message drops the fold draft, the body's only storage. `minor` — the pin paragraph said the pin gate skips exactly what `upgrade` skips, while it reads every tracked file. Both closed in `6eee058a` (one file, 3 lines): no commit between `archive N` and `fold N`, the squashed commit carries the fold draft as its message, and the gate treats as historical what `upgrade` does and reads everything else. The closure was read by diff as the second pass, with no findings. The generated `AGENTS.md` block carries the same gap in steps 5 and 7 and stays as `init` renders it: the fix belongs to the backslop template.

**Findings.** PB-247.1 (`minor`, in `minor/`): `README.md` and `README.ru.md` list three of the five gates; it waits for a batch, and its empty "Scope" is a `lint` warning until the batch is cut. Not filed, recorded here: the `probe` field has no verdict guarding it — `lint` does not compare the block with the config, and a dropped field is announced only by `init`; the "Mutation probes" section of `AGENTS.md` outside the block now partly repeats block step 4 and stays, because `test/process-rules.test.mjs` guards it.

**Documentation in the same pass.** `docs/guides/contributing.md` (pin paragraph, roles table, worker report, approver path), `docs/backlog/README.md` and `docs/archive/README.md` (v0.10.0 template), `docs/archive/LOG.md` (new, empty), the pin in `AGENTS.md`, `README.md`, `README.ru.md`, `docs/README.md`, `docs/ROADMAP.md` and `docs/TRACKS.md`, CHANGELOG under Unreleased.
