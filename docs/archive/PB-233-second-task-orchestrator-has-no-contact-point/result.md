# PB-233 · Result

**Closed 2026-09-17, completed.** The contact point now follows the SESSION rather than the binding. `joinBus` (`lib/server.js`) was the only place a socket was ever handed over, and it runs on a bus call FOR THAT TASK: `review` creating task B from a session bound to task A registered nothing for B's `orchestrator`, so B's warden read `knocks 0` / `channel: self-wake` and the reviewer's first `result` woke nobody. `orchestratedTasks(home, session)` (`lib/store.js`) is the one walk that answers which active tasks this session PROVABLY owns the `orchestrator` address of — `ownership(...).right === 'owner'`, so an ownerless task and a foreign one are both out — and `handOverContactPoints(home, {env, session})` (`lib/warden.js`) walks that set and calls each participant record's OWN driver `registerWake`. Three call sites, each for its own reason: `lib/review.js` right after `createTask`, because a reviewer can report inside the same turn and the guard would only repair it from the next turn end; `lib/spawn.js` after `bindIfOwner`, the same door for the other command that opens a task; `lib/guard.js` at every turn end, so a session back on a NEW socket replaces the stale one everywhere it orchestrates — which a one-shot copy at creation cannot do. The socket comes live from the environment, not copied from the bound task's record: that a CLI started from a Claude Code Bash tool inherits `CLAUDE_CODE_MESSAGING_SOCKET`, `_TOKEN` and `_SESSION_ID` was measured in the worker's own environment before the design was chosen.

**The card's OR was resolved by the card itself.** Bullet 2 offered either counting unread across every orchestrated task or naming the unwatched ones in the guard text; Verification line 2 — "with the knock channel disabled, the loop guard returns the turn on an unread message in B" — only the first satisfies. `guardVerdict` gains one branch, `liftedTaskUnread`, keyed `lifted:<id>:<n>` and naming each task by id and title. It is the LAST branch, below this address's own unread, its `UNANSWERED` debt and its pending hand-off, so what the session owes on its own account is repaired before what was sent to a task it lifted. What did NOT change is the guard's task choice: `identity.declaredTask ?? boundTaskId(...)` still picks the ONE task whose guard mark, end-of-turn mark, foreign-session refusal and warden revival this hook owns, one binding per session stays, and the shared `GUARD_BLOCK_LIMIT` still lets a turn through after two returns — a lifted mailbox nobody drains cannot hold a session forever. The predicate is ownership rather than the declared address, so a WORKER session that lifts a review is covered by the same walk.

**Verification.** The card's measurement was reproduced before it was repaired, by mutation rather than by hand: with `lib/store.js:789` mutated (`.right === 'owner'` → `.right === 'nobody'`) the lifted task's health record reads `{"knocks":0,"channel":"self-wake","knockError":"no contact point — the participant did not hand over a socket"}` — word for word what the card recorded live on 2026-09-17 — `npm test` exit 1, 2296 of 2302 verdicts, 0 unaccounted, 6 reddened. Each of the three doors then got a probe of its own after review round 1, all through `npm run probe`, `git status --porcelain` empty before each and after each restore: `lib/guard.js:464` → `node test/promptobus-guard.test.mjs` exit 1, 112 of 114, the two new guard-door checks and only those; `lib/spawn.js:1373` → `node test/promptobus-spawn.test.mjs` exit 1, 164 of 165; `lib/review.js:608` → `node test/promptobus-review.test.mjs` exit 1, 260 of 263; and the ordering probe (`handoffPendingVerdict` → `liftedTaskUnread` at the hand-off branch) exit 1, 162 of 165, reddening the new check together with the pre-existing PB-204.2 and PB-213 hand-off checks — which is the point: all three live in one guarantee. Verdict names base ↔ branch, diffed on a temporary worktree at `f3e4ac69` with sandbox temp paths normalised: 2290 → 2307, **removed 0**, added 17 (review 5, guard 7, warden 2, spawn 3), multiplicity changed 0. Gates of this acceptance, run in full on the merged tree, each exit code read from its own command: `npx github:Velklish/backslop#v0.8.0 gates` exit 0 — **gates 4, green 4**; `npm test` exit 0 over **69 of 69** test files and **2307 of 2307** verdicts, fail 0, todo 0, 163 s; `npx …backslop#v0.8.0 lint` exit 0, **no errors, warnings 3** — the three pre-existing triage entries whose parents are closed (`PB-139.1`, `PB-188.2`, `PB-225.1`), the owner's to move and untouched here; `npm run audit` exit 0, clean over **992** tracked text files and 136 packed entries; `npm run pins` exit 0, `#v0.8.0` throughout, **992 of 992** tracked files read. `npm run build` exit 0 on the slice before the archive. The merged tree was checked against the worker's head `88aeb39c`: `git diff HEAD 88aeb39c` empty over the whole tree, so nothing was lost or added in the squash.

**Documentation in the same pass.** `docs/reference/03-cli.md` — § the warden gains what a second task of a bound session gets today (the card's third bullet), and § Guard and warden opens on the widened state: the clean mailbox is read here and in every other task this session orchestrates, while the debts stay this task's. That split is review round 2: the first widened opener read as if an `UNANSWERED` debt or a pending hand-off in another task also held the turn, and it does not — `liftedTaskUnread` reaches other tasks through `countInbox` alone. `README.md` and `README.ru.md` follow. `CHANGELOG.md` under `## [Unreleased]` → `### Fixed`, carrying the narrow form from the start.

**What is left open.**

- **A Codex or Cursor session as the orchestrator is not verified.** `handOverContactPoints` calls each record's own driver and all three export `registerWake` with the same signature, but every check on this branch runs the Claude driver. The worker states it as a hypothesis, not a fact, and it is accepted as one.
- **The fixture drain in the guard suite is the accepted repair.** `loop guard: drained inbox clears the no-exit mark` went red because the LOOP_NOEXIT and LOOP_RELIVE blocks above it leave orchestrator mail unread under the same owner session, so the guard legitimately held the turn for those mailboxes; the repair is one `store.readInbox(...)` at the end of each of those two blocks. No check's subject, name or assertion changed, and the base ↔ branch name diff confirms it: removed 0.
- **`spawn --new-task` from a bound session rebinds the session; `review` skips `bindIfOwner` for a new task.** A deliberate difference between the two commands, outside this card and not filed as a finding.
- **No finding files were created** — nothing surfaced across the two review rounds that they did not close.
