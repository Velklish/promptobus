# PB-231 · Result

**Closed 2026-09-17, completed.** `dismiss` and `stop` now admit an approver of this task holding its own recorded session, by the same positive proof `sweep` already used, so the three-command cleanup recipe can be executed by the role it names. The proof has one home rather than two: `approverHere` moved out of `lib/sweep.js` into `lib/store.js`, where all three commands read it — a participant of THIS task with role `approver` whose recorded session is the calling one, read from the record's own session the way direct worker↔approver traffic is proven, the full id by equality and the short prefix only where no full id was recorded. The gate is not weakened for anybody else: a stranger and a call that names no session get the same head and the same route they got before, and `done` is deliberately not lifted with the pair — closing the run stays the owner's, and a verdict holds it there.

**The refusal gained its reason where there is one to give.** `unprovenApproverLine` names the half that failed: an approver record carrying no session of its own, or one on record under a session that is not this one, with both ids printed. It is said only where the task has an approver record and the call named a session, since a call that can name neither would otherwise be told about a door it cannot open either way. Review round 1 found the line built once for "the approvers" of the task: with a mixed set — one record on record under a session, one with none — that single sentence told the session-less record a foreign id and agreed with neither half. It is now built per RECORD, each approver named by its address and by its own state, with the calling session said once, and a verdict over the mixed set holds both halves.

**ADR-017 carries an amendment rather than a successor,** in the form ADR-004 uses twice. Its decision stands word for word for `done`; what changed is that the second door now stands on `stop` and `dismiss` too, read from one home, leaving the ownerless branch as the only difference between the sets of callers — the owner gate admits a session that names itself on a task with no recorded owner, and `requireSweeper` refuses it unless an approver proves the session. Round 1 raised this as the major finding: the ADR's last decision bullet said the directions of the two gates agree while the sets of callers do not, and that sentence had become false for `stop` and `dismiss`.

**Verification.** Gates of this acceptance, on the merged tree of this commit: `npx github:Velklish/backslop#v0.8.0 gates` exit 0 — **gates 4, green 4**. `npm test` code 0 over **69 of 69** test files in 157.7 s, **fail 0, todo 0**; `npx github:Velklish/backslop#v0.8.0 lint` code 0, **no errors, warnings 3** — the three pre-existing triage entries whose parents are closed (`PB-139.1`, `PB-188.2`, `PB-225.1`), which are the owner's to move and were not touched; `npm run audit` code 0, clean; `npm run pins` code 0, `#v0.8.0` throughout. The machine record with every count is `gates-approver-pb.json` in this run's journal. The card's own verification is met by suite: `test/promptobus-owner-gate.test.mjs` lifts an approver record with a session and calls both commands under that session's identity, and holds the stranger and the identity-less call to the owner-gate text; `test/promptobus-dismiss.test.mjs` and `test/promptobus-stop.test.mjs` carry the same pair on each command's own path.

**Three mutation probes on record, and a fourth declared as interrupted rather than dressed up as a red.** The recorded probe of the first round drops the foreign-session half of the proof (`lib/store.js:842`, mutated run exit 1; base total 41, passed 39, reddened 2, unaccounted 0): with that half gone `approverHere` admits ANY session on a task that merely has an approver record, and the stranger's `dismiss` went through where a refusal was expected — the guarantee the card asks not to weaken, caught by a verdict. Making `unprovenApproverLine` return nothing reddens the two verdicts that read the reason (exit 1; base total 41, passed 39, reddened 2, unaccounted 0). The recorded probe of round 1 reproduces the reviewed defect exactly — a record with no session of its own falling back to another approver's id (`lib/store.js:853`, exit 1; base total 43, passed 41, reddened 2, unaccounted 0). The fourth is the card's own literal probe, dropping the approver branch from `lib/dismiss.js`: it exits 1, but by ABORTING the file rather than reddening a verdict — with the branch gone `dismiss` refuses, a refusal is `fail()` → `process.exit(1)`, and `capture()` (unlike `expectFail`) does not swap `process.exit`, so the approver's own dismiss takes the process with it at 24 of 41 verdicts, leaving 17 unaccounted. The handover schema calls that an interrupted probe, so it is reported as one and the foreign-session probe stands in its place.

**Documentation in the same pass.** `docs/reference/03-cli.md` § The owner gate — the three cleanup commands share one proof, and it is named; the "refuse on `!allowed`, not on `gated`" paragraph now says that for `stop` and `dismiss` that is where the refusal starts rather than where it ends, and that `done` reads this gate and nothing else. `docs/adr/adr-017-the-owner-gate-is-a-positive-proof.md` — the amendment of 2026-09-17, and its row in `docs/README.md` now names the second door on `stop` and `dismiss` instead of the owner gate alone. `CHANGELOG.md` under `## [Unreleased]`.

**What is left open.**

- **This acceptance could not use what it accepted.** The bus running this run is the installed build, not this tree, so `dismiss`, `stop` and `sweep` still refuse this approver with the owner-gate text — the very defect the card fixes. Participant cleanup and worktree removal were therefore left to the orchestrator, by the brief's own instruction, and the fix is proven by suite and probe rather than by a live run of the three commands under this session.
- **The live measurement the card rests on is the consumer's, not this repository's.** Two approvers refused in a row on 2026-09-16, refusal text verbatim in the card; nothing in this tree re-measures it, and the suite stands in for it.
- **`done` stays owner-only** — the card put it out of scope on purpose and a verdict pins it there, so an approver still cannot close a run.
- **The consumer's acceptance recipe is not rewritten here.** The card leaves that to the consumer, which rewrites it against whatever this card decided.
