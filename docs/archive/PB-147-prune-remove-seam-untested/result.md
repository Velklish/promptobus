# PB-147 · Result

**Closed 2026-09-09.** Completed as a coverage-only change; the seam stays. `test/promptobus-done.test.mjs` now calls `sweepJournals` directly with injected removers in two isolated homes: a partial refusal (one journal deleted through `rmSync`, one remover call throwing) asserts `count` 1, `failed` 1, the refused journal still on disk, its exact "task <id> not removed: …" line on stderr and the green "journals removed: tasks 1" line on stdout with no yellow line; an all-refused sweep (two throwing removals) asserts `count` 0, `failed` 2, `gone` 0, both journals still on disk, both per-task lines, the yellow "journals not removed: none of 2 tasks came off (refusals 2)" line and no green claim. The age-fixture helper takes the home as a parameter so the three cleanup scenarios stay isolated. No runtime, reference, comment or CHANGELOG text changed: the deletion and warning behaviour is unchanged and the `remove` seam now has the caller it was written for, so the comment at `lib/prune.js:103` stays true.

**Verification.** Worker commit `466cf9f` (worktree of `worker:s-store`, on `main` `b59eed1`). Reproducer before the edit: `rg -n "sweepJournals|removeJournals|remove:" test` exit 1, no output — the suite had no caller for the seam. After the edit: `node --test test/promptobus-done.test.mjs` exit 0, 32/32 checks, 1/1 runner test. Gates on `466cf9f`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 632 tracked files, 119 tarball entries. Mutation probe after the commit, tests kept: `removeJournals` changed to ignore the injected remover and call `rmSync` directly → exit 1, 28/32 — the four new checks red with `{"count":2,"failed":0}` and "journals removed: tasks 2" claimed on both scenarios; restored → 32/32. Review: the orchestrator read the diff in full (one test file, +52/−10, the two required cases and the helper parameterisation) — no isolated reviewer session was spent on it. Approver: squash of the worker branch onto `main`; no merge conflict; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** None required: test-only, no behaviour or documented contract change (PB-124 precedent), so no CHANGELOG entry.

**Acceptance.** Implementation: Codex `gpt-5.6-sol` max (`worker:s-store`, strategy `quality`, bus task `pb-run-0909-t20260909-132344`). Review: orchestrator.
