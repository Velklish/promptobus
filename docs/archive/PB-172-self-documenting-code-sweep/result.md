# PB-172 · Result

**Исход:** закрыта частично. Уборка выполнена, **оба гейта сняты с релиза**, остаток вынесен в `PB-188`.

**Что сделано.** Длинные комментарии вынесены из `lib/**`, `src/**`, `bin/**` в гайды и справочник; заведена страница `docs/reference/05-drivers.md` с рядом в индексе `docs/reference/README.md`; в коде осталось 98 указателей на документы. `contributing.md` получил раздел о правиле и три ловушки переноса — относительные пути, дейксис, потеря прозы, — а также четыре измеренных способа, которыми зелёная проверка смотрит мимо своего предмета. Написан сканер `test/comment-scan.mjs` (`commentRegions`, `proseOf`, `longRuns`), устойчивый к блочным комментариям с голыми строками, к комментарию после кода и к `//` внутри строки.

**Чем проверено.** На `d71d8de`: `npm test` exit 0, 57/57 файлов, 1761/1761 проверок; `backslop lint` exit 0; `npm run audit` exit 0, 781 отслеживаемый файл, tarball 119 записей. **Эти числа надо читать с поправкой 1**: в том дереве `boundary.test.mjs` проходил за счёт пустого `templates/`, которого в чистом чекауте нет. Отдельно: 98 указателей с якорем, 0 неразрешённых — пересчёт по всем файлам `lib src bin templates` с разбором заголовков целевых документов.

**Что осталось за карточкой.** `test/comment-length.test.mjs` и `test/comment-links.test.mjs` едут как `todo` — идут и печатают, свалить набор не могут. Причина в `CHANGELOG` и в `PB-188`. Сканер и оба pending-списка с идентичностями прогонов остаются в дереве: следующий заход должен увидеть исчезновение долга, а не начать с чистой базы.

Отдельно — цена самой уборки, чтобы следующий не повторил: мой же `fix-pointer-prose` уничтожил шесть строк живой прозы, переписывая `lines[i-1]` без условия; восстановлены дословно, полнота доказана разностью множеств (6 → 0). Седьмая нашлась позже полным диффом по всему диапазону (`src/model-routing.ts:35`) и имела **другой** механизм — хвост, съеденный внутристрочной заменой. Генератор заголовков резал на 88 символах и делал заголовки-обрубки, которые правщик прозы потом копировал в указатели: восемь заголовков переписаны руками вместе с указателями.
