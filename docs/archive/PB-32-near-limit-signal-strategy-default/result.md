# PB-32 · Result

**Closed 2026-09-06.** Completed. The `near-limit` warning is computed in the resolver for every strategy and carried in `decision.warnings`, per harness, measured on the harness's representative — the same floor-aware best eligible tuple the pace table and the `balance` pick use — when the binding window is past `nearLimit.usedPercent` (80) or the underspend is below `nearLimit.underspend` (−15 points); the line names which test tripped and the strategy it would switch to (`economy` when every paced harness is over, `balance` otherwise), and stays silent when that strategy is already the effective one. `models strategy` prints the effective default and its layer; `--set <strategy>` writes `defaults.strategy` into the host's writable layer (atomic, `0600`, every other key kept, the file created with a `schemaVersion` when absent), `--clear` removes just the key and drops an empty `defaults` (a no-op when the key is absent); the stack is read once before the write, so a broken upper layer refuses with nothing written, and a layer above the writable one that also names the key draws a shadow warning. Precedence everywhere — `spawn`, `review`, `models` — is flag → merged `defaults.strategy` → none (legacy path unchanged, probe-free); a routed lift records `strategySource: "overlay:<layer>"` in `metadata.routing` when the strategy did not come from the flag. `defaults`, `nearLimit` and `account` join the overlay grammar and schema (`defaults.strategy` closed to the five values, `auto` refused by name; `account.<harness>.plan` keyed by a harness this CLI drives, printed by `models` and `models strategy` as the line a person adds — no writer). `skills/orchestrate` gains `balance` in its rubric and a "when `models` says an account is running short" section — propose, never switch, the exact command; `skills/solo-review` follows the recorded default and pins the reviewer to no harness. The golden pair gained one `near-limit` line born of its own numbers (the example's session window 19.93 points ahead of pace).

Review (isolated, xhigh): the near-limit representative differed from the pace table's under a role floor, `strategySource` missing from the 04-protocol member list, `models` not reading the recorded default (so the silence rule never fired), the shadow branch untested and reading other layers after the write, `__proto__` keys reaching the prototype in `mergeDefaults`/`mergeAccount` — all closed in `fbf631a`. Closure checked by the approver on the diff.

**Verification.** On `main` after the squash merge of the track's tip: `backslop lint` no errors; `npm run audit` clean, 401 tracked files and the tarball; `npm test` 47/47 files. Worker's mutation probe after `e3c722e`: the recorded default made to win over the flag → "a flag on the command line always wins over the default" red; restored.

**Documentation in the same pass.** `docs/reference/03-cli.md` (the `models strategy` synopsis, the precedence paragraph, the writer's behaviour and refusals, the one-question paragraph, the `near-limit` warning row, `models` reading the default), `docs/reference/04-protocol.md` (`strategySource`), `docs/guides/model-routing.md` (the three keys, the copyable overlay), `skills/orchestrate/SKILL.md`, `skills/solo-review/SKILL.md`, `test/fixtures/model-routing/README.md` (the goldens are regenerated; the layer paths come from the test's constants), `CHANGELOG.md`.
