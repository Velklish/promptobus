# PB-24 · Result

**Closed 2026-09-06.** Completed. The availability snapshot is `schemaVersion` 2: a window is `{ id, kind: session | weekly | monthly, lengthSec, usedPercent, resetAt, scope }` with `scope` one of `null`, `{ model, models? }` (a model-scoped window, `models` the ids the adapter resolved the display name to, absent when unresolved) or `{ pool: "auto", models }` / `{ pool: "api" }` (Cursor's two monthly pools, the `api` pool carrying no list because it is the complement); a harness carries `tier: { name, source } | null` with `source` in `credentials | probe | derived | user`, `credits { available, unlimited }`, `resetCredits { available }` and `spendControlReached` (booleans and counts, never amounts); a model carries `hidden`, and `flags` is a closed enum (`MODEL_FLAGS`, today `no-zdr`). `cache.js` discards a cache document of any other version (`stale_cache`, reported by `preflight` as "written by an older version and discarded"), and the projection (`windowOf`, `scopeOf`, `modelOf`, `tierOf`, …) drops an element that lacks `kind`, `lengthSec` or `scope`, a tier whose name or source is off the grammar, or a flag off the list — the verdict stands, the element goes. The decision document gains an optional top-level `harnesses` block (a projection of the snapshot through `snapshotEntry`, printed by `render.js` as `availability:`); `DECISION_SCHEMA_VERSION` is untouched here and moves in PB-30. The Codex adapter's windows gained `kind` (primary → session, secondary → weekly, lengths from `windowDurationMins`) and `scope: null` so they survive the projection; a window whose duration the payload does not name is not reported (PB-24.1 in triage asks what the notification path carries).

Review (isolated, xhigh, the second pass of the contract track): 8 findings — one critical (the Codex windows were dropped by the new projection, so `remaining` was 50 everywhere and an exhausted Codex hid for an hour under the wrong TTL), three major (an absent scope silently became an account-wide claim; `models validate` never printed nor checked the writable layer; the CHANGELOG said `tsc` enforces `writable`), four minor. All closed in `2a67a1f` and `fdcd0f2` (the two `validate`/CHANGELOG ones belong to PB-25). Closure checked by the approver on the diff: `main` equals the worker's tip `d1ca164` over every file of the track.

**Verification.** Gates on `main` after integration: `backslop lint` no errors; `npm run audit` clean, 372 tracked files and the tarball; `npm test` 46/46 files. Worker's mutation probes after the commits, one at a time with a revert between: the reader accepting any version → preflight 30/1; `windowOf` ignoring `kind`/`lengthSec` → 30/1; `scopeOf` widening a broken scope to the account → green at first, which exposed a coverage hole, closed by a `third-shape` window in the projection test → 30/1; `modelOf` taking any flag → 30/1; `withAvailability` not attaching the block → 14/2; the Codex adapter dropping `kind` → adapter-codex 22/4 (a round-trip guard adapter → projection, not a fixture); an absent scope as a claim again → 30/1.

**Documentation in the same pass.** `docs/reference/03-cli.md` (the `availability:` block of `models`, every new field in the verdict table, the discard and migration rules, the tier TTL row, the Codex windows), `test/fixtures/model-routing/README.md`, `CHANGELOG.md` (unreleased, one entry). `validate.js` untouched by design: snapshot rules live where the shape is held (projection, `contractBreach`, the JSON schema under the ajv tests).
