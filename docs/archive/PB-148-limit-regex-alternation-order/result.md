# PB-148 · Result

**Closed 2026-09-09.** Completed. In `lib/driver-claude.js` the late-start classification no longer depends on which alternation of `LIMIT_DETAIL` matched first. `LIMIT_RESETS` (`/\blimit\b[^\n]*\bresets\b/i`) is the single reset-wording pattern; `LIMIT_DETAIL` is built from its source without a capturing group and stays the limit gate for both `markLimitAtStart` and `sessionStall`; `markLimitAtStart` tests the gate as a boolean and then tests `LIMIT_RESETS` independently over the whole refusal, so a refusal that names both `hit your … limit` and `limit … resets` — including on separate lines — is recorded as `subscription_exhausted` instead of `manual_exhaustion`. The driver comments, the Claude section of the model-routing reference and the CHANGELOG describe the independent check.

**Verification.** Worker commits `6c942a3` + `8ae8c49` (worktree of `worker:drivers`). Reproducer red before the repair: `node --test test/model-routing-adapter-claude.test.mjs` exit 1, 50/51 — `a limit refusal with both phrases is subscription_exhausted`, `AssertionError [ERR_ASSERTION]: Expected values to be strictly equal: actual 'manual_exhaustion', expected 'subscription_exhausted'`. Gates on `8ae8c49`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 587 tracked files, 119 tarball entries. Mutation probes after commit, test kept: the independent reset check reverted to `Boolean(hit[1])` → exit 1 at `test/model-routing-adapter-claude.test.mjs:945` (actual `manual_exhaustion`); the capturing `LIMIT_DETAIL` and `Boolean(hit[1])` restored → exit 1, same assertion, 50/51; fix restored → exit 0, 51/51. Isolated review (Claude `claude-opus-5 xhigh`, `reviewer:drivers`): no critical or major findings, two minor (a dead capturing group and a duplicated reset literal; a comment saying "in the line" for a whole-output check), both closed in `8ae8c49`; closure checked by the orchestrator on the diff. Approver: squash of the worker branch onto `main` at `6b3a857`; the only merge conflict was the `[Unreleased]` › Fixed list of `CHANGELOG.md`, resolved as the union of entries; `backslop lint` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/03-cli.md` § Model routing (Claude Code: the late-start hook); `CHANGELOG.md` `[Unreleased]` › Fixed; the driver comments above `LIMIT_RESETS` / `LIMIT_DETAIL` and `markLimitAtStart`.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:drivers`, strategy `balance`, bus task `pb-run-0909-t20260909-132344`). Review: isolated Claude reviewer under the same task. Known limit, inherited from the task's own pattern: `limit` and `resets` split across a line break still read as `manual_exhaustion`; no live Claude Code refusal text is in the suite (PB-15), so the classification is proven on the suite's wordings only.
