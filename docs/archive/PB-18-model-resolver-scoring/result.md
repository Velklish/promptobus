# PB-18 · Result

**Closed 2026-09-05.** Completed. `lib/model-routing/resolver.js`: the pure `resolve({ role, strategy, constraints, policy, snapshot, liveParticipants, now })` — ADR-003's nine filter steps in order with the first match reported, `roleRatings` overrides read for the role being routed, scoring from the merged policy only (no literal weights), `remaining` from the most spent window with the neutral 50 % and the −10 for `unknown`, the live-participant penalty capped as one adjustment row, the reviewer floor as a choice rule with the soft fallback and its warning, the diversity bonus against live workers, the four-level tie-break, merge warnings copied as `{ code, message }`; allow lists of different selector kinds combine by AND; tuples of a harness absent from the snapshot are filtered, not excluded. `lib/model-routing/render.js`: `render(decision)` reproduces the golden text byte for byte with a grid that widens per column, and a guard naming the missing field. Both goldens (`decision.json`, `models.txt`) reproduced from `catalog.json` + `snapshot.json` on the first run — the fixture arithmetic was correct as written. Track `resolver` of the routing run, worker in Claude Code (opus, xhigh), one isolated review round (7 findings on coverage and documentation, all fixed; no functional defect).

**Verification.** `npm test` exit 0 — 42/42 files; `test/model-routing-resolver.test.mjs` 48 checks (per-strategy winners and the full order turning over, determinism under reversed inputs, constraints including one that leaves nothing, PAYG by flag and by overlay, unrated → runtime row, inventory exclusion, availability states, the unknown penalty including an available harness with no windows, two-window `remaining`, stale and timeout warnings, the participant penalty and its cap moved by overlay, reviewer filtering, floor both ways and moved by overlay, diversity by harness and by model, each tie-break level on catalogs built so each level's loser wins at every later level, allow/deny naming the layer, allow across kinds, role-rating overrides for score and for the floor, the merge-warning projection, three renderer checks). `npm run audit` exit 0 — 280 tracked files and the 107-entry tarball. `backslop lint` exit 0. Mutation probes, commit-first, 28, every one red with the module still loadable: the `quotaCost` inversion flipped; the cap dropped; the tie-break reordered three ways; a literal weight, penalty and floor; the diversity bonus with no worker; the five early-exit filter steps neutered by substitution; `remaining` from the least used window; the neutral 50 moved; the wall clock; excluded rows printed first; the score column narrowed; adjustments dropped; `roleRatings` dropped and read for the wrong role; the merge warning carried whole or dropped; allow lists ignored or OR-ed; the render guard reduced; the state column left out. Gates re-run on the merged `main` tree by the approver.

**Documentation in the same pass.** `docs/reference/03-cli.md` (`### Resolver`, the § Model routing intro naming all three libraries, the sentence that the command raises `constraint-unknown` / `harness-unknown` before `resolve`), `docs/guides/model-routing.md` (how allow lists of different kinds combine), `docs/GLOSSARY.md` (resolver, routing decision), `test/fixtures/model-routing/README.md` (which check normalises which paths), `CHANGELOG.md` under `[Unreleased]`. No findings filed.
