# PB-155 · Result

**Closed 2026-09-09.** Completed. `mergeWeights` and `mergeFlat` in `lib/model-routing/catalog.js` skip a JSON `__proto__` key before assignment, matching the guards `mergeDefaults` and `mergeAccount` already carried, so an overlay read from disk can no longer move the prototype of the `weights`, `penalties`, `bonuses`, `qualityFloor`, `balance`, `nearLimit` or `payg` block on the routed lift. Weight provenance is not recorded for the skipped key. A parity regression in `test/model-routing-catalog.test.mjs` merges a `JSON.parse`d overlay that names `__proto__` in all seven blocks and asserts each stays on `Object.prototype` with no inherited field.

**Verification.** Worker commit `947490d` (worktree of `worker:routing`): reproducer red before the repair — `node --test --test-name-pattern='policy overlay blocks keep their prototype' test/model-routing-catalog.test.mjs`, exit 1, `AssertionError [ERR_ASSERTION]: weights prototype moved` (actual `{ totallyNewField: 'x' }`); mutation probe after the commit — both guards removed, test kept: same command exit 1, same assertion; guards restored: exit 0, 1/1. Gates on `947490d`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, no errors; `npm run audit` exit 0, 583 tracked files, 119 tarball entries. Approver: the integrated tree equals `947490d` outside `docs/backlog`, `docs/archive` and `docs/TRACKS.md` (`git diff 947490d HEAD --stat`), and `backslop lint` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/03-cli.md` § Catalog and overlays (the guard is now part of the merge contract); `CHANGELOG.md` `[Unreleased]` › Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:routing`, strategy `balance`, bus task `pb-run-0909-t20260909-132344`). Review: the orchestrator read the diff in full; a one-screen parity fix by the pattern of its sibling guards, so no isolated reviewer session was spent on it.
