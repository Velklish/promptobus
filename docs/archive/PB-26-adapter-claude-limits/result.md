# PB-26 · Result

**Closed 2026-09-06.** Completed. The Claude Code adapter reads the tier offline from the credential record — the macOS keychain item `Claude Code-credentials` through `/usr/bin/security`, or `<CLAUDE_CONFIG_DIR|~/.claude>/.credentials.json` elsewhere — taking three fields and never parsing `refreshToken`; one `GET /api/oauth/usage` maps `limits[]` to three v2 windows: `session` (5 h), `weekly` (7 d) and `weekly-<name>` scoped to the model ids the driver's `MODEL_SCOPE_IDS` table resolves the display name to (an unresolved name keeps its window and binds nothing); `/api/oauth/profile` is asked for the tier only when the record names none. `available` is reachable; a token the adapter cannot read or past its expiry (checked as a number) is `quota_unknown`; 401/403 is `not_authenticated`; an account-wide row at 100 % that is not `is_active: false`, or one with `locked_reason`, is `exhausted` — sticky when the harness named no reset, and the message then names `--clear-exhausted claude`; a spent model scope exhausts nothing but that tuple's window. The three adapters share one stamp normaliser, `stampAtMs` in `cache.js`.

Review (isolated, xhigh, two passes): eight findings in the first — sticky exhaustion without a reset unexplained, the scope table duplicated inside the adapter, a false claim about `fable` being rated, `CLAUDE_CONFIG_DIR` ignored, `expiresAt` through `Number()`, `is_active` dropped without a word, two untested branches, the duplicated normaliser — closed in `0d5a7c0`; the second pass added `is_active` in the spend verdict and a deterministic budget test, closed in `7a13de1`; the third pass found only comments that PB-29 had made stale, closed in `175d1f0`. Closure checked by the approver on the diff.

**Verification.** On `main` after the squash merge of the track's tip: `backslop lint` no errors; `npm run audit` clean, 396 tracked files and the tarball; `npm test` 47/47 files (the Claude adapter file 50 checks, was 30). Worker's mutation probes: the credential record spread instead of picked → the three-fields check red; a scoped row counted as spending → red; an unresolved name guessed as an id → red; `is_active !== false` dropped → red; false positive on the fixture (display name renamed) → two red.

**Documentation in the same pass.** `docs/reference/03-cli.md` (the Claude section: where the credential is read, the call, the row → window table, 15 verdict rows, the `quota_unknown` cell of the reason table, the two sentences that said `available` is unreachable removed), `CHANGELOG.md`. Findings: PB-26.1 (README sample output) → PB-33; PB-26.2 (the fable id) closed by PB-29 landing.
