# PB-37 · Result

**Outcome:** completed.

**What was done.** ADR-005: ratings on the 1–10 scale with two dated anchors per benchmark, version and agent harness (floor → 1, ceiling → 10, linear, half up; SWE-bench Verified 60 → 96, Terminal-Bench 2.1 60 → 90, TB 2.0 50 → 85, Artificial Analysis 40 → 310 tokens/s, blended list price $2.50 → $30); no field; role floors worker ≥ 5 / reviewer ≥ 9; `speed` constant along the effort ladder, `quality` one band per step, `quotaCost` one band per step (the owner's decision at the table review — two per step was rejected as too steep); upward interpolation never confers the reviewer role; hypothesis rows re-stated on the new scale; ladders keep every rung and `validate` warns `ladder-indistinguishable`; normalisation `/9` in every strategy and in `spendPenalty`; catalog `schemaVersion` 2 (45 rows re-banded from their cited figures, ten reviewer rows: Fable and Opus high–max, Kimi K3 max, gpt-5.6-sol xhigh–ultra); overlay `schemaVersion` 2 — a v1 overlay carrying `ratings`, `qualityFloor` or `reviewerQualityFloor` is refused with the rewrite route, a v1 overlay with policy keys only is read unchanged; `models calibrate [--json] [--write] [--yes]` reads the telemetry file beside the cache, keys by (harness, model, effort) with the Claude aliases resolved through the driver's `options.modelAliases`, needs five runs per key, anchors proposals to the shipped catalog with a pivot (the most-observed eligible key keeps its band, others move by fixed ratio steps, ±2 per calibration; the largest window delta per run; a measured zero is below resolution and proposes nothing), prints `speed` and `quotaCost` proposals with the numbers and `quality` as a note, and `--write` merges only moved ratings into the user layer after a terminal confirmation (`--yes` for no TTY, refused alone), raising a v1 overlay to 2. Reference (03-cli, 02-host), guide, orchestrate and solo-review skills, glossary, schemas, CHANGELOG.

**Verification.** Predecessor (Codex `gpt-5.6-sol` medium, died on `subscription_exhausted` with the review remarks half applied — the orchestrator committed the work in progress) and successor (Claude `opus` medium) under `balance`: `npm test` 48/48 files (calibrate 34 checks, catalog 49); `backslop lint`, `npm run audit` clean; live `models validate` from a standalone workspace — catalog and two overlay layers hold, one `ladder-indistinguishable` (`claude-fable-xhigh` / `claude-fable-max`); live `models calibrate` on the fixture prints a pivot and two proposals; the whole catalog recomputed independently by the successor — 0 mismatches. Mutation probes: seventeen across the rounds; four stayed green and each was a real gap closed by a constructed negative case (a v1 overlay with ratings in the parity corpus, an upward-interpolated reviewer, a user overlay that differs from the shipped catalog, the command's shipped-vs-merged seam). Isolated review (`reviewer:adr2`, `claude-fable-high` under `balance`): two major (calibrate compared with the merged stack, drifting after `--write`; v1 floors loading silently as 1–10) and four minor — closed in 5020876 / bbc9b10, closure verified by the approver on the diff. Approver's checkpoint review of the ADR before the implementation: four remarks (ADR-003 supersession, catalog-anchored calibrate, no promotional anchor, terms) — closed. The owner approved the catalog table on 2026-09-06 with the one-band `quotaCost` step. Approver on the squashed main: gates green, live validate and picks recorded in the run journal.

**Findings.** PB-37.1 — a promotional band has no expiry signal (queued). PB-37.2 — `done` reads the sixty-second window without refreshing; the orchestrate skill carries the workaround (queued). PB-29.1 — closed with this task (below).
