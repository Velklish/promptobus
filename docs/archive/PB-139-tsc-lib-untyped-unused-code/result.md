# PB-139 · Result

**Closed 2026-09-17, completed — with one half of the recipe removed rather than delivered.** `tsconfig.json` now sets `noUnusedLocals` and `noUnusedParameters`, and the two dead declarations the card measured are gone: the `meta` parameter of `finish()` in `src/v1/engine.ts` together with the argument its two call sites passed, and the unused `import process from 'node:process';` in `src/migrate.ts`. A third one the flags surfaced on today's tree, which the card's 2026-09-06 measurement did not carry — an unused import in `src/v1/messages.ts` — went with them.

**The `@ts-check` half was written and then withdrawn inside the same task, because it was a certificate nobody could back.** The card asked for `// @ts-check` as the first line of the 19 `lib/*.js` files that import from `../dist`, as a boundary net against a silent break from a `src/` export rename. Review measured what that marker actually buys: `tsconfig.json`'s `include` stays `["src"]`, and no gate — not `npm test`, not `npm run build`, nothing in `scripts/` — ever runs `tsc` over `lib/`. The marker does something inside an editor's language service and nothing anywhere else. Run deliberately, `npx tsc --ignoreConfig --noEmit --allowJs --checkJs --target es2022 --module nodenext --moduleResolution nodenext --skipLibCheck lib/*.js lib/model-routing/*.js` restricted to those 19 files reports **1605 diagnostics** (`store.js` 258, `review.js` 235, `spawn.js` 132, `models.js` 129, `resolver.js` 117, `telemetry.js` 114, `guard.js` 107) — the files were never clean under the check the marker names. All 19 markers were removed, and the real work — a `tsconfig` for `lib/` and a gate that runs it — is filed as **PB-139.1** in triage with that measurement attached.

**Verification.** `npx tsc -p tsconfig.json --noEmit` exit 0 on the merged tree. Full gates on the worker's branch head `1a636fb`: `npx github:Velklish/backslop#v0.8.0 gates` exit 0 — gates 4, green 4 (`npm test` 2276 checks over 69 of 69 files; `backslop lint` 0 errors; `npm run audit` 965 tracked files, 136 tarball entries; `npm run pins` 965 of 965 read, 49 live refs). On this task's slice as merged: `npm run build` exit 0, `node --test test/comment-length.test.mjs test/comment-links.test.mjs` exit 0 — 33 of 33, `backslop lint` exit 0. The full gate set was re-run once on the merged tree carrying all six tasks. No mutation probe: this task changes a compiler setting and deletes declarations, carrying no test of its own — the flags are the check, and `tsc` exiting 0 is the whole of it.

**Review.** Four rounds by an isolated reviewer. Round 1 returned the `@ts-check` finding as major; it is the reason the marker was removed and PB-139.1 filed. Rounds 2 to 4 raised nothing on this task, and round 4 was clean.

**What is left open.** `lib/` is type-checked by nothing at all, and the overview now says so rather than implying otherwise — PB-139.1 carries the work. The card's own verification line (`grep -c '@ts-check'` showing 1 in each of the 19 files) is deliberately not satisfied: it asks for the marker this task concluded was misleading.

**Documentation in the same pass.** `docs/reference/01-overview.md` § the `src/`–`lib/` split, `CHANGELOG.md`.
