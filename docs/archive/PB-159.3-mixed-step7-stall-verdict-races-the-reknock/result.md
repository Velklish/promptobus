# PB-159.3 · Result

**Closed 2026-09-17, completed.** Step 7 of the shared scenario carried two different reds under one title, and the one written in the title's own words went red on sound code. The decision the card asked for first was taken and it went against the mechanism: for `stall.kind === 'unknown'` the predicate compares the participant's last send with the newest of three activation marks, the question that branch asks is whether anything came back since the last time we reached them, and a fresh un-answered knock has had no answer — so a knock landing after the reply legitimately makes the stall stand. Giving the send a grace of its own would have weakened the predicate for every caller, and an existing warden verdict already pins that contract (`: a successful knock counts as an activation too, not only a fetched mailbox`). **The test owes the precondition, not the mechanism**; `src/supervisor.ts` keeps its predicate and no behaviour in that file changes.

**One verdict became three, and each names its class in its own red text.** The precondition — the worker's last send is not older than its last activation — is waited for and asserted on its own line, the shape `PB-159.1` used for the `LISTED` branch, and a red there is the RE-KNOCK class in those words. The snapshot the predicate is asked about is a verdict of its own, and a red there is the LOST-SESSION class: a persist session off its tmux server reads as `kind: "stale"`, the second of the two reds. The original name is kept for the predicate alone, and a red there with the precondition green is the mechanism. The yielded turn no longer travels signed LOST-SESSION under a conjunct that meant something else. Review round 2 closed the residual window that was left: the precondition rested on stamps read before `wh.inspect`, so a knock in the gap would still have reddened the last verdict with the words "this is the mechanism"; it now asserts the stamps as the wait saw them AND the stamps standing beside the predicate.

**`lastActivation` is a public export, and that is the price of the split.** The check has to establish the same order the predicate compares, and a private maximum over the three marks written inside the check drifts from the predicate it exists to guard the moment a fourth mark is added or one is dropped. `src/supervisor.ts` exports it, `src/index.ts` re-exports the module wholesale so it is package surface rather than an internal convenience, `lib/status.js` re-exports it beside `stallStands`, and the scenario takes it by that same import. No call inside the package changed.

**Verification.** The card's own item, on the merged tree of this acceptance: `node test/promptobus-mixed.test.mjs` **51/51 green twice in a row**, as two separate processes on the head of this acceptance, each exit code read from its own command and 0 both times — both entries are in `gates-approver-pbq.json`, marked there as card verification runs rather than project gates, and the same file runs green inside `npm test`. Gates of this acceptance, on the merged tree of these two commits: `npx github:Velklish/backslop#v0.8.0 gates` exit 0 — **gates 4, green 4**. `npm test` code 0 over **69 of 69** test files, **fail 0, todo 0**; `npx github:Velklish/backslop#v0.8.0 lint` code 0, **no errors, warnings 3** — the three pre-existing triage entries whose parents are closed (`PB-139.1`, `PB-188.2`, `PB-225.1`), which are the owner's to move and were not touched; `npm run audit` code 0, **clean** over 989 tracked text files and 136 packed entries, where the worker's branch had it red on the one pre-existing finding it was told not to carry; `npm run pins` code 0, `#v0.8.0` throughout, 989 of 989 tracked files read. The first class was reproduced by hand before it was repaired — a stub round with the stamps written straight into the task store, one reply and one end of turn, and the answer flips with the knock's stamp alone: a knock 60 s before the reply gives `false`, a knock 60 s after it gives `true`, `node reknock.mjs` exit 0. No load, no live participant. That reproduction is now a standing verdict in `test/promptobus-warden.test.mjs` (`: a re-knock landing AFTER the reply makes that same end of turn a stall again`). The probe the card asks for is aimed at the comparison rather than at the step: `src/supervisor.ts:146`, `sent < since` → `sent > since`, `npm run build && node test/promptobus-warden.test.mjs` exit 1, 259 of 273 verdicts passed, 14 reddened, 0 unaccounted; a second probe (`src/supervisor.ts:88`, `Math.max(...at)` → `Date.now() + 60000`) exits 1 with 52 of 56 on `promptobus-e2e`, 4 reddened, and confirms the precondition line still reads the mechanism's own `lastActivation`. `git status --porcelain` was empty before each probe and after each restore. Verdict names base ↔ branch: `promptobus-warden` 272 → 273, `promptobus-e2e` 53 → 56, `promptobus-mixed` 48 → 51, `promptobus-spawn` 160 → 162 — **none removed**.

**Documentation in the same pass.** `docs/guides/hooks-and-trust.md` — a new `### lastActivation` section (what it answers, and that its three readers do not read it for the same thing: `stallStands` and `promptStands` compare it with the last send and treat `null` as "nothing to compare against", while `sessionBusy` reads it only on the no-session branch, compares it with the last end of turn and answers `false` on `null`), and `### stallStands` gains the paragraph saying that a knock after the reply makes the stall stand again by contract, so a check asserting "not stalled" owes itself the precondition. `docs/reference/05-drivers.md` reflowed to the neighbours' width. `CHANGELOG.md` under `## [Unreleased]` — `### Added` for the export, `### Fixed` for the split. The card's quote block mirrors the final verdict.

**What is left open.**

- **The second class was not reproduced.** The take-down of the tmux server under a persist session needs a clean runner and a real `cursor-agent`; `which cursor-agent` on this machine returns nothing and there is no runner here. The card's verification allows the alternative — the class named in the verdict text so a future red says which of the two it is — and that is what was done. The candidates the parent card lists (`harness-cursor.mjs` `armCleanup`, the stub's session list racing its own state) stay unconfirmed hypotheses; nothing here tested them.
- **The card's `## Context` describes the state before this work, and two of its sentences are now false of the tree.** "Stated as a hypothesis, not a fact: it is read off the two sources above and was NOT reproduced" and "Reproducing it needs a machine loaded enough for a re-knock to fall between the reply and the verdict, and the measurement was not made" — the first class WAS reproduced, by hand, without load, and the reproduction is a standing verdict. The card is left as it was written and the correction lives here.
- **A card whose verification is "this file green twice in a row" has no machine slot of its own.** `schemas/v1/handover-record.schema.json` sets `additionalProperties: false` at the root and again on `checks`, whose membership is five fixed names, so the two runs travel as gate-record entries with a tail saying what they are. That is a gap in the form, noted rather than worked around.
- **`PB-228` stays in triage** — `test/promptobus-mixed.test.mjs` red once under measured sibling-worker load, not reproduced on base. It is a third red on the same file and this card does not close it.
