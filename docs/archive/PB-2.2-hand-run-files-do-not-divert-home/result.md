# PB-2.2 · Result

**Closed 2026-09-05.** Completed, with the entry's framing corrected by measurement: 22 of 46 suite files imported no verdict helper, and none of them wrote under the real home — the two that did each carried a copy of the diversion (the routing command and contract files, writing `~/.promptobus/model-routing/cache.json`). The gap was the absence of one apply point, not a leak in progress. `test/home.mjs` is now that point — self-contained (built-ins only, its own temp home and keep-until-exit) — imported by every `test/*.test.mjs` and by `check.mjs` before any non-built-in import; the two hand-rolled copies are gone; seven files that had loaded package modules ahead of the diversion were reordered. Four sentinels in `tmpdir-sweep.test.mjs`: every suite file reaches the apply point; reaches it before any non-built-in; `check.mjs` imports `home.mjs` first; `home.mjs` and `hygiene.mjs` borrow only built-ins. Track `host` of the routing run, worker in Claude Code (opus, xhigh, bypassPermissions), one isolated review round (5 findings, all fixed). Gates on the track's tree: `npm test` 46/46 files, 1330 `check.mjs` verdicts and 560 `node:test` assertions, 0 failed; `npm run audit` clean; `backslop lint` clean; re-run on the merged `main` tree by the approver.

**Verification.** Probes: the apply removed from one file → red naming the file; one file's import sunk below `sandbox.mjs` → red naming the file and the modules ahead of it; `home.mjs` importing `sandbox.mjs` again → red; `check.mjs` importing `home.mjs` second → red; the directory scan emptied → red on the count.

**Documentation in the same pass.** `docs/guides/contributing.md`, the 22 file headers, `CHANGELOG.md`. Finding filed: PB-2.3 (`test/sandbox.mjs` cites a check that does not exist).
