# PB-94 · Result

**Closed 2026-09-09.** Completed. `.github/workflows/ci.yml` now installs ast-grep at an exact version (`@ast-grep/cli@0.45.3`), names the TypeScript build as an explicit step before Test (the stale "no source tree" comment is gone), runs the matrix on Node 20 and 22 — both inside `engines >=20` — and no longer carries a standalone `npm pack --dry-run` step, because the package test already packs the tarball and installs it. The backslop reference stays the tag `github:Velklish/backslop#v0.4.0` in both CI and `package.json` — the project's documented spelling, a tag the owner cuts in his own repository — and the package test asserts the two references are identical; guides/contributing.md records that mutable-tag decision and corrects its displayed process version to v0.4.0. No version bump, no new release automation; `scripts/audit-public.mjs` needed no change.

**Verification.** Worker commit `ff6a9dc` (worktree of `worker:verify`, on `main` `a6bbd89`). Reproducer red before the repair: `node --test test/promptobus-package.test.mjs` exit 1, 30/35 — no exact ast-grep version, no matching backslop refs, no named Build before Test, a one-major Node matrix, a duplicate Pack dry-run. After the repair 35/35; `npm run lint:backslop` exit 0. Gates on `ff6a9dc`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 660 tracked files, 119 tarball entries. Mutation probe after the commit, test kept: the unpinned `npm install -g @ast-grep/cli` restored → 34/35, "CI pins ast-grep to an exact version — CI says no exact version, wanted 0.45.3"; pin restored → 35/35. The real CI matrix was not executed locally; the workflow assertions and the local gates cover the changed claims. Review: the orchestrator decided the five policy alternatives the card offered and read the diff in full — no isolated reviewer session was spent on it. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** guides/contributing.md (mutable-tag policy, process version), CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:verify`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator.
