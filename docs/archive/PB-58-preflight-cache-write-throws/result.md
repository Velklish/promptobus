# PB-58 · Result

**Closed 2026-09-09.** Completed. `preflight` in `lib/model-routing/preflight.js` no longer dies on a cache it cannot write: the `writeEntries` call is wrapped in its own `try`, a failure is reported with `warn` naming the cache file, and the probed snapshot — every verdict already in hand — is returned. Nothing persisted means the next command probes again. The comment at the catch states the rule (the probes are the facts, the cache an accelerator) and its boundary: a write a person asked for explicitly through `clearExhausted` stays a refusal, and `clearExhausted`, `withCacheLock`, `cache.js`, `driver-claude.js` and `done.js` are unchanged. The regression in `test/model-routing-preflight.test.mjs` makes the cache directory unwritable (`0o500`), runs a probe, and asserts the snapshot is returned with one warning naming the path.

**Verification.** Worker commits `fd26cbe` + `01d8032` (worktree of `worker:routing`). Reproducer red before the repair: `node --test --test-name-pattern='an unwritable cache warns and returns the probed snapshot' test/model-routing-preflight.test.mjs` exit 1, `Error: EACCES: permission denied, open '…/model-routing/.tmp-cache.json-…'` from `writeFileAtomic`. Gates on `01d8032`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 588 tracked files, 119 tarball entries. Mutation probes after commit, test kept: the catch body replaced by `throw e` → exit 1 with the same `EACCES` failure; fix restored → exit 0, 1/1. Isolated review (Claude `claude-opus-5 xhigh`, `reviewer:routing`): one minor (the swallowed exception carried no reason in code), closed in `01d8032`; the reviewer traced the refusal path to the end (lock failure swallowed inside `withCacheLock`, then `writeFileAtomic`), confirmed `cacheFileOf` is exported, the handler cannot throw again, no dead `.lock` is left behind, and `dryRun` and `clearExhausted` are untouched; closure checked by the orchestrator on the diff. Approver: squash of the worker branch onto `main` at `3c15655`; the only merge conflict was the `[Unreleased]` › Fixed list of `CHANGELOG.md`, resolved as the union of entries; `backslop lint` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/03-cli.md` § Availability (the cache paragraph: a failed preflight write is a warning, not a failed probe); `CHANGELOG.md` `[Unreleased]` › Fixed; the comment at the catch.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:routing`, strategy `balance`, bus task `pb-run-0909-t20260909-132344`). Review: isolated Claude reviewer under the same task. Note for gate runs: the regression relies on directory permissions and fails loudly under root (CI containers) — a property shared with `test/promptobus-spawn.test.mjs`.
