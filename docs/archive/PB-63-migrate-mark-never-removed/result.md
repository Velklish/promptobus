# PB-63 · Result

**Closed 2026-09-09.** Completed. The migration mark is transient: `src/migrate.ts` writes `migrating.json` into the assembled store before the atomic switch, removes it after the former directory is removed on both the normal and the resumed path, and no longer accepts the `migrated.json` that earlier releases left behind as a resume token — a completed-migration record plus a legacy store reappearing at the same path now hits the existing side-by-side `GateError` with neither root touched. A mark whose former directory is already gone (removal interrupted between the two `rmSync` calls, or refused by the OS) is reported by `preflight()` as `needed: true, sweep: true`; `migrate()` — on the direct, the locked and the post-timeout re-preflight path — removes only that dead mark and returns an empty report, so the store door stays silent and the mark can never authorize a later deletion. The resumed-move warning in `lib/store.js` no longer claims a previous run finished the move. The stale marker-name comment in `test/promptobus-ambient.test.mjs` was corrected in the same pass (scope widened by the orchestrator).

**Verification.** Worker commits `1527c9f` + `582ec8c` (worktree of `worker:s-types`). Reproducer red before the repair: `node test/promptobus-migration.test.mjs` exit 1, 73 tests / 69 pass / 4 fail — `the old completed mark authorized cleanup` (actual `''`, expected `/both bus stores sit side by side/`), `'' !== 'GateError'`, `the reappeared legacy store was deleted` (`false !== true`); the review round's sweep regression red on `1527c9f` (`the dead cleanup mark survived the next open`, `the dead cleanup mark authorized a later deletion`), 79/79 green on `582ec8c`. Gates on `582ec8c`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 583 tracked files, 119 tarball entries. Mutation probes after commit, tests kept: `src/migrate.ts` restored from the parent commit → exit 1, 79 tests / 73 pass / 6 fail (`the dead cleanup mark survived the next open`, `the direct sweep left its mark behind`, `the reappeared legacy store was deleted`); fix restored → exit 0, 79/79. Isolated review (Claude `claude-opus-5 xhigh`, `reviewer:s-types`): no critical findings, three minor (orphaned-mark window; the ambient-test comment; the header comment's meaning of a missing mark), all closed in `582ec8c` (option (a) chosen by the orchestrator: sweep in `migrate()`, `preflight` mutation-free); closure checked by the orchestrator on the diff. Consumer check: no caller of `migrationNeeded` in the ATI CLI; inside the package the predicate means "call `migrate` before access", which a sweep plan satisfies. Approver: squash of the worker branch onto `main` at `46179e7`; the only merge conflict was the `[Unreleased]` › Fixed list of `CHANGELOG.md`, resolved as the union of entries; `backslop lint` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/02-host.md` § `legacyLayout()` (mark lifecycle, sweep rule, old-marker rule); `CHANGELOG.md` `[Unreleased]` › Fixed; the header comment of `src/migrate.ts`.

**Acceptance.** Implementation: Codex `gpt-5.6-sol` max (`worker:s-types`, strategy `quality`, bus task `pb-run-0909-t20260909-132344`). Review: isolated Claude reviewer under the same task.
