# PB-16.2 · Result

**Closed 2026-09-05.** Completed (with PB-15.4 merged in). The preflight resolves each declared binary once, before the race, memoised by tool name, under the same deadline as the probes, and hands each probe its `HostToolBin` as `ProbeRequest.toolBin`; the three adapters read it and call `resolveToolBin` nowhere; a harness whose resolve never ran within the budget reports `probe_timeout` naming the resolve, so a slow host and a slow harness send a person to different places. A harness the registry cannot answer for (`adapterFor` throws) is one `probe_failed` entry — the error's kind, not its text — and its neighbours are still probed. The contract in `src/model-routing.ts` says a probe must not block the event loop. `resolveToolBin` keeps its signature. Track `routing`, worker in Claude Code (opus, xhigh, bypassPermissions), one isolated review round (9 findings, all fixed).

**Verification.** Measured with a blocking stand-in host: three harnesses on one binary → one resolve, 1.0 s (3.0 s without the memo); three binaries at 400 ms against a 500 ms budget → two resolved, the third reported unresolved, run ends ~0.8 s. Probes, commit-first, each red: resolve removed from the preflight; the deadline check removed; the memo removed; each adapter resolving itself; each adapter blocking the loop; the per-name `try` removed (the healthy neighbour goes down with the broken one). `npm test` 46/46 on the track's tree; gates re-run on the merged `main` tree by the approver.

**Documentation in the same pass.** `docs/reference/03-cli.md` § Availability (the resolve step, what is left of the budget, the split Cursor rows), `CHANGELOG.md`.
