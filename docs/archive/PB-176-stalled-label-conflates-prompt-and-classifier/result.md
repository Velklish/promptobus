# PB-176 · Result

**Closed by removing a claim rather than by adding a detector.** The route for a dialog mark now
names all three readings — a prompt of the session's own work, a held peer message, a refusal
that has already returned — and asserts none; the tests that pinned the old assertion moved with
it and gained both the third reading and a negative on what was removed.

Verified: mutation probes in both directions on a committed tree — dropping the third reading
reddens exactly the check that asserts it, in two files; restoring the removed claim reddens
exactly the negative. What set the mark is written as an inference and not as a finding: the
field is transient, a held peer message is ruled out for that session, and the refusal is not
reproducible on demand, so nothing can pair a refusal with a registry read.

Outside the card: the word `STALLED` itself, which is the state machine's verdict across four
kinds and correct for three.

---
