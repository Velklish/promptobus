# PB-203.1 · Result

**Closed 2026-09-13.** Completed at write time, and the claim was deliberately kept narrower than the symptom. A participant whose journal record carries no session reference cannot be traced afterwards; the record now says so explicitly instead of omitting the field. `spawn` and `review` persist `metadata.session: null` when the lift returns no session (a non-null id is preserved untouched), and `status` distinguishes the explicit write-time marker — "session reference was not recorded at write time" — from a legacy record that carries neither field — "participant record is incomplete: no session reference". What the record does **not** claim is whether a session existed for a moment and was lost: the measured window proves that cannot be told apart. `worker:lint` started at 2026-09-04T08:48:50.578Z and the supervisor reported the session record absent at 08:49:23.656Z; those 33 seconds admit both histories, and the card refuses to choose between them.

**Verification.** Gates on the merged tree, commit `0b67ddf`, tracked tree clean: `npm test` code 0 with `65/65 test files passed`, `backslop lint` code 0, `npm run audit` code 0, `npm run pins` code 0 — gates 4, green 4; the worker's own record for its final tree landed as `gates-pb-handoff.json`. Target suites: `promptobus-review` 234/234 → 235/235, `promptobus-warden` 259/259 unchanged. Mutation probes after commit, one per persist callback: `id ?? null` → `id ?? undefined` reddened the silent-worker assertion and, separately, the silent-reviewer assertion, each with exit 1, and both restores returned exit 0.

**Documentation in the same pass.** `docs/reference/03-cli.md` § Spawn, `README.md`, `README.ru.md`, `CHANGELOG.md`.

**Left open, and one thing corrected on the way.** The first attempt let the new diagnostic *replace* the liveness line. That was reversed after the orchestrator asked whether the two rested on independent grounds: liveness comes from the `metadata.name` snapshot, which measures state whatever the journal says, so the diagnostic now stands **beside** it rather than instead of it — and the artificial `session: 'cyc1'` added to the CYCLE fixture during the first attempt was removed, since the fixture guards the same two `status` branches it always did. An isolated reviewer's first pass found that the new null-marker assertions exercised a non-zero launch exit, which returns before `persist()` is ever called — so reverting either callback would have erased the marker on the live code-0/no-registry path with every new assertion still green. Closed by extending the `SILENT_TASK` and `silentTask` cases to the live silent path, which is what the mutation probes above now target.
