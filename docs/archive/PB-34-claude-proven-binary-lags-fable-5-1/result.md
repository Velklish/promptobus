# PB-34 · Result

**Outcome:** completed.

**What was done.** The driver is proven on `claude` 2.1.263 (the owner updated the CLI on 2026-09-06 by decision): `PROVEN_CLAUDE_VERSION` 2.1.263, `MODEL_IDS` gains `claude-fable-5-1` and keeps `claude-fable-5` (both are first-party rows in the binary's baked catalog, both in its accepted-id array, `alias_migration` empty, the 5.1 row names `fallback_3p: "claude-fable-5"` — the vendor still serves the predecessor), `MODEL_SCOPE_IDS.fable` covers both ids (a scope names a family — the account's weekly "Fable" window binds both), the alias table re-read and unchanged. The reference says the proven Claude version is a measurement, not a floor (unlike the Cursor and Codex ones, which refuse a lift), what a lag costs, and how to re-prove (a lift into a throwaway standalone workspace and one bus message to a session on `idle`). Catalog: the `claude-fable-51` ladder (xhigh base, high and max interpolated) — `quality` 10 as a stated hypothesis (no SWE-bench Verified figure is published for Fable 5.1; the vendor's six head-to-head benchmarks put it ahead of Fable 5 on every one and none of them has an anchor pair, so the successor cannot sit below Fable 5's 10), `speed` 2 from Artificial Analysis 70 tokens/s, `quotaCost` 10 from the published $10/$50 per Mtok; Fable 5's `speed` moved from hypothesis to the cited 72 tokens/s (band unchanged); the Claude priority block renumbered; the guide's alias note now demonstrates the id-keying rule. CHANGELOG under Unreleased.

**Measured.** The baked catalog read offline from the 2.1.263 binary (no paid turn). Live lift and knock on 2.1.263: a participant in a standalone workspace lifted, its first turn ended on `idle`, a bus message at 07:56:26.5 was answered "knock landed" at 07:56:32.7 — 6.2 s, two short turns of the owner's plan; `claude agents --json` record form unchanged from 2.1.251.

**Verification.** Worker (Claude `opus` medium under `balance`): `npm test` 48/48 files, 1435 checks; `backslop lint` and `npm run audit` clean (431 tracked files); live `models validate` — catalog and two overlay layers hold, two `ladder-indistinguishable` warnings (both Fable ladders clamp at 10, decision 8); `models --strategy quality` lists `claude-fable-51-xhigh` as a routable candidate. Mutation probes after the commit: `claude-fable-5-1` dropped from `MODEL_IDS` → the adapter test and the catalog test both red (the row/inventory agreement is pinned from both sides); a priority moved out of order → `priority-not-canonical` names the new row. Isolated review (`reviewer:claude`, under `balance`): one major (the alias table was the limit-scope table, so `fable` with two ids stopped resolving in `calibrate`) and one minor (the calibrate fixture held its own alias table) — closed in e11f5d8 with a separate `MODEL_ALIAS_IDS` (`fable` → `claude-fable-5-1`, where the binary points it today; a record before the re-point is attributed to the successor, stated in the reference) and a fixture pinned to the driver's table with a one-id assertion; three probes red; closure verified by the approver on the diff. Approver on the squashed main: gates green.

**Open edges.** `quality` 10 is the ceiling: Fable 5.1, Fable 5 and Opus 5 share the band and the resolver separates them by `quotaCost` and the tie-breaks — ADR-005's clamp, stated in the row. Terminal-Bench 4.0 has no anchor pair (filed under PB-37 as PB-37.3).

**Findings.** PB-34.1 — the binary's baked table carries a `mythos` family (`claude-mythos-5-1`, first-party, same pricing tier as Fable 5.1) with no alias entry and a `haiku` alias the help does not print; the leaderboard lists Claude Mythos 5 at 95.5 % SWE-bench Verified — a band-10 model the catalog does not rate and the driver cannot name; entitlement unchecked (queued). PB-34.2 — the audit red on the orchestrator's files, a duplicate of PB-35.1 (closed as merged).
