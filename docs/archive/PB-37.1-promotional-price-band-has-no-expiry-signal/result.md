# PB-37.1 · Result

**Closed 2026-09-09.** Completed, on the owner's decision of 2026-09-09: the expiry lives on the citation. A `quotaCost` citation of a promotional figure carries `validUntil`, `listFigure` and `listBand` — machine-readable, so no check has to parse `evidence.text`; the three fields are lawful only on a `quotaCost` citation, and `validUntil` requires the other two, in both the hand-written grammar (`lib/model-routing/validate.js`) and `schemas/model-routing/catalog.schema.json`, which the parity corpus holds together. `promotionWarnings` in `lib/model-routing/catalog.js`, called beside `staleWarnings` from `mergeRouting`, raises `promotion-expired` when `validUntil` has passed — naming the row, the promotional figure, the list figure and the band the list price gives — as a warning only: the catalog stays loadable and the person re-bands on their own schedule, the way `stale-rating` behaves. The warning is validate-only and never reaches a decision (`DECISION_WARNINGS`, the decision schema and the 03-cli warning table are untouched, deliberately). The guide's re-rating section states the rule: when the vendor publishes an end date, `validUntil` is that date; when none is published, it is the last date the promotion was observed in force and the warning is the reminder to re-verify or re-band; without either date the promotional figure is not cited. The two shipped Gemini rows carry `validUntil: 2026-09-06` (the day the promotion was seen in force), `listFigure` and `listBand: 2`, and are not re-banded (task scope); the shipped catalog therefore prints two `promotion-expired` lines from `models validate` from 2026-09-07 until re-verified — stated in the CHANGELOG and here, so it is a decision on record.

**Verification.** Worker commits `613f37d` + `5c7a751` (worktree of `worker:routing2`). Reproducer red before the repair: `node --test --test-name-pattern='an expired promotional quota citation warns' test/model-routing-catalog.test.mjs` exit 1, `unknown field validUntil`. Gates on `5c7a751`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 620 tracked files, 119 tarball entries; catalog suite 55/55 including parity. Mutation probes after commit, tests kept: `validUntil` removed from the citation grammar → exit 1 (`unknown field validUntil`); `listFigure`/`listBand` removed → exit 1 (`unknown field listFigure, listBand`); fix restored → 1/1. Isolated review (Claude `claude-opus-5 xhigh`, `reviewer:routing2`): five major (the reference's citation field list; `promotion-expired` undocumented and three statements made false; facts regex-scraped from prose; the fallback printing the list figure as the promotion; `validUntil` set to the observation date without saying so) and three minor (`validUntil` accepted on any citation; an order-dependent test on a row that already carried the field; two warning families in one function), all closed in `5c7a751` under the orchestrator's decisions; closure checked by the orchestrator on the diff. Definition drift closed in-pass: the task named `staleWarnings` in `validate.js`; the mechanism lives in `catalog.js`. Approver: squash of the worker branch onto `main`; the only merge conflict was the `[Unreleased]` list of `CHANGELOG.md`, resolved as the union of entries; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/guides/model-routing.md` § Re-rating a row and § Checking a file; `docs/reference/03-cli.md` § Catalog and overlays (citation fields, `models validate` warnings); the `DECISION_WARNINGS` doc comment; `schemas/model-routing/catalog.schema.json` citation description; `CHANGELOG.md` `[Unreleased]` › Fixed.

**Acceptance.** Policy: owner's decision 2026-09-09 (`validUntil` on the citation). Implementation: Codex `gpt-5.6-luna` max (`worker:routing2`, strategy `balance`, bus task `pb-run-0909-t20260909-132344`). Review: isolated Claude reviewer under the same task. Open for the owner, not filed: whether `promotion-expired` should also reach routed decisions (three pinned places move together: `decision.schema.json $defs.warningCode.enum`, `DECISION_WARNINGS`, the 03-cli warning table).
