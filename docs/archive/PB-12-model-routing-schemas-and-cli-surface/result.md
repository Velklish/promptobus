# PB-12 · Result

**Closed 2026-09-05.** Completed. Four JSON schemas under `schemas/model-routing/` (catalog, overlay, snapshot, decision — the `models --json` output with weights, per-candidate components and an ordered adjustments list, exclusions, unrated runtime rows, warnings, constraints and overlays with `applied`, snapshot source and age). The CLI surface is written into `docs/reference/03-cli.md` under `## Model routing`, opening with "Not implemented yet": `models [--strategy] [--role] [--refresh] [--json]`, `models validate`, `models --clear-exhausted`, `--strategy` and `--allow-payg` on `spawn` and `review`, `--dry-run` reading the cache only (owner's decision). Codes are listed once there: 9 reason, 7 exclusion, 3 adjustment, 5 warning, 9 error codes (`PromptobusError`, registered in `ERROR_CODES` by PB-21). Golden fixtures as an input pair (`catalog.json`, `snapshot.json`) and an output pair (`decision.json`, `models.txt`) with the comparison convention in `test/fixtures/model-routing/README.md` (path and clock normalisation). The `models` text format was accepted by the owner as is. Same track, worker and review round as PB-11.

**Verification.** `test/model-routing.test.mjs`: 8 checks green — schemas validate their examples and the fixtures, every code listed exactly once and equal to the schema enums in both directions, the golden arithmetic (components sum to base, adjustments to total, descending order, one chosen maximum), the text rendering, and the legacy-behaviour check (`liftHarness` without `--harness` never touches `declaredTools()`, `resolveEffort(undefined)` is `null`, defaults intact) — plus 4 `node:test` `todo` checks titled "PENDING until PB-21" (decision JSON byte-for-byte, text output, help contract, error-code subset), the convention documented in the `test/run.mjs` header. Mutation probes, commit-first, each red then restored: field renamed in the decision fixture; a score changed; a code dropped from the reference only; a code dropped from a schema enum only; two candidate rows swapped in `models.txt`; `liftHarness` consulting `declaredTools()`; substitution probes on the two extractors (naive backtick grep, indentation-wide row scan) both red. Gates on the merged `main` tree: see the run log of the acceptance commit — `npm test`, `npm run audit`, `backslop lint`.

**Documentation in the same pass.** `docs/reference/03-cli.md` (`## Model routing`, the command-list line), `test/fixtures/model-routing/README.md`, `test/run.mjs` header (pending convention), `CHANGELOG.md` under `[Unreleased]`. Finding filed: PB-12.1 (shipped `schemas/v1` descriptions are Russian).
