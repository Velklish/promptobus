# PB-120 · Result

**Closed 2026-09-17, completed.** Both halves of the card are closed, and the first one took the generic branch the card offered rather than the collapsing one. `optionRefusal` in `lib/driver-claude.js` now reads `claudeDriver.options.effortMinVersion?.[effort]` and refuses on whatever it finds there, so a second entry in the map does what the type has been advertising all along; the refusal text is built from `effort` and `minVersion` instead of the hard-coded `ultracode` and `ULTRACODE_MIN_VERSION`. In `lib/driver-cursor.js` the inert `{ fresh: true }` argument is gone, and with it the unused `_name` parameter of `resolveTmux` — there is no cache in that module for a `fresh` flag to bypass, which is what made the option a description of behaviour that did not exist.

**The refusal text lost its measurement, and that is a deliberate consequence of generalising it.** The old message named the date and the binary the degradation was measured on (2026-08-28, 2.1.169) and advised `--effort xhigh` by name. Neither fact survives generalisation: a message produced for any effort in the map cannot carry a measurement taken for one of them, and a fixed alternative effort is wrong as soon as the map holds two. The review round that raised this asked for the ultracode-specific facts to come out; the text now says "a lower binary drops it with a stderr warning and lifts the session on the DEFAULT effort" and advises picking a lower effort.

**`src/driver.ts` is unchanged, on purpose.** Its doc comment for `effortMinVersion?: Record<string, string>` already described the generic shape accurately, and the shape is the one that was kept — there was nothing to bring into line.

**Verification.** `npm run probe -- lib/driver-claude.js --mutate "s/effortMinVersion\?\.\[effort\]/effortMinVersion?.ultracode/" --run "node test/promptobus-spawn.test.mjs"` — the generic lookup reverted to the old ultracode-only hard-code — mutated exit 1, restored exit 0, and four checks reddened, among them `a second effortMinVersion entry refuses on its own` and `an effort absent from effortMinVersion still passes unconditionally`, the two fixtures this task added. `git status --porcelain` empty before and after. Full gates on the worker's branch head `1a636fb`: `npx github:Velklish/backslop#v0.8.0 gates` exit 0 — gates 4, green 4 (`npm test` 2276 checks over 69 of 69 files; `backslop lint` 0 errors; `npm run audit` 965 tracked files, 136 tarball entries; `npm run pins` 965 of 965 read, 49 live refs). On this task's slice as merged: `node --test test/comment-length.test.mjs test/comment-links.test.mjs` exit 0 — 33 of 33, `npm run build` exit 0, `backslop lint` exit 0. The full gate set was re-run once on the merged tree carrying all six tasks.

**Review.** Four rounds by an isolated reviewer. Round 1 returned one minor on this task — the ultracode-specific measurement left inside the generalised text — closed by the rewrite above. Rounds 2 to 4 raised nothing on it, and round 4 was clean.

**What is left open.** Nothing on this card. `resolveTmux` still has no cache; adding one was out of scope by the card's own words, and the flag that implied one is now gone rather than made true.

**Documentation in the same pass.** `CHANGELOG.md`. No reference section changed: the `effortMinVersion` shape is unchanged, and the refusal text is not quoted in the reference.
