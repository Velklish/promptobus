# PB-245 · Result

**Closed 2026-09-23.** Completed. `claude-opus-5-5` joins `MODEL_IDS` beside `claude-opus-5` and the `opus` limit scope names both ids; the line count of `lib/driver-claude.js` is unchanged (653), so line citations into the file hold. The catalog gains the `claude-opus-55` ladder: `xhigh` base — `quality` 10 as a stated hypothesis under the successor rule (no SWE-bench Verified figure is published for Opus 5.5; every vendor head-to-head puts it above Opus 5), `speed` 3 from Artificial Analysis 93 tokens/s ("xhigh with fallback"), `quotaCost` 4 from the $4 / $20 launch price — with `max`, `high` and `medium` interpolated; the Claude priority block is renumbered 100…290. Pinned counts follow: 55 rated tuples, 16 reviewer rows, 15 approver rows, 18 base models, 11 with a `quality` hypothesis. The proven binary stays 2.1.263 and the `opus` alias with it; the re-proof, and whether an id read off a newer baked table needs a version floor in the static inventory, is PB-245.1 (queued, `major`).

**Verification.** Gates on the working tree before review: `npm test` 70/70 files, exit 0 (192 s); `backslop lint` exit 0; `npm run pins` exit 0; `npm run audit` exit 1 on PB-244 naming origin tracker ids — a card committed in this run, fixed in 4b4f8aea, then exit 0; `npm run codex-schema` exit 1, "codex 0.156.1 is installed, the fixtures were taken from 0.146.0" — the same exit and message on the base 4b4f8aea in a built clean clone, so it is the machine, not this change (PB-196). `models validate` exit 0, no new warning. Mutation probe on deb7eaa3: `npm run probe -- lib/driver-claude.js --mutate "s/'claude-opus-5-5', //" --run "node --test test/model-routing-adapter-claude.test.mjs test/model-routing-catalog.test.mjs"` — mutated exit 1 with four checks red (the inventory, the credential-less probe, the scope table, "every Claude model id is one the driver reports"), restored exit 0, tree clean before and after. Isolated review (a fresh read-only session): 8 findings — 2 `major` (a routed pick can land on Opus 5.5 on a build whose table lacks it; the Terminal-Bench 4.0 figure was attributed to Claude Code and banded by that pair), 6 `minor` (variant names, a stale count in a comment, the alias literal cut short, "the lift refused" true only for a routed lift, the successor test pinned for Fable only, PB-245.1 missing three alias texts) — all closed in the same pass: a caveat with the overlay `deny` route in the reference and CHANGELOG, the evidence text rewritten (no band computed from the vendor setup), the successor test run over both pairs. The second pass read the fix diff against the eight findings. Gates on the accepted tree (base deb7eaa3 plus the fixes, before the squash): 5 gates, 4 green — `npm test` 70/70 files, exit 0 (210 s); `backslop lint`, `npm run audit`, `npm run pins` exit 0; `npm run codex-schema` exit 1, the machine-side red measured on the base above.

**Documentation in the same pass.** `docs/reference/03-cli.md` (the rated-ids paragraph, a new "Opus 5.5 is in the list ahead of the proven binary" paragraph, the scope mapping), `docs/guides/model-routing.md` (the pinned-ids sentence, reviewer and approver row counts, base-model counts), `docs/ROADMAP.md` (55 rated tuples), CHANGELOG under Unreleased.
