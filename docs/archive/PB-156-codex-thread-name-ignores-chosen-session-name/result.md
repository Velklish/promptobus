# PB-156 · Result

**Closed 2026-09-10.** Completed. The Codex session record carries the readable session name the mechanism already chose (`Worker: <title> (<MMDD-HHMM>)`, `Review:` for a reviewer) as `name`, on the same channel as `task` and `address`; the holder passes `record.name` to `thread/name/set`, so the Codex UI shows the title-based name, and falls back to the machine string `promptobus:<task>:<address>` for a record written by an earlier version. Lookups by task and address are unchanged: nothing reads the thread name back. The driver's `naming` phrase now speaks only about the thread id, which is the harness's to choose, and the host reference describes how a participant session is named.

**Verification.** Worker commit `bc12c66`, worktree of `worker:codex`, squashed. Reproducer red before the repair: `node test/promptobus-driver-codex.test.mjs` exit 1, 126/128 — "Codex thread name equals the chosen session name in the participant record" (the thread was named `promptobus:<task>:worker:cdx`) and "two Codex workers on one task keep distinct title-based names"; after the repair 129/129. Gates on `bc12c66`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 681 tracked files, 119 tarball entries. Mutation probe, tests kept: the name field, the name selection and the phrase reverted → exit 1, 126/129 with the naming, chosen-name and distinct-names assertions; restored → 129/129. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/02-host.md` (how a participant session is named and passed through the driver's name seam), CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:codex`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator diff read; approver Павел Ким's orchestrator session.
