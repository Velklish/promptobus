# PB-45.1 · Result

**Closed 2026-09-09.** Completed. `snapshotSessions` in `src/driver.ts` tells a `GateError` from any other driver error: the participant still answers `unknown` and the walk continues, but the view carries `stall: { kind: 'unknown', reason }` with the refusal text — the variable `PROMPTOBUS_<HARNESS>_HOME` and `harnessStateHome` that PB-45 made the registry name — while every other error keeps the reasonless `UNKNOWN` exactly as before. `lib/status.js` prints that reason on the `unknown` line and keeps "there is nobody to ask about it" for a harness with no registry at all. `docs/reference/02-host.md` states the corrected boundary: the snapshot degrades any driver error to `unknown` so one participant cannot take `status`, `done` or the supervisor down, and a refusal is carried into that unknown as its reason rather than dropped.

**Verification.** Worker commit `0e52d3b` (worktree of `worker:drivers`). Reproducer red before the repair: `node --test test/driver.test.mjs` — `a GateError stays on its participant as unknown while the snapshot walks on` (actual `stall: null`), `status names a registry refusal and keeps the reasonless unknown sentence` (no `PROMPTOBUS_CURSOR_HOME` in the output). Gates on `0e52d3b`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 609 tracked files, 119 tarball entries; targeted 43/43. Mutation probe after commit, tests kept: the bare catch and the fixed sentence restored → exit 1, 41/43 with the two assertions above; fix restored → exit 0, 43/43. Review: the orchestrator read the diff in full against the task's "Work to do" — six lines in `src/driver.ts`, one in `lib/status.js`, one reference sentence — so no isolated reviewer session was spent on it. Approver: squash of the worker branch onto `main`; the only merge conflict was the `[Unreleased]` list of `CHANGELOG.md`, resolved as the union of entries; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/02-host.md` (the registry-refusal boundary paragraph); `CHANGELOG.md` `[Unreleased]` › Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:drivers`, strategy `balance`, bus task `pb-run-0909-t20260909-132344`). Review: orchestrator. Last task of track D in the 2026-09-09 run.
