# PB-85 · Result

**Closed 2026-09-09.** Completed. `lib/driver-cursor.js` shares one search list between Cursor binary discovery and tmux resolution: `searchPath()` returns `PATH` plus the expanded `CURSOR_INSTALL_DIRS`, `findCursorBin` walks it as before, and `resolveTmux` now runs `tmux -V` with `PATH` augmented by the same directories, so `doctor` and `spawn --harness cursor` resolve the same tmux and a Homebrew tmux invisible to a background job's `PATH` no longer refuses the spawn. The refusal reason names both `PATH` and the searched install locations. The version gate and the duplicated `TMUX_MIN_VERSION` are unchanged, and `lib/spawn.js` needed no edit: its `optionRefusal` wrapper already delegates to the driver's resolver. The regression in `test/promptobus-driver-cursor.test.mjs` isolates `PATH`, installs a stub `tmux` only under a temporary `HOME/.local/bin`, and reaches the real resolver through `cursorDriver.optionRefusal` without a `util` override; no Cursor or live tmux is started.

**Verification.** Worker commit `672f535` (worktree of `worker:drivers`). Reproducer red before the repair: `node test/promptobus-driver-cursor.test.mjs` exit 1 — `PB-85: real tmux resolver searches a known install location outside PATH — tmux: not found in PATH …`. Gates on `672f535`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 593 tracked files, 119 tarball entries. Mutation probe after commit, test kept: the fix reverted → exit 1, same check red with the new reason text, 113/114; fix restored → exit 0, 114/114. Review: the orchestrator read the diff in full against the task's "Work to do" (shared helper, reason wording, real-resolver test, `TMUX_MIN_VERSION` left duplicated) — one screen, no contract change — so no isolated reviewer session was spent on it. Approver: squash of the worker branch onto `main` at `57a9f11`; the only merge conflict was the `[Unreleased]` › Fixed list of `CHANGELOG.md`, resolved as the union of entries; `backslop lint` on the integrated tree exit 0.

**Documentation in the same pass.** `CHANGELOG.md` `[Unreleased]` › Fixed. The task names no reference section and none describes tmux discovery, so the reference is unchanged.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:drivers`, strategy `balance`, bus task `pb-run-0909-t20260909-132344`). Review: orchestrator.
