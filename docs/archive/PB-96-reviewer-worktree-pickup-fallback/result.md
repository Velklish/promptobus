# PB-96 · Result

**Closed 2026-09-10.** Completed. Directory pickup in `review` recognises the reviewer's own record: `claimingTasks` matches an active task by a worker's `worktreeOwner` as before, and also by a participant with `role === 'reviewer'` whose `metadata.repoAbs` canonicalises to the target directory. A repeat solo review on the reviewer's own directory therefore finds its task without `--task`, and a directory claimed by a worker worktree in one task and a reviewer record in another is refused with both tasks named. `--task` stays the explicit alternative when the target is ambiguous; the solo-review skill no longer says it is required on the repeat. The `repo` field is still not consulted: it sits on every reviewer record of a repository ever reviewed, while `repoAbs` is the exact directory.

**Verification.** Worker commit `e1fc536` (the fixture tightened after a first commit `5a2a4f5`, test-only), worktree of `worker:launch`, squashed. Reproducer red before the repair: `node test/promptobus-review.test.mjs` exit 1, 218/220 — "a reviewer's own directory without --task picks up its active task" (a new plan was opened instead of the reviewer-only task) and "a worker and reviewer claim on one directory — a refusal names both tasks" (empty message); after the repair 220/220, with the older no-task fixtures moved to a fresh clone because the exact `repoAbs` claim now correctly reaches a previously reviewed directory. Gates on `e1fc536`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 707 tracked files, 119 tarball entries. Mutation probe, tests kept: the reviewer fallback reverted → exit 1, 218/220 with the same two assertions; restored → 220/220. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/03-cli.md` § Review (pickup by worker worktree or reviewer `repoAbs`), `skills/solo-review/SKILL.md` step 4, CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:launch`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator diff read; approver Павел Ким's orchestrator session.
