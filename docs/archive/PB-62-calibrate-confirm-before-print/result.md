# PB-62 · Result

**Closed 2026-09-10.** Completed. `models calibrate --write` in text mode now prints the telemetry line and the whole proposal before it asks for agreement, so the person answers to lines they have seen; the non-interactive refusal (`--write` without `--yes` and stdin not a terminal) is raised regardless of whether any rating moved, including for an empty proposal, while a terminal with an empty proposal writes nothing and asks nothing. `--json` stays one document: its non-TTY refusal is still raised before the document is emitted. ADR-005, 02-host and 03-cli state the same contract.

**Verification.** Worker commit `01f3ab7`, worktree of `worker:telemetry`, squashed. Reproducer red before the repair: `node --test test/model-routing-calibrate.test.mjs` exit 1, 37/39 — "the proposal must be printed before confirmation: ask, output" and "Missing expected rejection." After the repair the same command exit 0, 39/39. Gates on `01f3ab7`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 673 tracked files, 119 tarball entries. Mutation probe, test kept: the text proposal block moved back below the confirmation → the TTY test red with "the proposal must be printed before confirmation: ask, output"; restored → 39/39. Open: an interactive terminal run was not performed by hand; the ordering is held by the TTY simulation in the regression test. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** ADR-005 § `models calibrate`, `docs/reference/02-host.md` § The writable layer, `docs/reference/03-cli.md` § Commands, CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:telemetry`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator diff read (no contract or security surface beyond the card); approver Павел Ким's orchestrator session.
