# PB-87.3 · Result

**Closed 2026-09-11.** Completed, by a measurement that needed no paid turn. On the new form (mechanism entries in the isolated home's `config.toml`, no `thread/start` override) `mcpServerStatus/list` answers when asked a few seconds after `thread/start` — it did not in the 2026-09-10 fixture. Two threads differing only in `disabled_tools`: the MCP server was asked `tools/list` in both and returned both tools, while the thread inventory listed `read_tool` where the key was set and `read_tool, write_tool` where it was not — app-server filters above the server. The ceiling is named: no client method returns the `tools` body of the request to the model, so enforcement is measured on the thread inventory, not on the request; the reviewer prompt phrase and 03-cli § Review say exactly that. The two paid turns the owner allowed (2026-09-11) both ended with an empty assistant message (`last_agent_message: null`, no `agentMessage/delta`, 94 of 100 output tokens reasoning) — the first lost to the home being removed before the rollout was read, the second by the model itself for a reason not established; both attempts are recorded in `test/fixtures/codex-app-server/0.146.0/DisabledToolsEnforcement-0.146.0-2026-09-11.json`. Worker of run 0911d (`codex` track), squashed with PB-161.

**Verification.** The fixture plus the checks in `test/promptobus-driver-codex.test.mjs` and the phrase check in `test/promptobus-review.test.mjs`; gates as for PB-161.

**Documentation in the same pass.** `docs/reference/03-cli.md` § Review, the fixture README, CHANGELOG.
