# PB-23 · Result

**Closed 2026-09-06.** Completed. `docs/adr/adr-004-subscription-balance.md` records the owner's decisions of 2026-09-06 as the contract of the subscription-balance series: the availability snapshot carries the tier and every window with `kind`, `lengthSec` and `scope` (account-wide, per model, per Cursor pool); a fifth strategy `balance` picks the harness with the largest underspend of its binding window — a choice layer over the `balanced` scoring, in percentage points, with `balance.band` and `balance.spendUnit` (5 each) and a `balance-fallback` warning when nothing can be paced; quality floors become policy values per role (`qualityFloor: { worker: 3, reviewer: 5 }`, `reviewerQualityFloor` as an alias); the reviewer is routed like a worker with the diversity bonus; overlay `deny` lists merge by union and `allow` by intersection, with selectors `byRole` and `flags`; exactly one overlay layer is `writable` and standalone keeps it at `<promptobusHome>/model-routing.json`; `defaults.strategy` is read when `--strategy` is absent; a `near-limit` signal names the strategy to switch to; the one question the tool cannot answer lives under `account.<harness>.plan` in the user overlay with no writer; catalog ratings come from published results by rank quintiles with effort levels interpolated from the base row; hidden models stay in the snapshot and are never picked; the v1 cache is discarded, not migrated; the snapshot moves to schema 2 and the decision document to 2 (in PB-30), the overlay stays 1. ADR-003 carries a partial-supersession header naming five sections; `docs/README.md`, `docs/ROADMAP.md` (goal 6) and `CHANGELOG.md` follow.

Review (isolated, xhigh): nine findings — one critical (the pace formulas gave shares while every number around them was in percentage points, so `balance` would have degenerated into `balanced`), three major (the decision document's version undecided; the ADR-003 header protecting the floor default it moves; three task texts contradicting the ADR), five minor. All nine closed in `ca168dc`: the unit is stated once (`underspend = (elapsedShare − usedShare) × 100`), the decision document moves to version 2 in PB-30, the ADR-003 header names row 2 as a decision whose default moves, the task texts were aligned on `main` by the approver (6942238, 130d690, becba34), ROADMAP goal 6 speaks of what the spike showed and the adapters can do, the `hidden` sentence of 03-cli is named superseded by PB-28, the closed list of flags is the snapshot schema's enum closed by PB-24, PB-23.1 lost its strikethrough, the glossary `strategy` row is named as moving. Closure checked by the approver on the diff.

**Verification.** On the worker branch at `ca168dc`: `backslop lint` no errors; `npm run audit` clean, 368 tracked files and the tarball; `npm test` 46/46 files at `31559f7` (the fixes touch `docs/` only). On `main` after integration: `backslop lint`, `npm run audit` and `npm test` as recorded in the acceptance commit. No mutation probe: the task adds no test.

**Documentation in the same pass.** The ADR itself; `docs/adr/adr-003-model-routing.md` (supersession header); `docs/README.md` (ADR row); `docs/ROADMAP.md` (goal 6); `CHANGELOG.md` (unreleased, one entry).
