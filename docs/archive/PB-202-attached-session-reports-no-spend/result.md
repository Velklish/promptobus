# PB-202 · Result

**Closed 2026-09-13.** Completed as a decision plus its documentation, not as a parser. The card's premise — that Codex rollouts carry token usage on disk while attached sessions report none — held on both halves, and the measurement corrected an older claim in PB-57.1 that Codex session paths carried no usage fields at all: 35 Codex sessions hold 11 933 `event_msg.token_count.info` records (Σ output 4 556 184), and Claude holds 52 748 `assistant.message.usage` records (Σ output 51 341 563). Neither carries model-active generation time, so both are usage evidence and neither is a throughput observation. The run leader's ruling, written into PB-57.1 and into the routing guide: the Codex rollout is **refused** as a throughput-sidecar source — it is written by the participant outside the bus and reading it would couple the driver to a harness-specific home and layout — and that sub-question is closed as refused rather than left deferred. Attached-session spend is documented as unavailable, not zero (`cost-state` present in 0 of 36 attached sessions in the baseline), which is the constraint PB-205 must respect: a run summary may not promise an attached-session amount. `turns` is now stated to be a bus-message count, not model effort. No parser, no calibration-scale change, no ADR-005 change.

**Verification.** Gates on the merged tree, commit `21b2648`, tracked tree clean: `npm test` code 0, `backslop lint` code 0, `npm run audit` code 0, `npm run pins` code 0 — gates 4, green 4. Target suites on the worker branch: `model-routing-telemetry` 67/67 → 68/68 (the added case is the Codex fixture regression), `model-routing-adapter-codex` 32/32, `model-routing-adapter-claude` 51/51, `promptobus-done` 32/32. Mutation probe after the commit: `npm run probe` broke the first `output_tokens` key in the fixture, the assertion "the Codex rollout fixture keeps usage evidence outside throughput" went red with exit 1, and the restore returned the suite green with exit 0 and the fixture intact. The refusal is behaviourally asserted, not only documented — the test checks `throughputObservationOf(event) === null` on a real rollout record.

**Documentation in the same pass.** `CHANGELOG.md`, `docs/guides/model-routing.md`, `docs/reference/03-cli.md` (§ Participant telemetry, `throughput` and `turns` rows, and a new attached-session paragraph), `README.md`, `README.ru.md`, the `lib/driver-codex.js` `logs` phrase, and the deferred card PB-57.1 — premise corrected, still deferred.

**Left open.** Model-active generation time is absent for all three harnesses, so `calibrate` still proposes no `speed` for any model; that half of PB-57.1 stays deferred with an unchanged return condition. The worker's full-suite run on its branch was red on seven files — the PB-206.2 identity-leakage and PB-206.3 socket-permission classes, both of them sandbox-specific; the merged-tree run above, taken after PB-206.2 landed, is green on all 65 files, so nothing of that redness belongs to this work.
