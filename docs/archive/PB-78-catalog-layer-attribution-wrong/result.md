# PB-78 · Result

**Closed 2026-09-09.** Completed. Every `GateError` on the `loadCatalog` path now carries the layer that raised it — `readLayerFile` (unreadable or invalid JSON), `readLayers` (missing shipped catalog, writable-layer count of the host declaration) and `mergeRouting` (non-object canonical, catalog `schemaVersion`, v1 overlay with scaled keys, unsupported overlay `schemaVersion`) tag the error with `{ kind: 'catalog' | 'host' | 'overlay', id }`. `mergedCatalog` in `lib/models.js` maps that tag straight to `catalog-invalid` or `overlay-invalid` and no longer re-reads the layer files or falls back to blaming the shipped catalog; a host-declaration refusal reads `the host routing declaration`, an untagged refusal stays `overlay-invalid` and the docstring says why (overlays are the files a person edits). The stale `schemaVersion !== 1` literal went with the old branch. A parity regression in `test/model-routing-catalog.test.mjs` runs five scenarios — two writable overlays, zero writable overlays, invalid JSON overlay, overlay `schemaVersion` 99, missing catalog file — through `validate({ host })` and `routingContext(host, …)` and asserts the same code, layer and subject on both sides.

**Verification.** Worker commits `a8b8d35` + `c3a153d` (worktree of `worker:routing`). Reproducer red before the repair: `node --test --test-name-pattern='routed layer failures name the same layer as validate' test/model-routing-catalog.test.mjs`, exit 1, `AssertionError [ERR_ASSERTION]: two writable overlays` (actual `catalog-invalid`, expected `overlay-invalid`). Gates on `c3a153d`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 584 tracked files, 119 tarball entries. Mutation probes after commit, test kept: the layer read in `mergedCatalog` ignoring `routingLayer` → exit 1 (`expected /routing layer "host"/`); the code mapping replaced by the constant `'overlay-invalid'` → exit 1, `AssertionError [ERR_ASSERTION]: missing catalog` (actual `overlay-invalid`, expected `catalog-invalid`); fix restored → exit 0, 1/1. Isolated review (Claude `claude-opus-5 xhigh`, `reviewer:routing`): one major (the parity test did not cover the catalog branch) and three minor (a wrong "contradiction" in the `catalog-invalid` row, `routing layer "host"` for a declaration, the untagged branch's code unexplained), all closed in `c3a153d`; closure checked by the orchestrator on the diff. Approver: squash of the worker branch onto `main` at `21dc5a2`; the only merge conflict was the `[Unreleased]` › Fixed list of `CHANGELOG.md`, resolved as the union of entries; `backslop lint` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/03-cli.md` § Error codes (`catalog-invalid` and `overlay-invalid` rows); `CHANGELOG.md` `[Unreleased]` › Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:routing`, strategy `balance`, bus task `pb-run-0909-t20260909-132344`). Review: isolated Claude reviewer under the same task.
