# PB-60 · Result

**Closed 2026-09-09.** Completed. `effectiveStrategy` in `lib/models.js` no longer drops a recorded strategy default in silence when the routing stack cannot be read: on a `GateError` it warns — naming the layer and its path through the new shared `routingLayerOf(error, host, catalogFile)` helper, which `mergedCatalog` uses for its refusal as well, so the two attributions cannot drift — points at `promptobus models validate`, and only then returns `null` for the legacy path. The wording is neutral about what happens next, because the three callers differ: `spawn` and `review` take the legacy path after the warning, while `models` without `--strategy` prints the same diagnostic and then refuses the same stack with `overlay-invalid`. The docstring records why this is a warning and not a refusal (reading the default and refusing on a broken stack are different facts; the refusal stays with `models` / `models validate`). Regressions in `test/model-routing-command.test.mjs` cover a malformed overlay and a `schemaVersion` 1 overlay with scaled keys while a user-layer default is recorded, for the lift path and for `models`.

**Verification.** Worker commits `3a18794` + `347963b` (worktree of `worker:routing`). Reproducer red before the repair: `node --test --test-name-pattern='a broken overlay warns before an unrouted lift takes the legacy path' test/model-routing-command.test.mjs` exit 1, `AssertionError [ERR_ASSERTION]: The input did not match the regular expression /routing layer "workspace"/` (only the plugin-directory warning was printed). Gates on `347963b`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 592 tracked files, 119 tarball entries; `node --test test/model-routing-command.test.mjs` 34/34. Mutation probe after commit, tests kept: the warning branch reverted to `if (e instanceof GateError) return null;` → exit 1 with the same assertion; fix restored → exit 0, 1/1. Isolated review (Claude `claude-opus-5 xhigh`, `reviewer:routing`): two major (the layer inference duplicated from `mergedCatalog`; "the lift … will take the legacy path" false for the `models` caller) and one minor (no rationale for warn-not-refuse), all closed in `347963b`; closure checked by the orchestrator on the diff. Approver: squash of the worker branch onto `main` at `3ee8b4a`; the only merge conflict was the `[Unreleased]` › Fixed list of `CHANGELOG.md`, resolved as the union of entries; `backslop lint` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/03-cli.md` § `models` (the broken-overlay paragraph: `spawn`/`review` warn then take the legacy path, `models` warns then refuses, `models validate` refuses); `CHANGELOG.md` `[Unreleased]` › Fixed; the `effectiveStrategy` docstring.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:routing`, strategy `balance`, bus task `pb-run-0909-t20260909-132344`). Review: isolated Claude reviewer under the same task.
