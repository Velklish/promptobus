# PB-13 · Result

**Closed 2026-09-05.** Completed. `models/catalog.json` ships with the package (19 tuples: 6 Claude, 6 Cursor, 7 Codex; all `subscription`, all prices `null` — no citable price source; every rating carries `source` and `evidence`; `fable` and `gpt-5.5` inventoried and deliberately unrated). `lib/model-routing/catalog.js`: `loadCatalog({ host, constraints })`, the pure `mergeRouting` over `host.routingPaths().overlays` in the host's order (weight sets and allow/deny lists replaced whole, everything else field by field, `sources` recording which layer last wrote each diagnosed key, `DEFAULTS` for untouched values), `DEFAULT_POLICY` with every ADR-003 number, `STALE_RATING_DAYS = 90` (the worker's number, documented — ADR-003 fixes none). `lib/model-routing/validate.js`: `validate({ host })` and the pure `validateLayers` — hand-written grammar kept in parity with the JSON Schemas by a corpus check (ajv is a devDependency); errors for shape, duplicate ids, unknown harness, effort outside the driver dictionary, weight sums, dangling overlay references, allow-and-deny of one name; warnings `stale-rating`, `priority-duplicate`, `priority-not-canonical`, all `{ code, message, … }`. Live inventory of the three harnesses taken without a participant or a model turn and recorded in PB-15…PB-17. Track `catalog` of the routing run, worker in Claude Code (opus, high, bypassPermissions for the inventory binaries), one isolated review round (8 findings: 7 fixed as written, the Cursor-id finding resolved by evidence — the ids were verbatim, the display names drop the level word — plus a listing fixture and an exact-id pin).

**Verification.** `npm test` exit 0 — 40/40 files; `test/model-routing-catalog.test.mjs` 27/27. `npm run audit` exit 0 — 264 tracked files and a 101-entry tarball, clean. `backslop lint` exit 0. Mutation probes, commit-first, each red then restored: layer order reversed (three checks); `checkCatalogShape` replaced by accept-all and by refuse-all (substitution both directions); a fake effort level in `effortLevelsOf`; a Cursor id re-pointed (the pin); the pin rewritten as a substring match (stays green — proves the exact-id parse is load-bearing); merge attribution dropped; stale warning without `message`; bare `ratings[tuple.id]` against a tuple id `constructor`; a broken overlay read as absent. Gates re-run on the merged `main` tree by the approver.

**Documentation in the same pass.** New `docs/guides/model-routing.md` (overlay example, priority scheme, `validateLayers`), `docs/reference/03-cli.md` `### Catalog and overlays` (after `### Files`, because the contract test slices that document by heading), `docs/README.md` row, `CHANGELOG.md` under `[Unreleased]`, `package.json` `files`. Findings filed: PB-13.1 (Claude rows keyed on aliases), PB-13.2 (no PAYG row ships), PB-13.3 (an overlay cannot clear an inherited allow/deny list — an owner decision on the overlay schema).
