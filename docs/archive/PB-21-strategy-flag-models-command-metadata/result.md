# PB-21 · Result

**Closed 2026-09-05.** Completed. `lib/models.js` (new) is the one routing gate the CLI has: `promptobus models [--strategy] [--role] [--refresh] [--json]`, `models validate`, `models --clear-exhausted <harness>`, and the same gate routes `spawn` and `review` under `--strategy`, `--allow-payg` and `--refresh` — strategy and role validated, the catalog merged, explicit constraints checked against the merged catalog and `host.declaredTools()` (`harness-unknown`, `constraint-unknown`) before the preflight, the preflight over the declared harnesses with `adapterOf`, then `resolve` and `render` or the JSON. Routing sits where `liftHarness` and `resolveEffort` sit — before any disk write, on a fresh lift only; a `--strategy` given with an existing record warns with the harness the record fixed; a re-review does not pay the preflight to be told the flag is ignored. Without `--strategy` the path is today's byte for byte. `--dry-run` on `spawn` and `review` reads the cache (`--refresh --dry-run` probes and writes nothing); `models` has no `--dry-run` — it reads the cache by default and probes only under `--refresh` (owner's decision; the orchestrator resolved PB-19.1 the same way). A cache-only decision is aged from the oldest entry's own `checkedAt`, and a preflight that probed nothing writes nothing (the one library line changed, in `lib/model-routing/preflight.js`); a cold cache renders `never checked`. The decision lives in the participant's `metadata.routing` behind the protocol accessor `routingOf` (no protocol bump); `promptobus status` prints strategy · tuple · score · snapshot age · warnings through the same renderer the lift prints. Nine routing codes plus `role-unknown` / `strategy-unknown` registered in `ERROR_CODES`; `--help` names `models`, `--strategy`, `--allow-payg`. Track `strategy` of the routing run, worker in Claude Code (opus, xhigh, bypassPermissions for the live runs), one isolated review round (6 findings, all fixed) plus the epoch-render edit.

**Verification.** `npm test` exit 0 — 46/46 files; `test/model-routing.test.mjs` 12/12 with the four former `todo` checks live (the two goldens byte for byte, the error-code subset, `--help`); `test/model-routing-command.test.mjs` 21+ checks (no-candidate leaves the store byte-identical with `--new-task`; `constraint-unavailable` told from `candidates-empty` by the constraints themselves; dry-run writes neither cache nor task state; `--refresh --dry-run` probes all three and writes nothing; the four refusals before the preflight with the probe counter at 0; `validate` and `--clear-exhausted` wired; the legacy plan carries no decision; `metadata.routing` round-trips through `status`; a repeat spawn does not re-route; a routed non-dry lift served from cache leaves the cache byte-identical; a mixed snapshot ages from the oldest entry; a re-review leaves the probe counter at 0; the seam `adapterFor` is the default). `npm run audit` exit 0 — 301 tracked files and the tarball. `backslop lint` exit 0. Mutation probes, commit-first, each red: the preflight stamp taken always; `dryRun` not reaching the preflight; constraints checked after the preflight; the routing field dropped from the record; every no-candidate refusal collapsing to `candidates-empty`; `createTask` in front of the routing gate (the probe first exposed a vacuous check, fixed in its own commit); the write guard removed; `Math.min` → `Math.max` on the stamps; `selected` back to exclusion codes; the hoist short-circuited; the epoch rendered as a number. Live on this machine with all three accounts logged in: `models --json` cold (`stale_cache` everywhere, still a pick), `models --refresh` (1.5 s for the three probes, Codex `available` with windows, chosen `codex-luna-medium` 73.10, cache mode 0600), the warm cache aged from the stored facts, `spawn --strategy balanced --dry-run --refresh` with the store and worktrees untouched and no participant lifted, and the refusals one clean line each. Gates re-run on the merged `main` tree by the approver.

**Documentation in the same pass.** `docs/reference/03-cli.md` (the "not yet" wording gone, `--dry-run` scoped to `spawn`/`review`, `[--refresh]` on the two lift synopsis lines — closing a drift the paragraph below had already promised —, the `harness-unknown` / `constraint-unknown` / `candidates-empty` rows, the age rule and the write guard), `docs/reference/04-protocol.md` (`## Participant metadata`), `README.md` and `README.ru.md` (`models`), `lib/driver-claude.js` refusal text (the "once that command exists" clause gone), `CHANGELOG.md` under `[Unreleased]`. Finding filed: PB-21.1 (`limit-hit-at-start` and `model_not_available` are published and nothing raises them).
