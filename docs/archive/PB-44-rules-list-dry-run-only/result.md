# PB-44 · Result

**Closed 2026-09-10.** Completed for this package. A real `spawn` prints the `worker rules:` block — the rule files handed to the participant — beside `sayModule` and `sayMcp`, and a new reviewer lift prints `reviewer rules:` the same way; the dry-run and real-lift lists are the same block, so the orchestrator's mandated check of a participant's first status against its rules no longer needs `--dry-run`. A re-review keeps its old-config behaviour: the reviewer works with the set it was lifted with, and printing a fresh list there would describe a set it does not have. The reference § Spawn and § Review say the lift names the rule files. The card's fork about the consumer's own skill and rules-protocol pages, which cite this print, is the consumer's follow-up: the package restored the print, so those pages become true again once the consumer pins this release.

**Verification.** Worker commit `1342cd1`, worktree of `worker:launch`, squashed. Reproducer red before the repair, after the fixture was made to require a non-empty block on both sides: `node test/promptobus-spawn.test.mjs` exit 1, 121/122 — "dry-run and real spawn print the same worker rules block — dry-run=worker rules:\n<path>/AGENTS.md · real=null"; `node test/promptobus-review.test.mjs` exit 1, 212/213 with the reviewer counterpart; after the repair 122/122 and 213/213. Gates on `1342cd1`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 701 tracked files, 119 tarball entries. Mutation probe, tests kept: the two real-lift blocks removed → spawn 121/122 and review 212/213 with the same assertions; restored → 122/122 and 213/213. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/03-cli.md` § Spawn and § Review, CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:launch`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator, one note on the reproducer (both sides were null, so an equality check would have passed trivially; the fixture now requires a non-empty block naming a rule file); approver Павел Ким's orchestrator session.
