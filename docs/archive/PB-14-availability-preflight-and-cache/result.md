# PB-14 · Result

**Closed 2026-09-05.** Completed. The adapter contract lives in `src/model-routing.ts` (states, the nine reason codes, sources, `ProbeRequest`, `ProbeVerdict`, `ProbeModel`, `ProbeWindow`, `AvailabilityAdapter`), re-exported through `promptobus/driver` and `promptobus`; `Driver` gains an optional `availability`, and `adapterOf(harness)` in `lib/drivers.js` hands a stand-in answering `unknown` / `probe_failed` for a driver without one — the three driver files are untouched and PB-15…PB-17 add one field each. `lib/model-routing/preflight.js` runs every adapter in parallel under one budget (`PREFLIGHT_BUDGET_MS = 15_000`, overridable), routes a late adapter to `probe_timeout`, gates every closed list of a verdict (`state`, `reason`, `source`, the stamp; a non-null reason on `available` is a breach) and returns a snapshot valid against `snapshot.schema.json`. `lib/model-routing/cache.js` writes `routingPaths().cacheFile` atomically with mode 0600, merges entries per harness, projects only the schema's fields (garbled `models`/`windows` elements dropped, never repaired; an unreadable stamp written as the epoch `NEVER_CHECKED`), applies the TTL cascade (exhaustion until `resetAt` or manual clear, transient 5 min, windows 60 s, else 1 h), and honours the run matrix: plain runs probe what is not live, `--refresh` re-probes everything but a sticky exhaustion, `--dry-run` reads the cache only, `--refresh --dry-run` probes and writes nothing. `clearExhausted` and the late-start hook `markExhausted` are library functions; CLI flags are PB-21's. Track `preflight` of the routing run, worker in Claude Code (opus, xhigh), one isolated review round (8 findings, all fixed).

**Verification.** `npm test` exit 0 — 40/40 files, 1316/1316 checks; `test/model-routing-preflight.test.mjs` 20/20 with stub adapters (available and leaky, slow, exhausted with and without reset, unauthenticated, throwing). `npm run audit` exit 0 — 262 tracked files and the tarball, clean. `backslop lint` exit 0. Mutation probes, commit-first, 23 in all, every one red then restored: the budget moved; the mode dropped or 0644 (the check pins the literal, which is why commit `ddd697d` exists); `dryRun` writing; the projection replaced by a spread (the fake token lands on disk); the dry run probing; `--refresh` lifting a sticky exhaustion; `clearExhausted` lifting any; thrown text carried into `message`; the window TTL swapped for the auth TTL; a late adapter reported `probe_failed`; the reason, source and state lists unchecked; a stateful reason of null; an unreadable stamp passing the gate or restamped to now; the diagnosis echoing nothing; garbled model or window repaired instead of dropped; flags not deduplicated; `lengthSec` 0 surviving; an absent harness carrying the run stamp. Gates re-run on the merged `main` tree by the approver.

**Documentation in the same pass.** `docs/reference/03-cli.md`: the § Model routing preamble now names exactly what is missing, and `### Availability: the adapter, the preflight and the cache` (after `### Files`) documents the contract, the run matrix, the TTL cascade, the projection rule and `markExhausted`'s source. `docs/GLOSSARY.md` (availability adapter, preflight, availability cache), `CHANGELOG.md` under `[Unreleased]`, `test/run.mjs` header (pool discussion). Findings filed: PB-14.1 (the reference section is machine-read and does not say so), PB-14.2 (a failed probe tells the person nothing about why — adapters should answer, not throw), PB-14.4 (the Cursor wake test loses its tmux session under a neighbouring run; renumbered by the approver, 14.3 was taken in `main`).
