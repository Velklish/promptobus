# PB-88.2 · Result

**Closed 2026-09-10.** Completed under the owner's decision of 2026-09-10 that the generated schema of the pinned codex-cli is the measurement. `test/fixtures/codex-app-server/0.146.0/` holds the approval request and response schemas and the `ServerRequest` method list that `codex app-server generate-json-schema` writes on codex-cli 0.146.0, with a README naming the command and the date; the stand in `test/harness-codex.mjs` sends the five measured approval requests and validates every request and reply against them with `ajv`. The holder follows the measured shapes: no `item` nesting is read; `pathsOfApproval` contains `cwd` and the `fileChanges` map keys (the `commandActions` walk added on the way was dropped again — neither it nor its v1 twin `parsedCmd` has a write variant, and the sandbox bounds a command's file access); `grantRoot` on `applyPatchApproval` and `item/fileChange/requestApproval` is denied for both roles as a root grant in a named field; `item/fileChange/requestApproval`, which carries no path on the shipped `thread/start` path, is refused with `item/fileChange/requestApproval carries no path to contain`; the reply rows are lawful — `{ decision: { denied: { rejection } } }` for the two legacy methods, `decline` for the two `item/*` approvals, `{ permissions: {}, scope: "turn" }` for permissions — and a server request with no `APPROVE` row is answered with a JSON-RPC error through `approvalReply`, so the literal `{ decision: 'denied' }` is gone from every path. Before this task every deny the holder sent was outside the schema's enums (`denied`, `reject`, `scope: 'once'`). 03-cli § The Codex holder says which shapes, which replies, that the legacy `applyPatchApproval`/`execCommandApproval` pair is deprecated and the `fileChanges` containment applies to it, and that a nullable `cwd` is refused fail-closed; the CHANGELOG line says what was wrong before.

**Verification.** Worker commits `1cc034f` + `d8ce7ea` (review follow-up), worktree of `worker:codex`, squashed. Reproducers red before the repair: `node test/promptobus-driver-codex.test.mjs` exit 1, 130/132 — `applyPatchApproval grantRoot is denied as a named escalation — {"allow":true}` and `measured fileChange approval without paths is denied because the request has no path — {"allow":false,"why":"action target is unreadable"}`; after the repair 132/132, after the follow-up 133/133 with the table-driven check of every `APPROVE` row against the response schemas and the error reply for the three requests without a row. Gates on `1cc034f`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 724 tracked files, 119 tarball entries. Gates on `d8ce7ea` (the follow-up): the Codex driver file exit 0, 133/133; `npm test` exit 0, 54/54 test files; lint exit 0, 0 errors; `npm run audit` exit 0, 724 tracked files, 119 tarball entries (escalated reruns after sandbox refusals on `dist/` and Unix sockets). Mutation probes, tests kept: the `grantRoot` guard disabled on `f986d72` (the same lines as `1cc034f`) → exit 1, 131/132 with `grantRoot escalation is denied for workers and reviewers` red; restored → green; the follow-up's reply-row gate probed on `d8ce7ea` by writing the invalid `scope: 'once'` into the permissions `no` row → exit 1, 132/133, `every measured approval reply row validates against its response schema` red with AJV's `allowedValues: ['turn', 'session']`; restored → 133/133. Load note: two standalone runs of the Codex driver file after the first probe were red 128/132 on the `long-first` fixture's 3000 ms lift window under a machine load average above 130 — the same red the orchestrator measured on `main` at `b8bd5b4` (09:50 UTC red, 09:51 green) — recorded under PB-159.1. Isolated security review (`reviewer:codex`, Claude `claude-opus-5` xhigh), two rounds: round 1 — four major (the fallback deny literal on the no-row path; no gate on the reply rows themselves; the invalid pre-task replies stated nowhere; 03-cli reading as if the `fileChanges` containment were live on the shipped path) and six minor (the `commandActions`/`parsedCmd` asymmetry; the doc quoting a line the holder no longer emits; a test name that no longer tested "outside cwd"; the permissions `ok` row's scope; network egress in `networkApprovalContext`; a nullable `cwd`), the first eight closed in `d8ce7ea`, the permissions row folded into PB-88.1, the network and non-approval requests filed as PB-88.3 and PB-88.4; round 2 — findings 1–7 and 10 closed in code, tests and documentation, two documentation clauses left (which of 03-cli's behavioural sentences are inferred rather than measured; the `-32601` answer for a request with no approval row, `config/read` included), both applied by the approver at integration; residual noted, not changed: `item/tool/requestUserInput` has no response fixture in the kept bundle, so its row is the one the table gate does not validate. Approver: squash of the worker branch onto `main`; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/03-cli.md` § The Codex holder (the measured shapes, the reply rows, the deprecated legacy pair, the nullable `cwd`); `test/fixtures/codex-app-server/0.146.0/README.md`; `CHANGELOG.md` `[Unreleased]` › Fixed (the PB-88 line reworded, the PB-88.2 line); the comments in `lib/codex-session.js` now say "measured on codex-cli 0.146.0 (generated schema), 2026-09-10".

**Acceptance.** Decision: the owner, Павел Ким, 2026-09-10 (schema as measurement). Implementation: Codex `gpt-5.6-luna` max (`worker:codex`, strategy `balance`, bus task `pb-run-0910-t20260910-085454`). Review: isolated Claude reviewer under the same task, read as a security review, plus the orchestrator's own read of the schema bundle; approver Павел Ким's orchestrator session. Findings: PB-88.3 (network egress asked through a command approval), PB-88.4 (the three non-approval server requests, `account/chatgptAuthTokens/refresh` among them), in triage.
