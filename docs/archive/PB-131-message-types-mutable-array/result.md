# PB-131 · Result

**Closed 2026-09-09.** Completed. `MESSAGE_TYPES` in `src/protocol.ts` is `Object.freeze`d, so it is immutable at runtime and `readonly string[]` in TypeScript; `MESSAGE_TYPES_V1` in `src/v1/model.ts` is that same object without a separate re-annotation. A JavaScript consumer can enumerate or copy the list but can no longer push a foreign type into it and thereby widen what the record validator and the engine gate accept for the rest of the process. A literal `as const` tuple was tried and rejected within the task's scope: it surfaced three `.includes(string)` type errors that would have needed casts or validator edits, so the frozen array keeps the surgical boundary. `test/mcp.test.mjs`'s `enum: [...MESSAGE_TYPES]` still copies into a plain array and type-checks unchanged. The regression in `test/v1-engine.test.mjs` attempts the mutation from JavaScript and proves the push throws, the length stays 7, and an injected type reaches neither the record validator nor the engine.

**Verification.** Worker commit (worktree of `worker:s-types`, `PB-131: freeze message types`). Reproducer red before the repair: `node --test test/v1-engine.test.mjs` exit 1, 118/122 — `MESSAGE_TYPES is mutable at runtime`, `the injected type reached the record validator`, `the engine accepted the injected type`. Gates on the commit: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 610 tracked files, 119 tarball entries; targeted v1-engine 122/122, mcp 35/35, v1-validate 6/6. Mutation probe after commit, test kept: `protocol.ts` and `model.ts` restored from the parent → the regression red again; fix restored → green. Review: the orchestrator read the diff in full against the task's "Work to do" (freeze, direct alias, the mcp test copy) — two source lines, one reference sentence — so no isolated reviewer session was spent on it. Approver: squash of the worker branch onto `main`; the only merge conflict was the `[Unreleased]` list of `CHANGELOG.md`, resolved as the union of entries; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/04-protocol.md` § Message types; `CHANGELOG.md` `[Unreleased]` › Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-sol` max (`worker:s-types`, strategy `quality`, bus task `pb-run-0909-t20260909-132344`). Review: orchestrator.
