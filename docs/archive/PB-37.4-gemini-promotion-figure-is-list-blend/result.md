# PB-37.4 · Result

**Closed 2026-09-09.** Completed. The `quotaCost` citations of `cursor-gemini-38-high` and `cursor-gemini-37-high` in `models/catalog.json` now carry the promotional blend as `figure` — "$2.25 per 1M tokens blended ($0.75 in / $3.75 out under the 50 % promotion)" — while `listFigure` stays the $4.50 list blend, `listBand` 2 and `quotaCost` 1; no band moved and no other row was touched. The `promotion-expired` warning that `models validate` prints for both rows from 2026-09-07 on therefore names the promotional figure band 1 rests on and the list figure it would fall back to, instead of the same $4.50 twice. The observation-date rule and the validate-only status of the warning are PB-37.1's and unchanged.

**Verification.** Worker commit `b87b29b` (worktree of `worker:routing2`, on `main` `b5c2d19`). Reproducer red before the catalog edit: `node --test --test-name-pattern='shipped Gemini promotion warnings distinguish promotional and list figures' test/model-routing-catalog.test.mjs` exit 1, "cursor-gemini-38-high: the promotional figure must name $2.25" (actual: the $4.50 text). After the edit the same command exit 0, 1/1; `node bin/promptobus.js models validate` exit 0 with the two `promotion-expired` warnings reading "$2.25 … list figure $4.50 … gives band 2" plus the two pre-existing `ladder-indistinguishable` warnings. Gates on the completed tree: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 629 tracked files, 119 tarball entries. Mutation probe after the commit, test kept: `cursor-gemini-38-high.figure` reverted to the $4.50 text → the targeted test exit 1 with the same assertion; restored → 1/1. Review: the orchestrator read the diff in full (two `figure` strings, one CHANGELOG bullet, one test reading the shipped catalog at a `now` after 2026-09-06) — no isolated reviewer session was spent on it. Approver: squash of the worker branch onto `main`; no merge conflict; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** CHANGELOG entry under Fixed; no reference or guide text names the two figures.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:routing2`, strategy `balance`, bus task `pb-run-0909-t20260909-132344`). Review: orchestrator.
