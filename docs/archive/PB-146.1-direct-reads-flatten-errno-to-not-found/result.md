# PB-146.1 · Result

**Closed 2026-09-10.** Completed. The direct artifact reads no longer flatten every filesystem failure into absence: `readArtifact` and `readBlob` in `src/v1/artifacts.ts` keep `artifact-not-found` for `ENOENT`, report any other read errno as the new code `artifact-broken` with the errno in `context`, and let a non-errno exception propagate — the blob half was widened in from the review, since the card's Work to do covers every artifact read; `artifact-read` is a new fault point beside `task-read`, fired before both reads (its `file` context says which) and reached through the engine's `faults` hook from `readArtifact` and `readArtifactContent`. 04-protocol § Artifacts states the rule; `ERROR_CODES` gains the code with an `Added` CHANGELOG entry. The 03-cli error-code table row the worker added was removed by the approver at integration: that table is the model-routing table (`strategy-unknown` … `limit-hit-at-start`), the store codes live in 04-protocol, and the brief's instruction to add a row there was the approver's mistake.

**Verification.** Worker commits `79f5583` + `a581f74` (review follow-up), worktree of `worker:store`, squashed. Reproducer red before the repair: `node --test --test-name-pattern="artifact metadata read classifies" test/v1-engine.test.mjs` exit 1, 0/1 — `AssertionError [ERR_ASSERTION]: Expected values to be strictly equal: + undefined - 'artifact-broken'` at `test/v1-engine.test.mjs:1252`; after the repair 1/1, the file 127/127, the model-routing reference checks 3/3; the blob case red on `79f5583` (`undefined` vs `artifact-broken` at `:1265`) and green on `a581f74`. Gates on `79f5583`: `npm test` exit 1, 53/54 test files — the red file `test/model-routing-adapter-cursor.test.mjs` ("cursor status did not answer within the 10000 ms left of the preflight budget", `:746`) under a machine load average of 128–198 from four parallel suites; standalone on the branch exit 0, 46/46, twice (79.9 s each); on `main` at `9e44ef0` standalone exit 0 twice, measured by the approver — recorded as PB-159.1, not attributed to this change; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 712 tracked files, 119 tarball entries (escalated reruns). Mutation probes after each commit, test kept: the metadata fix reverted → exit 1 with the same assertion; the blob classification reverted → exit 1 (`undefined` vs `artifact-broken` at `:1265`); restored → 1/1. Isolated review (`reviewer:store`, Claude `claude-opus-5` xhigh), two rounds: round 1 — two major (the 03-cli row; `artifact-read` missing from the fault-injection hook list) and two minor (`readBlob` still flattening errno — fixed in the follow-up; `listArtifacts` without `faults` — adjacent, left), the first three closed in `a581f74`; round 2 — one major (the CHANGELOG line named metadata only) and three minor (the fault-injection sentence not saying the hook has two sites; no on-disk assertion for the blob; docstrings silent on the new code), all four applied by the approver at integration as wording and one assertion, with `node --test test/v1-engine.test.mjs` rerun on the integrated tree. Approver: squash of the worker branch onto `main`; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/04-protocol.md` § Artifacts; `CHANGELOG.md` `[Unreleased]` › Added; the `FanoutStep` union in `src/v1/messages.ts`.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:store`, strategy `balance`, bus task `pb-run-0910-t20260910-085454`). Review: isolated Claude reviewer (`reviewer:store`, `claude-opus-5` xhigh) under the same task; approver Павел Ким's orchestrator session.
