# PB-70 · Result

**Closed 2026-09-09.** Completed. `test/promptobus-package.test.mjs` now gates the three places a release version is written by hand against each other: the first dated release heading of `CHANGELOG.md` (`## [x.y.z] — YYYY-MM-DD`) must name `package.json`'s version and a date, and when `HEAD` carries an exact git tag that tag must be `v<version>`; an untagged `HEAD` passes. `.github/workflows/ci.yml` now also runs the existing workflow on `v*` tag pushes, so a tagged release is checked by the same gates as `main` — no new release automation, and the `package.json` version itself was not touched. 01-overview names the three places the suite compares.

**Verification.** Worker commit `34c6889` (worktree of `worker:verify`, on `main` `b450410`). Reproducer red before the repair: with the first release heading mutated to `## [9.9.9] — Unreleased`, the package suite exit 1 naming package `0.5.1` against that heading. After the repair 30/30. Gates on `34c6889`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 657 tracked files, 119 tarball entries. Mutation probes after the commit, tests kept: the bad heading → 29/30 with the exact version/date assertion; a throwaway `v9.9.9` tag on `HEAD` → 29/30 with the exact tag assertion; the tag deleted and the tree restored → 30/30 (the repository's tags are unchanged: v0.4.0, v0.5.0, v0.5.1). Review: the orchestrator read the diff in full (two checks, one workflow trigger, one reference sentence, one CHANGELOG bullet) — no isolated reviewer session was spent on it. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** 01-overview.md, CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:verify`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator.
