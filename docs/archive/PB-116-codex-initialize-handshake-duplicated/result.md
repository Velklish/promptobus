# PB-116 · Result

**Closed 2026-09-10.** Completed. The `initialize` handshake of the Codex app-server is built in one place: `codexInitParams(record, env)` in `lib/codex-session.js` assembles `clientInfo` and the shared `capabilities` object; the holder calls it with the session record, and the availability probe in `lib/model-routing/adapter-codex.js` calls it with `record = null`, so the probe's `clientInfo.version` is the environment override or the package's own version from `package.json`, never the literal `'0.0.0'` it used to hard-code. 03-cli § Codex availability says that the handshake is shared between the probe and the holder, unlike the rate-limit requests, which share the reading of the protocol rather than the request.

**Verification.** Worker commit `1eeddfa`, worktree of `worker:codex`, squashed. Reproducer red before the repair: `node test/model-routing-adapter-codex.test.mjs` exit 1, 31/32 — `AssertionError [ERR_ASSERTION]: Expected values to be strictly equal: '0.0.0' !== '0.5.1'` at `test/model-routing-adapter-codex.test.mjs:282` (the stub app-server now captures the probe's `initialize` params); after the repair 32/32, and `test/codex-holder-spawn.test.mjs` 6/6 asserting the holder's own `initialize` request. Gates on `1eeddfa`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 738 tracked files, 119 tarball entries (escalated reruns). Mutation probe after the commit, test kept: the builder made to send `clientInfo.version = '0.0.0'` → exit 1, 31/32 with the same assertion; restored → 32/32. Approver: squash of the worker branch onto `main`; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/03-cli.md` § Codex availability; `CHANGELOG.md` `[Unreleased]` › Fixed.

**Acceptance.** Decision: the owner, Павел Ким, 2026-09-10 (taken into this run after two runs' exclusion). Implementation: Codex `gpt-5.6-luna` max (`worker:codex`, strategy `balance`, bus task `pb-run-0910-t20260910-085454`). Review: orchestrator diff read; approver Павел Ким's orchestrator session.
