# PB-162 · Result

**Closed 2026-09-11.** Completed. The overlay gained `caps.liveParticipants.<harness>` (integer ≥ 0, merged harness by harness, an absent key leaves the harness unbounded, `0` never chooses it). Under `balance` a harness whose live participants of this task reach the cap leaves the pace comparison but keeps its candidates, scores, pace rows and representative (`pace.atCap` on the rows, "· at its live-participant cap" in the `models` pace table); the other four strategies never read the cap. The warning `live-participant-cap` is raised only when the cap moved the pick (the pick is computed twice, with and without the capped representatives) and names the harness actually held out — the best-paced held-out representative leads the line, the others follow it; when every paced harness is capped the resolver falls back to the full set with the same code. `models validate` rejects malformed caps and a harness the CLI does not drive. The count is one task's (`liveTuples`: a record not dismissed and carrying a model), which the schema, the reference, the guide and the policy defaults now say; `penalties.liveParticipantCap` (score points) and `caps.liveParticipants` (participants) are told apart where the names sit together. Worker of run 0911d (`routing` track), branch `worktree-promptobus-0911d-routing-t20260911-164310`, squashed commits 363ab64 and df4b5f6 (plus the merge of main); the isolated reviewer (`quality`) raised three notes — the warning named the wrong harness and printed `cap of null` when removing a capped representative widened the band; "account" instead of "this task"; the name collision with `penalties.liveParticipantCap` — all closed in one commit, closure checked by the approver on the diff.

**Verification.** Worker on df4b5f6: `npm test` exit 0, 55/55 test files, 1724 checks; `npx github:Velklish/backslop#v0.4.0 lint` 0; `npm run audit` 0 (752 tracked files). `model-routing-resolver` 91 → 102 checks, `model-routing-catalog` 60 → 62. Mutation probes after the commits: the cap stops filtering (95/100), `>=` → `>` (94/100), the warning always raised (98/100), validate silent on an unknown harness (61/62), `lead = uncapped` (100/102), `heldOut` read off the pick (100/102), the "also at the ceiling" list dropped (101/102) — all red, all reverted. Approver after the squash: lint, test and audit — codes and numbers in the acceptance commit message.

**Documentation in the same pass.** `docs/reference/03-cli.md` (warning codes, catalog and overlays, `balance`), `docs/guides/model-routing.md`, `schemas/model-routing/overlay.schema.json` and `decision.schema.json`, CHANGELOG.
