# PB-43 · Result

**Closed 2026-09-09.** Completed, on the owner's decision of 2026-09-09: under `balance` every eligible `(harness, pool)` group contributes its own representative — the group's best tuple by the role's ordering — and every representative enters the existing leader band and `balanced`-score tie-break; a pool-less binding window is the account-wide group. `lib/model-routing/resolver.js` groups eligible rows by harness and pool and marks one representative per group; the near-limit warning stays one per harness, measured on its best-scored representative, which the reviewer proved keeps the previous behaviour. `lib/model-routing/render.js` prints one pace row per group, labels pool-scoped rows, and omits a no-window tuple's `no-pace` note when another pool of the same harness is paced (one note remains for a harness with no paced window at all). ADR-004 § The balance strategy steps 1–2 read `(harness, pool)` group with a dated amendment naming PB-43; 03-cli § Model routing describes the per-group representative, the pace table and the near-limit caveat. Cursor's Auto pool, which the best-scored api-pool tuple used to hide, now enters the pace comparison.

**Verification.** Worker commits `9b4c58a` + `af854fb` (worktree of `worker:routing`). Reproducer red before the repair: `node --test --test-name-pattern='balance compares each eligible Cursor pool' test/model-routing-resolver.test.mjs` exit 1, `AssertionError [ERR_ASSERTION]: each eligible Cursor pool must contribute its own representative` (actual `['cursor-api']`, expected `['cursor-api', 'cursor-composer']`). Gates on `af854fb`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 595 tracked files, 119 tarball entries; targeted 86/86. Mutation probes after commit, tests kept: the per-harness grouping restored → exit 1 with the same assertion; the render grouping fix removed → exit 1 (`2 !== 1`, a contradictory `no-pace` row printed); fix restored → exit 0. Isolated review (Claude `claude-opus-5 xhigh`, `reviewer:routing`): no logic defect, four minor (a `no-pace` row beside a paced pool; the near-limit row of the reference; the ADR note quoting a word the rewritten step no longer had; an unobserved sort), all closed in `af854fb`; closure checked by the orchestrator on the diff. Approver: squash of the worker branch onto `main`; the only merge conflict was the `[Unreleased]` list of `CHANGELOG.md`, resolved as the union of entries; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/adr/adr-004-subscription-balance.md` § The balance strategy (steps 1–2, dated amendment); `docs/reference/03-cli.md` § Model routing (`representative`, the pick, the pace table, the `near-limit` row); `CHANGELOG.md` `[Unreleased]` › Fixed.

**Acceptance.** Policy: owner's decision 2026-09-09 (one representative per pool). Implementation: Codex `gpt-5.6-luna` max (`worker:routing`, strategy `balance`, bus task `pb-run-0909-t20260909-132344`). Review: isolated Claude reviewer under the same task.
