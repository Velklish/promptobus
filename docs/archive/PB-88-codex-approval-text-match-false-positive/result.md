# PB-88 · Result

**Closed 2026-09-09.** Completed. The detached Codex holder's approval logic no longer matches text: `looksLikeConfigRead(method)` recognises `config/read` by method only, and `asksForEscalatedPermission(params, record)` reads six named slots — `sandbox`, `approvalPolicy`, `permissions` at the top level and under `params.item` (the nesting this file already assumes for `item/*` approvals) — each paired with its own vocabulary: `danger-full-access` for `sandbox` and `permissions`, the `dangerously-bypass*` CLI spellings for `permissions`, `never` and `on-failure` for `approvalPolicy`; keys and values of a permissions map are scanned, comparison is case-insensitive, and a value that merely echoes the mode the session already runs under is not an escalation, except `danger-full-access`, which is always denied. A diff body, a command line or a `reason` can no longer trigger a config or privilege denial, and the load-bearing invariant of the allow half is stated beside the slot list: adding `params` or `params.item` wholesale would reopen blob matching. Path containment (`resolveTarget`/`insideRoots`) is unchanged. `currentTime/read` has no static `APPROVE` row any more: `decideApproval` recognises it before the table and `onServerRequest` answers each request with a fresh timestamp — which is PB-150's repair, closed through this task. 03-cli § The Codex holder now documents what is denied and by which fields, the echo rule and the current-time answer.

**What is assumed.** The Codex config vocabulary is the documented enum for codex-cli 0.146.0; the nested `item` shape, the string-or-map form of `permissions` and whether app-server sends `approvalPolicy` in a server-to-client approval at all are assumed from this file's own convention, unmeasured — the code says so in a comment, and PB-88.2 records the measurement. The containment gap for a worker's `item/permissions/requestApproval` is PB-88.1.

**Verification.** Worker commits `5ebfcd9` (repair), `845537c`, `76952c8`, `17baae7` (review follow-ups), worktree of `worker:codex`, squashed. Reproducer red before the repair: `node test/promptobus-driver-codex.test.mjs` exit 1, 91/94 — an escalation flag inside an in-cwd patch diff, `config/read` text in an in-cwd command, and `workspace-write`+`outside` in unrelated fields were all denied. The review's deny cases were red on the first repair (95/100: nested and item-nested permissions, map key, `approvalPolicy: never`, mixed case) and the current-time restoration was red without its branches (104/105, two "unknown deny" holder lines and two warden lines). Final suite 105/105; elicitation suite 5/5. Gates on `17baae7`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 643 tracked files, 119 tarball entries. Mutation probes, tests kept: the `asksForEscalatedPermission` call removed → 94/104 with every deny check red; the current-time branches removed → 104/105 with the holder check red; restored → green. Review: isolated reviewer (`reviewer:codex`), read as a security review, two rounds — round 1 MAJOR: the deny half read top-level fields only, values only, without `approvalPolicy` vocabulary, case-sensitively, with one deny test and a probe that proved only the allow half; round 2 MINOR: field-agnostic matching would deny a restrictive `never` inside a permissions map, the "measured" claim overstated, the array branch untested, the echo hazard — all closed; the orchestrator caught and reversed an over-deletion of `currentTime/read` handling in the third commit before it shipped. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** 03-cli.md § The Codex holder, CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:codex`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: Claude `claude-opus-5` xhigh (`reviewer:codex`, strategy `quality`), two rounds; the orchestrator read the last two follow-ups.
