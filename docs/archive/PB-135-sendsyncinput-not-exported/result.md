# PB-135 · Result

**Closed 2026-09-09.** Completed. `SendSyncInput`, the parameter type of the public `Engine.sendSync`, is re-exported from `src/v1/index.ts` beside `SendInput`, and the package's `.` entry point carries it through `src/index.ts`'s star export, so a TypeScript consumer can name the synchronous send contract without a deep import or a copied shape. `src/v1/engine.ts` needed no change — its declaration was already `export interface`. The regression in `test/v1-engine.test.mjs` is a real consumer: a temporary installed-package-shaped project imports `SendSyncInput` from `promptobus` and is compiled with the repository's TypeScript compiler, per the task's binding "Next step" (no generic "every engine type must be public" rule was imposed).

**Verification.** Worker commit `39a86d0` (worktree of `worker:s-types`). Reproducer red before the repair: `node --test test/v1-engine.test.mjs` exit 1, 122/123 — `consumer compile failed: error TS2724: '"promptobus"' has no exported member named 'SendSyncInput'`. Gates on the commit's tree: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 612 tracked files, 119 tarball entries. Mutation probe after commit, test kept: the re-export removed → the consumer compile red again (TS2724); restored → 1/1. Review: the orchestrator read the diff in full against the task's "Work to do" — one export line, one reference paragraph — so no isolated reviewer session was spent on it. Approver: squash of the worker branch onto `main`; the only merge conflict was the `[Unreleased]` list of `CHANGELOG.md`, resolved as the union of entries; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/01-overview.md` § Entry points; `CHANGELOG.md` `[Unreleased]` › Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-sol` max (`worker:s-types`, strategy `quality`, bus task `pb-run-0909-t20260909-132344`). Review: orchestrator. Last task of track S-types in the 2026-09-09 run.
