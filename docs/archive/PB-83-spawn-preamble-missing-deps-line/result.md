# PB-83 · Result

**Closed 2026-09-10.** Completed. The dependency install of a fresh worktree now runs between the repository generator and the launch files, and its outcome reaches the participant's own preamble through `worktreeDepsLine(state)` beside the generator line: installed (already ran, need not be repeated), refused (the reason, the command to run by hand, and a request to say so in the first status), no lock (nothing to install), and the repeat spawn into a surviving worktree (not re-run or checked, look before relying on it). The plan carries the forecast for `--dry-run` and the real spawn rebuilds once with both outcomes, so the launch files are still written exactly once (PB-8). The trade-off is written down where the ordering is documented: Ctrl+C during `npm ci` can leave a recorded worktree without launch files until a repeat spawn rewrites them. `lib/worktree.js` needed no change.

**Verification.** Worker commit `a413851`, worktree of `worker:launch`, squashed. Reproducer red before the repair: `node test/promptobus-spawn.test.mjs` exit 1, 114/121 — seven new assertions on the preamble (fresh install named in the plan, a successful install named and not repeated, the repeat warning kept and npm not called again, `--dry-run` printing the intent without calling npm, no lock → nothing to install, a refused install naming reason, command and first-status instruction); after the repair 121/121. Gates on `a413851`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 682 tracked files, 119 tarball entries. Mutation probe, tests kept: the `worktreeDepsLine` interpolation removed → exit 1, 114/121, twice, first failure "the plan preamble names the dependency install that a fresh spawn will run"; restored → 121/121. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/03-cli.md` § Spawn (generator → dependency install → launch files, the preamble lines, the Ctrl+C trade-off), `skills/orchestrate/SKILL.md` (what the preamble now says about dependencies), CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:launch`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator diff read before the commit (ordering and the four outcomes against the card); approver Павел Ким's orchestrator session.
