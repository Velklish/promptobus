# PB-161 · Result

**Closed 2026-09-11.** Completed. A Codex participant now runs in its own `CODEX_HOME` under `$TMPDIR/promptobus-codex-homes/<task>-<address>-<hash>` (mode 0700, deterministic so a dry run names the path a real lift uses): a copy of the owner's `auth.json` (0600), a `config.toml` (0600) carrying only the mechanism's `[mcp_servers]` entries, and for a worker `[projects."<realpath of the worktree>"] trust_level = "trusted"`. The owner's `~/.codex` is read once for the credentials file and never written by the participant path (the availability probe still opens the owner's home, and the reference says so). The `thread/start` override of `config.mcp_servers` is gone — the isolated home has nothing to merge with — and `model_reasoning_effort` stays on the turn. A worker gets `<workspace root>/.codex/skills` copied into its worktree under `.codex/.gitignore`; a reviewer gets neither the copy nor trust (its cwd is the tree under review). The home does not outlive the participant: `dropSession` removes the home the record names, `done` calls the driver's optional `sweepParticipant` for a participant whose session died without a stop, any Codex lift or stop sweeps homes no record names, and the home is built after `writeSession` (a failed lift removes the record). The home path is refused unless root and home are plain directories owned by this uid (a symlink in a shared `$TMPDIR` is refused). ADR-007 records the decision and supersedes the consumer's rejection of an isolated home. Worker of run 0911d (`codex` track), branch `worktree-promptobus-0911d-codex-t20260911-164310`, squashed commits f1433a8, b799b9e, 7b976d6 (plus two merges of main); the isolated reviewer (`quality`) raised six notes — the home outliving a dead holder (major), the dry run not printing the home and describing `CODEX_HOME` as merely dropped, an unproven "no marketplace refresh" clause, a predictable path plus `mkdir -p` accepting a symlink, a tautological check — and a NUL byte in the driver source (a raw separator in a hash input, replaced by an escape without changing any path); all closed in one commit, closure checked by the approver on the diff.

Live measurements on codex-cli 0.146.0 without a paid turn: `codex login status` in the built home reports logged in (an empty home does not); `initialize` + `thread/start` brings the mechanism's server from the home's `config.toml` to `ready`; trust works only with the resolved path (an unresolved `/var/…` key is ignored with the "until the project is trusted" warning); the owner's config keeps its 89 sections and 20 `[projects]` records, no `sessions/` appears. Findings: PB-161.1 (`spawn` warns about a tracked `.cursor` path it overwrites and is silent about `.codex`), PB-161.2 (a Codex reviewer has no directory of its own — no skills, no project trust).

**Verification.** Worker on 7b976d6: `npm test` exit 0, 55/55 files; lint 0; `npm run audit` 0 (758 tracked files). `promptobus-driver-codex` 145 → 178 checks. Mutation probes after the commits: `realpathSync` → `path.resolve` in the trust key (found a hole, closed: 168/169), `stop` not removing the home (168/169), the network refusal removed (166/169), the `thread/start` override back (168/169), `auth.json` at 0644 (168/169), skills copied to a reviewer (168/169); after the review: `sweepParticipant` removed from `done` (177/178), `dropSession` not removing the home (177/178), orphan sweep disabled (177/178), `ownDirectory` removed (177/178). A live stand probe kills the holder with SIGKILL and runs `done`: the home is gone. Approver after the squash: lint, audit and test — codes and numbers in the acceptance commit message.

**Documentation in the same pass.** `docs/adr/adr-007-codex-participant-isolated-home.md` and its row in `docs/README.md`; `docs/reference/03-cli.md` (§ Spawn, § Review, § The Codex holder); CHANGELOG.
