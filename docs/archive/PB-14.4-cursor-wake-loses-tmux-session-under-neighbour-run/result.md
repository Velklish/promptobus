# PB-14.4 · Result

**Closed 2026-09-05.** Completed. The `pgrep` member reproduced on the first attempt, symmetrically in two concurrent full runs (A counted B's app-server, B counted A's holder and app-server). Every machine-wide read the suite makes is now scoped to the run that makes it: the Codex holder check's two patterns are qualified by paths this run alone owns (the holder's session file under the stand's state home, the stub app-server's own `bin` directory) with a sentinel that keeps the sandbox path in each; the Cursor driver test was audited (already scoped by the session mark and the pane pid); the Cursor wake test checks that the `tmux` it resolved is the stand's stub and that its session state sits in its own stand home. The tmux member did not reproduce in six concurrent runs, and the mechanism says why: the stub `tmux` keys sessions by the stand home, so the machine-wide server name never shared state — the one route to the real `cursor-agent` server was a suite process whose PATH lost the stub, which PB-2 closes. Production `cursor-agent` and `TMUX_TMPDIR = '/tmp'` untouched. `test/run.mjs` carries the register of machine-wide reads and the rule for a new one. Merged findings PB-15.3 and PB-17.1 (the same `pgrep` check hit by the Claude and Cursor tracks). Track `robust`, worker in Claude Code (opus, high, bypassPermissions), one isolated review round (no finding on this task).

**Verification.** Three pairs of concurrent full runs, six runs, all 45/45 exit 0 (13:42–13:52 UTC), with twelve leftover holder/app-server processes from a 2026-09-04 run alive throughout so the `before*` filters were exercised against real leftovers (killed afterwards, verified gone); `tmux -L cursor-agent list-sessions` → no server after the runs; no new holder or app-server process left by nine full runs. Mutation probes, commit-first: `HOLD_PATTERN` unscoped → the sentinel red (56/57); the wake test's tmux check pointed at `/usr/local/bin` → red (17/18). Gates re-run on the merged `main` tree by the approver.

**Documentation in the same pass.** `test/run.mjs` register, `docs/guides/contributing.md` (machine-wide reads), `CHANGELOG.md` under `[Unreleased]`.
