# PB-104 · Result

**Closed 2026-09-10.** Completed for the documentation the card names. The CLI reference no longer says that `models` shows hidden inventory rows or that `models validate` can call a row hidden: a row the harness lists and declines to offer is kept in the availability snapshot with `hidden: true` so the cache matches what the harness lists, while the resolver's inventory, the `runtime` list and everything `models` prints are the rows without the mark; the `models validate` clause drops the claim that hiding was ever partly fixed there. The sentence that already matched the code is untouched. The ADR-004 fork the card asked to record rather than resolve — whether a person should see the withheld rows and the exclusion detail should say "hidden" — is PB-104.1 for the owner.

**Verification.** Worker commit `18ff220`, worktree of `worker:docs`, squashed. Evidence before the change: the resolver suite `node --test test/model-routing-resolver.test.mjs` exit 0, 91/91 (the card cited 85 at an older commit), and a probe on the balance fixture for every strategy — `runtime` empty for the hidden row, no hidden text in the output, the rated-row detail naming the account rather than hiding, and `availabilityOf` carrying no model list; a reference-contract check exit 1, "hidden rows must be documented as retained in the availability snapshot", before the edit and exit 0 with four assertions after it. Gates on `18ff220`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 697 tracked files, 119 tarball entries (escalated reruns). Mutation probe: the two documentation edits reverted → the reference-contract check exit 1 with the same assertion; restored → exit 0. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** This task is documentation: `docs/reference/03-cli.md` § Model routing (two sentences), CHANGELOG entry under Changed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:docs`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator diff read; approver Павел Ким's orchestrator session.
