# PB-114 · Result

**Closed 2026-09-09.** Completed. The live-run sweep gaps are closed in the scripts that own them: `scripts/live-codex.mjs` and `scripts/live-e2e.mjs` now sweep previous-run sandboxes under their own prefixes with `sweepPreviousRuns` (keeping the current sandbox), `scripts/live-mixed.mjs` judges leftover run directories only against those born after the current run started (birth time, falling back to mtime — the live-canary pattern) and, like `scripts/live-cursor.mjs`, passes a `refused` list to the log sweep and prints the refused entries ("sweep refused (busy or foreign permissions): …") beside the summary. The suite's own sweep list keeps the `promptobus-live-*` prefixes excluded, and `test/tmpdir-sweep.mjs` now says why: cleanup of a live run belongs to that script, and the suite must never sweep a live participant's directory — this run's included.

**Verification.** Worker commit `ed65e5d` (worktree of `worker:verify`, on `main` `e9ea95c`). Reproducer red before the repair: `node test/tmpdir-sweep.test.mjs` exit 1, 20/26 — "live-mixed ignores run directories older than this run", "live-codex sweeps old sandbox directories with its own prefix", "live-e2e sweeps old sandbox directories with its own prefix", "live-mixed reports refused log sweeps", "live-cursor reports refused log sweeps"; the checks read the scripts as source and exercise the sweep helper, without importing a live script. After the repair 26/26; `node --check` on the four scripts and the two suite files exit 0. Gates on `ed65e5d`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 643 tracked files, 119 tarball entries. Mutation probe after the commit, tests kept: the production blocks removed → exit 1, 21/26 with the same five checks; restored → 26/26. Review: the orchestrator read the diff in full — no isolated reviewer session was spent on it. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Not run.** The five live scripts start real harness sessions and were not executed; the manual live check of stale-directory and refused-cleanup output remains for the owner.

**Documentation in the same pass.** Comments in the four scripts and in `test/tmpdir-sweep.mjs`; CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:verify`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator.
