# PB-151 · Result

**Closed 2026-09-09.** Completed. `injectText` in `lib/cursor-persist.js` writes the wake buffer with mode `0600` and `chmod`s it right after the write, so a `.buf` left on disk by an earlier release (or by a kill between the write and the cleanup) is repaired instead of inheriting its old mode; the buffer is removed in `finally` on every exit after its creation — `load-buffer`, `paste-buffer`, `send-keys` and the post-Enter wait — not only after a successful paste. The regression fixture in `test/promptobus-driver-cursor.test.mjs` drives deliveries under umask `077` and `000`, a pre-existing `0644` buffer, and each of the four post-creation exits; the mode probe is reset per delivery and reads as `null` when absent; the stub directory is prepended to the sealed `PATH`.

**Verification.** Worker commits `d6b9ced` + `18aee77` (worktree of `worker:drivers`). Reproducer red before the repair: `node test/promptobus-driver-cursor.test.mjs` 109/111, exit 1 (permissive umask observed mode `0666`; `load-buffer` failure left the `.buf`). Gates on `18aee77`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 583 tracked files, 119 tarball entries. Mutation probes after commit, tests kept: pre-fix write/cleanup placement restored → exit 1 with `wake buffer is 0600 under restrictive and permissive umasks` (mode 438) and `wake buffer is removed on every post-creation delivery exit` (`load-buffer` → `gone: false`); `chmodSync` removed → exit 1, `a pre-existing wake buffer is reset to 0600 before load-buffer` (mode 420); fix restored → exit 0, 112/112. Isolated review (Claude `claude-opus-5 xhigh`, `reviewer:drivers`): one major (`writeFileSync` `mode` does not touch an existing file) and two minor (mode probe not reset; `PATH` replaced the sealed one), all closed in `18aee77`; closure checked by the orchestrator on the diff. Approver: squash of the worker branch onto `main` at `050be4d`; the only merge conflict was the `[Unreleased]` › Fixed list of `CHANGELOG.md`, resolved as the union of entries; `backslop lint` on the integrated tree exit 0.

**Documentation in the same pass.** `CHANGELOG.md` `[Unreleased]` › Fixed. No `docs/reference/` section names the `.buf` file or session file modes (checked by the reviewer: `.buf`, `injectText`, `inject lock`, `sessions/`, `0600`), so the reference is unchanged.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:drivers`, strategy `balance`, bus task `pb-run-0909-t20260909-132344`). Review: isolated Claude reviewer under the same task.
