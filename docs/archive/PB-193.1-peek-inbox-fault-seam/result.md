# PB-193.1 · Result

**Closed 2026-09-17, completed.** `peekInbox` now takes its record reads through the same `FaultHook` seam `readInbox` and `glanceInbox` take — `fault('inbox-read', { task, participant, name, mode: 'peek' })`, immediately before `readRecord` — and `engine.peek` hands it the engine's hook, one line in `src/v1/engine.ts`. The two failure cases in `test/store.test.mjs` are errno injections rather than disk arrangements: the hook is armed per case by name and code, because it sees every step of every operation in that test and an unconditional throw would break the sends that set the mailbox up. Both cases now run on every platform and under every uid — the `process.platform !== 'win32' && process.getuid?.() !== 0` guard is gone, and with it the `chmodSync` import and the `chmod 000` file the old EACCES case needed.

**One verdict name is retired, and its successor holds the same three things.** `peek: a ref that cannot be opened is named in broken, not counted as absent` is replaced by `peek: an injected EACCES is named in broken, not counted as absent`, which asserts the same code `EACCES`, the same `attic: null`, and the same ref still sitting in the inbox — and runs where the old one was skipped. It is the only base verdict name this piece removes; the removal and its reason are in the worker's handover record.

**The dangling symlink stays, deliberately.** `peek: a ref that vanished between the listing and the read is a silent skip` still arranges its failure on the real disk and never touches the seam. The asymmetry is the point: an injection proves the classification, and only the disk proves the injection describes something the filesystem actually does.

**Verification.** Mutation probe through `npm run probe`, made after the commit, restored from the script's own pre-mutation snapshot, with `git status --porcelain` empty before and after: `npm run probe -- src/v1/messages.ts --mutate "s/      fault\('inbox-read', \{ task, participant, name, mode: 'peek' \}\);\n//" --run "sh <npm run build, then node --test test/store.test.mjs>"` — mutated exit 1, restored exit 0. The rebuild is inside `--run` on purpose: the seam is TypeScript and the suite reads the build, so a probe without it would have measured the previous compile. Counts: mutated `tests 35, pass 32, fail 3` — **32 passed of 35, 3 reddened, 0 unreached**; restored 35 of 35, exit 0. Reddened: `peek: an injected ENOENT on one ref is the same silent skip, with no skip of the case`, `peek: an injected EACCES is named in broken, not counted as absent`, and the parent suite `peek, glance and lastSentAt — reads that do not take the mailbox`, which `node:test` marks failed because its subtests failed — named here rather than dropped from the count. The symlink case stayed green under the mutation, which is what the card asked for. Full gates on the worker's branch head `ead1442`: `npx github:Velklish/backslop#v0.8.0 gates` exit 0 — gates 4, green 4 (`npm test` 69 of 69 files, 0 red, 3201 verdict names in 161.5 s; `backslop lint` 0 errors, 1 warning on a triage finding of another card; `npm run audit` 969 tracked text files and 135 packed entries; `npm run pins` 969 of 969 read). Slice gate on this commit: `npm run build` exit 0; `node --test test/comment-length.test.mjs test/comment-links.test.mjs` exit 0, 33 of 33; `backslop lint` exit 0, 3 warnings, all on findings in triage. On the merged tree carrying both tasks of this piece: `npx github:Velklish/backslop#v0.8.0 gates` exit 0 — gates 4, green 4 (`npm test` exit 0, 69 of 69 test files in 164.6 s, 3236 verdict lines, 0 red and 0 skipped; `backslop lint` exit 0, 0 errors and 3 warnings; `npm run audit` exit 0 over 981 tracked text files and 136 packed entries; `npm run pins` exit 0, 981 of 981 read). The approver's gate record carries the machine form.

**Review.** Raised no finding of its own in any of the six rounds the piece went through; the two verdict names of this card that changed between round 1 and round 2 are named in the worker's evidence. The card came out of `PB-193`'s own result.md as an open item, which is why it was a finding and not a new defect.

**On acceptance.** Merged over `PB-226` on `main` as the second of two slices by card. One conflict, in `CHANGELOG.md`: the branch commit carried the round-0 edition of `PB-226`'s entry beside this card's, and the tree already had `PB-226`'s final edition from the first slice — resolved by keeping that final edition and adding this card's entry beside it under `## [Unreleased]`. The auto-merged neighbours were checked rather than trusted: `src/v1/engine.ts` keeps `PB-139`'s `finish` without `meta` and `PB-226`'s two prevalidation refusals, and `src/v1/messages.ts` keeps `PB-139`'s dropped `taskDir` import. `docs/reference/04-protocol.md` § Fault injection carries this card's `mode: "peek"` sentence beside `PB-225`'s § Artifacts paragraph. Against the worker's head `ead1442`, `test/store.test.mjs` is line for line identical.

**What is left open.** The ENOENT injection proves the classification, not the race it stands for: nothing in the suite now arranges a real ref that disappears between the listing and the read for the *peek* path — the symlink witness covers the other half of the same silence. The hook in this test is armed by a mutable pair of variables that every case must reset, which is the same shape the other seam tests use and the same trap: a case that forgets to clear `refusedPeek` poisons the next one, and no assertion would say so.

**Documentation in the same pass.** `docs/reference/04-protocol.md` § Fault injection — `inbox-read` now names `mode: "peek"` beside `"read"` and `"glance"`; `CHANGELOG.md` under `## [Unreleased]`.
