# PB-61 · Result

**Closed 2026-09-09.** Completed. `models()` in `lib/models.js` refuses a flag outside the subcommand that owns it: `--set` and `--clear` without `models strategy`, `--write` and `--yes` without `models calibrate`, each as a `GateError` naming the correct form (`models strategy --set <name>`, `models calibrate --write`), raised before the fall-through so that bare `models` and the other subcommands no longer ignore them in silence. The guard sits inside `models()` beside the existing `--yes`-without-`--write` refusal, so a library caller gets the same protection; `lib/cli.js`'s parser is unchanged, it already forwards the options. 03-cli § Commands states the ownership rule next to the sentence that already stated it for `--yes`/`--write`.

**Verification.** Worker commit `6d19bbc` (worktree of `worker:routing2`). Reproducer red before the repair: `node --test --test-name-pattern='models rejects strategy and calibration flags without their subcommand' test/model-routing-command.test.mjs` exit 1, `Missing expected rejection: {"set":"economy"}` (:365). Gates on `6d19bbc`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 624 tracked files, 119 tarball entries; command suite 35/35; manual `node bin/promptobus.js models --write` exit 1 with the calibrate refusal. Mutation probe after commit, test kept: the `--set` guard disabled → exit 1 with the same assertion; restored → 1/1. Review: the orchestrator read the diff in full against the task's "Work to do" (four guards, four cases, one reference sentence) — so no isolated reviewer session was spent on it. Approver: squash of the worker branch onto `main`; the only merge conflict was the `[Unreleased]` list of `CHANGELOG.md`, resolved as the union of entries; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/03-cli.md` § Commands (`models`); `CHANGELOG.md` `[Unreleased]` › Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:routing2`, strategy `balance`, bus task `pb-run-0909-t20260909-132344`). Review: orchestrator.
