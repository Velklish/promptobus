# PB-80 · Result

**Closed 2026-09-10.** Completed. The CLI's top-level catch decides whether to hide a stack by class, not by name: `lib/cli.js` imports `GateError` and `PromptobusError` from `dist/index.js` and `HostResolveError` from `dist/host.js` (the pattern `lib/harness-home.js`, `lib/models.js`, `lib/liftoff.js` and `lib/host.js` already use) and tests `e instanceof …` for the three; the `constructor.name === 'ResolveError'` comparison, which matched nothing the package can throw or receive through `runPromptobus`, is deleted. An unrelated class merely named `GateError` now prints its stack; the package's own refusals still exit through `fail(e.message)` without one.

**Verification.** Worker commit `06256ff`, worktree of `worker:launch`, squashed. Reproducer red before the repair: `node test/cli.test.mjs` exit 1, 4/5 — "The input did not match the regular expression /GateError: name collision/. Input: '✖ name collision\n'" (the name check swallowed the stack of a local class named `GateError`); after the repair 5/5. Gates on `06256ff`: `npm test` exit 0, 54/54 test files (a first pooled run was 53/54 because the new test's `mkdtemp` prefixes were outside the suite's declared temp prefix — the tmpdir-sweep gate caught it, fixed before the commit); `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 696 tracked files, 119 tarball entries (escalated reruns after the sandbox refused `dist/` writes, keychain access and the nested build). Mutation probe, test kept: the `GateError` `instanceof` line returned to the name check → exit 1, 4/5, "/Error: name collision/" against `✖ name collision`; restored → 5/5. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** CHANGELOG entry under Fixed; no reference document describes the catch.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:launch`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator diff read; approver Павел Ким's orchestrator session.
