# PB-75 · Result

**Closed 2026-09-10.** Completed. The publicity audit gained a third check: a Cyrillic scan over the same tracked-files and tarball loops, limited to the runtime paths `bin/`, `lib/`, `src/`, `schemas/`, `templates/` and the generated `dist/` (tarball entries are normalised from `package/`, and `dist/protocol.js` maps to its source exemption). Two narrow exemptions, each with a one-line comment: the transliteration-table block of `src/protocol.ts` and the bilingual address-prefix regex of `templates/bus-hook.mjs`. A hit outside them is pushed into the same failures array, so the audit names the file and exits 1 like every other finding. The ROADMAP evidence line for goal 5, the contributing guide and the reference landing page now say that `npm run audit` in CI enforces the claim; the CI workflow needed no change because PB-94 already runs the audit.

**Verification.** Worker commit `9d688c6`, worktree of `worker:docs`, squashed. Reproducer before the repair: with a temporary Cyrillic comment in `lib/models.js` the old `npm run audit` exited 0, so the focused assertion "publicity audit must reject Cyrillic runtime text outside the allowlist" failed (`actual: 0`, `notStrictEqual`); after the repair the same probe made the audit exit 1 naming `lib/models.js` and `package/lib/models.js`; the probe was removed. Gates on `9d688c6`: `npm test` exit 0, 54 test files (49 pooled, 5 serial); `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 674 tracked files, 119 tarball entries. Mutation probe after the commit: both `scanCyrillic` call sites removed with the probe input present → the assertion exit 1 with `audit_status=0`; restored → the assertion exit 0 while the inner audit exited 1 naming both paths; probe input removed, clean audit exit 0 again. The card's verification is this manual probe, not a committed fixture: a permanent regression test would need a tracked Cyrillic file in a runtime path, which the gate itself forbids. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/ROADMAP.md` goal 5 evidence line, `docs/guides/contributing.md` (English runtime scope enforced by the audit), `docs/reference/README.md` (pointer to the gates), CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:docs`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator diff read (exemption regexes bounded to the two functional blocks; tarball normalisation covers `package/dist/`); approver Павел Ким's orchestrator session.
