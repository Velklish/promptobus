# PB-154 · Result

**Closed 2026-09-09.** Completed. The Cursor install validates the hook event map it is about to write, not only the events it generates: `assertCursorHookEvents` in `lib/install.js` now runs on the merged map returned by `mergeHookEvents`, so an unknown event name already present in `.cursor/hooks.json` (a typo, a future Cursor event this release does not know) refuses the install before any write — the existing file keeps its bytes and neither `promptobus.json` nor the manifest is partially written. The refusal names the unknown event and the known list. guides/install.md states the merged-map rule.

**Verification.** Worker commit `1de5f61` (worktree of `worker:hooks`, on `main` `6250572`). Reproducer red before the repair: `node --test --test-name-pattern='cursor install rejects unknown event names already present' test/install.test.mjs` exit 1 — the install printed `configured` and threw nothing ("Missing expected exception" at :423). After the repair 1/1; install suite 19/19. Gates on `1de5f61`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 651 tracked files, 119 tarball entries. Mutation probes after the commit, tests kept: the gate moved back to `ourEvents` → exit 1 with the same missing-exception assertion; the gate fed an invented event → the valid-install test exit 1 with `install: unknown Cursor hook event "invented" — known: sessionStart, beforeSubmitPrompt, stop, sessionEnd, afterFileEdit`; restored → 2/2. Review: the orchestrator read the diff in full (one argument change, one guide sentence, one CHANGELOG bullet, one test) — no isolated reviewer session was spent on it. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** guides/install.md, CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:hooks`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator.
