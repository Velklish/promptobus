# PB-132 · Result

**Closed 2026-09-09.** Completed. The task-id grammar has one home: `src/protocol.ts` declares the bounded `TASK_ID_RE` (`/^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$/`) and `src/v1/model.ts` imports and re-exports that same object, so the CLI-facing gate, `layout`, `validate`, `migrate`, the legacy store and the sidecar read one regex and an id the store would later refuse as `task-not-found` is refused at the gate. The dependency direction is the reverse of the task's suggestion, approved by the orchestrator: `model.ts` already imported from `protocol.ts`, so the store keeps depending on the protocol dictionary and no cycle appears. The store bound (128) won because the v1 paths and both shipped schemas already enforced it. Eight exact-bound fixtures pin the schemas against the hand-written grammar: task id 128/129, participant id 64/65, title 512/513, artifact filename 255/256. A data loss the bounded regex would have introduced was found by the worker and closed in the same pass: a non-empty entry under the former `tasks/` whose name is outside the grammar is refused by `preflight()` before any mutation — naming the entry, the grammar and the hand route — instead of being skipped by the migration filter and deleted with the former root; empty invalid directories stay ignored, and the check runs before the resumed-cleanup branch because that branch removes the former root whole.

**Verification.** Worker commits `a673745` + `5d8aacb` (worktree of `worker:s-types`). Reproducers red before the repair: `node test/v1-validate.test.mjs` exit 1, 5/6 (`the 129-character task id passed the CLI-facing gate`); `node test/promptobus-migration.test.mjs` exit 1, 79/82 (`no refusal`, `the former store was deleted`); the review round's empty-directory case red on `a673745` (82/83). Gates on `5d8aacb`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 598 tracked files, 119 tarball entries; targeted 83/83 and 6/6. Mutation probes after commit, tests kept: `protocol.ts` + `model.ts` restored from before the change → v1 validation exit 1, 5/6 (`the protocol and store task-id gates use different regex objects`); `migrate.ts` restored with the bounded regex kept → migration exit 1, 80/83 (`no refusal`, `the former store was deleted`); the broad first-version guard restored → migration exit 1, 80/83 (an empty invalid directory refused); fix restored → 83/83 and 6/6. Isolated review (Claude `claude-opus-5 xhigh`, `reviewer:s-types`): three minor (refusal broader than stated and misnamed; a child process where the file's idiom is `thrown()`; the `model.ts` header still reading as the regex's home), all closed in `5d8aacb`; closure checked by the orchestrator on the diff. Consumer side checked by the orchestrator: the consumer linter's value-home map names no home for `TASK_ID_RE`. Approver: squash of the worker branch onto `main`; the only merge conflict was the `[Unreleased]` list of `CHANGELOG.md`, resolved as the union of entries; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/02-host.md` § `legacyLayout()` (the refusal, its ordering and the empty-directory rule); `CHANGELOG.md` `[Unreleased]` › Fixed; the headers of `src/v1/model.ts` and `src/migrate.ts`. No other reference section states the id grammar.

**Acceptance.** Implementation: Codex `gpt-5.6-sol` max (`worker:s-types`, strategy `quality`, bus task `pb-run-0909-t20260909-132344`). Review: isolated Claude reviewer under the same task. Findings: PB-63.1 (`migrationNeeded()` on a sweep-only plan), recorded from this review.
