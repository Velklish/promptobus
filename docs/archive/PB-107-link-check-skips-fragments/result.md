# PB-107 · Result

**Closed 2026-09-09.** Completed. The publicity audit's link check (`scripts/audit-public.mjs`) no longer skips a Markdown link because its target carries a `#fragment`: the matcher now captures complete `[text](target)` links, with an optional `"title"`, splits the target at `#`, keeps pure in-file anchors exempt, and resolves the file half against the linking file — so `docs/does-not-exist.md#missing` is reported as "link resolves to nothing" with the original target in the message. The matcher's requirement of a complete link is deliberate: the task card's own prose example `](path "Title")` has no opening bracket and must not be read as a link. Anchor-name validation stays out of scope.

**Verification.** Worker commit `e9808b9` (worktree of `worker:verify`, on `main` `3c9b65b`). Reproducer: with a temporary tracked README link to `docs/does-not-exist.md#missing` and the old regex, `npm run audit` exit 0 ("publicity audit: clean"); with the repaired matcher the same probe exit 1 (`✖ link resolves to nothing: README.md → docs/does-not-exist.md#missing`), the fragment-less form likewise; the probe removed, the real tree exit 0. Gates on `e9808b9`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 661 tracked files, 119 tarball entries. Mutation probe after the commit: the old regex restored → the missing fragment passes the audit again; matcher restored → exit 1 with the exact finding; probe removed → exit 0. Review: the orchestrator read the diff in full (one regex, three lines of target handling, one CHANGELOG bullet) — no isolated reviewer session was spent on it. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:verify`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator.
