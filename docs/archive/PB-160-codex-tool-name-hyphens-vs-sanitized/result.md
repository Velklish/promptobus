# PB-160 · Result

**Closed 2026-09-10.** Completed. The tool name a Codex participant is told is now the key **as Codex exposes it**: `codexToolSegment` in `lib/driver-codex.js` replaces every character outside `[A-Za-z0-9_]` with `_` in both the override key and the tool segment, and `mcpToolName` — the one function behind the order text, the wake text (`orderBody`) and the consumer host's `toolName(server, name)` — builds `mcp__<sanitized key>__<tool>` from it. Under the consumer `ati-agents` the config key stays `ati-agents-promptobus` (Codex loads it with the hyphens) and the participant reads `mcp__ati_agents_promptobus__promptobus_send`, which is what codex-cli 0.146.0 lists. Before: the raw key was glued verbatim, and a call by the told name — `tools.mcp__ati-agents-promptobus__promptobus_send(...)` inside Codex's `exec` harness — was a subtraction, not a property: `failed`, 0 ms, no text, then five minutes of CLI workarounds (consumer finding ati-agents BL-517.2; the consumer's live run passed only because the model looked the names up in `ALL_TOOLS` itself). The holder log names a failed MCP call with server, tool, status and reason (`event item/completed mcp <server>/<tool> failed {…}`) instead of a bare `event item/completed`.

**Verification.** `test/promptobus-driver-codex.test.mjs` 139/139: told name equals the sanitized key and contains no `-`; a host with `commandName: 'ati-agents'` is told `mcp__ati_agents_promptobus__promptobus_send`; `toolName`, `PHRASES.tool`, the two-hosts check and the wake-text check all compare through `codexSees()`. Mutation — `codexToolSegment` returns the raw string: exit 1, 134/139, five verdicts red (`mcp__promptobus-promptobus__…`, `mcp__ati-agents-…`). `test/codex-holder-spawn.test.mjs` 7/7: the stub app-server emits an `item/completed` with a failed `mcpToolCall` and the holder log carries `mcp ati_agents_promptobus/promptobus_send failed {"message":"stub: tool not found"}`. Gates in the `pb-160` worktree after `backslop init`: `npm test` exit 0 (54/54 files), `backslop lint` exit 0, `npm run audit` exit 0 — the first pass was red on all three for names of the consumer contour in code, tests, doc and CHANGELOG (boundary test and publicity audit), scrubbed to a neutral `acme-tools` example. Live: consumer `live-codex.mjs` (ati-agents main `b371e820`, codex-cli 0.146.0, `gpt-5.6-sol`, read-only) with this branch's `lib/driver-codex.js` and `lib/codex-session.js` copied over the pinned v0.5.1 package and restored byte-for-byte afterwards — 10/11: the order text names `mcp__ati_agents_promptobus__…`, the participant calls `tools.mcp__ati_agents_promptobus__promptobus_send(...)` by the told name (`promptobus_mailbox` Ok 3 ms, `promptobus_send` Ok 5 ms), `LIVE-CODEX-HELLO` in 76.8 s, `stop` clean, `~/.codex/config.toml` unchanged. The one red verdict — «no new `app-server --stdio` processes» — counted pid 55141 whose cwd is `$TMPDIR/promptobus-test-run-*/promptobus-promptobus-mixed-*`: this package's own `npm test` running in parallel for the gates of this very commit, not the live circle; the verdict is system-wide by construction.

**Documentation in the same pass.** `docs/reference/02-host.md` § Consumer identity inside a harness; `CHANGELOG.md` `[Unreleased]` › Fixed.

**Acceptance.** Decision: the owner, Павел Ким, 2026-09-10 («Закрой сейчас сам»). Implementation: the ati-agents run 0910a orchestrator (Claude Fable 5.1) in worktree `pb-160`, merged into `main` beside the live run pb-run-0910. Not done here: the consumer re-pin (ati-agents BL-625) — until it lands, a Codex participant of the consumer works only when the model looks the tool names up itself.
