# PB-48 · Result

**Closed 2026-09-10.** Completed. `tallies` is exported from `lib/model-routing/telemetry.js` unchanged — it reads only sender, recipient, type and timestamp off the message envelope, the privacy-safe surface the participant telemetry record already exposes at `done`. `status` calls it once per task and appends `rounds N · questions N · results N` to a participant line beside `unread N` whenever at least one count is non-zero, so the orchestrator's step-up-after-two-rounds rule reads the count off the screen instead of counting review messages by hand; the judgment that stays with the orchestrator is whether a round showed progress. The CLI reference describes the fragment and the orchestrate skill's "Step up after two rounds" section points at it.

**Verification.** Worker commit `f1d107a`, worktree of `worker:telemetry`, squashed. Reproducer red before the repair: `node --test test/model-routing-command.test.mjs` exit 1, 36/37 — "The input did not match the regular expression /worker:status-rounds.*rounds 2/" (the line read `worker:status-rounds · unread 2 · alarm: self-wake`); the no-traffic omission case passed. After the repair 37/37; the telemetry suite 65/65. Gates on `f1d107a`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 689 tracked files, 119 tarball entries (escalated reruns after the sandbox refused `dist/` writes, keychain access and the nested build). Mutation probe, tests kept: the export and the status fragment removed → the targeted test exit 1 with the same assertion; restored → 1/1, worktree byte-identical to the commit. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/03-cli.md` (`status`), `skills/orchestrate/SKILL.md` § Step up after two rounds, CHANGELOG entry under Changed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:telemetry`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator diff read; approver Павел Ким's orchestrator session.
