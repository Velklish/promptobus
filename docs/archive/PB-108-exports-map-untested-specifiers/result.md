# PB-108 · Result

**Closed 2026-09-09.** Completed as a test change with one reference paragraph. `test/promptobus-package.test.mjs` now installs the packed tarball into a sandbox and, in a child Node ESM process with that install as its cwd, resolves and imports every public specifier of the `exports` map — `promptobus`, `promptobus/driver`, `promptobus/host`, `promptobus/hooks`, `promptobus/cli` and one concrete schema, `promptobus/schemas/v1/task.schema.json` — checking a named export of each (`PACKAGE_NAME`/`PROTOCOL_VERSION`, `createRegistry`, `isPromptobusHost`, `planPromptobusHooks`) and parsing the schema, so a key-only mapping or a target file missing from the tarball fails the package test rather than the first consumer. `package.json` and `lib/host.js` needed no change: the map was already correct; the check pins it. 01-overview describes the installed-tarball probe.

**Verification.** Worker commit `729059c` (worktree of `worker:verify`, on `main` `d57d1c0`). Reproducer: with `exports['./host'].default` temporarily pointed at a missing file, the existing key-presence check stayed green while the new probe failed exactly on `promptobus/host` (27/28, "Cannot find module … dist/missing-host-index.js"); restored → 28/28. Gates on `729059c`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 656 tracked files, 119 tarball entries. Mutation probe after the commit: the same missing-target substitution → 27/28 on the new check; restored → 28/28. Review: the orchestrator read the diff in full (one probe, one reference paragraph, one CHANGELOG bullet) — no isolated reviewer session was spent on it. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** 01-overview.md, CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:verify`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator.
