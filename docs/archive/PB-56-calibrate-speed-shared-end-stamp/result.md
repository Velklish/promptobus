# PB-56 · Result

**Closed 2026-09-10.** Completed within the scope the owner's PB-57 decision left it: completion-duration evidence only. A telemetry row's `endedAt` — and therefore `durationSec` — now ends at the participant's own last `result` message when there is one, then at its dismissal, and only then at the task's close; before, every participant of a run shared the close stamp, so a worker that finished hours before the reviewer was credited with the reviewer's waiting time. `tallies` keeps the newest valid `result` timestamp per sender, the row carries a nullable, additive `lastResultAt`, and the message and telemetry schemas describe the end stamp; `calibrate` is untouched — duration remains evidence, never a speed input.

**Verification.** Worker commit `70e5e4c` (worktree of `worker:telemetry`, on `main` `fe1c3fa`). Reproducer red before the repair: `node --test test/model-routing-telemetry.test.mjs` exit 1, 64/65 — "worker and reviewer durations end at their own result stamps, not the shared close" (both rows' `endedAt` equal to `recordedAt`, no `lastResultAt`). After the repair 65/65; calibrate 38/38; catalog 59/59. Gates on `70e5e4c`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 669 tracked files, 119 tarball entries. Mutation probe after the commit, tests kept: the `lastResultAt` branch of `endOf` deleted → 63/65 ("the duration runs from the lift to the worker's last result" and the distinct-stamp check red); restored → 65/65. Review: the orchestrator read the diff in full (the tally, the end order, two schema descriptions, one reference paragraph, one CHANGELOG bullet) — no isolated reviewer session was spent on it. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** 03-cli.md § Participant telemetry, `schemas/model-routing/telemetry.schema.json`, `schemas/v1/message.schema.json` (descriptions), CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:telemetry`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator.
