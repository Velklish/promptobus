# PB-3 · Result

**Closed 2026-09-05.** Completed. The Codex MCP key prefix is `${host.commandName}-` (`codexMcpPrefix(host)`), not a constant. It reaches the detached holder through the session record (`mcpPrefix`) — that process is handed one record file and has no host — and the prompt half through `driver.phrases.tool(server, name, host)`; the config key and every printed tool name are built by one function, `codexMcpName` refuses a missing prefix (the bare name is the collision itself), and the wake path (`renderNotification`, one parameter as the contract declares) reads the prefix off the record too, answering an old-shape record with "the session record predates the MCP key prefix; lift the participant again" rather than a throw. Track `host` of the routing run, worker in Claude Code (opus, xhigh, bypassPermissions), one isolated review round (5 findings, all fixed). Gates on the track's tree: `npm test` 46/46 files, 1330 `check.mjs` verdicts and 560 `node:test` assertions, 0 failed; `npm run audit` clean; `backslop lint` clean; re-run on the merged `main` tree by the approver.

**Verification.** `test/promptobus-driver-codex.test.mjs` 57 → 61 checks (two hosts in one process give different keys; the no-prefix refusal; the old-shape record; the seam without a key). Probes: the baked-in prefix restored → red; the record stripped of `mcpPrefix` → 38/59 with the lift refused out of the holder (the record channel is load-bearing); the refusal removed → red; `sessionPrefix` stubbed to null → red.

**Documentation in the same pass.** `docs/reference/02-host.md`, `docs/reference/03-cli.md` (the Codex subsection), `CHANGELOG.md` (`### Changed`: `DriverPhrases.tool` gained a third parameter; consumers meet it at the closing tag).
