# PB-2.1 · Result

**Closed 2026-09-05.** Completed. Exit path established by reproduction: the runner takes a file down with SIGKILL on both routes (the 300 s file timeout and Ctrl-C), which no exit hook, signal handler or `finally` in a suite file reaches — SIGKILL left the holder and app-server alive, SIGTERM and a clean finish left nothing. So the reap belongs to the holder: it watches its own session record every 5 s and, when the record is gone, kills its app-server and exits without writing (a log write with nowhere to go is dropped, so the removed tree is not recreated by a stderr line in the window). Beside it a run-level gate: `startHolder` appends pid and session file to `PROMPTOBUS_HOLDER_TRACE`, and the tail of a run — before the interrupt branch, so Ctrl-C is covered — refuses a holder still holding a live session record when the run ended. The twelve leftovers named by the entry were already gone from the machine. Track `host` of the routing run, worker in Claude Code (opus, xhigh, bypassPermissions), one isolated review round (5 findings, all fixed). Gates on the track's tree: `npm test` 46/46 files, 1330 `check.mjs` verdicts and 560 `node:test` assertions, 0 failed; `npm run audit` clean; `backslop lint` clean; re-run on the merged `main` tree by the approver.

**Verification.** `promptobus-driver-codex.test.mjs` 59 → 61 checks (the reap; the registry directory does not come back under a stderr loop). Probes on a nested runner: the record watch removed → red with both pids alive; a traced live process → red naming pid and session; the same trace with the process exited → green; a live process whose argv names another file → green; SIGINT mid-run → exit 130, the holder reported; the report put back inside the `else` → no holder line. Nine holder starts traced in one run of the Codex file.

**Documentation in the same pass.** `docs/reference/03-cli.md` (the holder section), `test/run.mjs` register, `docs/guides/contributing.md`, `CHANGELOG.md`.
