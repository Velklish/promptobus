# PB-77 · Result

**Closed 2026-09-09.** Completed. `endReader` in `lib/model-routing/telemetry.js` no longer borrows the resolver's `entryLive` to decide whether a cached window percentage may serve as the end reading of a participant's spend delta. Routing liveness keeps an exhausted entry until its reset, or indefinitely when the reset is unknown, and that held fact is not a fresh percentage: telemetry now has its own predicate — the window-bearing entry must have been checked within the sixty-second window TTL (`checkedAt + WINDOW_TTL_MS > now`) — and `usedPercentAtEnd` is `null` otherwise, so `models calibrate` cannot subtract a days-old frozen value from the spawn reading. `entryLive` and routing are unchanged.

**Verification.** Worker commit `c674b53` (worktree of `worker:telemetry`, on `main` `dcf9b6f`). Reproducer red before the repair: `node test/model-routing-telemetry.test.mjs` exit 1, 42/44 — "an exhausted entry with a future reset past the TTL gives no end reading" and "a sticky exhausted entry with no reset past the TTL gives no end reading", both with `usedPercentAtEnd` 55 where `null` was expected; both cases run the real `done` path against a seeded cache. After the repair 44/44. Gates on `c674b53`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 634 tracked files, 119 tarball entries. Mutation probe after the commit, tests kept: `entryLive` restored in `endReader` → exit 1, 42/44 with the same two failures; fix restored → 44/44. Review: the orchestrator read the diff in full (one predicate, the doc comment, the schema description, one reference sentence, two test cases) — no isolated reviewer session was spent on it. Approver: squash of the worker branch onto `main`; no merge conflict; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `schemas/model-routing/telemetry.schema.json` (`usedPercentAtEnd` description), 03-cli.md § Participant telemetry, CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:telemetry`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator.
