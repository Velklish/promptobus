# PB-227 · Result

**Closed 2026-09-16, completed.** The long-comment debt of `lib/`, `src/` and `bin/` is swept and no line of code moved with it. The lexer written for this pass counted **1266** runs longer than two lines over the 85 tracked code files of those three trees (`templates/` does not exist here), which is the card's own arithmetic from the other side: 1282 listed entries − 58 the pending list still names but no run answers + 42 runs outside the list. **188** of them sit in files another track holds — `lib/store.js`, `lib/spawn.js`, `src/v1/engine.ts`, `src/v1/store.ts`, `src/mcp/**` — leaving **1078** in scope, and all 1078 are closed: **30** carried a fact the documentation did not have and moved there with a pointer of at most two lines left beside the code, **11** retold a section that already said the same thing and were removed against a grep of that section, and **1037** were constraints compressed to two lines. Provenance left the code entirely — task numbers, dates, "review remark", and the retelling of the lines below a comment belong to git and to the tracker. The pending list itself is untouched: it is `PB-188`'s file and that card regenerates it from the tree.

**The ledger is not in this repository, and that is a gate's decision.** `pb227-ledger-3.md` carries one row per run of the base tree — `file:line → pile → destination` — and under each row the text that LEFT it, computed as a set difference rather than recalled: every comment line removed, minus every line this pass added to a comment, to `docs/` or to the CHANGELOG. By that measure 313 runs lost nothing, 197 lost wording only, and 568 lost something carrying a value; of the clauses in that last pile 534 are covered by a document this pass wrote and 1222 left the tree. A destination is claimed only on a strict match — 80 % of the clause's words and every distinctive token — because naming a document that does not carry the fact is worse than admitting the clause left. Filing it as `ledger.md` beside this result turned `backslop lint` red with **55 broken links**: the ledger quotes removed comments verbatim, and the relative pointers inside them (`cache.js`, `../util.js`, `../docs/reference/03-cli.md#spawn` and the like) resolved from `lib/` and `src/`, never from `docs/archive/`. Rewriting quoted evidence to satisfy a link checker would make it stop being evidence, so the ledger stays in the run's task journal rather than in the tree, and this paragraph is the pointer to it.

**Verification.** Gates of this acceptance, run on the merged tree of this commit: `npx github:Velklish/backslop#v0.8.0 gates` exit 0 — **gates 4, green 4**. `npm test` code 0 in 158 s over **69 of 69** test files and 2913 checks — 2909 green and 4 red, and all four red are the `todo`-marked ones below; `backslop lint` code 0, no errors; `npm run audit` code 0, clean over 958 tracked text files and 134 packed tarball entries; `npm run pins` code 0, 958 of 958 tracked files read, `#v0.8.0` throughout. The same four commands were green on the worker's branch head before the merge, and the set was run twice here — once on the merged working tree and again on the committed tree, with the same result both times.

**That the code did not change is measured, not asserted.** Both sides are stripped of comments by the same lexer and compared: over the 85 tracked files of `lib/`, `src/` and `bin/`, **code-changed 0**. The acceptance re-derived it independently on the merged tree with a second stripper — its own comment/string/template/regex state machine, comparing whitespace-normalised token text of the base against the merged working tree over the 60 files this pass touched — **0 files differing outside comments**; that checker was itself probed by appending one statement to `lib/status.js`, which it reported as a difference. No hunk of the allowed data-as-comment class appears: no `usage()` reads its own header in these trees. Nothing under `test/` is touched.

**Two `npm test` numbers need reading rather than trusting.** Four checks inside `test/comment-length.test.mjs` and `test/comment-links.test.mjs` fail and are marked `{ todo: … }`, so by `test/run.mjs`'s own contract the files still exit 0 and the gate is green. What they report is exactly what this pass did — swept files still named by the pending list, run ids no run answers any more, and the net pointer movement. Both fixtures are `PB-188`'s files and were not touched.

**Documentation in the same pass.** What moved is what a reader could not have got from the code. `docs/reference/02-host.md` — § Launching a process (the `cmd.exe` command-line rules, the three Windows routes, quoting, `PATHEXT`). `docs/reference/03-cli.md` — § The session id after a lift, the `done` cleanup order with its 2026-09-02 measurement, the notification budget sized from 768 live messages, the journal-growth measurement behind `PRUNE_DEFAULT_DAYS`, the `history` unit, the sticky-`knockError` rule, and in § Spawn the three live `settingsFile` keys with the interactive-dialog stall that `enableAllProjectMcpServers` avoids. `docs/reference/05-drivers.md` — § The two `mcp_servers` transports and the field that kills the config load, § The holder's record watch (with the twelve holders that outlived a run on 2026-09-04), § A stop returns after the record is gone, not after the command returns. `docs/guides/hooks-and-trust.md` — the `undefined`-versus-`null` snapshot guard of the watch round. `docs/guides/model-routing.md` — § Killing a probe's child: why three lines and not one, and the live-participant cap's measurement. `docs/reference/01-overview.md`. `docs/GLOSSARY.md` — three code-evidence links retargeted, one broken by this pass and two already stale at the base. One misfiled block was corrected in passing: the `HostLegacyLayout` documentation stood above `HostClone`. `CHANGELOG.md` under `## [Unreleased]`.

**Four measurements were pulled back out of the sweep, and a fifth out of the review.** A narrower scan asked which measurement figures from a dropped clause appear nowhere in the tree — not in a comment, not in a document. Four did not and all four are back in the documentation: the 2026-08-28 result that `CLAUDE_EFFORT` does not change session effort, injection first proven 2026-08-29, the `Stop` payload that must be read from stdin, and a bad model id refused in about two seconds without opening a chat. The first of those is the sharpest — the round-1 ledger claimed `03-cli § Spawn` already carried it and it did not, which is why destinations are now claimed only on a strict match. The fifth came from the reviewer reading about a hundred of the 568 value-losing runs: `realpathSync` keeps the caller's case, measured on macOS APFS with Node 25.2.1. That is a constraint on the containment gate and invisible from the code — on a case-insensitive filesystem the path comparison as written would be unsafe — so it is back in the two-line comment over `containmentPath` in `lib/codex-session.js`. A measurement is not provenance and does not come off with the dates.

**Review.** Two rounds. Round 1 raised five findings — a compression that took the reason for three live `settingsFile` keys with it, a ledger that did not say what left each run, three stale glossary links, line references to recompute, and a duplicated 0.14 s figure — all closed. Round 2 read about a hundred of the value-losing runs and found one real loss, returned above; it was reduced to that single minor finding and closed.

**What is left open.**

- **188 long runs remain in the tree** and every one is in a file this pass did not own: `lib/store.js`, `lib/spawn.js`, `src/v1/engine.ts`, `src/v1/store.ts` and `src/mcp/**` belong to another track, and the `test/comment-*` registries belong to `PB-188`.
- **One figure was dropped on purpose and has no home:** `REPORT §4.14`, a section number of a spike document that is not in this repository. The fact it pointed at is in `05-drivers.md`; a section number of somebody else's document is the provenance the owner's rule sends to the tracker.
- **Two occurrences of the same 0.14 s figure stay in `lib/cursor-persist.js`.** They are at the base commit as well, so the duplication is not this pass's, and rewriting them would be a change to code this card did not ask about.
- **1222 clauses left the tree** and are recorded in the run's ledger rather than in a document. That is the pass's cost, stated rather than hidden: the rule trades them for the tokens every read of the file was paying.
