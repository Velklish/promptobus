# PB-76 · Result

**Closed 2026-09-09.** Completed as a documentation change; it includes the former PB-111. guides/install.md and guides/hooks-and-trust.md now describe what the installer writes for Cursor today: a `stop` hook running `promptobus guard` only, in `.cursor/hooks.json` (`version` 1); bus feedback reaches a Cursor participant by driver injection, not by a project hook; there is no `postToolUse` hook and no `--output additional_context` — Cursor does not recognise that event, and an unknown event name in `.cursor/hooks.json` disables the file (which PB-154's merged-map validation now refuses before writing). The hook tables, the Cursor paragraphs and the skip-trust paragraph were aligned as a set, not the one sentence the card quotes; `lib/install.js` and its test needed no change.

**Verification.** Worker commit `5ecfa88` (worktree of `worker:hooks`, on `main` `e88e7c3`). Reproducer: the card's stale claims located by grep in both guides (four places) before the edit; after the edit the install suite is 19/19 (the card's "14/14" is its triage-time snapshot, five tests were added since by PB-103, 52, 53, 100 and 154). Gates on `5ecfa88`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 655 tracked files, 119 tarball entries. Mutation probe after the commit: a stale "additional_context" claim re-inserted into the install guide → the worker's document check exit 1 ("install guide must not promise Cursor additional_context"); restored → exit 0. Review: the orchestrator read the diff in full (two guides, one CHANGELOG bullet) — no isolated reviewer session was spent on it. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:hooks`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator.
