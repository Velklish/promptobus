# PB-104.1 · Result

**Closed 2026-09-10.** Completed by the owner's decision of 2026-09-10: the shipped behaviour stays and ADR-004 says so. Its `hidden: boolean` paragraph now records, dated, that a hidden row is carried in the availability snapshot so the cache matches what the harness lists, that the resolver's inventory and the `runtime` list are the rows with `hidden !== true`, that `models` prints those projections, and that a catalog tuple naming a hidden model is excluded as `model-not-in-inventory` with the detail naming the account; the sentence that promised a person would see the withheld rows is gone by clean replacement, together with the obsolete note about PB-28 superseding a 03-cli sentence. The `inventoryOf` doc comment in `lib/model-routing/resolver.js` tells the same story; 03-cli § Model routing already did (PB-104) and is untouched. No resolver or render code changed; snapshot fidelity is as PB-28 built it.

**Verification.** Worker commit `596765e`, worktree of `worker:verify`, squashed. Reproducer: a temporary consistency probe on ADR-004 exit 1 before the edit — `AssertionError [ERR_ASSERTION]: ADR-004 must not say models shows hidden rows` — and exit 0, 1/1 after; the probe was deleted before the commit. Gates on `596765e`: `npm test` exit 1, 53/54 test files — the red file `test/promptobus-driver-codex.test.mjs` (four `worker:long-first` first-turn checks, "no session record in the Codex registry" / "nobody to wake") under a machine load average above 100 from four parallel suites; standalone on the branch exit 0, 129/129, twice (escalated; a sandbox attempt died on `listen EPERM` for its Unix sockets); on `main` at `b8bd5b4` the approver measured the same file standalone red once (09:50 UTC, the same four checks) and green once (09:51 UTC) — recorded under PB-159.1, not attributed to this change; `model-routing-preflight.test.mjs` 35/35 and `runner.test.mjs` 33/33 in the same run; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 714 tracked files, 119 tarball entries (escalated reruns). Mutation probe after the commit, probe kept: the ADR repair reverted → the probe exit 1 with the same assertion; restored → exit 0, 1/1. Approver: squash of the worker branch onto `main`; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/adr/adr-004-subscription-balance.md` (the hidden-row paragraph, dated); the `inventoryOf` doc comment; `CHANGELOG.md` `[Unreleased]` › Changed.

**Acceptance.** Decision: the owner, Павел Ким, 2026-09-10 (amend the ADR, no resolver or render change). Implementation: Codex `gpt-5.6-luna` max (`worker:verify`, strategy `balance`, bus task `pb-run-0910-t20260910-085454`). Review: orchestrator diff read; approver Павел Ким's orchestrator session.
