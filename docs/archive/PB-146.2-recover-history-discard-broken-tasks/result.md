# PB-146.2 · Result

**Closed 2026-09-10.** Completed as decided at triage: the contract is documented, no type changes. `recover()` and `history()` cover readable tasks — both enumerate `listTasks().tasks` — and a caller that needs the unreadable ones pairs them with `listTasks().broken`, whose `code` PB-146 gave it; `RecoverResult.broken` keeps its meaning (unreadable records inside a readable task) and is not overloaded with `BrokenTask`. 04-protocol § Engine says so in one sentence. A test in `test/v1-engine.test.mjs` pins the contract on a store with one readable task (its own history and an in-flight intent) and one unreadable journal that also carries history and an open intent — so an implementation that walked every task directory would fail rather than pass vacuously — asserting that `history()` and `recover()` carry the readable task only and that `listTasks().broken` names the other.

**Verification.** Worker commit `88e8112`, worktree of `worker:store`, squashed. The test is a characterization of the decided contract and was green before the edit by design (`node --test test/v1-engine.test.mjs` exit 0, 128/128; the focused case 1/1); its proof is two mutation probes after the commit, test kept: `history()` made to include `listTasks().broken` ids → the focused test exit 1 with the unreadable task's id among the entries; `recover()` made to read the broken journal → exit 1 with `PromptobusError` `task-broken` and the journal's parse message; both restored → 1/1. Gates on `88e8112`: `npm test` exit 0, 54/54 test files (`v1-engine.test.mjs` 128/128); `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 721 tracked files, 119 tarball entries (escalated reruns). Approver: squash of the worker branch onto `main`; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/04-protocol.md` § Engine; `CHANGELOG.md` `[Unreleased]` › Changed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:store`, strategy `balance`, bus task `pb-run-0910-t20260910-085454`). Review: orchestrator diff read; approver Павел Ким's orchestrator session.
