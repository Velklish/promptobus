# PB-144 · Result

**Closed 2026-09-09.** Completed. The suite runner (`test/run.mjs`) now sweeps the stray socket directories that a cut-off run leaves under `/tmp` — the six test prefixes of `SOCK_PREFIXES`, now owned by `scripts/canary-runs.mjs` (the dead `adoc-` entry removed, and a two-way sentinel keeps the list equal to the literals the suite creates) — and does so without touching a live neighbour: the one-hour age cutoff filters candidates before anything else (children keep their run root young, and the file timeout is five minutes); each remaining directory is probed with a short child process that connects to every socket in it, and only a socket that answers ENOENT or ECONNREFUSED counts as dead (exit code 3, one Node never emits by itself) — a connect, a timeout, a signal, a throw or an uncaught exception all hold the directory. Suite sandboxes in `$TMPDIR` gain an owner marker (`.promptobus-run-owner.json`, pid and realpath of the run root) as the second line of defence; a live pid or any doubt holds. `sweepPreviousRuns` takes an `isLive` predicate and a `held` list (a thrown predicate holds); the five callers PB-114 added keep their behaviour. The Codex holders' own sockets are never candidates: they are files (`pb-cdx-*.sock`) outside the prefix list. contributing.md § Suite isolation describes the socket sweep, the marker, the probe and the hold-on-unknown rule; the runner prints swept, refused and held names.

**Verification.** Worker commits `eb426a2` (repair) and `42a8462` (review follow-up), worktree of `worker:verify`, squashed. Reproducer red before the repair: `node --test test/tmpdir-sweep.test.mjs` exit 1, 26/27 — "liveness evidence preserves a live neighbouring socket directory" (`swept: a2l-live-neighbour, a2l-stale · held: none`). After the follow-up 35/35 (a real `net.createServer().listen()` fixture proves the default probe holds a live socket, a plain `*.sock` file and an empty directory are swept; dead-pid and path-mismatch marker cases; a live-neighbour fixture with four Codex-holder and two Claude-reviewer children — private fixtures, never the real `/tmp`); runner suite 33/33. Gates on `42a8462`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 644 tracked files, 119 tarball entries. Mutation probes after the commits, tests kept: the liveness guard removed → 25/29 (live-neighbour and socket-preservation checks red); the probe verdict inverted (`!== SOCKET_DEAD_CODE` → `===`) → 34/35, "a live Unix socket is held by the default probe"; restored → green. Review: isolated reviewer (`reviewer:verify`) — MAJOR: the probe was executed by no test (an inverted verdict kept the suite green) and "dead" was exit 1, the code Node emits for any uncaught error, so the probe was fail-open; MINOR: the dead `adoc-` prefix had become a deletion rule, win32 was fail-open by default, the marker's path comparison lacked realpath, the guide was silent, dead condition and comments — all closed in `42a8462`, read by the orchestrator. Confirmed by the reviewer: no production socket falls under the prefixes, the cutoff runs before the probe, the probe is a no-op for the holder and the guard's sockets. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Residual.** A hand-run `node --test test/…` writes no owner marker and lands in the real `os.tmpdir()`; after an hour a neighbouring `npm test` may sweep it — pre-existing, held by the age cutoff only. The live scripts were not run.

**Documentation in the same pass.** guides/contributing.md § Suite isolation, the runner's prefix registry, CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:verify`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: Claude `claude-opus-5` xhigh (`reviewer:verify`, strategy `quality`); the orchestrator read the follow-up.
