# PB-47 · Result

**Closed 2026-09-10.** Completed. The three fenced synopsis lines of `skills/orchestrate/SKILL.md` match `lib/cli.js` again: `spawn` gained `--model`, `--effort`, `--permission-mode` and `--refresh`; `done` gained `--keep-sessions`; `review` gained `--base`, `--model`, `--effort`, `--permission-mode`, `--refresh` and `--dry-run` — the eleven flags an orchestrating session could not learn from the skill. A drift gate beside the existing `subcommands()` check in `test/cli.test.mjs` extracts each command's top-level `parseArgs` options from `lib/cli.js` and the flags of every fenced synopsis under `skills/*/SKILL.md`, and fails on a missing or extra flag for `spawn`, `done` and `review`. `lib/cli.js` itself needed no change: its help already carried the full set.

**Verification.** Worker commit `ee4f336`, worktree of `worker:docs`, squashed. Reproducer red before the change: `node --test --test-name-pattern='skill CLI synopses stay aligned' test/cli.test.mjs` exit 1 with the mismatch report (spawn missing effort, model, permission-mode, refresh; done missing keep-sessions; review missing base, dry-run, effort, model, permission-mode, refresh); after the change the gate 1/1 and the cli file 5/5. Gates on `ee4f336`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 699 tracked files, 119 tarball entries (escalated reruns). Mutation probe, test kept: `--refresh` removed from the spawn synopsis → the gate exit 1 with `{ command: 'spawn', missing: ['refresh'], extra: [] }`; restored → 1/1. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** This task is documentation: `skills/orchestrate/SKILL.md` synopsis, CHANGELOG entry under Changed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:docs`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator diff read; approver Павел Ким's orchestrator session.
