# PB-127 · Result

**Closed 2026-09-17, completed, against a re-measurement rather than against the card's file list.** Comments in `lib/` and `test/` no longer send a reader of this package to files that live only in the consumer CLI. Four files in `lib/` and thirteen under `test/` were rewritten: `lib/contract.js` now names `test/promptobus-mcp.test.mjs` as the live guard on `PROMPTOBUS_TOOLS` instead of a `lint` this package does not have; `lib/driver-claude.js`, `lib/spawn.js` and `lib/store.js` lost their pointers at `plugin.js`, `headless.js`, `smoke.js` and `guardhook.js`; `test/run.mjs`, `test/hygiene.mjs`, `test/sandbox.mjs`, `test/tmpdir-sweep.mjs` and nine `*.test.mjs` files stopped citing the eight test files that exist only downstream. Nothing but prose changed — no behaviour, no export, with one exception below.

**The card's own line list was stale, and its own verification section said to re-measure.** `PB-172`'s sweep had already closed several of the instances the card named — `src/fs/proc.ts`'s `pidAlive` provenance and `src/v1/messages.ts`'s unnamed "outside" reference among them — while more of the same defect class had collected in files the card never listed (`test/run.mjs`, `test/promptobus-review.test.mjs`, `test/promptobus-spawn.test.mjs` and others). The work followed the measurement. On the merged tree the card's three verification greps come back clean: the eight downstream module names return only `plugin.json` matches in `src/standalone.ts` and `test/promptobus-spawn.test.mjs`, which are a real local file and not a citation; the eight downstream test names return nothing at all; `src/fs/proc.ts` and `src/v1/messages.ts` carry no stale pointer.

**The one non-prose change: `lib/store.js` stopped re-exporting `INTENT_STALE_MS`.** The comment under review claimed the re-export had no reader inside `lib/`, and the first attempt kept the export and reworded the comment to say so. Review was right that this is the wrong repair — a re-export nothing reads is not a comment problem — so the export went. `grep -rn INTENT_STALE_MS lib/` finds it only where it is genuinely used.

**Two comments were shortened on acceptance, and both places are named here.** The branch predates the comment gates `PB-188` brought to `main`, so two of its rewrites landed over the two-line limit. `lib/spawn.js`, the `SKILL_KEYS` block: the rewrite ran to four lines and its identity changed, so its entry left `test/fixtures/comment-sweep-pending.json` — 76 runs owed for that file, now 75 — and the run itself is at two lines, keeping what `skillOverrides` does and dropping the sentence about a downstream loader keeping its key list in step, which is a cross-repository coupling note rather than a fact about this code. `lib/driver-claude.js`, the `REVIEWER_DENY` block: three lines, now two, keeping the pointer at `test/promptobus-review.test.mjs` that the review round asked for and dropping the remark that tightening the list moves a downstream smoke-test canary. The two literals in `test/comment-length.test.mjs` came down with the entry, 190 to 189, both the ceiling and the `seen` count; the registry was not rewritten.

**Verification.** No mutation probe: this task is prose, and the one behavioural change — dropping a re-export nothing reads — is held by the tree compiling and the suite staying green rather than by a check of its own. Its own evidence is the greps above. Full gates on the worker's branch head `1a636fb`: `npx github:Velklish/backslop#v0.8.0 gates` exit 0 — gates 4, green 4 (`npm test` 2276 checks over 69 of 69 files; `backslop lint` 0 errors; `npm run audit` 965 tracked files, 136 tarball entries; `npm run pins` 965 of 965 read, 49 live refs). On this task's slice as merged: `npm run build` exit 0, `node --test test/comment-length.test.mjs test/comment-links.test.mjs` exit 0 — 33 of 33, `backslop lint` exit 0.

**Review.** Four rounds by an isolated reviewer. Round 1 returned three minors on this task: the `INTENT_STALE_MS` re-export should go rather than be explained, `test/sandbox.mjs`'s `stubCommand` comment claimed a `platform` argument no test passes, and `lib/driver-claude.js`'s `REVIEWER_DENY` comment should name its local gate. All three are closed. Rounds 2 to 4 raised nothing on this task, and round 4 was clean.

**What is left open.** Where no present-day successor could be identified for a citation — several pointers at a `promptobus.test.mjs` whose historical content was never traced to one file here — the rewrite uses a general phrase ("elsewhere in this suite") rather than naming a file. That is honest and less useful than the handful where an exact successor was found and named. `scripts/audit-public.mjs` still does not catch a backticked bare filename, which is why this class of citation survived the extraction in the first place; widening it was deferred twice before (`PB-2.3`, `PB-22`) and is deferred again here, out of this card's scope by its own words.

**Documentation in the same pass.** `CHANGELOG.md`, for the `INTENT_STALE_MS` removal. The citation rewrites are comments, and the reference sections they point at already said the right thing.
