# PB-29 · Result

**Closed 2026-09-06.** Completed. `models/catalog.json` is rebuilt from the live inventories of 2026-09-06: 45 tuples (was 19) — Claude Code `claude-fable-5` (the id the 2.1.251 binary's own alias table resolves `fable` to, read offline; its successor 5.1 is PB-34), opus and sonnet on the effort ladder; Codex sol/terra/luna/gpt-5.5/gpt-5.4-mini with `ultra` where the harness lists it, ladders based on the effort the source measured (`max` for sol/terra/luna); Cursor only the families no other harness of the owner serves — composer-2.5, cursor-grok-4.6, gemini-3.8/3.7 flash, gemini-3.1-pro, kimi-k3, glm-5.2 — every `claude-*`, `gpt-5.6-*`, `gpt-5.5`, `gpt-5.4-mini` Cursor row gone. Every rating cites a published figure or says it has none: `evidence` grew the object form `{ text, sources[], interpolatedFrom, hypothesis }` with `basis`, `version`, `agentHarness`, `provenance`, `figure`, `fieldSize`, `url`, `date` per source; `validate` refuses a rating with neither a source, an interpolation from a base row that cites it (one hop, no rings), nor a stated hypothesis (`rating-without-source`), and a missing `evidence` counts as empty. Quality is rank-banded only inside one benchmark at one version with at least five rated models — SWE-bench Verified, six models — by `band = 5 − floor((pos − 1) × 5 / n)`, written into every base row; Terminal-bench could not band (the figure is model + agent harness, versions are incomparable), so nine of fifteen base models carry a quality hypothesis with the figure, the version and the nearest anchor named. `speed` is banded over nine published throughputs, `quotaCost` over thirteen list prices (`(in + out) / 2`, inverted). `roles` carry `reviewer` only at quality 5 — ten rows on two harnesses, no Cursor row; the test pins the one-sided rule `reviewer ⇒ quality ≥ 5`. `roleRatings` dropped; `prices` null with the list prices as the basis in `evidence`. `MODEL_IDS` in the Claude driver gains `claude-fable-5` (one line), and the Claude adapter test's inventory literal follows.

Review (isolated, xhigh; every band and rung recomputed by hand): nine findings, all closed in `d06e2f6` — the guide's "a hypothesis stays out" rewritten to the shipped reality; the Codex ladders rebased on the measured effort (which removed `codex-terra-ultra` from the reviewer role, an artefact of the wrong base); five price citations re-opened, two figures corrected (Gemini Flash list $1.50/$7.50 not the discounted rate; Kimi K3 $2.55/$12.75) and `quotaCost` re-banded; the rank-cut formula written down; the role test made one-sided and the two-harness consequence stated; the guide's "run once" corrected for fable; the `validate` table; the two `citationChecks` holes (missing `evidence`, interpolation rings) and the rung test checking the declared step count against the family's own ladder. Closure checked by the approver on the diff. The worker filed **PB-29.1** (now deferred to the ADR pass, PB-37): the field is unbandable across benchmarks and versions, the agent harness is part of a figure, rank bands have no good field size, and — surfaced by the base-row fix — `speed` is banded over throughput and interpolated as if it were latency, which clamps low-effort Codex rows to 5.

**The owner's decision (2026-09-06)**: the benchmark bands stand as the rule produces them; the owner's disagreements go into the user overlay (deny by role, `ratings`), not into the catalog; the finer scale and the reading of local telemetry are the next series.

**Verification.** Gates on `main` after the squash merge (CHANGELOG by union of entries, 03-cli by one merged `validate` paragraph): `backslop lint` no errors; `npm run audit` clean, 378 tracked files and the tarball; `npm test` 46/46 files (catalog file 34/34); the worker's `promptobus models validate` ✔ on the branch. Mutation probes (worker, after each commit): `evidence.sources` stripped from a rated row → the source rule and four catalog tests red, `models validate` refuses with three `catalog-invalid` findings naming the row and each rating; `codex-sol-xhigh` speed nudged one band → the ladder test red naming base, steps and ladder.

**Documentation in the same pass.** `docs/reference/03-cli.md` (catalog section, the `validate` paragraph), `docs/guides/model-routing.md` (hypotheses, the fable read, the `validate` table), `CHANGELOG.md` (two entries), `test/fixtures/model-catalog/README.md` unchanged. Approver's own edits at acceptance: PB-13.2 (deferred) counts 45 tuples and 135 price fields; PB-29.1 moved to deferred with the tag as return condition.
