# PB-37.2 · Result

**Closed 2026-09-09.** Completed with the owner's choice of the card's second option. `done` now refreshes, on its own, the availability of exactly the window-bearing harnesses represented by the telemetry rows it is about to append — `preflight({ refresh: true })` on that set under the existing 15 s `PREFLIGHT_BUDGET_MS`, one budget for the whole set — and hands the probed snapshot in memory to `appendTelemetry`, so the record's `usedPercentAtEnd` is a reading taken at the close rather than whatever the sixty-second cache held. A probe that refuses, times out or throws never blocks the close: the row is written with `null` for that reading and `done` prints one line per gap — `telemetry: <harness> window <id> not re-read (<reason>) — end reading absent`, per missing window when the entry is fresh but incomplete, once per harness when the entry is absent, stale or held. The reason vocabulary is documented: `probe_failed`, a timeout, `stale_cache`, `no_window_reading`, or a held entry's own reason such as `subscription_exhausted` (a sticky exhaustion is never re-probed under `refresh`, by design). No other path — `status`, `dismiss`, the warden — probes. The manual `promptobus models --refresh` step before `done` is gone from the two skills, the reference and both READMEs by clean deletion.

**Verification.** Worker commits `8f8d62d` (repair), `4cba50a` and `7448fd9` (review follow-ups), worktree of `worker:telemetry`, squashed. Reproducer red before the repair: `node test/model-routing-telemetry.test.mjs` exit 1, 47/49 — "done refreshes the recorded harness before reading its end window" (`{"end":null,"probes":0}`) and "a refused refresh warns with the reason and still closes the task". After the final follow-up 57/57. Gates on `7448fd9`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 635 tracked files, 119 tarball entries. Mutation probe after the last commit, tests kept: `lib/done.js` and `telemetry.js` reverted to the parent → exit 1, 53/57 (the fresh-exhausted, available-no-windows, successful-refresh-missing-scoped-window and partial-coverage checks); restored → 57/57. Review: isolated reviewer (`reviewer:telemetry`), two rounds — round 1 MAJOR: the warning predicate was the routing state rather than the telemetry freshness (a fresh `exhausted` answer warned falsely), both READMEs still carried the manual step, plus six minors (probe set wider than the window-bearing set, a fabricated snapshot suppressing the cache fallback, comment placement, the test seam, a contradictory reference sentence, PB-77's cache path left unguarded); round 2 confirmed all eight closed and raised one new MAJOR — `.some` over the spawn windows reported partial coverage as complete and let the tuple's binding window go `null` silently — plus four minors; the orchestrator ruled per-window reporting (the same rule one level down) and read the final follow-up. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** 03-cli.md § Participant telemetry and the `done` command section, `skills/orchestrate/SKILL.md`, `skills/solo-review/SKILL.md`, README.md and README.ru.md, CHANGELOG entry under Fixed. ADR-005 § `models calibrate` needed no change: it never stated the manual step.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:telemetry`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: Claude `claude-opus-5` xhigh (`reviewer:telemetry`, strategy `quality`), two rounds; option chosen by the owner, Павел Ким.
