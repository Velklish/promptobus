# PB-123 · Result

**Closed 2026-09-10.** Completed. `successorHint` takes an injectable `probe` (default `probeContactPoint`), the two successor tests call it instead of the test-only `successorVerdict`, and that export is deleted — no reference remains in `lib`, `src` or `test`. `readEvent` is exported for its test and races the stdin read against its own deadline, `HOOK_STDIN_MS` (1000 ms): on timeout it destroys the still-open stream and parses what was accumulated, so a hook whose parent wrote the payload without closing the pipe keeps its event (and with it the session id the hook process is not promised in its environment), while a stream that never writes yields an empty payload, exactly as an unparsable one does. The normal path, where EOF arrives at once, does not wait.

**Verification.** Worker commit `612c5bc`, worktree of `worker:warden`, squashed. Reproducer red before the repair: `node --test test/promptobus-guard.test.mjs` exit 1, 64/67 — "readEvent does not wait for a stdin stream that never ends" (timed out), "successor: live owner — probe is not called" (`undefined · 0`, the seam was not exported), "successor: successorHint sees SUCC's dead socket and stays silent about the live TASK" (`null`). After the repair 67/67; after the review additions 68/68. Gates on `612c5bc`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 679 tracked files, 119 tarball entries; `git diff --check` exit 0. Mutation probe, tests kept: `readEvent` returned to the unbounded loop → exit 1, 66/68, twice, "readEvent returns an empty payload when stdin never ends" and "readEvent keeps a complete payload when stdin never ends" (both `null`); restored → 68/68. Open: no measured incident of the stdin hang exists; the deadline follows the package's stated principle. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** CHANGELOG entry under Fixed; the constant is documented in a comment beside it. The reference does not describe the hook's stdin handling, so no reference change was required.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:warden`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator, one round before the commit — MAJOR: the timeout discarded an accumulated payload; fixed by parsing `raw` on timeout, plus a dedicated deadline constant and a test for a complete document on a never-closing stream. Approver Павел Ким's orchestrator session.
