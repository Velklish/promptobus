# PB-153 · Result

**Closed 2026-09-09.** Completed. `inspect()` in `lib/driver-cursor.js` formats the idle note ("the turn ended, turns in total N") from `record.turns ?? 0`, the counter the wake hook owns, instead of `turn.ended` — the transcript parser's end-of-file marker count that its own comment says cannot count turns. `readTranscript`'s `ended` field and `registerWake` are unchanged; `lib/status.js` prints the driver note verbatim, so it needed no change. A fixture in `test/promptobus-driver-cursor.test.mjs` patches a session record to `turns: 2` and asserts the note says "turns in total 2" while the transcript's `ended` count differs.

**Verification.** Worker commit `a139a38` (worktree of `worker:drivers`). Reproducer red before the repair: `node test/promptobus-driver-cursor.test.mjs` exit 1, 112/113 — `PB-153: status uses the recorded Cursor turn count, not transcript markers` (note `turns in total 1`, transcript ended 1). Gates on `a139a38`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 592 tracked files, 119 tarball entries. Mutation probe after commit, test kept: `record.turns ?? 0` reverted to `turn.ended` → exit 1, same check red, 112/113; fix restored → exit 0, 113/113. Review: the orchestrator read the diff in full against the task's "Work to do" — a one-line change and a nine-line fixture, exactly the two items the task names — so no isolated reviewer session was spent on it. Approver: squash of the worker branch onto `main` at `b24ed48`; the only merge conflict was the `[Unreleased]` › Fixed list of `CHANGELOG.md`, resolved as the union of entries; `backslop lint` on the integrated tree exit 0.

**Documentation in the same pass.** `CHANGELOG.md` `[Unreleased]` › Fixed. No `docs/reference/` section describes the note's wording, so the reference is unchanged.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:drivers`, strategy `balance`, bus task `pb-run-0909-t20260909-132344`). Review: orchestrator.
