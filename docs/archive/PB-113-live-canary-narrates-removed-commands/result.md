# PB-113 · Result

**Closed 2026-09-10.** Completed. The live canary's narration matches what it does since the package's extraction: Step 1 is "tarball installed in a clean workspace, the installed bin answers `--version`", the comments describe the tarball install, the version check, the live run and the whole-run home snapshot, and the references to `release-gates.mjs`, `base.js`, `sync` and `doctor` are gone. The redundant intermediate verdict (`homeAfterSync`, `syncDiff` and its check) is deleted — the `finally` verdict already compares the same fields over the whole run; the unused bare clone (`baseOrigin`, `mirrored`) and its cleanup are deleted; the unused dynamic import of `hooksState` is deleted. The `resolveToolBin` re-import the card cites at an older commit no longer existed on the current tree. A sentinel in the tmpdir-sweep suite fails if the script names a removed command or file again or loses its whole-run snapshot. The canary itself was not run: live scripts spend subscriptions and are outside the worker boundary.

**Verification.** Worker commit `455f9c2`, worktree of `worker:docs`, squashed. Reproducer: the sentinel ": live-canary narrates only current checks and keeps its whole-run home snapshot" red on the parent script; `node --check scripts/live-canary.mjs` exit 0 after the change. Gates on `455f9c2`: `npm test` exit 0, 54/54 test files (tmpdir-sweep 37/37); `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 690 tracked files, 119 tarball entries. Mutation probe, test kept: `scripts/live-canary.mjs` restored from the parent → exit 1, 36/37 with the sentinel; restored → 37/37; `git diff --check` clean. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** The script's own comments and step names; CHANGELOG entry under Fixed. No reference document describes the canary's steps.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:docs`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator diff read; approver Павел Ким's orchestrator session.
