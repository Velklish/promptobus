# PB-100 · Result

**Closed 2026-09-09.** Completed. `promptobus install`, `install --dry-run` and `install --check` now share one content delta: `pendingWrites` in `lib/install.js` compares every planned file's bytes with what is on disk (and a removal with the file's existence) once, before the three paths branch, so `--dry-run` lists only the writes and removals that would change content — a byte-identical clean tree prints only the summary line — and `--check` reports exactly the same set as drift. The machine-local manifest joins that delta: an absent or field-changed `.promptobus/manifest.json` is drift and a pending write, while foreign keys inside an existing manifest are preserved (they are spread from the previous document, noted in a comment). guides/install.md's flag table describes both flags as the same content delta.

**Verification.** Worker commit `aef17ef` (worktree of `worker:hooks`, on `main` `39edd8e`). Reproducer red before the repair: `node --test --test-name-pattern='--dry-run writes nothing; --check reports drift and returns non-zero' test/install.test.mjs` exit 1 — on a byte-identical clean tree the old dry-run printed four "would write" lines. After the repair the install suite 18/18; deleting only the manifest gives `check` exit 1 with `drift: .promptobus/manifest.json` and a dry-run naming that one pending write. Gates on `aef17ef`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 650 tracked files, 119 tarball entries. Mutation probes after the commit, test kept: the pending filter replaced by the raw write list → exit 1 (`1 !== 0` on the clean check); the drift call given an empty list → exit 1 (`0 !== 1` on the missing manifest); restored → 1/1 each. Review: the orchestrator read the diff in full (one shared filter, `driftOf` reduced to the pending list plus the config field, the guide table, one CHANGELOG bullet, one test) — no isolated reviewer session was spent on it. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** guides/install.md (the `--dry-run` / `--check` rows), CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:hooks`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator.
