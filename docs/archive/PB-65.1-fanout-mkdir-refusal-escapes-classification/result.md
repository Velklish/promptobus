# PB-65.1 · Result

**Closed 2026-09-10.** Completed. `linkOnce` in `src/v1/messages.ts` creates the recipient directory inside its own classified `try`: a refusal whose errno is in `LINK_REFUSALS` (EPERM and EACCES among them) becomes `link-refused` with the directory — not the ref — as the note's target, the intent stays for a later pass, and `openEngine({ recover: true })`, `status`, `history` and `prune` keep working; the hard link keeps its own `try`, where `EEXIST` still means "the ref is already there". The two are split on purpose, and the split covers both callers of `linkOnce` — `completeFanout` and `materialize`: measured on Node 25.2.1, a recursive `mkdirSync` over a file throws `EEXIST` for the direct path and `ENOTDIR` for a nested one, so under one shared `catch` a file sitting where the inbox directory belongs would have read as "ref already placed" and the intent would have been dropped in silence. `LINK_REFUSALS` is unchanged, as the card decided: `EROFS` and `ENOSPC` on the directory still escape, recorded as PB-65.2. 04-protocol § Engine states the rule beside the PB-65 paragraph with the word "classified"; the CHANGELOG line says the same.

**Verification.** Worker commits `b172113` + `8885eb2` (review follow-up), worktree of `worker:store`, squashed. Reproducer red before the repair: `node --test test/v1-races.test.mjs` exit 1, 31/32 — an uncaught `Error: EACCES: permission denied, mkdir '…/inbox/w-api'` from `linkOnce` → `recoverTask` → `openEngine` (the recipient directory absent under `inbox/` at `0o500`, one pending intent, a dead lease pid); after the repair 32/32, the regression asserting `code === 'link-refused'` on the send half, the directory in `report.failed[0].note`, and `status`/`history`/`prune` exit 0 with the intent retained. Gates on `b172113`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 715 tracked files, 119 tarball entries. Gates on `8885eb2` (the follow-up): `node --test test/v1-races.test.mjs` exit 0, 32/32; `npm test` exit 0, 54/54 test files; lint exit 0, 0 errors; `npm run audit` exit 0, 715 tracked files, 119 tarball entries (escalated reruns after sandbox refusals on `dist/` and the keychain). Mutation probes after each commit, test kept: `mkdirSync` back outside the `try` → the regression red with the raw `EACCES`; the split reverted → red, actual `EACCES`, expected `link-refused`; restored → 1/1 and 32/32. Isolated review (`reviewer:store`, Claude `claude-opus-5` xhigh), two rounds: round 1 — two major (the shared `catch` turned a mkdir `EEXIST` into a silent message loss; the second caller `materialize` unweighed) and four minor (the ref path in the note; "classified" missing from 04-protocol and the CHANGELOG; the paragraph formatting; a regex assertion that a raw error would also pass), all closed in `8885eb2`; round 2 — no blocking finding; one minor (the comment above `linkOnce` still said `EEXIST` is never a refusal, true of the link half only), applied by the approver at integration as one comment line. Approver: squash of the worker branch onto `main`; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/04-protocol.md` § Engine; `CHANGELOG.md` `[Unreleased]` › Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:store`, strategy `balance`, bus task `pb-run-0910-t20260910-085454`). Review: isolated Claude reviewer under the same task, plus the orchestrator's errno probe; approver Павел Ким's orchestrator session. Findings: PB-65.2 (`EROFS`/`ENOSPC` on the directory are not classified), in triage.
