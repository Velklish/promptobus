# PB-36 · Result

**Closed 2026-09-06.** Completed. `promptobus done` appends one JSON Lines record per participant that lifted a session — routed or not — to `telemetry.jsonl` beside the availability cache in the account-scoped routing directory, mode `0600`, local only. The record ties together what the bus knew and nobody kept: the tuple and the strategy that chose it (`strategy: null` for an explicit `--model`), `strategySource` (written by PB-32), the lift and end stamps with the duration, the bus traffic (turns, review rounds, questions, results — counted from the canonical `messages/`, not from `history/`, which holds only what a mailbox fetched), and the windows that applied to the tuple at the lift (`metadata.routing.windows`, PB-30) with a reading at the close from the cache when it is fresh. A positive delta on the binding window is the run's spend in the account's own unit — the evidence `quotaCost` never had; participants of one account overlap, so the record names `concurrentParticipants` on the same harness and leaves the division to the calibration pass (PB-37). Written on `done` only when this call moved the task active → done — a repeated `done` (idempotent `closeTask`) appends nothing; a dismissal is a field, `dismissedBeforeDone`. Privacy is the acceptance criterion: no prompt text, no message body, no path, no branch, no session id, no email; the task key is an opaque local key without salt — a key, not a secret — so the slug a person typed never reaches the file. Two gates hold it: a byte grep of the written file in the tests and the closed schema. `promptobus models` prints one line — record count and file size, or "unreadable (<reason>)" — beside the decision in the text run only; `--json` stays one document. `done` does not probe, so the end reading is as fresh as the cache: run `promptobus models --refresh` right before `done` to record it, else the record carries the start value only. The scope projection is `cache.js`'s own (`scopeOf`, `modelList` exported).

Review (isolated, xhigh): two major — the windows field nobody wrote at the branch's base (true then; PB-30 landed it on `main`, and the follow-up added an end-to-end test through a real routed lift) and the repeated-`done` duplication — and seven minor (a diverged scope projection copy, the concurrency count without the lift filter, a bypassed accessor, a swallowed read error, an over-promising "one-way" wording, glossary rows for PB-33, a tautological test). All closed in `840b7b1`; closure checked by the approver on the diff.

**Verification.** On `main` after the squash merge of the worker tip (which already carried `main` at ceaf375): `backslop lint` no errors; `npm run audit` clean, 386 tracked files and the tarball; `npm test` 47/47 files (the telemetry file 42/42). Worker's mutation probe on the merged base: `sessionRef` written into the record → five red — the privacy grep and four schema validations including the end-to-end routed record; reverted.

**Documentation in the same pass.** `docs/reference/03-cli.md` (§ Participant telemetry: fields, when written, what it never holds, growth and clearing, the refresh-before-done step, the terms handed to PB-33), `docs/reference/02-host.md` (the file lives beside the cache the host names; the account's, not one workspace's), `CHANGELOG.md`.
