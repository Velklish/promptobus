# PB-39 · Result

**Outcome:** completed.

**What was done.** The Codex holder confirms the lift at `turn/started` with the thread id: the record carries `firstTurnStartedAt` / `firstTurnEndedAt`, `readyMs` is the preamble plus a 15 s wait for `turn/started` (133 s), and `PROMPTOBUS_CODEX_TURN_MS` no longer enters readiness — it bounds the acknowledgement of later `turn/start` requests, and the holder's socket wait follows that budget (the outer wait was 30 s regardless, so a value above 30 s silently did nothing). A wake while a turn runs is a queued `turn/start`; `turn/steer` is gone from the driver, the version gate's rationale and the live script's list. `inspect` reports a running first turn as `is alive (the turn is running)`. The dead `waitStarted` op is removed. Reference § The Codex holder carries the three measured facts and the explicit caveat that `review/start` is held to the same criterion on assumption until measured; CHANGELOG under Unreleased.

**Measured on codex-cli 0.146.0 app-server** (worker, no paid turn): no rollout file after `thread/start` (checked at 0, 0.5, 2 and 5 s); the rollout exists once `turn/started` arrives; a `turn/start` while busy is accepted and answered with a second turn id; `turn/started` is emitted before the model is reached, so a failing first turn emits it too (PB-39.1).

**Verification.** Predecessor (Codex `gpt-5.6-sol` medium, died on `subscription_exhausted`) and successor (Claude `opus` medium) under `balance`: `test/promptobus-driver-codex.test.mjs` 67/67 with a fake app-server that queues turns and models both never-ending and never-starting first turns; `npm test` 47/47 files; `npm run audit` clean; `backslop lint` clean; three mutation probes after commits — the old criterion restored → 63/66 with the original bug text ("is not in the list", "nobody to wake"); alive on `thread/start` → 64/66; the socket wait left at its default → 66/67 with "the holder did not reply in 30000 ms". Isolated review (`reviewer:codex2`, `claude-fable-high` under `balance`): one major (the socket wait) and five minor — all closed in edfbcd8 / 9932e91, closure verified by the approver on the diff. Approver, live on the owner's account after a reset credit (2026-09-06 08:41 UTC, standalone workspace, this branch's CLI): a Codex worker whose first turn runs `sleep 150` lifted in ~10 s — `spawn` returned while the turn ran, `status` said `is alive (the turn is running)` — a `task` message sent at 08:47:44 (store `sendMessage`) was knocked by the warden, fetched by the participant at 08:47:53 and answered with `result: wake landed` at 08:48:00 — 16 s end to end; a reviewer lifted with `review/start` on the worker's worktree in 7 s (08:48:48 → 08:48:55), so the real binary emits `turn/started` for a review and the caveat in the reference is answered. The reviewer's own report did not arrive within the six minutes before the probe task was closed (`is alive (the turn is running)` throughout) — that is the reviewer's turn, not the lift, and is noted as an observation for the next Codex reviewer use, not a defect of this task. `promptobus done` on the probe task stopped both threads and dropped their records.

**Findings.** PB-39.1 — a failed first turn is not surfaced (queued). PB-40 — a fresh worktree has no generated skills until `backslop init` (filed by the orchestrator during this task's run).
