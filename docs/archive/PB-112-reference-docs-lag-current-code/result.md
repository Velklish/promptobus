# PB-112 · Result

**Closed 2026-09-10.** Completed for the package's own reference; the consumer-side pointer and lint the triage names stay a follow-up in that repository. `docs/reference/04-protocol.md` now carries the store, fan-out, recovery, validation and fault-injection contract from the source (intents, canonical links, recipient references, the `INTENT_STALE_MS = 30_000` lease, recovery outcomes and the test-only fault seams) instead of leaving it to the consumer's Russian notes. `docs/reference/02-host.md` names every current member of `PromptobusHost` in one table — five readonly fields and forty-five methods, signatures and null-or-absent semantics — where the card, citing an older commit, counted forty-nine. `docs/reference/01-overview.md` lists the whole task layout (`.lock/`, `intents/<id>.owner`, `broken/inbox/<participant-id>/`, `broken/artifacts/`, `broken/messages/`) with the writer and the safe-delete rule for each path, and its `./host` row is current.

**Verification.** Worker commit `42e5d9f`, worktree of `worker:docs`, squashed. Reproducer before the change: an inline verifier against the source exited 1 with a 63-line assertion (five missing 04-protocol sections, the missing `INTENT_STALE_MS` and marker, missing 02-host table members from `kind` on, missing 01-overview layout paths, the stale `./host` row); after the change it exits 0, "50 host members and required reference contracts present". Gates on `42e5d9f`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 684 tracked files, 119 tarball entries (escalated reruns; the sandboxed attempts failed only on `dist/` writes, keychain access and the nested build). Mutation probe after the commit: the three reference files restored from the parent → the verifier exit 1, "02-host missing table member: kind"; restored → exit 0. Approver: squash of the worker branch onto `main`; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** This task is documentation: 01-overview, 02-host, 04-protocol, CHANGELOG entry under Changed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:docs`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator (the member count discrepancy resolved in favour of the live interface); approver Павел Ким's orchestrator session.
