# PB-121 · Result

**Closed 2026-09-10.** Completed. The Codex driver's `prepare()` returns `argv: ['app-server', '--stdio']` with the assignment as its own `prompt` field and a driver-supplied `dryRunCommand`; its `spawn()` reads `plan.prompt` instead of the last argv element. `retargetDiff` in `review` writes the rebuilt prompt to `plan.prompt` only and no longer searches argv by value. The generic `--dry-run` print of `spawn` uses the driver's descriptive command line when one is supplied and keeps the runnable `argv … <prompt>` template for drivers whose argv is the real command. The exported driver contract declares both fields: `LaunchPlan.prompt?` (the assignment text when the harness does not take it positionally) and `LaunchPlan.dryRunCommand?`, with `argv` documented as excluding a prompt delivered through a non-positional channel. The `turn/start` delivery mechanism in `codex-session.js` is untouched.

**Verification.** Worker commits `bb4049c` and `a4996ec`, worktree of `worker:codex`, squashed. Reproducer red before the repair: `node test/promptobus-driver-codex.test.mjs` exit 1, 123/125, three runs — ": argv is app-server --stdio, prompt separate; no files on disk" (the prompt was absent) and ": Codex --dry-run does not present the prompt as a positional app-server argument" (the output contained `app-server --stdio <prompt>`); after the repair exit 0, 125/125. Gates on `a4996ec`: `npm run build` exit 0; `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 672 tracked files, 119 tarball entries. Mutation probes, tests kept: production PB-121 changes reverted → the fixture exit 1, 123/125 with the same two assertions, restored → 125/125; the `LaunchPlan` fields reverted and rebuilt → a typed fixture failed with `TS2353 … 'prompt' does not exist in type 'LaunchPlan'`, restored → exit 0. Approver: squash of the worker branch onto `main` with one wording change in the `prompt` JSDoc (the delivery channel belongs to the driver that has one, `turn/start` given as the example); CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/03-cli.md` (`--dry-run` for Codex is descriptive: the prompt travels in a `turn/start` request), `src/driver.ts` JSDoc of `LaunchPlan`, CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:codex`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator, one round — MAJOR: the two fields were set on the JavaScript object but absent from the exported `LaunchPlan` interface; fixed in `a4996ec`. Approver Павел Ким's orchestrator session.
