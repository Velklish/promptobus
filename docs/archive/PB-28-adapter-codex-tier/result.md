# PB-28 · Result

**Closed 2026-09-06.** Completed. The Codex adapter reads the plan off the `account/rateLimits/read` answer — `tier: { name: planType, source: "probe" }` — and still never calls `account/read`, which returns the account's email; `credits` as two booleans with the balance read and not carried; `resetCredits` from the payload's top level (`rateLimitResetCredits.availableCount`), surfaced and never spent; `spendControlReached` carried and named only when true; hidden model rows kept with `hidden: true` (the resolver reads them out of the inventory, PB-30). The windows already carry `kind` session/weekly and `scope: null` from the PB-24 follow-up. Per-model efforts and speed tiers are not recorded in v1: the snapshot's model object is closed and ADR-004 keeps the service tier out of the tuple key — PB-24.2 (triage) holds the proposal; the Codex stub already carries `supportedReasoningEfforts` and `additionalSpeedTiers` for the day it is answered.

Review (isolated, xhigh, one pass over the track's final diff): four minors, all text that had gone false (the `MODEL_SCOPE_IDS` comment, the `liveToken` JSDoc, a signature in 03-cli, a sentence in `codex-session.js` saying hidden rows are left out), closed in `175d1f0`; the standing cross-track note on `remainingOf` was already PB-30's and landed with it.

**Verification.** On `main`: `backslop lint`, `npm run audit`, `npm test` 47/47 (the Codex adapter file 31 checks, was 25). Worker's mutation probes: hidden rows dropped again → red; the credit balance travelling → two red; the tier source naming the method → two red; reset credits read from the snapshot instead of the payload → two red; false positive on the stub (`planType` removed) → two red.

**Documentation in the same pass.** `docs/reference/03-cli.md` (the Codex section gains the informational table; the "hidden rows are left out" sentence replaced), `test/fixtures/model-routing/README.md` (the golden set separated from the adapter fixtures), `CHANGELOG.md`.
