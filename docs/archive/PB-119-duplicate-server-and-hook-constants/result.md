# PB-119 · Result

**Closed 2026-09-07.** Completed by the route the triage section fixed in advance — one dependency-safe compiled source, no project-wide `allowJs` to remove a constant. `src/contract.ts` declares the bus server name, `src/hooks.ts` imports it as `BUS_SERVER` instead of declaring its own literal, and `lib/contract.js` re-exports the compiled value the way `lib/host.js` and `lib/harness-home.js` already do. The literal `'promptobus'` for the bus server now exists in exactly one place; the remaining occurrences in the tree are the package name, the standalone command name, the Codex client name and test fixtures, none of which feed the hook matcher. `npm run build` confirmed the new dependency direction creates no cycle.

The guard-event pair went the same way: `lib/driver-claude.js` lost its own `const GUARD_HOOK_EVENT = 'Stop'` and imports the compiled declaration. Both stale comments named by the merged evidence are gone — the false claim in `src/hooks.ts` that the package has no `contract.js` of its own, and the citation in `lib/driver-claude.js` of a file that does not exist in this repository. `lib/contract.js`'s header no longer claims to be a dependency-free leaf and says what it now depends on, and why the property that mattered — help text readable without resolving the repository — survives, since `dist/contract.js` has no imports of its own.

Review corrected one claim rather than any code. The reference sentence said the `Stop` event has a single declaration and that "the two doors cannot rename the event independently". There is a third door: `lib/install.js`, the module that generates and cleans harness hook settings, spells the event names out as literals while importing `BUS_HOOK_MATCHER` from `dist` in the same file — so the import route is open there and only the names are hand-written. `lib/install.js` is outside this task's scope and was correctly left alone; the sentence was narrowed to the two doors this task actually tied, and the untied third is filed as PB-119.1.

The review also found that the new equality assertion cannot redden after the fix — `lib/contract.js` and `dist/hooks.js` now re-export the same binding, so the two sides are one value — and that the check's name promised a comparison it no longer performs. The test is not decorative: the pinned literal and the full `BUS_HOOK_MATCHER` do catch a mutation in `src/`. Its label was corrected to claim only that.

No CHANGELOG entry, deliberately. The task's own text carries that instruction from the merged PB-128 evidence: the server name and the guard event keep their values, no export name changes, and behaviour is identical. The one fact this does change is contributor-facing — `lib/contract.js` now needs a built `dist` — and it is recorded where a contributor meets it, in that file's header.

**Verification.** `node test/promptobus-package.test.mjs` exit 0, 21/21; `node test/hooks.test.mjs` exit 0, 4/4; `npm run build` exit 0. Mutation probe: replacing the re-export in `lib/contract.js` with a divergent literal fails the new assertion at 20/21, naming both sides. `BUS_HOOK_MATCHER` still builds to `mcp__promptobus__(promptobus_send|promptobus_mailbox)`, pinned by the same check. Gates green on the assembled `main`.

**Documentation in the same pass.** `docs/reference/03-cli.md` § Guard, stating what the two tied doors guarantee and, explicitly, that `lib/install.js` is not yet held to the declaration.
