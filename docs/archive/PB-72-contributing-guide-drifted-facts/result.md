# PB-72 · Result

**Closed 2026-09-10.** Completed as a documentation change. guides/contributing.md no longer states facts the tree contradicts: the backslop version is named only by the pin in `backslop.json` (the duplicated literal in the opening sentence is gone); the gates sentence refers to the `backslop.json` `gates` array instead of naming one command; `npm run audit` and what it scans (tracked files, the packed tarball, tracked links) are described instead of "publicity checks when they land"; the suite-isolation section counts its five rules; and the English-only rule is scoped to the runtime directories the ROADMAP sweep actually covers (`bin/`, `lib/`, `src/`, `schemas/`, `templates/`), with `README.ru.md` allowed Cyrillic and `scripts/` and `test/` named as outside the current sweep. `backslop.json`, `scripts/audit-public.mjs` and `docs/ROADMAP.md` were read as sources and left unchanged. The card's historical `v0.3.0` literal was already gone (PB-94 had corrected it); the duplication itself remained and is removed.

**Verification.** Worker commit `d801db0` (worktree of `worker:docs`, on `main` `1ceaa2c`). Doc checks after the edit: no `v0.3.0`, no "when they land", no "Four rules"; the gates sentence, the audit description, "Five rules" and the Cyrillic scope present — each by grep with its exit code. Gates on `d801db0`: `npm test` exit 0, 54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 671 tracked files, 119 tarball entries. Mutation probe after the commit: the stale lines restored → the worker's assertion exit 1 ("contributing guide must not duplicate the backslop version"); restored → exit 0. Review: the orchestrator read the diff in full — no isolated reviewer session was spent on it. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** This task is the documentation; CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:docs`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator.
