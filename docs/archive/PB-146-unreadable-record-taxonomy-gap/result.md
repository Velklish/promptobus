# PB-146 · Result

**Closed 2026-09-09.** Completed. The unreadable-record taxonomy is consistent between readers. `BrokenTask` carries `code: ErrorCode`, copied from the typed `PromptobusError` that `readTask` raised, so a bulk `listTasks` walk tells a journal written by a newer mechanism (`schema-version-unsupported`) from a damaged one (`task-broken`) without parsing diagnostic text; a non-protocol exception is no longer flattened into the broken list. `readTask` classifies its read errno: `ENOENT` stays `task-not-found` (the lawful race with a neighbour removing the task), any other errno is `task-broken` with the errno in `context`, so an existing but unreadable journal is no longer declared absent. `readArtifact` answers `schema-invalid` for metadata that was found, read and failed to parse (isolated into `broken/artifacts` when the move succeeds) and keeps `artifact-not-found` for a genuinely missing record. A `task-read` fault point makes the errno cases deterministic in the suite. 04-protocol § Artifacts states the artifact codes; the CHANGELOG records the new field under Added and the reclassifications under Fixed.

**Verification.** Worker commits `1449fdf` + `ff2c98e` (worktree of `worker:s-store`). Reproducers red before the repair: `node --test --test-name-pattern="unparseable metadata is schema-invalid|bulk task listing preserves" test/v1-engine.test.mjs` exit 1, 0/2 (corrupt artifact `artifact-not-found`, expected `schema-invalid`; both bulk codes `undefined`); the review round's `bulk task listing classifies a non-ENOENT journal read refusal as task-broken` red on `1449fdf` (actual `[]`). Gates on `ff2c98e`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 610 tracked files, 119 tarball entries; targeted 3/3, focused v1 152/152. Mutation probes after commit, tests kept: the non-`ENOENT` path reclassified to `task-not-found` → exit 1, 2/3; the list and artifact taxonomy reverted → exit 1, 1/3; fixes restored → 3/3. Isolated review (Claude `claude-opus-5 xhigh`, `reviewer:s-store`): one major (the new `code` field published an unreadable journal as absent) and two minor (`code: string` where the domain is exact; the isolation sentence without the "when the move succeeds" caveat), all closed in `ff2c98e`; closure checked by the orchestrator on the diff. Consumer side checked by the orchestrator: the consumer CLI branches on none of these codes. Approver: squash of the worker branch onto `main`; the only merge conflict was the `[Unreleased]` list of `CHANGELOG.md`, resolved as the union of entries; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/04-protocol.md` § Artifacts; `CHANGELOG.md` `[Unreleased]` › Added (`BrokenTask.code`) and › Fixed; the `BrokenTask` and `FaultHook` doc comments.

**Acceptance.** Implementation: Codex `gpt-5.6-sol` max (`worker:s-store`, strategy `quality`, bus task `pb-run-0909-t20260909-132344`). Review: isolated Claude reviewer under the same task. Findings: PB-146.1 (narrowed to the artifact-side direct read after this task closed the task side), PB-146.2 (`recover()`/`history()` discard `listTasks().broken`), both in triage.
