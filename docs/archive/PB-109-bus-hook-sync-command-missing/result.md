# PB-109 · Result

**Closed 2026-09-09.** Completed. The generated bus hook's header no longer hardcodes `<commandName> sync` as its regeneration command: `templates/bus-hook.mjs` carries `__SYNC__`, and `renderBusHook` in `src/hooks.ts` renders it from the host's existing `syncHint()` member — so a standalone install prints "Generated by `<commandName>` install", the command that actually regenerates the file, and a consumer host prints its own. The install adapter's narrowed hook host forwards `syncHint()`; no host member was added and no command shape expanded. 02-host.md now states that the shipped `bin/promptobus.js` always supplies `commandName: 'promptobus'` and the package version to `createStandaloneHost`, so those two fields in `promptobus.json` never affect the shipped bin — the config policy is unchanged and documented.

**Verification.** Worker commit `6f15147` (worktree of `worker:hooks`, on `main` `3be3f5a`). Reproducer red before the repair: `node --test test/hooks.test.mjs` exit 1, 4/5 — "The input did not match the regular expression /Generated by alpha install/" (the header said "alpha sync"). After the repair hooks 5/5, host 11/11, promptobus-host 17/17. Gates on `6f15147`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 658 tracked files, 119 tarball entries. Mutation probe after the commit, test kept: `host.syncHint()` replaced by the literal "promptobus sync" → the named test exit 1 with the same assertion; restored → 1/1 (rebuilt `dist/` each time). Review: the orchestrator read the diff in full (one template placeholder, one `Pick<>` narrowing, one forwarded member, one reference paragraph, one CHANGELOG bullet, tests) — no isolated reviewer session was spent on it. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** 02-host.md, CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:hooks`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator.
