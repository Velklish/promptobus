# PB-84 · Result

**Closed 2026-09-10.** Completed. The CLI help text says what `effectiveStrategy` already does: for `spawn` and `review`, without `--strategy` the recorded workspace default (`promptobus models strategy`) routes the call, and only when no layer records one does the command take the unrouted path (ADR-004); for `models`, `--strategy` uses the recorded workspace default and falls back to `balanced` only where no layer records one. The help names the behaviour, not the code identifier; the precedence chain stays owned by the CLI reference § Model routing, which was correct and is untouched.

**Verification.** Worker commit `4a59848`, worktree of `worker:docs`, squashed. Reproducer red before the change: the extended '--help names models, --strategy and --allow-payg' case in `test/model-routing.test.mjs` exit 1, "The input was expected to not match the regular expression /Without --strategy\s+nothing\s+is routed and the command takes today's path/"; after the change the case 1/1, the model-routing file 16/16, and `node bin/promptobus.js help` exit 0 with the corrected clauses. Gates on `4a59848`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 694 tracked files, 119 tarball entries (escalated reruns). Mutation probe, test kept: the help fix in `lib/cli.js` reverted → the case exit 1 with the same assertion; restored → 1/1. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** The help text itself in `lib/cli.js`; CHANGELOG entry under Fixed. `docs/reference/03-cli.md` § Model routing needed no change.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:docs`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator, one wording note before the commit (no code identifier on the help screen); approver Павел Ким's orchestrator session.
