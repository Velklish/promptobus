# PB-230 · Result

**Closed 2026-09-17, completed.** The Cursor hook-event list is no longer the five names a live spike happened to prove: `KNOWN_HOOK_EVENTS` in `lib/driver-cursor.js` is the twenty-one-name `_E` table of the `cursor-agent` `index.js` bundle, read on 2026-09-17 from the installed `2026.09.10-fd3934a` with no live session, and the build it was read from is recorded beside it as `HOOK_EVENTS_SOURCE_VERSION` — deliberately a different number from `PROVEN_CURSOR_VERSION` (2026.09.02), since that one dates a lift and this one dates a read. The spike's five stay named as `PROVEN_HOOK_EVENTS` and the suite holds them to a subset of the inventory. The second copy in `lib/install.js` stays a copy under the existing equality assertion rather than an import — the installer would otherwise pull the whole Cursor driver graph in for a list of strings — and the driver suite's `length === 5`, which read a count nobody could derive, is replaced by a check that reads the list: twenty-one distinct names, the recorded build, and the five proven ones inside them. An unknown name is still refused before any write, by name: a `postToolUse` group already in a project's file now installs because the recorded build knows that name, while `postToolUseFailed` for `postToolUseFailure` is still refused.

**The card asked for a list, and review round 1 found what a list cannot do.** The gate compares names and never a version, so on a build where one of the sixteen unproven names is absent, a file carrying it passes the gate and every hook in it dies in silence — the original failure moved one build along. Raising `PROVEN_CURSOR_VERSION` would refuse builds that work today, so what stands instead is a warning and never a refusal: `install` reads the merged event map for names inside the inventory and outside `PROVEN_HOOK_EVENTS` and says the name, the build the inventory was read from, and that another build would kill the whole file without a word. The line is said on four paths, not three — `install`, `--check`, `--dry-run` and `uninstall` share one plan, and `uninstall` leaves a foreign inventory-only group in the file exactly as the other three do, so it warns about it too.

**Verification.** Gates of this acceptance, on the merged tree of this commit: `npx github:Velklish/backslop#v0.8.0 gates` exit 0 — **gates 4, green 4**. `npm test` code 0 over **69 of 69** test files in 157.7 s, **fail 0, todo 0**; `npx github:Velklish/backslop#v0.8.0 lint` code 0, **no errors, warnings 3** — the three pre-existing triage entries whose parents are closed (`PB-139.1`, `PB-188.2`, `PB-225.1`), which are the owner's to move and were not touched; `npm run audit` code 0, clean; `npm run pins` code 0, `#v0.8.0` throughout. The machine record with every count is `gates-approver-pb.json` in this run's journal. The worker's own record on its branch head `0e089170` is `gates-pb-3.json`, where the same set was **gates 4, green 2, not run 1** on a red `npm run audit`: that red was this card's own file naming a consumer's memory service, red on the base commit `51eece97` as well, and it is closed on `main` by the queue commit `f4662705` rather than by this branch — the audit is green here.

**Four mutation probes, each after its round's commit, through `npm run probe` and its own pre-mutation snapshot.** Dropping one inventory name from the driver list reddens the verdict that reads the list (mutated run exit 1; base total 129, passed 128, reddened 1, unaccounted 0). Dropping the same name from the installer's copy reddens the equality assertion that lets the copy stay a copy (exit 1; tests 19, pass 18, fail 1). Making `cursorUnprovenHookEvents` read nothing silences the warning and reddens it (exit 1; tests 21, pass 20, fail 1), while its sibling verdict — that an install of only proven names says nothing about the inventory — stays green under that mutation by construction, which is why both are in the suite. The recorded probe of the last round is the design the review rejected, the warning gated on `!uninstalling` (`lib/install.js:483`, exit 1; tests 22, pass 21, fail 1, unaccounted 0), reddening `uninstall warns about the same inventory-only group, which it does not touch either`. A first attempt at that last probe is declared rather than dropped: written with `\&\&` inside a double-quoted shell string it put literal backslashes into the file, which then stopped parsing — a mutation that ran no check, redone rather than reported as a red.

**Documentation in the same pass.** `docs/reference/03-cli.md` § Cursor hook events — which names are proven live and which are inventory only, and the warning the four paths say. `docs/guides/install.md` — the validation paragraph now says a known-but-unproven name is kept and warned about rather than failing the command, and § After install shows the line, since that block is what a person reads as the whole of the output. `docs/guides/hooks-and-trust.md`. `CHANGELOG.md` under `## [Unreleased]`.

**What is left open.**

- **The inventory is a read of one build, and no live Cursor session was raised for this card.** Sixteen of the twenty-one names are unseen firing here, in `agent -p` or in the IDE; the card put that measurement out of scope and this pass did not add it. Being outside the proven five is not evidence against a name either — `afterMCPExecution` is inventory only here while a consumer outside this package hangs a tracker on it and measured it firing on 2026-08-24, which is that consumer's measurement and not this repository's.
- **The gate still compares names and never a version.** The warning is what stands in place of the refusal that would have cost working builds; a second build that drops an inventory-only name is a case this pass warns about and does not prevent.
