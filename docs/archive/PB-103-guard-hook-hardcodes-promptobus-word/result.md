# PB-103 · Result

**Closed 2026-09-09.** Completed. The loop-guard hook no longer spells the word `promptobus` into the command it writes to project settings: `guardHookCommand` in `src/hooks.ts` quotes the argv the host answers from a new required member, `PromptobusHost.guardArgv(args)`, which returns the complete launch argv for the guard without a leading `node` — the layout entry plus the host's own subcommand path, so a host whose commands live under a prefix answers `[layoutBinPath(), 'promptobus', 'guard', …]` and a standalone host with a configured command name answers `[layoutBinPath(), 'guard', …]`. The default standalone entry keeps the established `promptobus guard` spelling, so every shipped configuration installs a byte-identical command on both platforms: only the identity values (`--role`, `--task`, `--home`) are shell-quoted, as before. The install ownership fallback derives its needle from the same host argv instead of a literal, and the standalone default command name is one constant. 02-host.md § What the host must answer states the member, its return value, the prefixed example and that it is required; the CHANGELOG carries a Changed entry naming the member and the three call sites (`install`, `spawn`, `review`) that throw `TypeError` on a host without it.

**Consumer obligation.** A host implementation outside this repository must add `guardArgv` before pinning the next release, otherwise `install`, `spawn` and `review` fail at hook assembly. The orchestrator carries this to the consumer's own backlog.

**Verification.** Worker commits `1146f3d` (repair) and `b22e5fe` (review follow-up), worktree of `worker:hooks`, squashed. Reproducer red before the repair: `node --test test/install.test.mjs` exit 1, 14/15, at :270 — argv `['promptobus', 'guard']` where `['guard']` was expected for a non-default command name; the test installs through the real path, reads the command back from `.claude/settings.json` and runs the parsed argv through `runPromptobus`. After the repair 15/15; hooks suite 4/4; guard suite 65/65 (a win32 string case added in the follow-up). Gates on `b22e5fe`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 634 tracked files, 119 tarball entries. Mutation probes, tests kept: the standalone `guardArgv` reverted → install test exit 1 (`['bus', 'guard']` vs `['guard']`), restored → 1/1; the quoting fix reverted → guard suite exit 1, 64/65 at the win32 case, restored → 65/65. Review: isolated reviewer (`reviewer:hooks`) — MAJOR: the new required member was invisible to a consumer (Fixed-only CHANGELOG bullet, contract sentences in the standalone section); MINOR: uniform quoting changed the win32 command and would have flagged every existing Windows manifest as drift, the ownership fallback still spelled the literal, the default command name was duplicated; all closed in `b22e5fe`, read by the orchestrator. Confirmed by the reviewer: a bare `guard` dispatches under a non-default name (cli.js shifts argv[0] only when it equals `host.commandName`; the shipped bin forces `promptobus`); the test executes rather than string-matches. Approver: squash of the worker branch onto `main`; no merge conflict beyond the CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** 02-host.md § What the host must answer and § Standalone host, CHANGELOG entries under Changed and Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:hooks`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: Claude `claude-opus-5` xhigh (`reviewer:hooks`, strategy `quality`); the orchestrator read the follow-up.
