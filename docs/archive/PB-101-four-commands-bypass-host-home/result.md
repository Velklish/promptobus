# PB-101 · Result

**Closed 2026-09-09.** Completed. `status`, `history`, `dismiss` and `prune` now take the task-store home from `host.promptobusHome()`, as `spawn`, `review` and `done` already did, instead of rebuilding `<workspaceRoot>/.promptobus` through the adapter-internal `promptobusHome(root, host)`; a host that deliberately places its bus elsewhere no longer gets four commands silently reading an empty journal. The comment in `lib/store.js` now describes what it counts: the adapter-internal assembly is the migration entry `resolveIdentity` uses when a process declares `PROMPTOBUS_HOME`, and commands ask their host for its already chosen home. The host contract (`docs/reference/02-host.md` § What the host must answer) states the duty the swap moves onto the host: a host whose `legacyLayout()` is not `null` MUST run the package-root `preflight()` and, when the plan says so, `migrate()` inside its own `promptobusHome()`, and a preflight refusal surfaces from that member; the standalone host returns its configured `home` and declares `legacyLayout() === null`, so there is nothing to skip. Neither shipped host loses former-layout migration: the orchestrator verified that the ATI host's member routes through its own store module's `ensureStore` → package-root `preflight` and `migrate`.

**Verification.** Worker commits `581e7b7` (repair) and `66626ef` (review follow-up), worktree of `worker:s-store`, squashed. Reproducer red before the repair: `node test/promptobus-host.test.mjs` exit 1, 11/16 — "status reads the store home declared by the host" (no active tasks), "history reads the store home declared by the host" (task is not in the journal), "dismiss writes the store home declared by the host" (dismissed=undefined), "prune reads the store home declared by the host" (nothing to remove), and the static gate naming `dismiss.js, history.js, prune.js, status.js`; after the repair 16/16, after the follow-up 17/17. Focused suites: history 21/21, dismiss 26/26, done/prune 28/28, all exit 0. Gates on `66626ef`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 626 tracked files, 119 tarball entries. Mutation probe after the follow-up commit, tests kept: the four command files restored from the pre-PB-101 commit `194dcb6` → exit 1, 12/17 with the same four command failures and the gate naming all four files; restored → 17/17. Review: isolated reviewer (`reviewer:s-store`) accepted the four call sites and found one major — the contract paragraph granted as an option what the code now requires, and named `lib/store.js`'s helper, which is not in the package's `exports` map, as the host's door — and one minor — the static gate read only the top level of `lib/`, matched one spelling of the argument, and grepped comments; both closed in `66626ef`: the gate now walks `lib/` and `bin/` recursively, strips comments, flags any free `promptobusHome(` call outside `lib/store.js`, and carries a classifier self-check (comment, member call, aliased root). Approver: squash of the worker branch onto `main`; no merge conflict; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/02-host.md` § What the host must answer (the paragraph above); CHANGELOG entry under Fixed with the migration statement for both shipped hosts.

**Acceptance.** Implementation: Codex `gpt-5.6-sol` max (`worker:s-store`, strategy `quality`, bus task `pb-run-0909-t20260909-132344`). Review: Claude `claude-opus-5` xhigh (`reviewer:s-store`, strategy `quality`); the orchestrator read the follow-up and verified the ATI-host claim.
