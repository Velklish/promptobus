# PB-93 · Result

**Closed 2026-09-09.** Completed. Cursor's binary is resolved one way at lift and at stop: `CURSOR_BINS` in `lib/driver-cursor.js` is ordered `cursor-agent`, `agent`, `cursor`, so `findCursorBin` — used by the host at lift and by `stop` — prefers the name Cursor itself installs, and `liveBin` now takes the stop caller's `env` and forwards it to `findCursorBin`, so the binary resolved for `persist stop` and the environment the command runs with are the same object instead of the resolve silently reading `process.env`. The `CURSOR_TOOL` docstring says `cursor-agent` is first in the search order. `findCursorBin` and `liveBin` are exported for the regression, which drives the real resolver with a stub `cursor-agent` beside a stub `agent` and with a caller environment that differs from `process.env`.

**Verification.** Worker commit `f12ee39` (worktree of `worker:drivers`). Reproducer red before the repair: `node test/promptobus-driver-cursor.test.mjs` exit 1, 114/116 — the two PB-93 checks (binary order, caller environment). Gates on `f12ee39`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 1 on the worker's tree for a reason outside PB-93 — the publicity gate flagged the orchestrator's own `PB-63.1` finding file on `main` (`origin CLI name`), repaired on `main` in `ec297a1`; on the integrated tree `npm run audit` exit 0. Mutation probe after commit, tests kept: old binary order and `process.env` lookup restored → exit 1, 114/116; fix restored → exit 0, 116/116. Review: the orchestrator read the diff in full against the task's "Work to do" (reorder, `env` forwarding, docstring) — one screen, no contract change — so no isolated reviewer session was spent on it. Approver: squash of the worker branch onto `main`; the only merge conflict was the `[Unreleased]` list of `CHANGELOG.md`, resolved as the union of entries; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/02-host.md` § Tool binaries (Cursor's stop path re-resolves with the `cursor-agent` preference through `PATH` and the known install directories, under the environment passed to `stop`); `CHANGELOG.md` `[Unreleased]` › Fixed; the `CURSOR_TOOL` docstring.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:drivers`, strategy `balance`, bus task `pb-run-0909-t20260909-132344`). Review: orchestrator.
