# PB-31 · Result

**Closed 2026-09-06.** Completed. Overlay lists merge by union for `deny` and by intersection for `allow` over the layers that state one — an empty intersection is `[]`, not an absent key, and the resolver tells the two apart; a ban written in any layer stands and no higher layer lifts it. Two selectors: `byRole: { worker | reviewer: { allow, deny } }` (a role is a condition of applicability, so it nests rather than sitting beside the kinds) and `flags` (the closed list `MODEL_FLAGS` from `cache.js`, today `no-zdr`, checked after the inventory step because it needs the snapshot's model row; "silence is not absence" both ways — `deny.flags` matches nothing on a tuple with no row, `allow.flags` is not satisfied by it). Provenance became `sources.rules` — every list any layer wrote, in layer order, with its role — so a `denied-by-policy` detail names the rule path and every layer that wrote it and says a ban is lifted only where it was written. `validate` gains `allow-intersection-empty` and `deny-covers-allow` as findings under `overlay-invalid` with a `rule` field (a new named check cannot be a new error code: `models validate` raises `verdict.errors[0].code`), `allow-shadowed-by-deny` as a warning, and `flag-not-in-inventory` joins the decision's warning codes; allow + deny of one name in one layer stays an error; an unscoped pair is reported once with an `at` from the rule that fired. `models validate` prints the deny rules in force with their layers. Overlay `schemaVersion` stays 1 — every file on disk stays valid.

Review (isolated, xhigh, over the working copy): ten findings on PB-31 + PB-30 together, one major (the pace penalty read the unrated `quotaCost`, PB-30's); PB-31's own — the removed `DEFAULTS` import still referenced, duplicated per-role findings with an `at` naming a path nobody wrote, five checks without tests, the unused `rules` parameter, a re-export nobody read, an edit scar — all closed in `249bdc3`; a second pass found the earlier ten closed and three minors (`04-protocol` metadata list, a PB-36 reference the merge resolved, two imports) closed in `e746c97`.

**Verification.** On `main` after the squash merge of the worker tip (which already carried `main` at de6d981): `backslop lint` no errors; `npm run audit` clean, 381 tracked files and the tarball; `npm test` 46/46 files. Worker's mutation probe after `54d8701`: the deny merge back to replacement → "two layers with different deny lists — BOTH bans hold" and two neighbours red; re-confirmed after `249bdc3`.

**Documentation in the same pass.** `docs/reference/03-cli.md` (exclusion/warning codes, the union and intersection rule with a two-layer example, the two selectors, the three checks, step 4a of the resolver), `docs/guides/model-routing.md` (the combine table, the ban-is-final paragraphs, a copyable overlay), `CHANGELOG.md`.
