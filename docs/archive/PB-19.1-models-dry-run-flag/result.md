# PB-19.1 · Result

**Closed 2026-09-05.** Merged into PB-21: the orchestrator decided that `models` has no `--dry-run` (it reads the cache by default and probes only under `--refresh`; `--dry-run` belongs to `spawn` and `review`), and PB-21 corrects the `models` paragraph of 03-cli accordingly. The skill was written on that reading already.

**Verification.** Not applicable — a merge; the paragraph fix is verified in PB-21.

**Documentation in the same pass.** Not required.
