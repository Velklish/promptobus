# PB-140.1 · Result

**Closed 2026-09-10.** Completed. The fourteen seams that kept a `promptobus <verb>` literal as the default of their hint parameter — the store's task-resolution and send refusals (`resolveTaskId`, `sendMessage`), the three drivers' stall routes and the Claude option refusal and late-start mark, the detached Codex holder's status hint, the Claude availability adapter's clear hint, the pure resolver's strategy hint — now take the formatted command from their host-aware callers with no fallback: the hint is required, the tests that call a seam directly pass one, and the literal gate in `test/cli.test.mjs` runs with an empty allowlist as a plain "no literal outside a formatter" check. None of the changed functions is reachable through the package's `exports` map (`.`, `./driver`, `./host`, `./hooks`, `./cli`, `./schemas/*`), and the consumer host calls none of them, so there is no CHANGELOG entry — the card's triage decision.

**Verification.** Worker commit `f415bbb`, worktree of `worker:launch`, squashed. Reproducer red before the repair: `node --test test/cli.test.mjs` with the allowlist emptied, exit 1, 5/6 — `AssertionError [ERR_ASSERTION]: Expected values to be strictly deep-equal` at `test/cli.test.mjs:262`, fourteen actual entries from `lib/codex-session.js:1077: status` to `lib/store.js:930: status`, expected `[]`; after the repair 6/6. Gates on `f415bbb`: `npm test` exit 0, 54/54 test files (an earlier pooled run at load average 204 was 53/54 with `test/promptobus-cursor-wake.test.mjs` red at step 5 — "delivery into a turn IN PROGRESS succeeds" answered by the inject lock still held; standalone exit 0, 18/18, twice; on `main` at `9e44ef0` the approver measured the same step red twice standalone at load 184 — recorded under PB-159.1, not attributed to this change); `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 712 tracked files, 119 tarball entries (escalated reruns). Affected files escalated: 152/152 including `promptobus-warden.test.mjs` 240/240. Mutation probe after the commit, test kept: the fourteen fallback literals restored → `node --test test/cli.test.mjs` exit 1, 5/6 with the same fourteen entries; restored → 6/6. Approver: squash of the worker branch onto `main` (no conflicts against PB-159 and PB-146.1); `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/03-cli.md` (the literal-command gate paragraph: no allowlist). No CHANGELOG entry (see above).

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:launch`, strategy `balance`, bus task `pb-run-0910-t20260910-085454`). Review: orchestrator diff read — the required-hint seams are all called with a formatted hint inside the package (`lib/dismiss.js`, `lib/done.js`, `lib/server.js`, `lib/warden.js`, `lib/review.js`, the routing context), and the holder's status hint is carried in the session record since PB-140; approver Павел Ким's orchestrator session.
