# PB-115 · Result

**Closed 2026-09-09.** Completed. `socketPath` in `lib/codex-session.js` keeps the nested socket under the registry's `sessions` directory, named by the hash of the session ref; when that nested path exceeds the 103-byte `sun_path` limit on Darwin, the `/tmp/pb-cdx-<hash>.sock` fallback now hashes the registry `sessions` directory together with the session ref (`sessionsDir(env) + NUL + ref`), so two registries that run a participant under the same ref no longer resolve to one socket and cannot remove or take over each other's holder. `lockFile` and `holdMain` are unchanged; the holder lock stays per registry. 03-cli § The Codex holder documents the limit, the fallback scope and the per-registry lock.

**Verification.** Worker commit `ce91567` (worktree of `worker:codex`, on `main` `dcf9b6f`). Reproducer red before the repair: `node test/promptobus-driver-codex.test.mjs` exit 1, 86/87 — "long registry homes scope fallback socket paths by registry home", both registries answering `/var/folders/…/T/pb-cdx-d85e9db98847.sock`. After the repair 87/87. Gates on `ce91567`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 634 tracked files, 119 tarball entries. Mutation probe after the commit, test kept: the production hash reverted to `hash(ref)` → exit 1, 86/87 with the same collision assertion; restored → 87/87. No live holder, socket or session was started or signalled: the proof is fixture-based, as the run requires of the Codex worker editing its own driver. Review: the orchestrator read the diff in full (one hash input, one comment, one reference paragraph, one CHANGELOG bullet, one test) — no isolated reviewer session was spent on it. Approver: squash of the worker branch onto `main`; no merge conflict; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** 03-cli.md § The Codex holder, CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:codex`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator.
