# PB-125 · Result

**Closed 2026-09-09.** Completed. `writeEntries` in `lib/model-routing/cache.js` now applies the single `HARNESS_RE` (`^[a-z][a-z0-9-]{0,31}$`, defined and exported from `cache.js` — `validate.js` consumes it internally, no second copy) to every incoming harness key before it is added to the snapshot, so a malformed workspace declaration such as `Claude_Code` never becomes an invalid property key in the schema-validated cache. The call returns `{ doc, dropped }`; preflight names the dropped keys once in a warning that also names the cache file and still returns the probed snapshot, and when every incoming key is dropped no cache file is created. `HARNESS_RE.source` is pinned against the snapshot's `harnesses.propertyNames.pattern` and both overlay `account` patterns in the catalog parity suite, beside PB-124's pairs. The task's boundary is kept: preflight.js and the standalone host still accept the declaration; only the cache boundary rejects it.

**Verification.** Worker commits `cb5d4da` (repair) and `84e86eb` (review follow-up), worktree of `worker:routing2`. Reproducer red before the repair: `node --test --test-name-pattern='writeEntries drops a harness name outside the snapshot property-name grammar' test/model-routing-preflight.test.mjs` exit 1 at :642, "an invalid harness key reached the cache" (actual: the projected entry under `Claude_Code`, expected `undefined`); the written document also had to pass Ajv snapshot validation. After the repair the same command exit 0, 1/1. Gates on `84e86eb`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 625 tracked files, 119 tarball entries; preflight suite 35/35. Mutation probe after the follow-up commit, tests kept: the `HARNESS_RE` guard disabled → `writeEntries drops every invalid harness name without creating an empty cache` exit 1 at :651, "a cache with no valid harnesses is not written"; restored → 1/1. Review: isolated reviewer (`reviewer:routing2`) on `cb5d4da` found four minors — an unused `export { HARNESS_RE }` in validate.js, a silent drop, an unpinned grammar in four literals, and an unpinned all-invalid path — all closed in `84e86eb`; the orchestrator read the follow-up diff in full and confirmed no caller consumed the former return value (`markExhausted` ignores it). Approver: squash of the worker branch onto `main`; no merge conflict; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Findings recorded elsewhere.** The reviewer's re-check of the PB-37.1 catalog data (the same worker) found the two shipped Gemini promotion citations carrying the list blend as `figure` — PB-37.4 (queue); and the open question whether `promotion-expired` should reach routed decisions — PB-37.5 (triage, owner's decision).

**Documentation in the same pass.** `docs/reference/03-cli.md` § Availability cache: the drop, the preflight warning, and the no-file case; CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:routing2`, strategy `balance`, bus task `pb-run-0909-t20260909-132344`). Review: Claude `claude-opus-5` xhigh (`reviewer:routing2`, strategy `quality`) plus the orchestrator on the follow-up.
