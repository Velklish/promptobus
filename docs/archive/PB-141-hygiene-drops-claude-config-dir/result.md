# PB-141 · Result

**Closed 2026-09-07.** Completed. `applyHygiene`'s home branch no longer deletes `CLAUDE_CONFIG_DIR` unconditionally: a nested apply keeps the value while the diverted home that issued it is live. The preserve is a strict equality test against `<live home>/.claude`, not containment — the only caller that can reach the branch is `test/home.mjs`, the only place setting a different config dir is `test/harness.mjs`, which runs inside a test body after the module-load pass and clears the variable itself, and equality preserves exactly the value the `if (home)` branch issues, which is the discipline of the seal branch this was told to mirror. Liveness is tested on `env[HOME]`, not on the config dir: the runner mkdtemps the home and never creates `<home>/.claude`, so testing the latter would have made the preserve dead on arrival.

The probe change went through one review round. The first attempt added the `home.mjs` import to the broad runner probe `b-dom.test.mjs`, which made that probe run `applyHygiene` on its own environment — and since that function sets the warden switch and drops the session-leak list before reaching the home branch, four neighbouring verdicts stopped discriminating the runner and would have passed on a leak. The accepted shape isolates the two passes: `b-dom.test.mjs` imports nothing and measures the runner's environment alone, and a separate `c-nested.test.mjs` carries the nested apply with its own verdict.

**Verification.** The task's reproduction prints the preserved `<tmp>/.claude` instead of `(dropped)`, and a foreign path still prints `(dropped)`. `node test/runner.test.mjs` exit 0, 33/33. Mutation probe: reverting to the unconditional delete gives exit 1, 32/33, with only the nested-preservation verdict red; restored. The pair is isolating in both directions — dropping the preserve branch reddens the nested verdict while the runner-environment verdict stays green, and stopping the runner from setting the variable reddens both. `npm test` exit 0, 48/48 on the branch tip. Reviewed by an isolated reviewer session, which raised the disarmed-verdicts finding above and confirmed its closure.

**Documentation in the same pass.** Header comments in `test/hygiene.mjs` and `test/home.mjs` now describe the preserve and say that any path other than `<home>/.claude` is dropped — the earlier wording said "outside it", which reads as containment and does not match the equality test. `CHANGELOG.md` — an entry under `[Unreleased]`.
