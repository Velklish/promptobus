# PB-208.2 · Result

**Closed 2026-09-17 at the `PB-188` acceptance, completed: the three runs were shortened and the registry was left untouched.** Each of the three now stands at two lines: `heldSync` in `src/fs/lock.ts` (11 lines before), `withDirLockAsync` in the same file (7), and `orphanBlobs` in `src/v1/artifacts.ts` (8). The card's first instruction was to read the sections before writing anything down, and that is what happened — [protocol](../../reference/04-protocol.md) § Store layout already carried every nuance the three blocks were holding: that the nesting licence is the synchronous stretch and nothing wider, that an `await` breaks the premise so asynchronous holders of one path queue instead of inheriting it, and that `blobNamed` counts a hard link beyond the blob file as a name. So the documentation gained nothing and the comments lost the sentences, each keeping a pointer to that section.

**Why it was this commit and not a later one.** The three are the whole of the length gate's offender list on the merged tree, and `PB-188` turns that gate back on in the same commit: before the shortening the walk saw **193** runs against the `seen === 190` floor and named all three. Shortening them made both verdicts green with no edit to `test/fixtures/comment-sweep-pending.json` and none to the literal — the registry only ever shrinks, and three entries added at a merge would have been its first exception.

**Verification.** `node --test test/comment-length.test.mjs` on the merged tree: **13 tests, 13 pass, 0 fail, 0 todo**; the first check's offender list is empty and `seen` is 190. Before the shortening the same file was **13 tests, 11 pass, 2 fail, 0 todo**, exit 1 — `193 !== 190` on the floor, and the list `['src/fs/lock.ts:57 — 11 lines', 'src/fs/lock.ts:161 — 7 lines', 'src/v1/artifacts.ts:243 — 8 lines']` against `[]` on the offenders. Acceptance gates on the merged tree: `npx github:Velklish/backslop#v0.8.0 gates` exit **0**, **gates 4, green 4**, `npm test` exit 0 over 69 of 69 test files with 0 todo.

**Documentation in the same pass.** None was needed: [protocol](../../reference/04-protocol.md) § Store layout already said all three things, which is the outcome the card named as possible. The CHANGELOG entry of `PB-208.1` describes the arrangement these comments point at and is unchanged.

**What is left open.** Nothing of this card. The sixty-three long runs of `lib/store.js` were out of scope and stay in the pending list, which `PB-188` regenerated and which only shrinks from here.
