# PB-73 · Result

**Closed 2026-09-10.** Completed. The publicity audit's `FORBIDDEN` list gained an eighth entry for the bare brand, assembled from fragments like the other seven and bound to context because the brand appears as an ordinary word in evidence cards, in the CHANGELOG and in tests: on the runtime paths (`bin/`, `lib/`, `src/`, `schemas/`, `templates/`, `dist/`, and their `package/` tarball forms) the whole word (case-insensitive) and the camelCase identifier prefix are forbidden; the `<brand>-workspace-<hex>` id shape is forbidden in every tracked text file and tarball entry except `docs/archive/` and `docs/backlog/`, which are historical records. The three leaks are gone: the store's routing helper is the unexported `routingPolicy`, the host contract comment says "the consumer's implementation", and the canary comment describes the id shape without the two captured ids; `dist/host.d.ts` follows from the rebuild.

**Verification.** Worker commit `60264f5`, worktree of `worker:docs`, squashed. Reproducer before the repair, with the entry present and the three fixes absent: `npm run audit` exit 1 naming `lib/store.js` and `package/lib/store.js` (camelCase rule), `src/host.ts` and `package/dist/host.d.ts` (whole-word rule) and `scripts/live-canary.mjs` (id-shape rule). After the fixes: `npm run audit` exit 0, 678 tracked files, 119 tarball entries; `npm pack --dry-run --json` exit 0 still lists `lib/store.js` (55,454 bytes) and `dist/host.d.ts` (12,836 bytes). Gates on `60264f5`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0. Mutation probe after the commit: the entry removed → a focused static assertion "PB-73 audit matcher must retain the context-bound origin-brand entry" exit 1; restored → exit 0. The probe is static because the tool refuses to write the forbidden literals back into tracked files; the behavioural evidence is the pre-fix audit above. No live canary was run. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** CHANGELOG entry under Fixed; the audit script's entries are self-describing and the reference documents do not list them.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:docs`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator, two guidance rounds before the commit (context-bound rules instead of a whole-word entry over every text file; path exemption for evidence cards instead of a Markdown code-span exemption); approver Павел Ким's orchestrator session.
