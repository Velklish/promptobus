# PB-66 · Result

**Closed 2026-09-09.** Completed. In `src/v1/messages.ts` a filesystem refusal is no longer read as a torn record. `recoverTask` reads the intent file in its own `try`: `ENOENT` skips, any other errno is reported as a `BrokenNote` with the errno in `code` and the intent and its lease stay in place for the next attempt while recovery continues through the task's other intents and, under `recover()` without an argument, through the other tasks; only a `JSON.parse` or schema failure still isolates the record into `broken/messages`. `readInbox` treats a non-`ENOENT` read or rename errno the same way — a note for that one ref, the ref stays in the inbox, and the walk returns every message it already moved to history instead of throwing from the middle of it. Non-errno (programmer) errors still propagate. Three fault seams (`intent-read`, `inbox-read`, `history-ref`) inject errnos portably instead of relying on `chmod` semantics.

**Verification.** Worker commits `3748881` + `7f63bec` (worktree of `worker:s-store`). Reproducer red before the repair: `node --test test/v1-engine.test.mjs` exit 1, 113 pass / 3 fail — `AssertionError: the first recovery hid the filesystem refusal` (actual `undefined`, expected `EACCES`), `Error: EACCES: injected inbox read refusal`, `Error: EACCES: injected history rename refusal`; the review round's three intent tests red on `3748881` (`Error: EACCES: injected intent read refusal` escaping `openEngine`), 3/3 green on `7f63bec`. Gates on `7f63bec`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 583 tracked files, 119 tarball entries. Mutation probes after commit, tests kept: the three I/O repairs reverted → exit 1, 0/3 (same assertion and two EACCES escapes); the intent-read `throw` restored → exit 1, 0/3 (`EACCES: injected intent read refusal` in each); fix restored → exit 0, 3/3. Isolated review (Claude `claude-opus-5 xhigh`, `reviewer:s-store`): one major (the intent-read `throw` aborted the whole recovery pass and engine open) and four minor (protocol sentence broader than `peek` honours; stale rename comment; widened domain of `BrokenNote.code` undocumented; unbounded re-knock on an unreadable ref), the first four closed in `7f63bec`, the fifth documented in 04-protocol and recorded as PB-66.1 for the owner's policy decision; closure checked by the orchestrator on the diff. Approver: squash of the worker branch onto `main` at `dd97eef`; the only merge conflict was the `[Unreleased]` › Fixed list of `CHANGELOG.md`, resolved as the union of entries; `backslop lint` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/04-protocol.md` § Message types (consuming read, errno in `BrokenNote.code`, repeated knocks until the condition clears, `recover()` per-record rule); `CHANGELOG.md` `[Unreleased]` › Fixed; the `BrokenNote.code` doc comment.

**Acceptance.** Implementation: Codex `gpt-5.6-sol` max (`worker:s-store`, strategy `quality`, bus task `pb-run-0909-t20260909-132344`). Review: isolated Claude reviewer under the same task. Findings: PB-66.1 (bounded retries — owner decision), in triage.
