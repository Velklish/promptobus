# PB-53 · Result

**Closed 2026-09-09.** Completed. `promptobus install` no longer rewrites the configuration file of a harness it was not asked to install: `mergeHookEvents` now reports whether it actually removed an owned hook group, and the install plan adds a write for an unselected harness only when such a leftover group came off — otherwise the existing file stays byte-for-byte untouched, so a hand-formatted foreign Cursor or Codex file is not re-serialized and does not acquire `hooks`/`version` keys, and `install --check` no longer reports permanent drift for a file it does not manage. When an earlier install's group is removed, the file is written for that cleanup only and `--check` reports it as drift until it is applied. guides/install.md states the byte-identical rule and the cleanup case.

**Verification.** Worker commit `ea32fd8` (worktree of `worker:hooks`, on `main` `b8cdcf4`). Reproducer red before the repair: `node --test --test-name-pattern='install leaves hand-formatted unselected harness files byte-identical and check clean' test/install.test.mjs` exit 1 — the foreign Cursor file was re-indented and the Codex file gained a `hooks` key. After the repair the install suite 18/18. Gates on `ea32fd8`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 649 tracked files, 119 tarball entries. Mutation probe after the commit, test kept: the guard `if (!selected && !merged.removed) continue` inverted → the same test exit 1 with the same diff; restored → 1/1. Review: the orchestrator read the diff in full (one boolean out of `mergeHookEvents`, one guard in the plan, one guide paragraph, one CHANGELOG bullet, one test) — no isolated reviewer session was spent on it. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** guides/install.md, CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:hooks`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator.
