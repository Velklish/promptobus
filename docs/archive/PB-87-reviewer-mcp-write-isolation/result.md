# PB-87 · Result

**Closed 2026-09-10.** Completed under the owner's decision of 2026-09-10 (option C): the reviewer's MCP write isolation is mechanical for the Claude driver and honestly documented as prompt-only for Cursor and Codex. The host contract gained the optional member `participantDenyTools(role)`, which returns the exact `{ server, tool }` pairs the host knows to be write tools of its canonical external MCP servers — never the bus, never inferred from names; the standalone host returns `[]`. `review` asks it only for a driver that declares the new optional capability `mcpDenyTools`, drops the bus server, lets the driver translate the pairs into its own deny syntax and passes the merged list to `prepare`; the Claude driver translates to `mcp__<server>__<tool>` and writes them into `permissions.deny` after its built-in list. The reviewer prompt names the two layers for Claude and states the prompt-only boundary for Cursor and Codex; the bus tools stay available for `result` delivery. The capability snapshot's new optional flag is a contract extension: the schema, model, validator, exact-bound fixtures and 04-protocol carry it, and an older reader of a newer journal gets `schema-version-unsupported`. The remaining design — Cursor's deny syntax, Codex's per-tool path and a completeness signal that lets `review` fail closed — is PB-87.1.

**Verification.** Worker commit `ebc671d`, worktree of `worker:launch`, squashed. Reproducer red before the repair: `node test/promptobus-review.test.mjs` exit 1, 206/207 — "PB-87: Claude reviewer denies the host-classified MCP write tool" (deny list `["Edit","Write","NotebookEdit","Bash","WebFetch","WebSearch"]`); after the repair 212/212, v1-validate 7/7, driver 43/43. Gates on `ebc671d`: `npm test` exit 0, 54/54 test files on the rerun (the first pooled run had `codex-holder-spawn.test.mjs` red alone; standalone 5/5, and the approver measured it green twice on `main` at `0dcdecb` — recorded under PB-159); `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 688 tracked files, 119 tarball entries. Mutation probe, tests kept: the `reviewerDenyTools` call replaced by the built-in list → exit 1, 211/212 with the same assertion; restored → 212/212. Enforcement evidence, measured by the approver with no external write (Claude Code 2.1.263, two headless sessions in the workspace with the bus server attached): `--settings` denying `mcp__promptobus__promptobus_task` withheld the tool from the session's tool list (the model saw only `promptobus_mailbox` and `promptobus_send`, `permission_denials` empty); the control with an empty deny list offered the tool and the call reached the permission engine, which named that id. No live Cursor or Codex probe was run. Approver: squash of the worker branch onto `main` with one conflict in `docs/reference/02-host.md` resolved by adding the member's row to the table PB-112 introduced; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/02-host.md` (the member's row and paragraph, the consumer obligation before its next pin, the compatibility diagnosis), `docs/reference/03-cli.md` § Review (the two-layer guarantee), `docs/reference/04-protocol.md` (the optional capability flag), CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:launch`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator — checkpoint analysis by the worker, decision by the owner Павел Ким, one mid-flight note (the capability flag is a contract extension and must be documented as one); approver Павел Ким's orchestrator session. Consumer obligation: a host exposing canonical external MCP servers must implement `participantDenyTools` before its next package pin, beside `guardArgv`.
