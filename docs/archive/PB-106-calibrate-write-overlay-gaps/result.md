# PB-106 · Result

**Closed 2026-09-10.** Completed. The CLI reference now says what ADR-005 already said and the code already did: `calibrate --write` replaces a hand-set rating on a tuple and field it proposes and keeps every other key and every rating it did not propose. One `writableDocument` guard, shared by `calibrate --write` and `strategy --set`/`--clear`, reads the writable overlay and refuses before any write when the file is present and its JSON is not a plain object (array, `null`, string, number), naming the path and what was found, the way `readLayerFile` refuses invalid JSON; an absent file is still created. The file on disk stays byte-for-byte the person's.

**Verification.** Worker commit `ac69ecf`, worktree of `worker:telemetry`, squashed. Reproducer red before the repair: `node --test --test-name-pattern='present non-object' test/model-routing-calibrate.test.mjs` exit 1, "Missing expected rejection."; the same-field characterisation (proposed `quotaCost` replaces the hand-set one, other keys survive) passed on the baseline as ADR-005 predicts. After the repair: the focused pair 2/2, calibrate 42/42, command 35/35; a manual probe with `[1,2,3]` in the user overlay exits 1 with "routing layer <path> is not a JSON object: found an array" and leaves the file unchanged, and `strategy --set`/`--clear` refuse the same file the same way. Gates on `ac69ecf`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 682 tracked files, 119 tarball entries. Mutation probe, test kept: the guard reverted → exit 1 with "Missing expected rejection."; restored → 1/1. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/03-cli.md` § Commands (the corrected same-field contract for `calibrate --write`; the non-object refusal for both commands), CHANGELOG entry under Fixed. ADR-005 needed no change: its rule already stated the per-tuple, per-rating merge.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:telemetry`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator diff read; approver Павел Ким's orchestrator session.
