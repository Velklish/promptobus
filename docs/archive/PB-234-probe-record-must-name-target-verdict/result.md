# PB-234 · Result

**Closed 2026-09-17, completed.** `mutationProbe.expected` is an optional array of verdict names in `schemas/v1/handover-record.schema.json`, recorded before the run. `probeIntentRefusal` checks it in `recordRefusal`, beside the schema pass rather than inside it — JSON Schema cannot compare two of its own fields, and this repository's reader implements a subset that deliberately cannot either. The check is a subset, not an equality: a neighbouring verdict catching the mutation is evidence the mutation bit, not a defect. The refusal prints both lists in full and names separately those that never reddened, and it branches its diagnosis — nothing declared went red means the probe measured something else, while part of it means the mutation bit and either the remaining verdicts do not cover the broken line or the targets were declared wrong. The worker preamble asks for the field; `handoverEvidence` names a probe that arrived without it, because deleting two lines is otherwise the cheapest escape from a refusal and leaves no trace. The field is optional, which is what keeps records written by the previous version passing; two suites send one without it, so that compatibility is a verdict rather than a claim.

**Verification.** `backslop gates` on the merged tree: **gates 4, green 4**, exit 0 — `npm test` 69/69 files, `backslop lint` 0 errors, `npm run audit` clean across 997 tracked files, `npm run pins` 997/997. The author's own gates ran green on every sha the branch passed through, the last being `248b597b`. Mutation probe on `lib/handoff.js:74`, `!reddened.has(name)` → `false`: mutated run exit 1, 94 of 98 passed, 4 reddened, unaccounted 0 — all four names declared before the run; `git status --porcelain` empty before the probe and after the restore. Verdict names against the base: none removed, 10 added, by static extraction of headings rather than two full runs — the weaker measurement, labelled as such here as it is in the record. Review: three rounds by an isolated reader, 5 findings, then 1, then 0; the round that returned nothing is what ended it.

**Documentation in the same pass.** `docs/reference/04-protocol.md` § The handover record — what the field proves, and the two things it does not: it does not catch a record composed dishonestly after the run, and it does not catch an aborted run, whose declared names can all be present while every verdict below the abort produced nothing. `README.md` — the schemas row listed the gate record and not the handover record. `CHANGELOG.md`.

**What this does not close.** The field covers one corner of a three-cornered shape: the wrong thing reddened. Two corners remain open and both are recorded — a run that aborted partway, whose numbers look complete, and numbers that describe a different invocation than the exit code beside them. Three findings from this run sit in triage: the release skew that keeps a new schema field unsendable until the version carrying it ships, a probe recorded against a tree the hand-off has already left, and the numbers-against-exit-code gap. None is fixed here.
