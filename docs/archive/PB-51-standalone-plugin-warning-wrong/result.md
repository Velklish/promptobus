# PB-51 · Result

**Closed 2026-09-10.** Completed. `participantPluginDir` in `lib/spawn.js` branches on a `null` `pluginDir()` first and returns `null` with no warning: a host that declares no plugin directory is not misconfigured, and no command changes that. The warning stays for a host that does declare a directory whose manifest is absent, and it names the path actually checked — `workspaceRoot()` joined with `pluginManifestRel()` — instead of the hardcoded `.claude-plugin/plugin.json`. `skillsNote` says such a host ships no workspace-skills plugin instead of asserting a directory that was never declared. The host reference says a `null` `pluginDir()` produces no plugin warning. `src/standalone.ts` needed no change.

**Verification.** Worker commit `b62e7e9`, worktree of `worker:launch`, squashed. Reproducer red before the repair: `node test/promptobus-spawn.test.mjs` exit 1, 122/124 — the standalone host printed "⚠ null: plugin directory is missing or has no .claude-plugin/plugin.json manifest … Run promptobus install" and `skillsNote` read "not attached — plugin directory is missing", and a declared-plugin host named the hardcoded manifest rather than the checked path; `node test/promptobus-review.test.mjs` exit 1, 216/217 with the same warning in a standalone dry-run. After the repair 124/124 and 217/217. Gates on the tree before the last edit: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 704 tracked files, 119 tarball entries; the only edit after them was one comment line in `lib/spawn.js`, held by `node --check` on that file and the two test files (exit 0 each). Mutation probe, tests kept: the `dir === null` branch removed → spawn 123/124 and review 216/217 with the warning at `.promptobus/plugin.json`; restored → 124/124 and 217/217. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/02-host.md` (the standalone host's `pluginDir()` is `null` and that is not a warning), CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:launch`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator diff read; approver Павел Ким's orchestrator session.
