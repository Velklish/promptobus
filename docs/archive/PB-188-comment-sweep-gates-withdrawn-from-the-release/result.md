# PB-188 · Result

**Closed 2026-09-17, completed.** The two comment gates are back, and each of the four round-5 bypasses is closed by a check that fails without the repair. **(1) The scanner** is a lexer carrying state across lines — an open block, a string, a template literal with a stack for the `${…}` nested in it, a regular-expression literal, and the continuation after a `*/` followed by more comment on the same line. Against nineteen shapes the new lexer is 19/19 and the old scanner 15/19, red on the trailing comment, on a code line with a trailing comment failing to break a run, on `/* x */ // one` followed by two `//` lines, and on `*/ work();`. **(2) and (3) Both baselines are multisets**: the length list owes a run once per copy, so a second copy of an already-allowed block is refused, and the link baseline counts targets per file, where a `Set` was hiding ten duplicate pointers in nine (file, target) pairs. **(4) The semantic gate** compares `section.file`, requires a real declaration or an enclosing symbol, and refuses to leave a pointer unclassified: 173 pointers, one counted class each, the sum asserted against a walk that does no grouping. The old baseline's certificate of "98 pointers, 0 discrepancies by symbol" was `bound 48 + fileLevel 48 + citation 2` — everything else was skipped uncounted. The pending list was regenerated from the tree as a multiset — **190 runs in 10 files, replacing 1282 entries** of which 1102 no run answered any more — and from there it may only shrink; this card sweeps no debt. `templates/` left `TREES`, four days after its last file went, because a walk over nothing had been reporting the same green as a walk over everything. `PB-188.1`, the fourth wrong binding, was found by the rebuilt gate and closed in the same pass.

**The floor, and why it was half the fix.** Every verdict the length gate carries asserts that a list is EMPTY, and a walk that reads no file fills those lists exactly as a walk that reads all of them does: measured before the repair, emptying the walk outright left **11 of 11 verdicts green**, and so did disabling each of its refusal branches one at a time. The floor — `judged === tracked.length` (85) and `seen === 190` — separates "read nothing" from "read everything" and says nothing about the judging, so four of those five mutations stayed green with it in place. The walk is therefore `surveyTree`, a function the checks drive themselves over real files with a listing written for the occasion, each count compared with a number computed from the same file rather than with `> 0` or a literal. **All five mutations of that measurement are red now.** Found by self-check against this repository's own rule that a gate says what it looked at, not by review.

**Verification.** Gates of this acceptance, run on the merged tree before the commit was made: `npx github:Velklish/backslop#v0.8.0 gates` exit **0** — **gates 4, green 4**; `npm test` exit **0** over **69 of 69 test files**, harness checks **2234 of 2234**, `node:test` **915 tests, 915 pass, 0 fail, 0 todo** — the `todo` count is the point, because these two gate files carried nine of them on base; `npx github:Velklish/backslop#v0.8.0 lint` exit **0**; `npm run audit` exit **0**; `npm run pins` exit **0**.

**Probes, twenty-seven in all, each run after its commit through `npm run probe`** — snapshot restore, never git, `git status --porcelain` empty before each and after each restore, `unaccounted` 0 in every row. Fourteen on `8531124` covering the four bypasses, the block walk, the pending ceiling, the token classifier and the two seam defects of the binding; three on `f9ae2d5` for the refusal classes produced through the walk; three on `a4cae88` for the floor, of which `floor1` is the same mutation that had left 11 of 11 green; five on `f172348` for the judging branches, of which `w-first` keeps one long run out of seventy-six and reddens where `> 0` would have passed; two on `c78f0cb` for the swept-debtor guard, `w-guard` pointing `debtor` at a file with no debt and getting a sentence where a `TypeError` used to come out.

**What the gate does not catch, stated rather than left to be discovered.** It reads the BINDING and never the prose: of the three bindings corrected by hand in the release run, the rebuilt gate is red on two at the commit before they were touched and **green on the third** — `src/legacy-store.ts`, a module contract published as the documentation of `seq` while the comment did stand above `let seq = 0;`. A person reading the section is the only check there is, and [contributing](../../guides/contributing.md) § the sweep says so.

**Found at the acceptance, fixed inside the merge.** The link gate's class split moved when the branch met `main`: seven pointers added by the `PB-208.1` and `PB-217` acceptances — six file/target pairs, `citation` +6 and `bound` +1 — none of them broken, mismatched or unbound. The class counts in `test/fixtures/comment-links-baseline.json` were updated to `citation 36, bound 50`, which is the action the test's own message asks for and the guide already describes: a pointer added since the baseline is reported as a count and allowed. Measured on base `478f37d`: the same two checks are red there, inside their `todo` marks, so the debt was on `main` and masked. The pending list and the `seen === 190` literal were **not** touched; the three long runs the merge left standing were shortened under `PB-208.2` in this same commit.

**Documentation in the same pass.** [contributing](../../guides/contributing.md) § the sweep carries the definition of a comment run and, for each gate, what it catches and what it does not. [drivers](../../reference/05-drivers.md) § `sweepParticipantHomes` is the heading and `Source:` line `PB-188.1` corrected. The CHANGELOG entries are under `## [Unreleased]`, in `Changed` and in `Fixed`.

**What is left open.**

- **The debt itself is untouched**, by this card's scope: 190 long runs in 10 files remain, and the gate only forbids new ones.
- **The gate's scope is narrower than the rule.** It judges `lib/`, `src/` and `bin/`; the owner's rule is repository-wide, so a long comment under `test/` or `scripts/` still rests on the author.
- **A swap at an unchanged count** is invisible to the two ceilings — only the fixture's diff separates it from sweeping, and contributing says so.
- **A division after `)` is read as a division, not a regex.** Harmless unless that regex carries a quote or a comment marker; the tree has no such line today.
- **The walk test is bound to two live files**, `lib/spawn.js` as the debt carrier and `lib/cli.js` as listed-and-clean. Sweeping either breaks it honestly, now with a sentence naming the repair; the repair is a different filename, never a weaker assertion.
- **`PB-188.2` stays in triage** — the `--mutate` replacement half, which cost three probes on this pass.
