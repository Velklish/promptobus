# PB-15 · Result

**Closed 2026-09-05.** Completed. `lib/model-routing/adapter-claude.js`, declared by `lib/driver-claude.js` as `availability: claudeAvailability(PROBE_MODELS)`: one async probe (`spawn` over `planRun`, deadline taken before `resolveToolBin`, stderr not piped, `SIGKILL` on the deadline, the timeout handler answering without waiting for `close` so a grandchild holding the pipe cannot delay the verdict) that runs `claude auth status --json` — the only non-interactive auth check Claude Code 2.1.251 offers — and reads `loggedIn` alone; the e-mail, org and subscription fields it prints never reach the verdict or the disk. Verdicts: no binary `unavailable` / `binary_missing`; logged out `unavailable` / `not_authenticated`; logged in `unknown` / `quota_unknown` with `version` and the alias inventory (`fable`, `opus`, `sonnet` — `claude` publishes no model listing) and no `windows`; an unreadable answer `quota_unknown` "auth could not be verified"; our own kill `probe_timeout`; any other signal or a binary that will not run `probe_failed`. `available` is unreachable for Claude in v1 (no limit source). Late-start hook `markLimitAtStart` on the two lift refusal branches (`sayLimit` in `lib/liftoff.js`, `host: plan.host` in the spawn context of `lib/spawn.js` / `lib/review.js`): a refusal naming a reset writes `subscription_exhausted`, one explaining nothing `manual_exhaustion`, both sticky with `resetAt: null` (no parsing of the harness's words), and the refusal text names the cache file and the two ways to lift the mark. The adapter imports no driver module and stays out of `DRIVER_OWN`; it is in `DRIVER_PRIVATE`. Track `adapter-claude` of the routing run, worker in Claude Code (opus, high, bypassPermissions for the live probe), one isolated review round (7 findings: 6 fixed, the `adapterOf` docstring left to the Cursor track).

**Verification.** `npm test` exit 0 — 42/42 files, 1316/1316 checks; `test/model-routing-adapter-claude.test.mjs` 21/21 on a stub binary. `npm run audit` exit 0 — 283 tracked files and the tarball, clean. `backslop lint` exit 0. Mutation probes, commit-first, 19 in all, every one red with the module loadable: the alias map and the default off the list; argv with a bare word; each refusal state; the mark inventing a reset; the limit gate dropped (every refusal a limit); an unreadable answer read as a logout; the auth answer spread into the verdict; the host guard dropped; `run()` back in place of the spawn (a 20 ms timer beside the probe must still fire); a foreign signal as `probe_timeout`; the reset branch not choosing `subscription_exhausted`; the refusal line dropped or not carried by the lift; a mechanism file importing the adapter; the timeout handler waiting for `close` (red at 5.25 s against an `sh` wrapper holding the pipe). Live probe on this machine: full preflight through the real registry, binary and cache in 1.1 s, `quota_unknown`, mode 0600, no account field on disk; a wrapper holding the pipe → `probe_timeout` at 306 ms against a 300 ms budget. The late-start pattern rests on stubs only — no real limit refusal was captured. Gates re-run on the merged `main` tree by the approver.

**Documentation in the same pass.** `docs/reference/03-cli.md` (`### Claude Code: what its adapter asks` with the command and the key list, never the values; the § Availability intro rewritten generically for adapters landing one harness at a time; why the mark is sticky), `test/promptobus-adapter.test.mjs` (`DRIVER_PRIVATE` with the reason it is not in `DRIVER_OWN`), `test/tmpdir-sweep.mjs`, `CHANGELOG.md` under `[Unreleased]`. Findings filed: PB-15.1 (`HostToolBin.version` read by every driver, declared by none), PB-15.2 (`manual_exhaustion` prose vs code), PB-15.3 (the Codex holder check counts a neighbouring run's processes), PB-15.4 (the contract permits a blocking probe).
