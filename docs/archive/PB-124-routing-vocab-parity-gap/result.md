# PB-124 · Result

**Closed 2026-09-09.** Completed. Every closed routing vocabulary is now pinned against its schema twin in `test/model-routing-catalog.test.mjs`, beside the `MODEL_FLAGS` assertions that were the only such pin before: `WINDOW_KINDS` against the `kind` enums of `snapshot.schema.json` and `telemetry.schema.json`; `TIER_SOURCES` and `TIER_NAME_RE.source` against the snapshot's `tier.source` enum and `tier.name` pattern; `STRATEGIES` against the overlay's `defaults.strategy` enum and the decision schema; `SELECTOR_KINDS` against `selectors` and `roleSelectors`; `WEIGHT_KEYS`; `DECISION_WARNINGS` against `$defs.warningCode.enum` (sorted); and a new named `EXCLUSION_CODES` list in `resolver.js`, gathering the seven strings that were scattered across `policyExclusion`, `constraintExclusion` and `exclusionOf`, against `$defs.exclusionCode.enum`. `TIER_NAME_RE` and `WEIGHT_KEYS` are exported for the test; no vocabulary value and no schema changed, so there is no CHANGELOG entry, per the task.

**Verification.** Worker commit `8716adc` (worktree of `worker:routing`). Reproducer red before the repair: `node --test --test-name-pattern='routing vocabularies and their schema enums stay one closed list' test/model-routing-catalog.test.mjs` exit 1 at :725, "cache.js must export TIER_NAME_RE for schema parity". Gates on `8716adc`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 622 tracked files, 119 tarball entries; catalog suite 53/53. Mutation probe after commit, test kept: `'daily'` added to `WINDOW_KINDS` with the schemas untouched → exit 1 at :729 (schema `[session, weekly, monthly]` versus code `[…, daily]`); restored → 1/1. Review: the orchestrator read the diff in full against the task's "Work to do" (three exports, one gathered list, nine pairs, no behaviour change) — so no isolated reviewer session was spent on it. Approver: squash of the worker branch onto `main`; no merge conflict; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** Not required: a test-only change with all nine pairs already agreeing; the doc comment on `EXCLUSION_CODES` names its purpose.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:routing`, strategy `balance`, bus task `pb-run-0909-t20260909-132344`). Review: orchestrator.
