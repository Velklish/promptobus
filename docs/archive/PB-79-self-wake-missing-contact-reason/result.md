# PB-79 · Result

**Closed 2026-09-10.** Completed. The warden's no-contact-point branch now has the same reason comparison as its two siblings: a fixed reason string, a guard on `channel !== 'self-wake' || knockError !== why`, and inside it `channel`, `knockError` and `wake = null` set unconditionally. A participant whose knock was refused and whose contact point then disappeared gets one journalled transition to self-wake, its health mark carries the current reason instead of the stale refusal and the dead fingerprint, and `status` prints that reason through its existing self-wake renderer; `lib/status.js` needed no change.

**Verification.** Worker commit `ad38191`, worktree of `worker:warden`, squashed. Reproducer red before the repair: `node --test test/promptobus-warden.test.mjs` exit 1, 238/240 — "a vanished contact point logs the no-contact-point fallback" (events `[]`) and "a vanished contact point clears the stale knock error and fingerprint" (health kept `channel: self-wake`, the old socket in `wake`, `knockError: ENOENT`). After the repair the same file exit 0, 240/240; `npm run build` exit 0. Gates on `ad38191`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 677 tracked files, 119 tarball entries. Mutation probe, test kept: the branch restored to the channel-only guard, rebuilt → exit 1, 238/240 with the same two assertions; restored, rebuilt → 240/240. Open: no live harness run; the fixture is the evidence. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/03-cli.md` § Guard and warden (a stale refusal is replaced by the no-contact-point reason and `status` shows it), CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:warden`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator diff read against the card (the change is confined to the one branch the card names); approver Павел Ким's orchestrator session.
