# PB-159 · Result

**Closed 2026-09-10.** Completed. `test/model-routing-preflight.test.mjs` runs in the serial group of `test/run.mjs`, after the pool and without neighbours, with its rationale beside the other serial files (its elapsed guards — a 200 ms budget, a 400 ms in-flight probe, the 1 s memoised resolve, the budgeted resolves — measure the machine's neighbours; the counted `ticks` check is sound under load and moves with the file); the runner's end summary now keeps the last twenty lines of a red file's output under `output tail:`, so a pooled red names its assertion instead of only its exit code; `docs/guides/contributing.md` § gates lists the six serial files and why. The assertions themselves were not rewritten, as the card's Decision said.

**Verification.** Worker commit `8347ed2`, worktree of `worker:verify`, squashed. Reproducer before the repair: a scratch copy of the runner with one deliberately red file exited 1 and its summary read only `✖ .pb159-red-probe.test.mjs — code 1` while the assertion `AssertionError [ERR_ASSERTION]: PB-159 summary probe assertion` sat in the per-file output; after the repair the same run exited 1 with `output tail:` and that assertion in the summary. Focused: `node --test test/model-routing-preflight.test.mjs` exit 0, 35/35; `node --test test/runner.test.mjs` exit 0, 33/33. Under load — four worker suites on one eight-core machine, no extra runs started — three consecutive `npm test`: load 174.86/169.88/152.28 → exit 1, 53/54 (the red was `test/model-routing-adapter-cursor.test.mjs:226`, `probe_timeout` for `quota_unknown`); 159.93/156.68/154.06 → exit 0, 54/54; 114.83/166.01/161.81 → exit 0, 54/54; the preflight file 35/35 in all three. The red file standalone: exit 0, 46/46, twice — recorded as PB-159.1 with the other reds of this run. Gates on `8347ed2`: `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 712 tracked files, 119 tarball entries (escalated reruns after sandbox refusals on the keychain and `dist/`). Mutation probe after the commit, probe kept: the tail repair reverted → the summary again omitted `output tail:`; restored → `runner.test.mjs` 33/33. No CHANGELOG entry: test infrastructure and a guide. Approver: squash of the worker branch onto `main`; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/guides/contributing.md` § gates; the serial-group rationale comment in `test/run.mjs`.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:verify`, strategy `balance`, bus task `pb-run-0910-t20260910-085454`). Review: orchestrator diff read; approver Павел Ким's orchestrator session. Finding: PB-159.1 (wall-clock reds in other files under the same load), in triage.
