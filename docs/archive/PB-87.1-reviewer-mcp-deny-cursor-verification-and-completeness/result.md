# PB-87.1 · Result

**Closed 2026-09-10.** Completed under the owner's decisions of 2026-09-10. Codex: the reviewer's MCP write isolation is mechanical for the classified tools — the Codex driver declares `mcpDenyTools`, keeps the host's `{ server, tool }` pairs as structured deny rules through `prepare`, and `mcpConfig` attaches each tool to its server entry as `disabled_tools`, which `codexMcpServers` in the holder now carries through the prefixed `config.mcp_servers` it sends on `thread/start`. Completeness: `participantDenyTools(role)` answers `{ tools, complete }` (`HostMcpToolClassification`); the standalone host answers `{ tools: [], complete: true }`; `review` refuses before any launch file is written when a mechanical driver meets an incomplete answer, or no member on a host that declares canonical external servers, naming the host and the unclassified servers. Cursor stays prompt-only, its live verification deferred as PB-87.2. The no-turn measurement on codex-cli 0.146.0 is `test/fixtures/codex-app-server/0.146.0/McpServerStatusList-0.146.0-2026-09-10.json`: `initialize` and `thread/start` accepted a thread-scoped `config.mcp_servers` entry carrying `disabled_tools` and the server reported `ready`; the thread-scoped `mcpServerStatus/list` did not reply within the bounded window, so the capture proves configuration acceptance and readiness, not a returned tool list — 03-cli § Review says exactly that. 02-host carries the member's new shape and the consumer obligation before its next pin.

**Verification.** Worker commits `2d926e1` + `bbdd103` (security-review follow-up), worktree of `worker:launch`, squashed. Reproducers red before the repair: the Codex driver file exit 1 — `PB-87.1: Codex carries the classified MCP tool into prefixed disabled_tools`, the translated server config carrying only its `url` and no `disabled_tools`; the review file exit 1, 220/221 — `PB-87.1: Codex refuses an incomplete external MCP classification before launch` (the plan launched with no refusal). Gates on `2d926e1`: `npm test` exit 0, 54/54 test files (load average 20.7 at start — the last worker on the machine); the Codex driver file 140/140 with the capture and `disabled_tools` checks; the review file 222/222 with the complete/incomplete classification checks; `npm run build` exit 0; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 742 tracked files, 119 tarball entries (escalated runs). Mutation probe after the commit, tests kept: the `disabled_tools` assignment in `mcpConfig` removed → the Codex driver file exit 1, 139/140, `PB-87.1: Codex carries the classified MCP tool into prefixed disabled_tools` red with the server config showing no `disabled_tools`; restored → 140/140. Gates on `bbdd103`: the review file 224/224, the Codex driver file 140/140, `npm test` exit 0, 54/54 test files (load average 10 at start), lint exit 0, 0 errors, `npm run audit` exit 0, 742 tracked files, 119 tarball entries. Probe on the follow-up, tests kept: the declared set switched back to `mcp.external` → the review file exit 1, 222/224 (`Codex refuses a host with declared servers and no classification member` and `Codex refuses an incomplete external MCP classification before launch` red); restored → 224/224. Isolated security review (`reviewer:launch`, Claude `claude-opus-5` xhigh), two rounds: round 1 — one critical (the completeness gate read `HostServers.external`, the set of servers that did NOT travel to the participant, so a host without the member whose servers all travel launched fail-open under a prompt claiming the mechanical layer, and the refusal named the wrong servers; the fixture host had listed one server in both maps), two major (the reviewer prompt asserted mechanical denial the fixture did not measure; the Codex layer covers only mechanism-supplied server entries while app-server lifts the owner's personal MCP set beside them) and four minor (the refusal also stopped a re-review; the breaking `{ tools, complete }` migration unnamed; a degenerate refusal text; the compat paths untested) — all closed in `bbdd103`: the declared set is what travels (`mcp.descriptor.servers` minus the bus), `HostServers.external` is documented as "did not travel", the boundary sentence is each driver's own `phrases.mcpBoundary` (Codex's says configuration accepted, enforcement unobserved, personal set under the prompt layer), the refusal skips `plan.reuse`, 02-host and the CHANGELOG name the breaking shape, the two compat paths are tested; round 2 — all seven closed, nothing blocking; two minor and two nits: a re-review under a regressed host answer dropped the refusal without a word (a warning added at integration), the required `phrases.mcpBoundary` driver member unnamed in the CHANGELOG (named at integration), the prose trigger narrower than the code's ("every server the host hands the participant", both references), a dangling antecedent (left). The turn-level measurement of `disabled_tools` enforcement and the personal-set override are PB-87.3, filed at acceptance. Approver: squash of the worker branch onto `main`; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/02-host.md` (the member's row and paragraph), `docs/reference/03-cli.md` § Review (the Codex layer, the fixture, what was and was not measured), `test/fixtures/codex-app-server/0.146.0/README.md`; `CHANGELOG.md` `[Unreleased]` › Changed (the member's shape) and › Fixed (the Codex layer; the PB-87 line reworded).

**Acceptance.** Decisions: the owner, Павел Ким, 2026-09-10 (Codex through `disabled_tools`; Cursor deferred; completeness signal). Implementation: Codex `gpt-5.6-luna` max (`worker:launch`, strategy `balance`, bus task `pb-run-0910-t20260910-085454`). Review: isolated Claude reviewer under the same task; approver Павел Ким's orchestrator session. Consumer obligation: a host exposing canonical external MCP servers must implement `participantDenyTools` in the `{ tools, complete }` shape before its next package pin.
