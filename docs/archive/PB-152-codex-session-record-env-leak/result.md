# PB-152 · Result

**Closed 2026-09-09.** Completed with the owner's choice of the card's first option. The Codex session record written at `spawn` no longer carries `childEnv`, the copy of the caller's whole shell environment that only the detached holder read, once, when it spawned `codex app-server`. `startHolder` now receives `sessionEnv(env)` — the same sanitized environment as before, with `CODEX_HOME` dropped and `PROMPTOBUS_CODEX_HOME` set — and `holdMain` passes its own inherited environment to `app-server`; the drop list is unchanged (an allowlist is the card's Out of scope). Both bare `mkdirSync` calls in `lib/codex-session.js` create their directories with mode `0700`, so the sessions directory no longer lists active participants to other local accounts; an existing directory is not re-chmodded, and 03-cli § The Codex holder says so. The sidecar option (a `0600` snapshot unlinked after the spawn) was not taken: a spawn-time snapshot is not required, the holder's inherited environment is equivalent.

**Verification.** Worker commit `b363cc4` (worktree of `worker:codex`, on `main` `1c21587`). Reproducer red before the repair, twice: `node test/promptobus-driver-codex.test.mjs` exit 1, 88/90 — "the session record does not persist the caller environment" (the record listed `childEnv`) and "a freshly created Codex sessions directory is private" (mode `755`). After the repair 90/90; the harness stand records the app-server environment so the test asserts `CODEX_HOME` absent and `PROMPTOBUS_CODEX_HOME` present; the mode assertion is skipped on win32 with a named reason. Gates on `b363cc4`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 637 tracked files, 119 tarball entries. Mutation probe after the commit, tests kept: the production code restored to its parent → exit 1, 88/90 with the same two assertions; fix restored → 90/90. `rg childEnv lib/ src/` finds no reader after the fix. No live holder or session was started or signalled. Review: the orchestrator read the diff in full (two `mkdirSync` modes, one `startHolder` argument, one `holdMain` env, one record field removed, reference paragraph, CHANGELOG, tests) — no isolated reviewer session was spent on it. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** 03-cli.md § The Codex holder, CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:codex`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator; option chosen by the owner, Павел Ким.
