# PB-138 · Result

**Closed 2026-09-10.** Completed. The publicity audit resolves its root from `import.meta.url` through `fileURLToPath` instead of `import.meta.dirname`, so it runs on Node 20.0–20.10 as well as on the current line, and it launches `npm` and `tar` through the shared process runner `run` of `lib/exec.js`, so Windows resolves the `.cmd` wrapper the same way every other child does. A review follow-up added `checkRun`: build, pack, extract and list each require no launch error and exit status 0, the failure names the command, and a timeout is labelled as such; before it, a failed `npm run build` could leave the audit printing a clean verdict over an empty tarball. No change to `lib/exec.js` was needed.

**Verification.** Worker commits `dd10538` and `d5d4112`, worktree of `worker:verify`, squashed. Reproducer red before the repair: `node test/promptobus-package.test.mjs` exit 1, 36/38, twice — "publicity audit derives its root through import.meta.url — audit-public.mjs must use path.dirname(fileURLToPath(import.meta.url))" and "publicity audit launches npm and tar through the shared process runner — raw launchers: npm, npm, tar, tar". Gates on `d5d4112`: `npm test` exit 0, 54/54 test files (package test 39/39); `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 671 tracked files, 119 tarball entries. Mutation probe, test kept: `scripts/audit-public.mjs` restored to `dd10538` (no `checkRun`) → exit 1, 38/39, twice, "publicity audit refuses a failed build — tarball:  · 0 entries" while the child printed a clean verdict; restored → 39/39. Open: the run was on macOS, Node 25.2.1; a Windows or Node 20.0–20.10 run was not performed and no CI matrix cell was added (out of the card's scope). Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** CHANGELOG entry under Fixed; the reference documents do not describe the audit's launch mechanics, so no reference change was required.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:verify`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator diff read, one round (the `run()` status was unchecked after build, pack and extract — fixed in `d5d4112`); approver Павел Ким's orchestrator session.
