# PB-102 · Result

**Closed 2026-09-09.** Completed. The mixed-version diagnosis is wired: `bus()` in `lib/store.js` caches engines by store home and reader version and refuses an unversioned first open as a programming error instead of silently selecting `0.0.0`; every entry point selects `host.version` for the home it actually works with — CLI dispatch for the host-home commands (`spawn`, `review`, `status`, `done`, `dismiss`, `history`, `prune`), and the warden, the guard (right after `resolveIdentity`, so a session whose `PROMPTOBUS_HOME` differs from the host's home is read with the right vocabulary) and the MCP server bootstrap for the home they resolve. A low-level helper reaching an unselected home warns once per home ("unversioned reader"). `createTask`, `claimOwnership` and the automatically registered foreign sender stamp the selected writer version on their participant records, warn with the home and record when none was selected, and never invent a release — the synthetic `0.0.0` default is gone, and the CHANGELOG records the changed record shape. `cmpVersion` in `src/v1/store.ts` compares the numeric core after cutting prerelease and build tails while the diagnosis keeps the original strings, so an unfamiliar field written by a newer plain, prerelease or build version returns `schema-version-unsupported` naming both versions instead of generic journal corruption. The auto-registered-sender finding was found and closed by the worker inside this task; the boundary fork (per-command edits versus two doors) was decided by the orchestrator for the doors.

**Verification.** Worker commits `492bdad` + `b730e67` (worktree of `worker:s-store`). Reproducers red before the repair: `node test/promptobus-adapter.test.mjs` exit 1, 59/61 — plain `0.6.0` and prerelease `0.6.0-rc.1` writers returned `task-broken` "journal does not match the schema: participants[0] extra fields: brandNewField"; the folded sender case 64/65; the review round's guard case `node test/promptobus-guard.test.mjs` exit 1, 63/64 (`bus: reader mechanism version is required on the first open of …/mixed-reader-declared-home`), adapter 66/68 (empty warning lists). After the repair: adapter 68/68, guard 64/64, warden 236/236, MCP 106/106, mcp 35/35, cli 4/4. Gates on `b730e67`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 620 tracked files, 119 tarball entries. Mutation probes after each commit, tests kept: runtime files restored from the parent → adapter exit 1 (59/65, then 66/68) and guard exit 1 (63/64) with the exact assertions above; fix restored → green. Isolated review (Claude `claude-opus-5 xhigh`, `reviewer:s-store`): one major (the guard resolved its own home while the door primed the host's, so worker and reviewer sessions read with a `null` reader) and two minor (the dropped synthetic version unrecorded and silent; the doors list incomplete), all closed in `b730e67`; closure checked by the orchestrator on the diff. Approver: squash of the worker branch onto `main`; the only merge conflict was the `[Unreleased]` list of `CHANGELOG.md`, resolved as the union of entries; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/04-protocol.md` § Engine (the per-entry-point rule, the record stamping and the diagnosis); `CHANGELOG.md` `[Unreleased]` › Fixed (two entries); the comments at `engine`, `bus` and `participantRecord`.

**Acceptance.** Implementation: Codex `gpt-5.6-sol` max (`worker:s-store`, strategy `quality`, bus task `pb-run-0909-t20260909-132344`). Review: isolated Claude reviewer under the same task. Out of scope by the task: existing on-disk records are not restamped.
