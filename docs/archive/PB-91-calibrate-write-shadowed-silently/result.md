# PB-91 · Result

**Closed 2026-09-10.** Completed. `models calibrate --write` now checks, before it writes, every proposed `(tuple, rating)` pair against the layers declared above `user` in the host's routing paths; after a successful write it warns in the phrasing of `strategy --set`, naming the highest shadowing layer and the shadowed pairs, and the `--json` document carries the same set as `write.shadowedBy: [{layer, tuple, ratings}]`. The merge in `applyOverlayToTuples` now records per-tuple, per-rating provenance in `sources.ratings`, so a calibration rating line names the overlay the "overlay" value actually came from instead of assuming it is the writable one; pure callers without provenance keep the old wording. The target layer stays `user` (ADR-005).

**Verification.** Worker commit `3151d40`, worktree of `worker:telemetry`, squashed. Reproducer red before the repair: `node --test --test-name-pattern='names higher-layer rating shadows' test/model-routing-calibrate.test.mjs` exit 1, "Expected values to be strictly equal: 0 !== 1" (no shadow warning). After the repair: calibrate 41/41, catalog 59/59. Gates on `3151d40`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 675 tracked files, 119 tarball entries. Mutation probe, tests kept: the production hunks of the three files reverted → the same test exit 1 with `0 !== 1`; restored → exit 0. Approver read: a missing higher layer reads as no data (`readLayerFile` returns `present: false`), the highest layer wins per pair, and the warning goes to the console so the JSON document stays one document. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** ADR-005 § `models calibrate`, `docs/reference/03-cli.md` § Commands (the warning beside `--write`, the source label on rating lines), `docs/reference/02-host.md` § The writable layer (the `user`-layer exception carries the shadow warning), CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:telemetry`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator diff read; approver Павел Ким's orchestrator session.
