# PB-189 · Result

**Closed 2026-09-17, completed.** A rebuilt provenance header no longer signs itself with the reader's copy, and the discriminator the card rests on is finally pinned by a verdict. `provenanceFromRecord` filled `packagePath`, `packageVersion` and `packageIssue` from the module-level `OWN_PACKAGE` of whichever copy happened to be reading, so a session record written by another copy — an earlier release that had no such fields — came back with a holder-journal header naming a copy that never raised it. Measured from the tree before the fix: `provenanceLine(provenanceFromRecord({}))` printed `package=<worktree>/package.json@0.11.0`. The reconstruction now reports `package=unresolved (the record does not name the copy that wrote it)`. The lift path did not move: `launchProvenance` reads `import.meta.url` of the executing copy and always fills the fields, so a live lift line and a fresh record are unchanged.

**The writing half was already there; what was missing was a check that could fail.** All five existing provenance verdicts asserted the TREE's own `package.json` path while running from the tree, which is true of *any* resolution at all — including a wrong one taken from `cwd` or from the record — so nothing pinned the discriminator the card names. A second copy of `lib/provenance.js` is now run from another directory and has to name itself (`0.0.0-other`, against the tree's own version), and the reconstruction branch gets a verdict of its own. That copy is one file, which works only while `lib/provenance.js` imports nothing local; review round 1 asked for that failure mode to be a verdict rather than a dead file, so the import is wrapped and a local import added there lands as one red verdict naming the cause instead of an `ERR_MODULE_NOT_FOUND` that takes the whole file down.

**Verification.** Two mutation probes aimed at this card, both run after the commit through `npm run probe`, `git status --porcelain` empty before each and after each restore: putting the reader fallback back (`lib/provenance.js:91`) — mutated run exit 1, 161 of 162 verdicts passed, 1 reddened, 0 unaccounted; taking `PACKAGE_PATH` from `process.cwd()` (`lib/provenance.js:5`) — exit 1, 161 of 162, 1 reddened, 0 unaccounted. The second probe's red text is the card's own incident in miniature: the second copy reported `package=<worktree>/package.json@0.11.0`, the tree's path at the tree's version, while executing from somewhere else. Verdict names base ↔ branch on `test/promptobus-spawn.test.mjs`: 160 → 162, none removed. Gates of this acceptance, on the merged tree of these two commits: `npx github:Velklish/backslop#v0.8.0 gates` exit 0 — **gates 4, green 4**. `npm test` code 0 over **69 of 69** test files, **fail 0, todo 0**; `npx github:Velklish/backslop#v0.8.0 lint` code 0, **no errors, warnings 3** — the three pre-existing triage entries whose parents are closed (`PB-139.1`, `PB-188.2`, `PB-225.1`), which are the owner's to move and were not touched; `npm run audit` code 0, **clean** over 989 tracked text files and 136 packed entries, where the worker's branch had it red on the one pre-existing finding it was told not to carry; `npm run pins` code 0, `#v0.8.0` throughout, 989 of 989 tracked files read. The worker's machine record is `gates-pbq-3.json` and the acceptance's is `gates-approver-pbq.json`, both in this run's journal.

**Documentation in the same pass.** `docs/reference/05-drivers.md` § provenance — a record that carries no package field reports `unresolved`, and why a check run from the tree does not demonstrate that the field follows the executing copy. `docs/guides/contributing.md` § what the stands prove — a live measurement without the executing copy recorded is not attributable. `CHANGELOG.md` under `## [Unreleased]` → `### Fixed`.

**What is left open.**

- **The live lift the card was returned to the queue for was not made here.** `ls node_modules/promptobus` in the worker's worktree returns nothing — there is no installed copy to raise a participant from — and a live lift is a paid, outward-facing step. The check is closed by fixture and probe; the card's live half rests on the orchestrator, which reports that the consumer's spawn today already prints `package=…/node_modules/promptobus/package.json@0.11.0`, the installed copy's own path rather than the tree's. That measurement was not made by this acceptance and is not evidence this record holds.
- **A version alone still does not separate two copies.** Path is the discriminator, by construction and now by verdict; a `package=…@<version>` in a lift line settles nothing on its own, since the tree copy and the installed copy report the same number.
- **Exact code identity stays out of reach.** A path and a reported release do not distinguish two revisions at that same location, including an in-place replacement. The new fields deliberately do not claim that stronger fact, and `PB-177` is a different reason for the same surprise.
