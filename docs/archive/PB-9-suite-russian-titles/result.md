# PB-9 · Result

**Closed 2026-09-05.** Completed. The suite speaks English: `check()`, `test()` and `describe()` titles and details and every comment block in 24 files under `test/**` translated one to one — the same files, the same order, the same verdict count per file (adapter 59, ambient 8, copy 10, dismiss 26, done 28, guard 63, harness 22, history 21, legacy-fixture 27, mcp 110, review-ladder 5, review 192, spawn 99, warden 236, worktree 64, driver-cursor 104, driver 40, mcp.test 35, migration 69, races 12, store 30, v1-engine 113, v1-races 26, v1-validate 5, tmpdir-sweep 17, each identical before and after). Fixtures that stand in for a human (task titles, message bodies, briefs, the frozen `legacy-v061` snapshot) stay Russian by the entry's own rule; fixtures that stand in for mechanism output (driver error and reason strings, stand turns, fault-injection messages) follow the real English text of `lib/`. Seven checks in `promptobus-mcp.test.mjs` that grep a hint the mechanism no longer prints are marked dead in place rather than rewritten, and listed in PB-9.1 beside the two in `promptobus-host.test.mjs`. Track `english` of the routing run, worker in Claude Code (sonnet, high) with one subagent per file, one isolated review round (6 findings, all fixed) and a rebase onto the `robust` track's merge. The `«…»` quotes left in `test/harness-cursor.mjs` and `test/harness.mjs` wrap English words — a convention, not a gap.

**Verification.** `npm test` exit 0 — 46/46 files, every per-file count equal to the baseline (the three files the `robust` track extended carry their new counts: host 8, runner 32, driver-codex 57, cursor-wake 18). `npm run audit` exit 0 — 318 tracked files and the 112-entry tarball. `backslop lint` exit 0. No mutation probe: no check's logic changed; the reviewer balanced the diff (782 `check(` removed / 782 added, 76/77 `test(` with the +1 in prose). Gates re-run on the merged `main` tree by the approver.

**Documentation in the same pass.** `CHANGELOG.md` under `[Unreleased]`. Finding filed: PB-9.1 (dead localisation branches in two test files).
