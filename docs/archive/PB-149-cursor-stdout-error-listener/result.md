# PB-149 · Result

**Closed 2026-09-09.** Completed. `runOut` in `lib/model-routing/adapter-cursor.js` now registers `child.stdout.on('error', () => {})` beside its other stdout listeners, with the same explanation the Claude and Codex adapters carry: `close` already carries the outcome, so the listener needs no body — it exists only so an unhandled stream error cannot reach the process and crash a routed command instead of yielding a verdict. `docs/reference/03-cli.md` § Availability states the invariant once for all three adapters.

**Verification.** Worker commit `2d595b7` (worktree of `worker:routing2`). Per the task, the acceptance is static: before the change `rg -n "child\.stdout\.on\('error'" lib/model-routing/adapter-cursor.js` exit 1 (no match) while the Claude and Codex adapters matched; after the change it names the listener at line 478; reproducing the stream `'error'` emission is out of scope (PB-17 measured this class of guard as unforceable on Node 25.2.1), so no behavioural test was added. Gates on `2d595b7`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 613 tracked files, 119 tarball entries. Mutation probe after commit: the listener removed → the static check exit 1; restored → exit 0. Review: the orchestrator read the diff in full against the task's "Work to do" (one line, one reference sentence, one CHANGELOG entry) and confirmed the listener matches the Claude adapter's pattern, so no isolated reviewer session was spent on it. Approver: squash of the worker branch onto `main`; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/03-cli.md` § Availability: the adapter, the preflight and the cache; `CHANGELOG.md` `[Unreleased]` › Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:routing2`, strategy `balance`, bus task `pb-run-0909-t20260909-132344`). Review: orchestrator.
