# PB-105 · Result

**Closed 2026-09-09.** Completed, on the owner's decision of 2026-09-09 (option (a)): the near-limit signal proposes `economy` from the same set the line is raised on. In `lib/model-routing/resolver.js` a paced harness is **short** when either test is true — its binding window at or past `nearLimit.usedPercent` or its underspend below `nearLimit.underspend` — and `economy` is proposed when `short.length === paced.length`, `balance` otherwise; the level-only `over` set and its predicate are gone, and the comment that was false for a set short only by rate is corrected. Under the default `--strategy balance` the case "every account ahead of its pace, none at the level threshold" now prints its three lines proposing `economy` instead of nothing. ADR-004 § The near-limit signal says explicitly that "past the threshold" is the union of the two tests and carries a dated PB-105 amendment recording the owner's decision; the 03-cli near-limit row and `skills/orchestrate/SKILL.md` agree with the predicate. The golden pair moved from `Propose --strategy balance` to `economy` as a consequence of the rule, and the fixtures README explains why.

**Verification.** Worker commits `2f15770` + `b4840af` (worktree of `worker:routing`). Reproducer red before the repair: `node --test --test-name-pattern='rate-only short harnesses propose economy when balance is running' test/model-routing-resolver.test.mjs` exit 1 at :1247, `0 !== 3`, on the new fixture `near-limit-rate-snapshot.json` (claude 78 % / 40.48 % elapsed → −37.52; codex 79 % / 62.5 % → −16.50; cursor 70 % / 47.92 % → −22.08 — the task's own measurement). Gates on `b4840af`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 619 tracked files, 119 tarball entries; targeted near-limit set 7/7, resolver suite 91/91. Mutation probe after `2f15770`, test kept: the level-only predicate restored → exit 1, `0 !== 3`; union restored → 1/1 (`b4840af` is documentation only). Isolated review (Claude `claude-opus-5 xhigh`, `reviewer:routing`): one major — option (a) read as taken on the task's own authority, answered by recording the owner's decision — and two minor (the fixtures README contradicting the new golden output; the ADR section rewritten without an amendment mark), both closed in `b4840af`; closure checked by the orchestrator on the diff. Approver: squash of the worker branch onto `main`; the only merge conflict was the `[Unreleased]` list of `CHANGELOG.md`, resolved as the union of entries; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/adr/adr-004-subscription-balance.md` § The near-limit signal (definition of short, PB-105 amendment); `docs/reference/03-cli.md` (the `near-limit` row); `skills/orchestrate/SKILL.md` § When `models` says an account is running short; `test/fixtures/model-routing/README.md`; `CHANGELOG.md` `[Unreleased]` › Fixed.

**Acceptance.** Policy: owner's decision 2026-09-09 (union predicate). Implementation: Codex `gpt-5.6-luna` max (`worker:routing`, strategy `balance`, bus task `pb-run-0909-t20260909-132344`). Review: isolated Claude reviewer under the same task.
