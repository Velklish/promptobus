# PB-16 · Result

**Closed 2026-09-05.** Completed. `lib/model-routing/adapter-cursor.js`, declared by `lib/driver-cursor.js` as `availability: cursorAvailability(CURSOR)`: two async calls under one deadline taken before `resolveToolBin` — `status` then `models` — with stderr ignored, ANSI stripped, ids kept whole with the effort suffix, `(NO ZDR)` carried as `flags: ['no-zdr']`, `version` from the host's resolve. Auth is decided by text only, three ways (`in`, `out`, `unreadable`) — the exit code of `status` is 0 in both states; a healthy account is `unknown` / `quota_unknown` with the inventory and no `windows` (Cursor exposes no limit source); logged out is `unavailable` / `not_authenticated`; a missing binary `binary_missing`; a binary that will not start `probe_failed`; a slow one the preflight's `probe_timeout`; an unreadable listing `probe_failed` rather than an empty inventory. No late-start hook: the Cursor start path returns only strings the package composes itself (the pane is killed without `capture-pane`), so a limit and a wrong model id are indistinguishable — documented in the reference and filed as PB-16.1. Track `adapter-cursor` of the routing run, worker in Claude Code (opus, high, bypassPermissions for the live probe), one isolated review round (6 findings, all fixed).

**Verification.** `npm test` exit 0 — 42/42 files, 1316/1316 checks; `test/model-routing-adapter-cursor.test.mjs` 16/16 on a stub binary (no real `cursor-agent` in the suite). `npm run audit` exit 0 — 281 tracked files and the tarball, clean. `backslop lint` exit 0. Mutation probes, commit-first, 23 in all, 22 red with the module loadable each time: the driver drops `availability`; a healthy probe claims `available`; empty `windows`; the adapter fills `rated`; the version dropped; the effort suffix stripped; the `NO ZDR` mark dropped; a row is any two-field line; ANSI not stripped; every account read as logged in; only the positive login pattern; a missing binary as `not_authenticated`; a slow binary as `unavailable`; a budget per call; an unreadable listing as an empty inventory; the `status` output in `message`; a misspelt reason; the exit code deciding auth; neither-pattern output as `not_authenticated`; a failed launch as a timeout; the deadline after resolve; the `setEncoding` probe stays green because every byte the parse reads is ASCII (reported, not papered over). Live probe on this machine: 211 models, 20 `no-zdr`, 1.97 s, no account address anywhere in the verdict. Gates re-run on the merged `main` tree by the approver.

**Documentation in the same pass.** `docs/reference/03-cli.md` (`### Cursor`, the two auth rows, why late-start classification is absent), the `adapterOf` docstring in `lib/drivers.js`, `test/tmpdir-sweep.mjs` (`promptobus-adapter-`), `CHANGELOG.md` under `[Unreleased]`. Findings filed: PB-16.1 (no late-start signal on the Cursor start path), PB-16.2 (a synchronous `resolveToolBin` blocks the preflight budget timer and the neighbouring adapters — a host-contract question shared by all three adapters).
