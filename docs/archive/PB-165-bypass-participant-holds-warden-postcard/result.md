# PB-165 · Result

**Closed 2026-09-11.** Completed. Measured on claude 2.1.263 with one background lift per permission mode and the warden's postcard sent through the package's own `knockSocket`: under `bypassPermissions` the session holds the postcard as a "Held peer message" dialog (`status: waiting`, `waitingFor: "permission prompt"`) while still finishing its turn; under `dontAsk`, `acceptEdits` and `auto` the postcard is delivered without any key; with `"crossSessionInbound": "accept"` in the settings file the dialog does not appear at all. The Claude driver now writes that key into the participant's `--settings` file only under the mode measured as bypassing prompts (`PROMPT_BYPASS_MODES`), and a live lift on the file the driver itself writes showed the postcard body reaching the session with the record never leaving `busy`/`working`. The stall predicate (`promptStands` in `src/supervisor.ts`) reads the bus's own marks instead of the record: a participant that both closed its turn and reported after its last activation is not stopped by a dialog, so no person is called to it; either mark alone keeps the stall (a turn closed before the knock is real deafness). The card's wording "a session with a held postcard and a running turn" could not be met from the record — a held postcard and a real prompt look the same there, and `state` differs between binary builds — so the distinction lives in the warden's marks; recorded in 03-cli. Worker of run 0911d (`routing` track), squashed commit 46346d2; the isolated reviewer raised no notes on this task.

**Verification.** Worker on 46346d2/df4b5f6: `npm test` exit 0, 55/55 files (the new `test/promptobus-driver-claude.test.mjs`, 22 checks); lint 0; audit 0. Mutation probes: the key written under every mode (15/17), the predicate losing the "reported" half (21/22 plus the e2e step 9 going red), `<=` → `>=` in the predicate (18/22). Live checks: five lifts, one per mode, plus one lift on the driver-written settings file — held dialog false, postcard body true, no `waitingFor`. Not measured: `manual` and `plan` modes (no key written there by the same rule). Known gap, named in 03-cli: a turn started without an activation would have its dialog read as a held postcard; the bus starts no such turn.

**Documentation in the same pass.** `docs/reference/03-cli.md` § Spawn, CHANGELOG.
