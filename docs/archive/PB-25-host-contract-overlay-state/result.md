# PB-25 · Result

**Closed 2026-09-06.** Completed. `HostRoutingOverlay` gains `writable?: boolean`; `readLayers` requires exactly one writable layer whenever any layer is declared and refuses zero and two, naming the layers (a host with no overlays stays legal); `dist/host.d.ts` follows through `pretest`. The standalone host moves its `workspace` layer to `<promptobusHome>/model-routing.json` and marks it writable; `user` stays under the user's home and is read-only for the tool; `<workspaceRoot>/model-routing.local.json` is no longer read and has no fallback. `models validate` carries `writable` into its layers, prints `[writable]`, and reports a declaration without exactly one writable layer as an `overlay-invalid` finding on the `host` layer rather than a throw — this is the command a person runs to learn their stack is broken. `writable` stays optional in the type on purpose: a required field would make every layer of every host declare a boolean that is false on all but one; a consumer that forgets it learns at the first routing command from the `GateError` of `readLayers`, and from `models validate`.

Review (isolated, xhigh, in the contract track's second pass): three findings on this task — `validate` neither printed nor checked the writable layer (closed already in the task commit), the CHANGELOG claimed `tsc` enforces the field (rewritten to name where the consumer learns it), `models strategy --set` named in the present tense (now "PB-32 adds …"). Closure checked by the approver on the diff.

**Verification.** Gates on `main` after integration: `backslop lint` no errors; `npm run audit` clean, 372 tracked files and the tarball; `npm test` 46/46 files. Worker's mutation probes after the commits: `readLayers` accepting any number of writable layers → catalog 29/1; standalone `workspace` back in the repository root (with `dist` rebuilt inside the probe — the first attempt was a false green because the host test imports `dist`) → host 9/1; `models validate` not marking `[writable]` → command 21/1.

**Documentation in the same pass.** `docs/reference/02-host.md` (new section "The writable layer": the rule, why the refusal is on declaration, the consumer's condition that the file is not committed, mark the topmost layer), `docs/reference/03-cli.md` (the new check in the `validate` list), `docs/adr/adr-003-model-routing.md` (the supersession header now names the `workspace` path of its Overlays table), `CHANGELOG.md`. Open edge for PB-33: `docs/guides/model-routing.md` still names `<workspaceRoot>/model-routing.local.json` in its layers table and its "Save it as …" line.
