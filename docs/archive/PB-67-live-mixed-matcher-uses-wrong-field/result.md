# PB-67 · Result

**Closed 2026-09-09.** Completed. The three matchers of `scripts/live-mixed.mjs` no longer compare `m.from` — a field a stored protocol v1 record does not carry — but use the shared `sentBy(message, address)` predicate, now exported from `test/scenario.mjs`, which compares the record's normalized `sender` id with `store.addrDir(address)`. A live mixed-lineup run therefore finds the worker's and the reviewer's messages instead of waiting for an address spelling the store never writes. 04-protocol § Message types now states that stored records carry `sender` and `recipients` as normalized participant ids (`worker:demo` is stored as `worker-demo`).

**Verification.** Worker commit `ae495be` (worktree of `worker:verify`, on `main` `dcf9b6f`). Reproducer red before the repair: `node test/promptobus-mixed.test.mjs` exit 1, 46/48 — "live-mixed said matcher finds a worker message sent through the v1 store" and "live-mixed reviewer matcher finds a reviewer result sent through the v1 store", the printed records carrying `sender: "worker-e2e"` / `"reviewer-e2e"` and no `from`. After the repair 48/48. Gates on `ae495be`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 634 tracked files, 119 tarball entries. Mutation probe after the commit, test kept: `sentBy` reverted to `m?.from === addr` → exit 1, 41/48 with the same two assertions red (and the existing scenario's downstream steps); restored → 48/48. Review: the orchestrator read the diff in full (one exported predicate, three call sites, one reference paragraph, one CHANGELOG bullet, one stub regression) — no isolated reviewer session was spent on it. Approver: squash of the worker branch onto `main`; no merge conflict; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Not run.** `scripts/live-mixed.mjs` itself was not executed: live scripts start real harness sessions and spend the owner's subscriptions, and the run forbade them to workers. The fix is proven on the predicate through the store; the live acceptance run of the script remains for the owner to perform when a mixed lineup is next needed.

**Documentation in the same pass.** 04-protocol.md § Message types (stored sender normalization), CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:verify`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator.
