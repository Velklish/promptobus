# PB-158 · Result

**Closed 2026-09-10.** Completed. When `done` removes a worktree on the proven `remove` disposition — the two content measurements of PB-6, `git merge-tree --write-tree` and `git patch-id --stable`, say the branch's work is in the default branch — it now deletes the branch in the same step with a force delete, since `-d` cannot confirm a squash; the deletion stays restricted to the branch the spawn created (the `worktree-` prefix), exactly as the ancestry case already was. Every other disposition is untouched: an unmerged branch, a dirty tree or an unreadable comparison still keep both the directory and the branch and say why; a branch a worker moved to itself is not the mechanism's to delete; remote branches are never touched. The `done` hint for a stuck branch no longer claims `-d` misread a squash. The reference § done states what is removed, on what evidence, and what is left standing.

**Verification.** Worker commit `6b38a9d`, worktree of `worker:launch2`, squashed. Reproducer red before the repair: `node test/promptobus-worktree.test.mjs` exit 1, 67/68 — "squashed branch: the directory and mechanism branch are removed" (`branchDeleted: false`, `branchStuck: worktree-a2a-squashed`); after the repair 68/68 and the done file 32/32. Gates on `6b38a9d`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 697 tracked files, 119 tarball entries. Mutation probe, test kept: `-D` returned to `-d` → exit 1, 67/68 with the same assertion; restored → 68/68, `git diff --exit-code` 0. Approver read: `removeWorktree` has one caller, in `done`, reached only for a dead session and a disposition other than `keep`, so the force delete never reaches an unproven branch. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/03-cli.md` § done, CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:launch2`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator diff read; approver Павел Ким's orchestrator session.
