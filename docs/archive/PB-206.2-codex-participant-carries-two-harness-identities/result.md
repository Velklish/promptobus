# PB-206.2 · Result

**Closed 2026-09-13.** Completed, with the card's stated mechanism disproved along the way and rewritten before the work started. The card claimed a Codex participant exports two harness identity variables at once; an environment snapshot taken by the worker inside a live participant showed only `CODEX_THREAD_ID`. The real collision lives in the suite: fixtures inherited whatever identity the ambient process carried, so a Codex-run suite turned Claude-shaped foreign-session checks into owner actions. The fix is one shared four-variable list (`HARNESS_IDENTITY_VARS`) cleared at both boundaries where a fixture gets its home — the pooled runner and the direct-file path — before the fixture installs its own session. Live scripts keep their deliberately narrower policy, and the list is parity-checked against the three driver `identityVar` declarations plus the Claude host identity.

**Verification.** Gates on the merged tree, commit `a55740d`, tracked tree clean: `npm test` code 0 (168999 ms, 65 files, 27 suites reporting `fail 0`, none above zero), `backslop lint` code 0, `npm run audit` code 0, `npm run pins` code 0 — gates 4, green 4. Three mutation probes by the worker, each mutating one committed file and restoring a clean tree: no-op instead of the deletion loop reddened `promptobus-dismiss` on its three foreign-owner checks and `runner.test.mjs` on all four surviving `foreign-*` identities; replacing the list's `CODEX_THREAD_ID` entry reddened the driver/list parity assertion in `session-identity.test.mjs`; every restore returned code 0. Reviewed by an isolated reviewer over the full branch diff (2717 lines) — no findings against this diff; its one major finding is about the direct route shipped in PB-206 and is recorded as PB-206.6.

**Documentation in the same pass.** `CHANGELOG.md`, `docs/guides/contributing.md`, `README.md`, `README.ru.md`.

**Left open.** A live Cursor participant would be the second environment where the former collision reproduces; the implementation covers and parity-checks `CURSOR_CONVERSATION_ID`, but this run had no Cursor participant, so that half stays a prediction rather than a measurement.
