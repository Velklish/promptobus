# PB-17 · Result

**Closed 2026-09-05.** Completed. `lib/model-routing/adapter-codex.js`, declared by `lib/driver-codex.js` as `availability`: a fresh `codex app-server --stdio` under the driver's own `sessionEnv`, `initialize`, `account/rateLimits/read` (measured to be a real request on codex-cli 0.146.0 — the `account/rateLimits/updated` notification never arrives without a thread; the bounded wait for it stays as the fallback for a binary without the method), `model/list`, then the child is killed — no `thread/start`, no turn. Windows `primary`/`secondary` (`lengthSec` = `windowDurationMins` × 60, `resetAt` from the epoch seconds, a flat snapshot read as the primary window), `exhausted` / `subscription_exhausted` with the reached window's reset, a refused limit read `unavailable` / `not_authenticated` (the model list still answers when logged out, so it proves nothing about auth), no answer `unknown` / `quota_unknown`, a timeout `probe_timeout`, a broken channel or a binary that will not start `probe_failed`, hidden models dropped, `getAuthStatus` and `account/read` never called (a token and the e-mail), no stderr pipe at all. `listedModels` extracted into `lib/codex-session.js` and shared with the start path, whose refusal is unchanged. Track `adapter-codex` of the routing run, worker in Claude Code (opus, high, bypassPermissions for the live probe), one isolated review round (8 findings: 7 fixed, the `adapterOf` docstring left to the Cursor track).

**Verification.** `npm test` exit 0 — 42/42 files; `test/model-routing-adapter-codex.test.mjs` 23/23 against the stub app-server (`CODEX_STUB_PROBE` flags `hang`, `unsupported`, `unauthenticated`, `no-notify`, `hidden`, `stderr`, `no-models`, `flat`; the stub's `threads/` directory asserted empty after every probe). `npm run audit` exit 0 — 280 tracked files and the tarball, clean. `backslop lint` exit 0. Mutation probes, commit-first, 24 aimed, 23 red with the module loadable: the `unknown variant` and `no reply to` needles substituted, the hidden filter neutered, a real stderr pipe wired into `message`, the flat-snapshot fallbacks, the `no-models` branch, the `DRIVER_PRIVATE` entry (an import from `lib/status.js` reddens the gate), the `resetIso` range gate, the timeout/channel split, and the dead search-order ternary the first round removed; the one green probe is the pipe `'error'` listeners — measured on Node 25.2.1, a failed spawn and a write after exit raise no uncaught exception, so nothing can pin them; they stay. Live probe on this machine: `available` in 1.26 s, five models, two windows, no new file under `~/.codex/sessions`; logged-out measured with an empty `CODEX_HOME`. Gates re-run on the merged `main` tree by the approver.

**Documentation in the same pass.** `docs/reference/03-cli.md` (`### Codex availability`, the two binary rows, the two calls never made), `test/run.mjs` register (the file's 25× margin), `test/promptobus-adapter.test.mjs` (`DRIVER_OWN`, `DRIVER_PRIVATE`), `test/tmpdir-sweep.mjs` (`promptobus-adapter-`), `CHANGELOG.md` under `[Unreleased]`. Finding filed: PB-17.1 (the codex driver test counts holder and app-server processes machine-wide — the same family as PB-14.4).
