# PB-182 · Result

**Исход:** моя половина закрыта, **карточка в целом — нет**. В архив я бы её не отправлял; см. ниже.

**Что сделано.** Codex-участник перестал наследовать личность родителя: `lib/driver-codex.js:50` — `SESSION_ENV_DROP = [...PARENT_SESSION_ENV]`, список живёт в общем `lib/session-env.js` и несёт 13 имён, среди них пара сообщений warden'а и `CLAUDE_CODE_HOST_SESSION_ID` — имя, которого в списке не было и которое нашёл живой прогон, а не рассуждение.

**Чем проверено — и это слабое место.** Собственного теста у этой правки **нет**: коммит `fced046` тронул `CHANGELOG.md`, `lib/driver-codex.js`, `lib/session-env.js` и ни одного файла набора. Проверено чтением и тем, что набор остался зелёным, а это не одно и то же. Единственный тест, который вообще смотрит на `SESSION_ENV_DROP` — `test/promptobus-review.test.mjs:266–268` — берёт список **из `lib/driver-claude.js`**, то есть ко мне не относится; и, по чтению (пробы не ставил), он тавтологичен: `SESSION_ENV_DROP.every((k) => !(k in plan.env))` на пустом списке истинно, второе условие тоже, — то есть он не поймает удаление имени из списка, он проверяет только, что план чтит тот список, который есть.

**Что осталось за карточкой.**
1. **Cursor.** `lib/driver-cursor.js:220` роняет четыре имени — `SESSION_ENV_VAR`, `AGENT_CLI_SOCKET_PATH`, `AGENT_CLI_LOG_PATH`, `CURSOR_AGENT_SOCKET` — и ни одного имени родительской сессии. Это прямо в области карточки и не сделано.
2. **`lib/store.js` `sessionIdentity`** — вторая половина области, не моя.
3. **Мой комментарий в `lib/session-env.js:1–2` сейчас шире правды:** «A participant must not be able to read its parent's session at all, whichever harness lifted it» — это принцип карточки, а не поведение дерева. Claude роняет только `CLAUDE_PID` и `CLAUDE_EFFORT` (`lib/driver-claude.js:270`), Cursor — четыре имени выше. Строка ссылается на открытую `PB-182`, поэтому читается как намерение, но читается и как описание. Если карточка уходит в архив раньше, чем закрываются пункты 1–2, эту строку надо переписать на «сегодня это верно для Codex» — скажи, и пришлю.

---

# Список: что я не закрывал или закрывал не полностью

- **`PB-182`** — половина, и не самая проверенная. Подробности выше. В архив, на мой взгляд, рано.
- **`PB-172`** — уборка сделана, гейты сняты; часть предмета карточки уехала в `PB-188`.
- **`PB-173`** — без README.
- Остальные четыре (`PB-166.2`, `PB-168`, `PB-171`, `PB-177`) — целиком мои и закрыты.
- `PB-180`, `PB-185`, `PB-188` — открыты, не пишу.

Ничего не менял: дерево на `d71d8de`, чисто.
