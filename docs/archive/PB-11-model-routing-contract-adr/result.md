# PB-11 · Result

**Closed 2026-09-05.** Completed. ADR-003 (`docs/adr/adr-003-model-routing.md`, Accepted) fixes the model-routing contract: four strategies with the weight table and the normalisation formulas, the tuple catalog, overlay precedence `canonical → host overlays (user, workspace) → CLI constraints`, availability states and reason codes with TTLs and the 15 s budget, the nine-step resolver order with penalties, reviewer floor, diversity bonus and tie-break, start behaviour with no write before a candidate, participant `metadata` without a protocol bump, and the plan's "not in v1" list. The host contract gains one required method, `routingPaths(): { cacheFile, overlays: [{ id, path }] }`, typed in `src/host.ts` and answered by `createStandaloneHost` (`~/.promptobus/model-routing/cache.json`; overlays `user`, `workspace`). The owner confirmed the four recorded decisions (PAYG excluded by default; reviewer floor a policy value; one account per harness; unknown remaining limit penalised) and reversed one plan sentence at acceptance: `--dry-run` reads the cache only, live probes need `--refresh`. Track `contract` of the routing run, worker session in Claude Code (opus, xhigh), one isolated review round (11 findings, all fixed).

**Verification.** `npm test` exit 0 — 39/39 files, 1316 `check.mjs` verdicts and 376 `node:test` assertions passed, 0 failed, 4 deliberate `todo` until PB-21. `npm run audit` exit 0 — 252 tracked files and a 98-entry tarball, clean. `backslop lint` exit 0. New check in `test/host.test.mjs`: two hosts with different roots share `cacheFile` and the `user` overlay, `workspace` follows the root, and no routing path starts with `promptobusHome()`; the routing types leave through both entry points (`dist/host-index.d.ts`, `dist/index.d.ts`). Mutation probes, commit-first: paths hung off `promptobusHome()` → red; overlay order reversed → red; each type dropped from one entry point → red. Gates re-run after the merge into `main` (numbers in the PB-12 result, same tree).

**Documentation in the same pass.** `docs/reference/02-host.md` (method list, standalone paragraph, "Model-routing paths" section, the note that host implementations must add the method), `docs/GLOSSARY.md` (strategy, model catalog, tuple, availability snapshot, subscription limit, overlay), `docs/ROADMAP.md` (goal 4), `docs/README.md` (ADR row), `CHANGELOG.md` under `[Unreleased]` — `### Changed` for the required host method. Findings filed: PB-11.1 (ROADMAP evidence stale), PB-11.2 (`HostClone` not re-exported).
