# PB-64 · Result

**Closed 2026-09-10.** Completed. The warden's contact-point fingerprint is the socket address alone: a pid or hand-over-time change on the same socket, which the Claude Stop hook and the participant's bus server produce at every ordinary turn end, no longer counts as a move, so it neither re-knocks inside the retry threshold nor bypasses the `busy` gate. The mailbox cutoff is separated from the move flag: the health mark records the session behind the last successful knock (`wakeSession`), and the whole mailbox is repeated only when the contact point's session differs from it; a Cursor or Codex turn-counter rewrite of the socket still wakes immediately but carries only what arrived after `knockedTo`. The wake record now treats the session as part of its identity, so a session change rewrites an otherwise equal record. The comments that stated the false equation between a rewritten contact point and a restart are corrected.

**Verification.** Worker commit `85a6573`, worktree of `worker:warden`, squashed. Reproducer red before the repair: `node --test test/promptobus-warden.test.mjs` exit 1, 236/238 — ": a pid-only rewrite of the same contact point waits for the retry threshold" (a second knock with unread 2 repeating both messages) and ": a same-session turn-counter rewrite wakes immediately with only new messages" (unread 2 with both messages). After the repair the same file exit 0, 238/238; the existing restart fixture names distinct sessions. Gates on `85a6573`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 671 tracked files, 119 tarball entries; `npm run build` exit 0. Mutation probe, test kept: the two runtime lines (fingerprint and cutoff) reverted, rebuilt → exit 1, 236/238 with the same two assertions; restored, rebuilt → 238/238. Baseline on the untouched tree: `npm test` exit 0, 54/54 (the sandboxed attempt could not write `dist/`, the escalated run counts). Open: no live harness run; the card's manual repro (three unread, pid-only rewrite at +11 s) is covered by the fixture only. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/03-cli.md` § Guard and warden (fingerprint by socket, cutoff by session), CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:warden`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator diff read against the card and the three drivers' `registerWake` (the Claude server and Stop hook resolve the same session id, so the cutoff is stable across the two writers); approver Павел Ким's orchestrator session.
