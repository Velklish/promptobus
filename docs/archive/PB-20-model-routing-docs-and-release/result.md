# PB-20 · Result

**Closed 2026-09-05.** Completed, all but the tag: the documentation set of the routing series read as a whole. `docs/reference/03-cli.md` § Model routing opens with the six things kept apart (a maintainer's rating, runtime availability, the remaining subscription limit, money cost, an explicit constraint, the two decisions — strategy by the agent, tuple by the CLI) and a maintainer note on the machine-read code tables that names the sliced headings without their `###` prefix — the first draft of that note broke the slicer it documents; the `manual_exhaustion` and `source` rows say what the code writes. `02-host.md` declares `HostFile.generate` (and `src/standalone.ts` carries the documentation-only field); `01-overview.md` says its version line is hand-written and moves only with a release; `04-protocol.md` verified field by field against `routingOf`; `guides/model-routing.md` names the cache beside the two overlays and separates money from `quotaCost`; `guides/install.md` and both READMEs lose the `promptobus 0.1.0` banner and the false "help does not list install"; the READMEs gain a `## Model routing` section with a captured run; `GLOSSARY.md` moves every routing term's evidence onto the shipped artefact and adds `quota cost`, `pay-as-you-go`, `constraint`, `runtime model` (36 rows, every evidence path resolves); `ROADMAP.md` goal 4 carries the shipped series and a measurement, goal 3 loses the same false tail. `CHANGELOG.md` `[Unreleased]` is one release note — Added 7, Changed 4, Fixed 7 — with the host-contract changes a consumer meets at one release in one bullet; the approver kept the consolidation and added the PB-22 bullet from `main`. `skills/` ships: `package.json` `files` gains `skills` (tarball 112 → 114 entries). Track `docs` of the routing run, worker in Claude Code (opus, high, bypassPermissions), documentation reviewed by the orchestrator from the diff.

**Verification — the acceptance evidence the release needs, all on this machine, 2026-09-05.** `models --refresh` over Claude, Cursor and Codex twice: 2.57 s and 2.22 s (Codex warm — an app-server had run at 16:17; no cold number was manufactured); codex `available` with two windows, claude and cursor `quota_unknown`; chosen `codex-luna-medium` 73.10 over 19 candidates. Cache `-rw-------` at `~/.promptobus/model-routing/cache.json`, `grep -iE '@|orgId|token'` → nothing. `models --json` valid against `urn:promptobus:model-routing:decision` (Ajv2020, four schemas registered). Staged exhaustion through the real cache: `markExhausted(host, 'codex')` → every codex tuple `harness-exhausted`, `claude-sonnet-medium` chosen; `--refresh` does not lift it; `--clear-exhausted codex` does, and the next run shows `never checked` for the dropped entry (the age is the facts', not the run's). `npm pack --dry-run`: 114 entries with the catalog, the four routing schemas and both skills. One full bus round in a throwaway repository with the standalone host: `spawn --strategy balanced` (dry-run first, then live: `claude-sonnet-medium` 62.50, worktree and session lifted in 2.9 s), the worker committed the one-line change and reported; `review --strategy quality` picked `claude-opus-xhigh` 67.50 — diversity by model, the workspace declaring one harness; `status` printed the routing line for both; the reviewer reported no findings; `done` closed the round and kept the unmerged worktree with PB-6's report naming the state. Gates on the track's tree and re-run on the merged `main` tree: `npm test` 46/46, `npm run audit` clean, `backslop lint` clean.

**Documentation in the same pass.** This task is the documentation. Findings filed: PB-20.1 (the `models` text buries the decision under 205 unrated Cursor rows), PB-20.2 (the overview's version line has no gate).
