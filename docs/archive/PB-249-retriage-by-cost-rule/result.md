# PB-249 · Result

**Closed 2026-09-23.** Completed by worker pb249 in eight commits over `39316bc2` (`21bee0e7` to `aea7b9a2`), squashed into the closing one. Every open card carries a `Cost` label, with a dated "Re-triage, 2026-09-23" section (queue, deferred) or an `Evidence` section (minor) that lists each checkable claim re-run against the tree with its command and exit code; a claim that could not be re-run is stated as an assumption. Snapshot (`backslop status` exit 0; Cost counted by `grep -l '^- \*\*Cost:\*\*'`): before, on `39316bc2` — queue 27 (1 with Cost), deferred 15 (0), triage 1 (0), minor 1 (1); after, on the merged tree with this card archived — queue 24 (24 with Cost: 21 major, 3 minor), deferred 11 (11: 6 major, 5 minor), triage 0, minor 13 (13: 12 minor, 1 `major (hypothesis)`; 13 with an `Evidence` section). Moves: queue → `minor/` PB-188.2, PB-139.1, PB-225.1, PB-237, PB-238; triage → `minor/` PB-228 (`major (hypothesis)`: the load premise is unestablished); deferred with a fired return condition → `minor/` by its label PB-146.3, PB-66.2, PB-13.2 (the owner's wording restored as the fired one, the narrower reading left as a proposal); deferred with a fired return condition → queue PB-65.2. Two return conditions were reworded to be checkable: PB-24.1, PB-87.2. Filed: PB-249.1, PB-249.2, PB-249.3 (minor, in `minor/`), and the minor batches PB-250 (PB-225.1, PB-237, PB-238; rides with PB-240) and PB-251 (PB-247.1, PB-249.1–.3; rides with PB-245.1 and PB-239). The queue is ordered by cost, `major` first and the two batches last; PB-248 heads it by the owner's order for run 2026-09-23, and PB-244 is last among `major`, blocked on the consumer's work. Duplicates: no whole-card duplicate; PB-214's third work item was merged into PB-191 and replaced by a pointer; the overlaps PB-187 ↔ PB-246 and PB-191 ↔ PB-194 are recorded in both cards and not merged. Rejection candidates: none, every subject was found alive with the command in its card. Handed to the owner, the card left open: PB-24.2 needs only the owner's yes or no, its "if no" branch half done by PB-28. Tool work outside `docs/backlog/`, kept because reverting it turns `lint` red: `mv 146.3 minor` rewrote one incoming link in `docs/archive/PB-201-gate-protocol-artifact/task.md`, and `mv 13.2 minor` the PB-13.2 link in `docs/TRACKS.md`.

**Verification.** Worker, on `736ad395`, clean tree: `npx --yes github:Velklish/backslop#v0.10.0 gates --keep-going --require-clean` exit 1, gates 5, green 4 — `npm test` exit 0 (70/70 files, 2320/2320 checks), `backslop lint` exit 0, `npm run audit` exit 0 (1029 tracked files, 136 packed entries), `npm run pins` exit 0 (75 live refs in 14 files), `npm run codex-schema` exit 1, "codex 0.156.1 is installed, the fixtures were taken from 0.146.0"; on the base `39316bc2`, a detached worktree, `npm test` exit 0 (70/70, 2320/2320) and `npm run codex-schema` exit 1 with the same line, so the red is the machine, not this change (PB-196). Verdict names base → branch: 3255 against 3255, none removed, none added. On `aea7b9a2`: `backslop lint`, `npm run audit`, `npm run pins` exit 0. Mutation probe: not run, the piece changes no code and no test. Approver: `git merge --squash aea7b9a2` onto `39316bc2` exit 0, no conflicts, 53 files (+624/−49); on that tree with this closure staged, before the commit, `npx --yes github:Velklish/backslop#v0.10.0 gates --keep-going` exit 1, gates 5, green 4 — `npm test` exit 0 (70/70 files, 2320/2320 checks), `backslop lint` exit 0, `npm run audit` exit 0 (1030 tracked files, 136 packed entries), `npm run pins` exit 0 (75 live refs in 14 files), `npm run codex-schema` exit 1 with the same line; on the base `39316bc2`, a detached worktree after `npm ci`, `npm run codex-schema` exit 1 with the same line. `git diff --cached aea7b9a2` outside this card's archive directory is only the card leaving `queue/`.

**Review.** One round: reviewer:pb249, an isolated session, on `736ad395` — critical 0, major 1, minor 6. The major, PB-13.2's fired return condition reworded so that it would not fire, was closed in `aea7b9a2` by recording the condition as fired and routing the card to `minor/` by its label. The six minor findings (PB-87.2, PB-229, PB-243 and PB-159.2 wording, the CHANGELOG claims, PB-244 ahead of eleven doable `major` cards) were closed in the same commit, together with the orchestrator's order to put PB-248 at the head of the queue. The orchestrator read the closure by the diff `736ad395..aea7b9a2` (18 files, +34/−31): cards, CHANGELOG and `mv` link rewrites only, no second review round.

**Documentation in the same pass.** `docs/ROADMAP.md`, Prioritisation principle: the queue is ordered by the cost label, and the existing rule applies within a label. `CHANGELOG.md` under Unreleased / Changed. The cards themselves carry the re-triage record.
