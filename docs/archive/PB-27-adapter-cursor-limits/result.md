# PB-27 · Result

**Closed 2026-09-06.** Completed. The Cursor adapter reads the bearer from the keychain item `cursor-access-token` first and `CURSOR_API_KEY` second (the env path is unmeasured: its refusal is `quota_unknown`, a refused keychain token is `not_authenticated`; a token it cannot read is `quota_unknown`, because `cursor-agent status` has already answered the auth question); `cursor-refresh-token` is never asked for; the backend URL is the one https-only field taken from `cli-config.json`. One `POST DashboardService/GetCurrentPeriodUsage` becomes two windows of the cycle's own length: `monthly-auto` with the inventory ids the bucket covers (exact name or `name + "-"`) and `monthly-api` with no list, the complement. Tier `included:<cents>` / `derived`. `exhausted` only when both pool windows were read and both are spent; the nudge is rewritten from its threshold and target model; the harness's `displayMessage` reaches a diagnosis only on the exhausted branch, bounded and sanitised. Nothing but the two pool percentages, the cycle and the included amount is read.

Review (isolated, xhigh): eight findings — `CURSOR_API_KEY` tried before the measured credential with its refusal condemning the harness, one spent pool read as both, `Number(null)` in the tier and the nudge, a stub id list that did not match the stub, and four smaller — closed in `8861935`; the third pass corrected the `liveToken` JSDoc that still said the environment came first. Closure checked by the approver on the diff. The card's "missing token → not_authenticated" was superseded by the adapter's reading and ratified on the card.

**Verification.** On `main`: `backslop lint`, `npm run audit` (396 tracked files), `npm test` 47/47 (the Cursor adapter file 42 checks, was 22). Worker's mutation probes: bare-prefix pool matching → red (`vega` claiming `vegabond-3`); the `api` pool carrying a list → red; `every()` over one window → red; the nudge label quoted verbatim → two red; `http:` accepted → red; the env key read first → red; an env refusal as `not_authenticated` → red; `displayMessage` on the available branch → two red; false positive on the fixture (bucket loses `cursor-grok-4.6`) → four red.

**Documentation in the same pass.** `docs/reference/03-cli.md` (the Cursor section: credentials, the call, the cycle → two pools table, the `auto` id rule, the derived tier, 17 verdict rows, the "a successful probe is unknown" sentence removed), `CHANGELOG.md`.
