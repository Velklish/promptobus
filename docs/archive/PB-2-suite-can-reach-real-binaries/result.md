# PB-2 · Result

**Closed 2026-09-05.** Completed. The suite's `PATH` is sealed in one place — `applyHygiene` in `test/hygiene.mjs`, the two callers being the runner and `check.mjs` — to a per-run directory holding a symlink per binary in the measured allowlist `REACHABLE_BINARIES` (`ast-grep`, `env`, `git`, `node`, `npm`, `pgrep`, `ps`, `sh`, `sleep`, `tar`); no harness binary is on it, so an unstubbed `cursor`, `codex`, `claude` or `tmux` fails to resolve instead of reaching the machine (the probe with the seal opened: `resolved harness binaries: cursor,codex,tmux` — this machine carries them). Every suite file composes its PATH from the one it was handed, so no test file changed. The escape gate: `lib/exec.js` appends every name `run()` resolved (execute bit required, like `firstOnPath`) to `PROMPTOBUS_EXEC_TRACE`, and the tail of `test/run.mjs` refuses a run in which a resolved path lies outside its own run directory (both `os.tmpdir()` spellings accepted on macOS); the boundary — `run()`, not a test file's own `spawnSync` — is written in the register, in `contributing.md` and in `exec.js`. The two owner decisions: the Cursor driver's `options.tool` is `cursor-agent` (a shared `CURSOR_TOOL` constant with the availability adapter; the harness id stays `cursor`), and the harness session registries take their home from the host — `harnessStateHome(harness)` on `PromptobusHost`, standalone answering `~/.promptobus/<harness>`, precedence env → host → a `GateError` naming both; `~/.cursor` keeps its real default; the host is bound once per process in `lib/harness-home.js` (first binding wins — the documented exception to the no-singleton rule). The live scripts set their registry variables explicitly. Track `robust`, worker in Claude Code (opus, high, bypassPermissions), one isolated review round (8 findings, all fixed).

**Verification.** `npm test` exit 0 — 45/45 files; `test/runner.test.mjs` 32/32 with six new checks (one-directory PATH inside the run; no harness binary resolves; the reachable ones do; a fabricated escape reddens a nested run; the gate names the command and the path; an unresolved name is not counted); `test/host.test.mjs` 8/8 (standalone answers `harnessStateHome`; no binding and no env var refuses by name; the second binding is ignored). Three pairs of concurrent full runs green (the same nine runs as PB-14.4); round one had caught a regression of this task — `sleep` missing from the allowlist made a fixture die with exit 127 and a check race — which a single run had passed. Mutation probes, commit-first: the seal opened → 30/32 naming the real binaries; a fabricated trace line → the nested run refuses; the execute-bit test removed → a non-executable decoy appears in the trace as a false escape; the first-binding rule removed → red. Gates re-run on the merged `main` tree by the approver.

**Documentation in the same pass.** `docs/guides/contributing.md` (sealed PATH, the trace boundary), `docs/reference/02-host.md` ("Harness state homes", "Tool binaries"), `test/run.mjs` register, `lib/cli.js` header (the binding exception), `CHANGELOG.md` under `[Unreleased]` (`### Changed` for the two contract moves). Findings filed: PB-2.1 (twelve holder/app-server processes outlived a 2026-09-04 run), PB-2.2 (a test run by hand writes under the real home).
