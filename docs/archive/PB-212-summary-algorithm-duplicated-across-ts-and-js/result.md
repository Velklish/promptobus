# PB-212 · Result

**Closed 2026-09-17, completed.** The role-summary algorithm has one implementation again. `lib/model-routing/telemetry.js` no longer carries a second copy of `telemetrySummary` and its helpers — 222 lines, the role projection, the quota coverage, the window key and the summary fields — and re-exports the function from `../../dist/index.js` instead, for the same reason `lib/warden.js` re-exports its threshold rather than holding a twin of it.

**The card's subject was the parity check, not the duplication, and that is what the change answers.** `PB-205` had added a check that the public and the internal implementations return the same thing. It was honest and it did catch divergence — but on the third review pass of that task the reviewer found a defect present in **both** copies, the measured-record counter incremented on every numeric delta without validating harness and scope, so a role reached "ambiguous" on empty evidence. Parity stayed green the whole time: both sides were wrong in agreement. Two implementations guarded by their own equality cannot catch a defect they share, and one implementation cannot have that class of defect at all.

**The parity checks went with the second copy, because they had become tautologies.** `test/model-routing-telemetry.test.mjs` loses `public and private quota summaries stay in sync` and the paired public-side assertions inside three other checks — comparing a function with itself proves nothing. In their place the file opens with `telemetry.telemetrySummary is the same function as the public one, not a twin`, a reference-identity check: it is the one assertion that fails the moment a second copy comes back. Every expected-value check of the algorithm's behaviour is unchanged and still runs.

**Verification.** `npm run probe -- src/telemetry.ts --mutate "s/recordsCount > 1 \|\|/recordsCount > 100 ||/" --run "npm run build && node test/model-routing-telemetry.test.mjs"` — the multi-record ambiguity rule defeated in the one remaining implementation — mutated exit 1, restored exit 0, reddening `quota scope keys canonicalize equivalent shapes and reject duplicate ids` with the state reading `measured` where it should read `ambiguous`. The probe mutates a TypeScript source the JS tests consume compiled, so the rebuild is inside `--run`. `git status --porcelain` empty before and after. Full gates on the worker's branch head `1a636fb`: `npx github:Velklish/backslop#v0.8.0 gates` exit 0 — gates 4, green 4 (`npm test` 2276 checks over 69 of 69 files; `backslop lint` 0 errors; `npm run audit` 965 tracked files, 136 tarball entries; `npm run pins` 965 of 965 read, 49 live refs). On this task's slice as merged: `npm run build` exit 0, `node --test test/comment-length.test.mjs test/comment-links.test.mjs` exit 0 — 33 of 33, `backslop lint` exit 0. The full gate set was re-run once on the merged tree carrying all six tasks.

**Review.** Four rounds by an isolated reviewer. Round 1 asked that this task's mutation probe go through `npm run probe` rather than a hand-rolled edit-and-restore; the probe recorded above is that re-run, and no code changed for it. Rounds 2 to 4 raised nothing on this task, and round 4 was clean.

**What is left open.** Nothing on this card. `lib/model-routing/telemetry.js` still owns everything else it did — the record version, the append path, the snapshot reads — and only the summary algorithm moved.

**Documentation in the same pass.** `docs/reference/03-cli.md`, `CHANGELOG.md`.
