# PB-136 · Result

**Closed 2026-09-09.** Completed. The schema-versus-grammar parity corpus in `test/model-routing-catalog.test.mjs` now carries lawful and unlawful overlay documents for the ADR-004/005 blocks it had never seen — `balance`, `nearLimit`, `defaults` and `account` — so the JSON Schema and the hand-written grammar in `validate.js` can no longer diverge on them unnoticed; a `validateLayers` regression pins that an `account` answer for a harness this CLI does not drive is refused as `overlay-invalid` on the layer that wrote it. `validate.js` and `overlay.schema.json` were deliberately not changed: the gap was coverage, and the validator and schema already agree. 03-cli § Catalog and overlays states that `account` uses the CLI harness vocabulary.

**Verification.** Worker commit `59454d5` (worktree of `worker:routing2`). Evidence that the old corpus missed the gap: with the schema's `account` property-name pattern narrowed from `{0,31}` to `{0,2}` the parity test still passed before the new documents were added. Gates on `59454d5`: `npm test` exit 0, 52/52 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 615 tracked files, 119 tarball entries; targeted 2/2. Mutation probe after commit: the same schema narrowing → exit 1, `overlay document 16: schema says false, the hand-written grammar says true`; schema restored → 1/1. Review: the orchestrator read the diff in full — tests, one reference sentence, a CHANGELOG entry, no production change — so no isolated reviewer session was spent on it. Approver: squash of the worker branch onto `main`; the only merge conflict was the `[Unreleased]` list of `CHANGELOG.md`, resolved as the union of entries; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/03-cli.md` § Catalog and overlays; `CHANGELOG.md` `[Unreleased]` › Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:routing2`, strategy `balance`, bus task `pb-run-0909-t20260909-132344`). Review: orchestrator.
