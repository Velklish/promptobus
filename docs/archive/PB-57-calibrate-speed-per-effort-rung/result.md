# PB-57 · Result

**Closed 2026-09-10.** Completed with the owner's choice of the checkpoint's first option, dated 2026-09-10 in ADR-005 § `models calibrate`: `speed` keeps its meaning — published output throughput in tokens per second, one value per (harness, model), constant along the effort ladder — and calibration reads a throughput observation, never seconds to close. The telemetry record gains an optional `throughput` observation (`outputTokens`, `generationDurationSec`, `tokensPerSecond`, each `null` where the harness gives none; the pair wins when it yields a rate, otherwise a token-weighted mean of direct rates); the loop guard appends each turn's evidence to a 0600 sidecar beside its guard mark (`waits/<addr>.throughput.jsonl`, no lock, no journal write, a refused write never touches the turn), a lift of the same address truncates the sidecar (best effort, a warning on failure), and `done` reads the sidecars once and projects them into the rows. `models calibrate` pools throughput by (harness, model) across rungs and records, dismissed ones included, proposes one band and writes it to every rated rung of the model — a model without an observation gets no proposal and says so; `durationSec` stays in the record and the report as completion-duration evidence, never a speed input; the per-effort duration-driven behaviour and its tests are gone. `quotaCost` is unchanged.

**Producers today.** Cursor's Stop payload carries `output_tokens` and no generation duration or rate, so its observation is partial and yields no rate; Claude and Codex expose no participant usage fields at all, so theirs is `null`. No measurement was invented: until a harness reports a usable observation, `calibrate` proposes no speed. Recorded as PB-57.1.

**Verification.** Worker commits `263eeab` (repair), `60d95d8`, `7e108de`, `f9e8cbd` (review follow-ups), worktree of `worker:telemetry`, squashed. Reproducer red before the repair: `node --test test/model-routing-calibrate.test.mjs` exit 1, 35/36 — "speed pools throughput across a model ladder and proposes one band for both rungs" (`5 !== 4`, the old code kept the catalog speed). After the last follow-up: calibrate 38/38, telemetry 64/64, guard 66/66, spawn 116/116. Gates on `f9e8cbd`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 647 tracked files, 119 tarball entries. Mutation probes, tests kept: the pooled proposal replaced by the catalog speed → red; the projection cut to four tuples → a rung missing; `filter(acceptedPiece)` restored → `4 !== 5`; the sidecar append returning false → the guard check red; the `isAddress` guard removed → the valid participant's row lost; an unwritable sidecar → the lift check red before the last fix. Review: isolated reviewer (`reviewer:telemetry`), three rounds — round 1 MAJOR: the band was written only to observed rungs, the guard took the task lock and rewrote `task.json` on every Stop, and its out-of-lock `metadata` write could erase a dismissal; round 2 MAJOR: one malformed participant address would have lost the whole task's telemetry, plus pair components summed across different turns, no sidecar lifecycle, a `null` reason, the CHANGELOG silent on the new file; round 3 (orchestrator): sidecar cleanup must never fail a lift — all closed. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** ADR-005 § `models calibrate` (dated decision), 03-cli.md (§ Participant telemetry, calibrate, the sidecar), guides/model-routing.md, GLOSSARY.md (speed pivot, throughput observation, completion duration), README.md and README.ru.md, skills/orchestrate/SKILL.md, `schemas/model-routing/telemetry.schema.json`, CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:telemetry`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: Claude `claude-opus-5` xhigh (`reviewer:telemetry`, strategy `quality`), two rounds, plus the orchestrator; option chosen by the owner, Павел Ким.
