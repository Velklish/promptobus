# PB-66.1 · Result

**Closed 2026-09-10.** Completed under the owner's decision of 2026-09-10: the retry stays unbounded and no ref is isolated for a filesystem refusal; what changes is what a person sees. `glanceInbox` in `src/v1/messages.ts` reads each ref in its own `try`: `ENOENT` is the owner taking the record between the listing and the read, any other errno becomes a `BrokenNote` (`name`, `code`, the message) beside the readable messages, a non-errno exception propagates, and the `inbox-read` fault seam PB-66 added reaches the read deterministically; the ref stays in place for a later glance. The supervisor carries every such note into the postcard as a preview line (`unreadable ref <name>: <errno>`) beside the message previews, and its journal event names the errno and the ref (`unreadable refs: EACCES <name>`), so `promptobus status` — which prints the warden journal's tail — says why the unread count does not drain. `glanceInbox` now returns a plain `{ messages, broken }` — the engine's public `glance()` keeps `MessageV1[]` by taking `.messages` — and the `inbox-read` seam carries `mode: 'glance' | 'read'` so a test can refuse one reader and not the other; the health mark carries `unreadableRefs`, rewritten on every glance so it clears itself when the refusal clears, and `promptobus status` prints it beside `alarm`; the postcard cursor `knockedTo` does not advance while a glance has refusals, so a message that sorts before a refused ref is still named once the refusal clears. 04-protocol § Message types states the rule beside the PB-66 sentence; the CHANGELOG line says the same.

**Verification.** Worker commits `aecdb01` + `b0487ca` (review follow-up), worktree of `worker:store`, squashed. Reproducer red before the repair: the new case in `test/driver.test.mjs` — a ref refused through the `inbox-read` seam — exit 1 at `test/driver.test.mjs:565`, the first postcard carrying only the ordinary preview and neither the errno nor the ref name; after the repair 1/1, `driver.test.mjs` 44/44, `store.test.mjs` 30/30, and the second half of the case — the refusal cleared, the next knock after `KNOCK_RETRY_SEC` delivers the message. Gates on `aecdb01`: `npm test` exit 1, 51/54 test files — three pooled reds under a machine load average of 94–129, none in this task's files: `codex-holder-spawn.test.mjs` 4/5 ("startHolder surfaces the spawn failure through waitReady instead of sitting out the budget", 2700 ms, `ENOENT`; standalone exit 0, 5/5, twice; on `main` at `5ed8a72` exit 0 twice, measured by the approver at 11:36 UTC), `model-routing-adapter-cursor.test.mjs` :746 and `promptobus-driver-codex.test.mjs` long-first — both already on record under PB-159.1; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 737 tracked files, 119 tarball entries (escalated reruns). Gates on `b0487ca` (the follow-up): `npm test` exit 1, 53/54 — the one red `runner.test.mjs` 32/33 ("an auto-lift trace paints the run red, even though the files passed", the serial-group file whose nested pool measures the machine); standalone on the branch exit 0, 33/33, twice (12.0 s, 10.3 s), and on `main` at `dc2dbf4` exit 0 twice (11:54 UTC, load 73–99) — machine load, recorded under PB-159.1; `driver.test.mjs` 44/44 in the pool; lint exit 0, 0 errors; `npm run audit` exit 0, 737 tracked files, 119 tarball entries. Mutation probe after the first commit, test kept: the broken-ref previews removed from the postcard → the focused case exit 1 at `test/driver.test.mjs:565` (`false == true`); restored → 1/1. After the follow-up, test kept: the `!broken.length` cursor guard removed → `driver.test.mjs` exit 1, 43/44, at `:585` (`assert.ok(second.messages.some((message) => message.body === 'the first message'))`); restored → 44/44. Isolated review (`reviewer:store`, Claude `claude-opus-5` xhigh), two rounds: round 1 — one major (the `knockedTo` cursor stepped over a refused ref, so a message sorting before it was never named once the refusal cleared) and, on the orchestrator's four questions, the shape decision (`{ messages, broken }` over an array with hidden fields), the status-line half of the owner's decision (a health mark, rewritten every glance), the synthetic preview's empty `ts`, the two-site `inbox-read` seam without a discriminator, and the non-errno throw — all closed in `b0487ca`; round 2 — nothing blocking, one optional nit (an unreachable branch that reset the mark on an empty inbox), removed by the approver at integration; the cost of the cursor guard — the readable box re-listed in every postcard while a refusal holds, bounded by the preview budget — weighed and accepted. Approver: squash of the worker branch onto `main`; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/04-protocol.md` § Message types; `CHANGELOG.md` `[Unreleased]` › Fixed; the `glanceInbox` doc comment.

**Acceptance.** Decision: the owner, Павел Ким, 2026-09-10 (unbounded retry, the cause named). Implementation: Codex `gpt-5.6-luna` max (`worker:store`, strategy `balance`, bus task `pb-run-0910-t20260910-085454`). Review: isolated Claude reviewer under the same task; approver Павел Ким's orchestrator session.
