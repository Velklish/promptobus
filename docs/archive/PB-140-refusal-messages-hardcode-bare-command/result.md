# PB-140 · Result

**Closed 2026-09-10.** Completed. Every refusal and stall hint that named a bus subcommand — the eighteen sites the extended gate found on the current tree (the card's twenty-seven were counted at an older commit) across `dismiss`, `done`, the three drivers, `models`, `review`, `spawn`, `store` and the Codex holder — now spells the command through `host.busCommand([...])`. Where the site has a host, it formats the command itself; where it does not (the store's task-resolution and send refusals, the drivers' stall routes and option refusals, the detached holder, the Claude availability adapter, the pure resolver), the nearest caller that has a host — the CLI command, `status`, the warden report, the bus service (`serviceFor(host)`), the routing context — formats it and passes the finished string down as a hint, so no module contract gained a host object. On a refusal path the hint is guarded: a partial host without `busCommand` gets the refusal without the hint sentence, never a TypeError. Fourteen of those seams keep their former text as the hint's default and are named in the gate's allowlist with a reason each; removing those defaults is PB-140.1. The PB-1 gate in `test/cli.test.mjs` gained a second check that scans non-comment string literals under `lib/` for `promptobus <known-subcommand>` outside a formatter call and fails on any literal outside that allowlist.

**Verification.** Worker commit `ccfda37`, worktree of `worker:launch`, squashed. Reproducer red before the repair: `node --test test/cli.test.mjs` exit 1, 5/6, "Expected values to be strictly deep-equal" with the list of literal sites (thirty-three recursive, thirty direct under `lib/*.js`); after the repair 6/6. One regression found and fixed on the way: the first version formatted a hint inside the `overlay-invalid` refusal path and broke `model-routing-catalog.test.mjs` on a partial host stand-in (59/60 on the branch against 60/60 three times on `main` at `1338ebe`, measured by the approver) — the guard above restored 60/60 without touching the fixture. Gates on `ccfda37`: `npm test` exit 0, 54/54 test files; `npx github:Velklish/backslop#v0.4.0 lint` exit 0, 0 errors; `npm run audit` exit 0, 708 tracked files, 119 tarball entries. Mutation probe, tests and docs kept: the runtime files of the commit reversed → exit 1, 5/6 listing eighteen literal hits from `lib/dismiss.js` to `lib/spawn.js`; restored → 6/6. Approver: squash of the worker branch onto `main`; CHANGELOG union; `backslop lint` and `npm run audit` on the integrated tree exit 0.

**Documentation in the same pass.** `docs/reference/03-cli.md` (the gate checks the command name, the wrapper and now literal strings), CHANGELOG entry under Fixed.

**Acceptance.** Implementation: Codex `gpt-5.6-luna` max (`worker:launch`, strategy `balance`, bus task `pb-run-0909b-t20260909-184312`). Review: orchestrator, three notes — no host plumbing through module contracts (finished hint strings instead), the allowlist matched by file, command and text rather than line number, and no `commandName`-derived fallback on a refusal path (a guarded hint instead); one attempted "pre-existing failure" finding rejected with base measurements. Approver Павел Ким's orchestrator session.
