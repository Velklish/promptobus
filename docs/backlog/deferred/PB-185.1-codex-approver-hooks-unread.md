# PB-185.1 · A Codex approver does not get the hooks file Codex reads

- **Scope:** `lib/approver.js`, `lib/driver-codex.js`, [hooks-and-trust § A participant's hooks are not the workspace's](../../guides/hooks-and-trust.md#a-participants-hooks-are-not-the-workspaces)
- **Created:** 2026-09-25
- **Parent:** PB-185
- **Dependencies:** none

## Context

Finding discovered while working on PB-185.
Evidence: an approver lift sets the session working directory to the clone root. The home hooks file is chosen by that working directory, while the hooks document is written into the sandbox. A worker and a reviewer were measured; an approver was not.

<!-- quote:../../../lib/approver.js -->
    cwd: cloneRoot,
<!-- /quote -->

<!-- quote:../../../lib/driver-codex.js -->
  const planned = (plan.files ?? []).find((file) => file.path === path.join(workdir, CODEX_HOOKS_REL) && file.text);
<!-- /quote -->

## Work to do

- Write the approver's hooks where Codex reads them, and measure SessionStart for that role.

## Out of scope

- Whether an approver may run on Codex at all — PB-222 decides that.
- The worker's and the reviewer's hooks files, which PB-185 settled.

## Verification

- A Codex approver's holder journal carries `hook/started sessionStart` from a hooks file whose command names `--role approver:…`.

## Deferred

- **Deferred:** 2026-09-26
- **Reason:** an approver cannot be lifted on Codex today — the approver role is Claude-only, and whether it may run on Codex is PB-222's decision; there is no Codex approver whose SessionStart could be measured.
- **Return condition:** PB-222 lifts an approver on Codex.
