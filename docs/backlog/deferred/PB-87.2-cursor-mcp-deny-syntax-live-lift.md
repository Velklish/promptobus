# PB-87.2 · Cursor's MCP deny-pattern syntax for the reviewer needs one live lift, deferred until the Cursor window is restored

- **Scope:** `lib/driver-cursor.js` (`cliConfig`), [03-cli](../../reference/03-cli.md) § Review
- **Created:** 2026-09-10
- **Dependencies:** PB-87.1
- **Cost:** major

## Context

Split from PB-87.1 by the owner's decision of 2026-09-10. `.cursor/cli.json` `permissions.deny` is proven only for `Write(**)` and `Shell(**)` (`lib/driver-cursor.js:343-344`, `cliConfig`); whether it accepts an MCP tool pattern, and in which spelling, cannot be read from the CLI's help or from this repository — it takes one live Cursor lift with a candidate pattern in `deny` and a reviewer that tries the tool. That lift spends a Cursor session, and on 2026-09-10 the owner chose not to spend one: the owner's own records say the included pool is spent until 2026-09-23, while the availability probe reports `available` — the two do not agree, and neither settles the syntax question.

## Work to do

- One live Cursor lift with an MCP tool pattern in `permissions.deny` (candidate spellings: the bare tool name, `<server>:<tool>`, `mcp__<server>__<tool>`), a reviewer prompt that asks it to call the tool, and the verdict recorded with the Cursor version.
- Either add the translation to `lib/driver-cursor.js` under the `mcpDenyTools` flag, or record in 03-cli § Review that Cursor cannot deny an MCP tool mechanically.

## Out of scope

- The Claude and Codex layers — PB-87, PB-87.1.

## Verification

- A live Cursor reviewer with a classified write tool either cannot call it (mechanical) or the reference states plainly that Cursor is prompt-only.

## Deferred

- **Deferred:** 2026-09-10
- **Reason:** The only proof is a live Cursor lift, which spends a Cursor session the owner chose not to spend now.
- **Return condition:** on or after 2026-09-24, the owner's permission for one live lift is recorded in this card with its date. The owner's own records decide whether the included window is restored; the probe is not the signal, since on 2026-09-10 it reported `available` while those records said the pool was spent.

## Triage — 2026-09-10

- **Track:** L — Spawn, review and command guidance.
- **Priority:** P3.
- **Evidence level:** `lib/driver-cursor.js:343-344` at `3ccdf27`; the Cursor state is as the owner's records and the probe report it on 2026-09-10.
- **Next step:** on return, run the lift and close by the verdict.

## Re-triage, 2026-09-23

Checked on `39316bc2` from the repository root. **Cost `major`**: the reviewer is read-only by contract. On Cursor a classified MCP write tool is held back by the prompt alone, while files and shell stay mechanically denied (`REVIEWER_DENY`). 03-cli already warns: "Cursor remains prompt-only until its MCP deny syntax is verified" (`docs/reference/03-cli.md:111-112`).

- The deny list is unchanged: `cliConfig` is at `lib/driver-cursor.js:261-262` (the triage cites `:343-344` at `3ccdf27`), and `REVIEWER_DENY` is `Write(**)`, `Shell(**)` (`:163`). Cursor is the one driver whose capabilities lack `mcpDenyTools`; `lib/driver-claude.js:597` and `lib/driver-codex.js:910` declare it.
- **Return condition: not fired, and reworded to be checkable.** "After 2026-09-23" had not arrived on the day of this check, and the owner's permission leaves no trace in the tree. The condition now names the date and where the permission is recorded. It names no probe signal: `promptobus models` reports the Cursor harness state and the windows of the `auto` and `api` pools (`lib/model-routing/adapter-cursor.js:54-55`, `:131`, `:139`), not an included pool, and the probe disagreed with the owner's records on 2026-09-10.
