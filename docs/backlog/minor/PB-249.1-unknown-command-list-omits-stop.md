# PB-249.1 · The unknown-command refusal lists every command but stop

- **Scope:** `lib/cli.js` (`SUBCOMMANDS`)
- **Created:** 2026-09-23
- **Parent:** PB-249
- **Cost:** minor

## Evidence

Finding discovered while working on PB-249.

Evidence: Read on 39316bc2 during PB-249: `lib/cli.js:13` SUBCOMMANDS is 'spawn, review, models, status, done, sweep, dismiss, history, prune, guard, warden, mcp, install, uninstall' — no `stop`; the dispatcher has `case 'stop'` at `lib/cli.js:299` and `node bin/promptobus.js help` lists it, so the unknown-command refusal at `lib/cli.js:416` names every command but `stop`.
