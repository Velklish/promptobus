# PB-264.1 · tmux-teardown-list-line

- **Scope:** `lib/cursor-persist.js` (`stopSession`, `readTmuxSessions`), [05-drivers § Cursor: tmux by absolute path](../../reference/05-drivers.md#cursor-tmux-by-absolute-path)
- **Created:** 2026-09-26
- **Parent:** PB-264
- **Cost:** minor (hypothesis)

## Evidence

Finding discovered while working on PB-264.

Evidence: Measured 2026-09-26 on tmux 3.6b in a scratch server of my own (own -L name tdp264, own TMUX_TMPDIR, never cursor-agent or the default socket): 200 rounds of `new-session -d -s probe` then `kill-session -t probe` (the last session on the server) then `list-sessions` polled in a tight loop for 300 ms per round. 13319 total list-sessions calls across the 200 windows; every single one exited 1 with the already-measured line 'no server running on <socket>' — no other wording (such as a teardown-specific 'server exited unexpectedly') appeared in any of the 200 post-kill windows. stopSession's poll (lib/cursor-persist.js:801, :808-:809) is therefore not observed to see anything but the measured no-server line while the server tears down after its last session dies; isNoServerLine is unchanged, no code touched for this finding.
