# PB-241.1 · npm run probe --run with a build step reads a compile error of the mutation as red

- **Scope:** [contributing](../../guides/contributing.md)
- **Created:** 2026-09-25
- **Parent:** PB-241
- **Cost:** minor

## Evidence

Finding discovered while working on PB-241.

Evidence: On 7972389e, npm run probe -- src/fs/lock.ts --mutate 's/if \(!held\?\.pid \|\| pidAlive\(held\.pid\)\) return false;/if (!held?.pid || true) return false;/' --run 'npm run build >/dev/null && node test/promptobus-lease.test.mjs' printed '✔ probe: red with the mutation (exit 1)' with no reddened check listed: the mutation left pidAlive unused, tsc (noUnusedLocals) refused the build, and the && chain never ran the test. The probe cannot tell a build refusal from a red check; the same mutation keeping pidAlive referenced reddened 4 named checks.
