# PB-178.1 · A Cursor or Codex MCP child has no harness identity, so the mailbox copy is not what that session receives.

- **Order:** 290
- **Scope:** [02-host § Session identity](../../reference/02-host.md#session-identity), [03-cli § ownership](../../reference/03-cli.md#ownership--the-owner-gate-of-done-stop-and-dismiss)
- **Created:** 2026-09-26
- **Parent:** PB-178
- **Cost:** major
- **Dependencies:** none

## Context

Finding discovered while working on PB-178.

The suite starts `promptobus mcp` with the caller's environment, so the identity variable is already in the child:

<!-- quote:../../../test/promptobus-harness-identity.test.mjs -->
  const child = spawn(process.execPath, [BIN, 'mcp'], {
<!-- /quote -->

<!-- quote:../../../test/promptobus-harness-identity.test.mjs -->
    copy = await mailbox(foreignEnv, main, {});
<!-- /quote -->

Neither harness builds that environment for its own MCP server. codex-cli 0.146.0 hands the child eleven generic variables and nothing harness-specific:

<!-- quote:../../../docs/reference/02-host.md -->
**The member answers for ONE path, and the distinction is measured rather than cautionary.** It answers for a command the session runs. It does NOT answer for the session's own MCP server: codex-cli 0.146.0 hands an MCP server child eleven variables — `HOME LOGNAME PATH PWD SHELL SHLVL TERM TMPDIR USER _ __CF_USER_TEXT_ENCODING` — with nothing harness-specific among them, so on that path identity from the environment is impossible, not merely unsupported. On the other path the old reader was worse than empty: a live Codex participant's environment carries its orchestrator's `CLAUDE_CODE_SESSION_ID`, and the reader returned **the parent's id** as the participant's own.
<!-- /quote -->

Cursor replaces the child's environment and passes a record pointer instead:

<!-- quote:../../../docs/reference/02-host.md -->
entry because Cursor replaces the child's environment. Claude declares `null` and keeps
<!-- /quote -->

With no session, the owner gate is `no-identity` and `gated` is false:

<!-- quote:../../../lib/store.js -->
  if (!session) return { gated: false, allowed: false, right: 'no-identity', owner, session };
<!-- /quote -->

The mailbox tool peeks when the call is proved foreign or names no session, and fetches otherwise:

<!-- quote:../../../src/mcp/server.ts -->
        const { messages, broken } = peek
          ? service.peekInbox(home, task, role)
          : service.readInbox(home, task, role);
<!-- /quote -->

The orchestrator's own MCP server under Cursor or Codex was not measured on codex-cli 0.156.1. The 0.146.0 record above is the evidence that child has no identity.

## Work to do

- Show what `promptobus_mailbox` does in a Cursor MCP child and in a Codex MCP child whose environment is the one the harness actually builds, not a shell that already carries the identity variable.

## Out of scope

- The shell-command path and `promptobus mcp` started from that shell. Those are what PB-178 measured.

## Verification

- A Codex MCP child and a Cursor MCP child, each with the environment that harness builds, are refused the originals. Showing that the child has no session identity does not close this card: without identity the call takes `readInbox` and the originals, and that fail-open is removed only by [PB-178.2](../../archive/LOG.md#pb-178.2). The 0.146.0 eleven-variable list is not treated as a measurement of 0.156.1.
