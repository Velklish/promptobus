# PB-65.2 · A stray file at a store directory path (EEXIST, ENOTDIR) or a read-only or full volume (EROFS, ENOSPC) on the recipient-directory mkdir still escapes classification, and EEXIST cannot simply join LINK_REFUSALS
- **Order:** 140
- **Scope:** `src/v1/artifacts.ts` (`LINK_REFUSALS`, `linkFailure`), `src/v1/messages.ts` (`linkOnce`), [04-protocol](../../reference/04-protocol.md) § Engine
- **Created:** 2026-09-10
- **Dependencies:** PB-65.1
- **Cost:** major

## Context

Returned from `deferred/` on 2026-09-23 (PB-249): its return condition, "the next store/engine run", fired with the store and engine runs of 2026-09-12 to 2026-09-17, and none of them took the card — see the re-triage below.

Finding of the PB-65.1 review (isolated reviewer, run `pb-run-0910-t20260910-085454`), from the source. PB-65.1 classifies a refusal on creating the recipient inbox directory through `linkFailure`, whose list `LINK_REFUSALS` (`src/v1/artifacts.ts:164`: EXDEV, ENOTSUP, EOPNOTSUPP, EPERM, EACCES, EMLINK) was drawn for a hard link and left unchanged by the card's decision. The refusals most likely on a `mkdir` are not in it: `EROFS` (a read-only volume), `ENOSPC` (a full one), and — measured by the orchestrator on Node 25.2.1 — `EEXIST` when a file sits where the inbox directory must be and `ENOTDIR` when it sits deeper on the path. All four escape `recoverTask` as raw errnos, and `recover()` at `bus()` open still takes every store command down for them — loudly now, where a shared `catch` would have read the mkdir `EEXIST` as "ref already placed" and dropped the intent in silence, but the crash class PB-65.1 was opened for. The same holds for the link itself, where PB-65 left the list narrow on purpose. Hypothesis, not measured: a store on a read-only volume, or with a stray file at `inbox/<participant>`, plus one open intent makes `status`, `history` and `prune` exit 1 after PB-65.1.

**A trap for the repair:** `EEXIST` cannot simply join `LINK_REFUSALS`. The `EEXIST → return false` branch of the link half of `linkOnce` is what makes recovery idempotent, and `materialize` (`src/v1/messages.ts`, around line 259) reads the raw code beside the list — the comment at `src/v1/artifacts.ts:161-165` warns of exactly this for `ENOENT`. The directory half needs its own classification, not a wider shared list. Also in view: a `mkdir` `ENOENT` (a path component removed in a race) would read in `materialize` as "a neighbour materialized first" and answer `intent is gone and there is no canon` for a live intent — retryable, no loss, unmeasured.

## Work to do

- **The owner's decision, 2026-09-25:** join. The mkdir half of `linkOnce` gets a typed classification of its own with the errno named — `EROFS` and `ENOSPC` as a condition a retry may clear, `EEXIST` and `ENOTDIR` as a refusal naming the stray path — with a regression per errno through a fault seam; `LINK_REFUSALS` stays untouched. "Stay and document" is not taken.
- Decide (owner) whether the directory half of `linkOnce` gets its own classification — `EROFS`, `ENOSPC`, `EEXIST`, `ENOTDIR` on the mkdir become `link-refused` (or a code of their own) with the errno named, a retry being the right answer for a volume that comes back writable and a refusal the right answer for a stray file — or whether the taxonomy stays as PB-65 drew it and 04-protocol says which errnos are classified on each half.
- If it does: a regression per errno through a fault seam (a new `mkdir` point beside the `ref` step), the way PB-66's seams inject errnos portably; `LINK_REFUSALS` itself untouched; one sentence in 04-protocol § Engine.

## Out of scope

- The mkdir/link split of PB-65.1 — landed.

## Verification

- For "join": the regression is red before and green after; for "stay": 04-protocol names the classified list and nothing else changes.

## Deferred

- **Deferred:** 2026-09-11
- **Reason:** engine store errno classification on the directory half of `linkOnce`, outside the harness-parity runs of 2026-09-11; the owner's decision on classifying `EROFS`/`ENOSPC`/`EEXIST`/`ENOTDIR` is still pending.
- **Return condition:** the next store/engine run, or a live task that hits one of these errnos on the recipient-directory mkdir.

## Re-triage, 2026-09-23

Checked on `39316bc2` from the repository root. **Cost `major`**: a stray file at an inbox path, or a read-only or full volume, with one open intent makes the first bus open of every store command throw a raw errno. The code path is verified; that `status`, `history` and `prune` exit 1 is still an assumption. The ENOENT race is reported as permanent loss for an intent still on disk.

- **Return condition: fired.** `git log --oneline --since=2026-09-11T00:00:00 -- src/v1/messages.ts src/v1/artifacts.ts src/v1/store.ts src/v1/engine.ts` → exit 0, 16 commits. Code-bearing store and engine runs among them include `b1758679`, `4fb9f610` (PB-204.2), `c6f387f6` (PB-193), `4894d0e7` (PB-208.1), `ba65bce6` (PB-226) and `9c5e8a7c` (PB-193.1, "peekInbox reads through the FaultHook seam" — the kind of seam this card asks for on `mkdir`). None touched `linkOnce`: `git log --oneline --since=2026-09-11T00:00:00 -G"linkOnce|linkFailure|LINK_REFUSALS" -- src/v1/` → `256e441e` only, the comment sweep.
- The gap holds, on moved lines: `LINK_REFUSALS` is `src/v1/artifacts.ts:120`, with the same six members (the card cites `:164`). `linkFailure` returns anything else raw (`:124-131`). `linkOnce` classifies its `mkdir` through it (`src/v1/messages.ts:71-73`) and keeps `EEXIST → return false` for the link (`:79`). Recovery rethrows anything but `link-refused` (`:516`), and `lib/store.js:269-270` opens the engine and reports recovery with no catch.
- `materialize` is at `src/v1/messages.ts:145-154` (the card cites around `:259`), and the ENOENT comment is at `src/v1/artifacts.ts:118-119`.
- **Correction:** "retryable, no loss" for a `mkdir` ENOENT race is not what the code reports. Recovery maps a `link-refused` with errno `ENOENT` to `intent-lost` (`src/v1/messages.ts:517`), and 04-protocol says that code "is not retried" (`docs/reference/04-protocol.md:87`). The branch does not delete the intent (`continue`, `:519`), so whether a later pass picks it up again is unverified.
- 04-protocol names no errno list: `grep -c "EXDEV\|EROFS\|ENOTDIR" docs/reference/04-protocol.md` → 0. No `mkdir` fault point exists: `grep -n "EROFS\|ENOSPC\|ENOTDIR" src/v1/*.ts` → exit 1.
- Not tree-checkable: the `EEXIST`/`ENOTDIR` measurement on Node 25.2.1 (no date or command).
- Belongs in the same run as PB-146.3 and PB-66.2 in `minor/`: the same engine files and the same seam technique.
