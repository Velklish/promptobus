# PB-208.2 · Three comment runs of the PB-208.1 code stand over the two-line limit in files the PB-227 sweep had cleared

- **Scope:** `heldSync` and `withDirLockAsync` in `src/fs/lock.ts`, `orphanBlobs` in `src/v1/artifacts.ts`; the rule is the owner's decision of 2026-09-12, carried by [AGENTS.md](../../../AGENTS.md) § Comments
- **Created:** 2026-09-16
- **Dependencies:** PB-188 holds the withdrawn gate that would have named these runs

## Context

Found at the acceptance of PB-208.1, on the merged tree rather than on either side alone. PB-208.1 was written on `4137ee3`, where `src/fs/lock.ts` carried four comment runs over two lines and `src/v1/artifacts.ts` carried nine; PB-227 (`256e441`) then swept both files clean. Two of the runs the acceptance had to keep are code the review asked for in so many words — the `held` → `heldSync`/`heldAsync` split and the rewritten `orphanBlobs` docstring that describes the *new* meaning of the predicate — so they were resolved in favour of the PB-208.1 text and came over the two-line limit with it.

Measured with the scanner already in the tree, `test/comment-scan.mjs` § `longRuns`, run over the same files in three trees:

| file | `4137ee3` (the task's base) | `0c026f8` (main after the PB-227 sweep) | merged tree of this acceptance |
|---|---|---|---|
| `src/fs/lock.ts` | 4 runs | none | 2 runs — `:57` (11 lines), `:161` (7 lines) |
| `src/v1/artifacts.ts` | 9 runs | none | 1 run — `:243` (8 lines) |

Both of the lock runs and the artifacts run carry the same text as at the worker head `7dc22d9` (`05456988e341`, `82ef7979c0b9`, `8cce38a894f7`), so nothing about them is the merge's own invention — the merge only removed the pre-existing long runs around them, which is what leaves these three standing alone.

`lib/store.js` is NOT part of this: its set of long runs is byte-identical on base and on the merged tree, sixty-three runs with the same hashes, and it belongs to a track that has not swept yet.

Nothing is red from this: both checks of `test/comment-length.test.mjs` are marked `{ todo: … }` — PB-188 withdrew the two comment gates from the release — so `npm test` exits 0 and the acceptance gates are green. The rule these runs break has no gate at all and rests on the author of the change, which is why this is a card and not a gate failure.

## Work to do

- Bring each of the three to at most two lines. What does not fit is what the reviewer asked to be written down — the licence of the synchronous nesting, why an asynchronous holder queues instead of inheriting it, and what a hard link counts as when a blob is judged nameless — so the place for it is [protocol § Store layout](../../reference/04-protocol.md) and [cli § sweepArtifacts](../../reference/03-cli.md), both of which already carry the arrangement, with a pointer left beside the code.
- Read the two sections first: if they already say it, the comment loses the sentence and gains nothing.

## Out of scope

- The sixty-three long runs of `lib/store.js`. They are on base as well and belong to the track that owns that file.
- Restoring the withdrawn gates, and rebuilding the pending ledger `test/fixtures/comment-sweep-pending.json` that the PB-227 sweep left stale. Both are PB-188.

## Verification

- `node --test test/comment-length.test.mjs` names none of the three in the first check's list, and the list does not grow anywhere else.
- Each function still says what it said: the fact each block carries is either in the two lines that remain or in the section its pointer names.
