# PB-87.3 · Whether codex-cli withholds a tool listed in disabled_tools from the model is unmeasured, and the owner's personal MCP set lifted beside the mechanism's entries stays under the prompt layer
- **Scope:** `lib/driver-codex.js` (`mcpConfig`, the reviewer prompt phrase), `lib/codex-session.js` (`codexMcpServers`), `test/fixtures/codex-app-server/0.146.0/`, [03-cli](../../reference/03-cli.md) § Review
- **Created:** 2026-09-10
- **Dependencies:** PB-87.1

## Context

Finding of the PB-87.1 security review (isolated reviewer, run `pb-run-0910-t20260910-085454`). PB-87.1 places the host-classified write tools into each mechanism-supplied Codex server entry as `disabled_tools`, and its no-turn fixture proves that codex-cli 0.146.0 accepts the key on `thread/start` and brings the server up — not that app-server withholds the tool from the model: the thread-scoped `mcpServerStatus/list` never answered, and no turn was sent (the owner's decision of 2026-09-10 was to spend no live turn). Two residuals follow. First, enforcement is unmeasured, so the Codex reviewer's prompt says what was measured rather than claiming the mechanical layer outright. Second, app-server always lifts the owner's personal MCP set beside the mechanism's prefixed entries (`lib/driver-codex.js`, the personal-set note; `lib/codex-session.js`, the merge notes), so a personal entry under the canonical name carries the same write tools undenied — the mechanical layer covers the servers the mechanism supplies, and the personal set stays under the prompt layer. Closing the second needs a `config.mcp_servers` override entry under the unprefixed canonical name carrying only `disabled_tools`, which `codexMcpServers` skips today (a record with neither `command` nor `url`) and which the merge notes say can kill the config load when the name is absent from the personal config — so it needs its own measurement too.

## Work to do

- Decide (owner) whether to spend one paid Codex turn: a reviewer thread with one classified tool in `disabled_tools`, asked to list its tools, with the answer captured as a fixture beside the 0.146.0 no-turn one — and, if enforcement holds, whether an override entry for a personal server under its canonical name is measured the same way.
- If measured and enforced: the Codex prompt phrase becomes the measured absolute; 03-cli § Review drops the residual sentence. If not enforced: the Codex driver drops `mcpDenyTools` and returns to prompt-only, with the reason in 03-cli.

## Out of scope

- The completeness signal and the `{ tools, complete }` shape — PB-87.1.
- Cursor — PB-87.2.

## Verification

- A fixture named by version and date records the turn-level observation; the prompt phrase and 03-cli say exactly what it shows; `npm test` green.
