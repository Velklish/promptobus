# PB-217.1 · Two comment runs of the PB-217 code stand over the owner's two-line limit once the sweep has reached their files

- **Scope:** `recordRefusal` in `lib/handoff.js`, `schemaErrors` in `lib/schema.js`; the rule is the owner's decision of 2026-09-12, carried by [AGENTS.md](../../../AGENTS.md) § Comments
- **Created:** 2026-09-16
- **Dependencies:** PB-188 holds the gate that would have named these runs

## Context

Found during the acceptance of PB-217, on the merged tree rather than on either side alone. PB-217 was written on `4137ee3`, where `lib/handoff.js` was still a file the comment sweep had not reached, so a long doc block in it was outside what the scanner judged, and `lib/schema.js` did not exist at all. PB-227 (`256e441`) then swept `lib/handoff.js`, and after the merge both blocks are plain violations of the two-line rule.

Measured with the scanner already in the tree, `test/comment-length.test.mjs`, first check:

- merged tree of the PB-217 acceptance: `[ 'lib/handoff.js:60 — 6 lines', 'lib/schema.js:170 — 5 lines' ]`
- base `0c026f8`, same check, same command: eight runs, **all** in `lib/store.js`, each reported as `a long run the list does not name`; no entry in `lib/handoff.js`, and `lib/schema.js` is not in that tree.

Both runs are doc blocks over a function, five and six lines including the delimiters:

<!-- quote:../../../lib/schema.js -->
```js
/**
 * Where the document breaks its schema: `{ at, note }` per fault, empty — it is accepted.
 * A schema the reader does not fully implement throws instead of answering: a partial
 * verdict presented as a pass is the failure this check exists against.
 */
```
<!-- /quote -->

Nothing is red from this: both checks of `test/comment-length.test.mjs` are marked `{ todo: … }` — PB-188 withdrew the two comment gates from the release — so `npm test` exits 0 and the acceptance gates are green. The rule they break has no gate at all and rests on the author of the change, which is why this is a card and not a gate failure.

## Work to do

- Compress both runs to at most two lines, or move what does not fit into `docs/reference/` and leave a pointer, whichever the content asks for. The `lib/handoff.js` block carries a reference link on its own line, so the pointer half of it has a home already.
- Check the rest of the PB-217 and PB-208.1 code the same way: those two files are what the scanner names today, and the scanner judges only files the sweep is recorded as having reached.

## Out of scope

- The eight runs in `lib/store.js` on base. They are the debt of another track, they are on base as well, and this card adds nothing to them.
- Restoring the withdrawn gates. That is PB-188.

## Verification

- `node --test test/comment-length.test.mjs` names neither run in the first check's list.
- The two functions still say what they said: the fact each block carries is either in the two lines that remain or in the section a pointer names.
