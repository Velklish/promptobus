# PB-88.4 · item/tool/call, account/chatgptAuthTokens/refresh and attestation/generate are answered with a JSON-RPC error — whether the holder should ever answer the token refresh is undecided
- **Scope:** `lib/codex-session.js` (`onServerRequest`, `APPROVE`), `test/fixtures/codex-app-server/0.146.0/ServerRequest.json`, [03-cli](../../reference/03-cli.md) § The Codex holder
- **Created:** 2026-09-10
- **Dependencies:** PB-88.2

## Context

Finding of the PB-88.2 security review (isolated reviewer, run `pb-run-0910-t20260910-085454`). The measured `ServerRequest` list carries three requests that are not approvals and have no `APPROVE` row: `item/tool/call`, `account/chatgptAuthTokens/refresh` and `attestation/generate`. Since PB-88.2 the holder answers them with a JSON-RPC error (`-32601`) instead of an approval-shaped object. `item/tool/call` fires only for client-declared dynamic tools, which the holder declares none of; `attestation/generate` is upstream attestation and not on the turn path. `account/chatgptAuthTokens/refresh` is different: for an account on ChatGPT auth it is how app-server asks the client to refresh a token that expired mid-turn, and an error reply ends the turn with an auth failure rather than a policy refusal. Whether the holder should ever answer it — and with what, since the reply carries tokens — is undecided; today the refusal is at least lawful.

## Work to do

- Decide (owner) whether the holder answers `account/chatgptAuthTokens/refresh` (it would have to obtain a token without holding one, which the mechanism client has never done) or keeps refusing, with the refusal named in 03-cli so an auth error mid-turn is read as expected.
- Either way, one sentence in 03-cli § The Codex holder listing the three non-approval requests and the holder's answer.

## Out of scope

- The approval rows themselves — PB-88.2.

## Verification

- The three requests answered as decided, asserted against the fixture list; `npm test` green.
