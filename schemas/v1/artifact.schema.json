{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:promptobus:v1:artifact",
  "title": "Artifact",
  "description": "Artifact metadata. The blob is addressed by SHA-256 and deduplicated within a task.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "id", "sha256", "filename", "size", "blob"],
  "properties": {
    "schemaVersion": { "const": 1 },
    "id": {
      "type": "string",
      "pattern": "^[0-9]{8}T[0-9]{9}-[0-9]{4}-[0-9a-f]{6}$"
    },
    "sha256": {
      "description": "Digest of the content, read as a stream.",
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "filename": {
      "description": "The original file name. One piece of content can live under several names — several records, one blob.",
      "type": "string",
      "minLength": 1,
      "maxLength": 255,
      "pattern": "^[^/\\\\]+$",
      "not": { "enum": [".", ".."] }
    },
    "size": {
      "type": "integer",
      "minimum": 0
    },
    "blob": {
      "description": "The blob's path inside the task directory.",
      "type": "string",
      "pattern": "^blobs/[0-9a-f]{64}$"
    }
  }
}
