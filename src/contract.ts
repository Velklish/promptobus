// Harness-neutral literals shared by the compiled package and its adapter modules.

/** Name of the bus server in participant MCP configurations and hook matchers. */
export const PROMPTOBUS_SERVER = 'promptobus';
